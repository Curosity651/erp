# OMS + WMS 物流平台 · 完整技术设计方案（增量版）

> 🟥 **勘误（2026-06-28）**：以《现状-设计差异对照表.md》为准。修订：① **决策 S 作废**——`wms_stock_flow` 已存在，改为 ALTER 加双租户列；② 库存=**方案②**（新建 `wms_physical_inventory` SSOT + 现有 `wms_inventory` 降级聚合）；③ 软删除用 **`deleted`+`@TableLogic`**（非 `deleted_at`）；④ 附录 A 的 `wms_warehouse`/盘点表已存在（盘点真名 `wms_stocktake_order`），入库/出库/退货执行层已存在，§6.4/§6.5/§7.5/§7.6 为"对接/扩展现有"而非新建。

> 本文是对《设计方案.md》(权威基线，数据结构以其为准) 与《业务需求说明书.md》(业务流程以其为准，含附录 A 新表) 的**增量技术设计**。
> 沿用基线全部铁律：双层租户 (C1)、三入口角色 (C2)、WMS 模块化单体同进程 Bean 调用 (C3)、库存 SSOT 仅 WMS 可写 (C4)、双 ID (C5)、计费链路分离 (C6)、FIFO 防超卖 (C7)、已定稿业务规则 (C8)；不用外键、软删 `deleted_at`、时间 UTC。
> 章节号沿用基线编号。所有 DDL 收敛到 **§九 迁移脚本一处**（正文小节只引用，不双写）。

## 修订说明（相对《设计方案.md》变更索引）

| 章节 | 变更类型 | 一句话摘要 |
|---|---|---|
| §4.3 | 更新 | `oms_outbound_order`/`oms_inbound_order` 补手工收件人、头程跟踪号；明确自定义单**共用现有表 + `source_type` 判别**（决策 P） |
| §4.4 | 更新 | `wms_physical_inventory` 补 `zone_id`+`allocatable`(冗余) 并把 `uk_batch` 纳入 `location_code`(决策 R)；`wms_warehouse` 补结构参数；`wms_location` 补容量；`sku` 补仓储属性；状态枚举补全 |
| §4.8 | 新增 | 补建基线缺失的 `wms_stock_flow`（双 ID 库存流水，决策 S） |
| §5.5 | 新增 | 统一错误码登记表（WMS 侧续 `WmsResultCode` 4xxxx 段） |
| §6.4 / §6.5 | 新增 | 自定义入库单 / 出库单完整状态流（共用表，来源不同流程一致） |
| §6.6 | 新增 | 货主侧盘点 / 库存写接口下线，库存改只读快照（C4 落地，决策 T） |
| §7.3 | 更新 | `WmsCallbackService` 由 `OmsApiClient`(REST) **纠偏为同进程注入 `omsService`**（对齐 C3） |
| §7.4 | 新增 | 仓库预览接口（按排聚合 `RackVO`） |
| §7.5 | 新增 | 上架算法 `WmsRackingService`（拆出 `putawayInventory` 纯写库存内部方法） |
| §7.6 | 新增 | 退货质检流程 `ReturnOrderService`（状态机守卫，回传唯一触发） |
| §九 | 新增 | 增量迁移脚本 `04_schema_increment.sql`（含 `zone_id`/`allocatable` 回填）+ 回滚 |
| §十 | 新增 | 接口契约补 `cancelInboundOrder` 及 5 个 WMS→OMS 同进程回调 |
| §11.4–11.7 | 更新/新增 | 三入口完整路由 + BallCat 菜单映射；权限命名规范；货主盘点菜单解绑（修正 path 冲突） |
| §十五 | 新增 | 决策 P～T |

---

## 一、§4 数据库 Schema 增量

### 【更新 §4.3】4.3.1 自定义出/入库单建模决策（→ 决策 P）

**结论：自定义单与平台/采购单共用现有 `oms_outbound_order` / `oms_inbound_order` 表，靠基线已有的 `source_type` 判别，不独立建表、不新增 `order_type`。**

判定口径（应用层强制，不依赖 DB 外键/CHECK）：
- 自定义出库单 = `source_type='MANUAL'` 且 `erp_order_id IS NULL`（对齐业务 §2.5）；销售出库单 = `source_type='PLATFORM'` 且 `erp_order_id` 非空（§2.4）。
- 自定义入库单 = `source_type='MANUAL'` 且 `purchase_order_id IS NULL`（§2.3）；采购入库单 = `source_type='PURCHASE'` 且 `purchase_order_id` 非空（§2.2）；退货入库 = `source_type='RETURN'`（§1.4/§2.6）。`purchase_order_id` 基线本就可空，无需改可空性。

理由（与"不推翻基线"一致）：共用 `tenant_id` 行级隔离一套规则；列表/统计单查询即可；复用 `oms_sync_outbox`(`OUTBOUND_CREATED`/`INBOUND_CREATED`) 与统一状态机；WMS 下游只认 `wms_outbound_order`/`wms_inbound_order`，与来源无关。仅当"字段集差异极大 / 状态机完全不同 / 合规要求物理分表"三者之一成立才拆表——当前均不成立。详见 §十五·决策 P。

补充字段（自定义出库单需手工录入收件人，平台单运行时由 `erp_order` 带出；自定义入库单需头程跟踪号）：DDL 见 §九`[4][5][6]`。WMS 出库单镜像同名收件字段以支持自定义单打面单/签出（§九`[5]`）。物流产品/计费无需新增字段——`oms_outbound_order` 已有 `logistics_product_id / logistics_fee / logistics_currency`，自定义单与平台单共用同一物流产品选择与链路二计费（§8.4），与 `source_type` 无关。

> 应用层约束：`source_type='MANUAL'` 出库单 `receiver_name/receiver_phone/receiver_address` 三者非空校验在 `OmsOutboundService` 强制，失败抛 `BizException`；`PLATFORM` 单允许为空。

### 【更新 §4.4】4.4.1 `wms_warehouse` 结构参数 + 库位批量生成

附录 A·`wms_warehouse` 缺业务 §1.1.1 的"排数/每排层数/每排列数"。补结构参数（DDL §九`[1]`），驱动 `wms_location`(附录 A·A3) 批量生成：

- 库位总数 = `rack_rows × rack_layers × rack_columns`；编码 `location_code = {rack_no}-{补零层}-{补零列}`，`rack_no = rack_no_prefix + 序号`（如 `A`→`A1`），层/列按 `code_pad_width` 补零（对齐 §1.1.1 例 `A1-03-03`）。
- 生成前须先存在一个默认 `STANDARD` 分区(`wms_zone`)承接新库位；`location_generated=1` 时拒绝重复生成（幂等，防 `uk_wh_loc` 冲突，抛 `WmsResultCode.LOCATION_ALREADY_GENERATED(40009)`）。
- 生成产出的 `DISTINCT rack_no` 即 `wms_rack_assignment.rack_no` 的唯一合法取值来源；货架分配(§1.1.4)只能从已生成排号勾选，引用完整性由应用层校验。
- `default_location_type` 与 `wms_location.location_type` 取值同源，统一码值 `BIG/SMALL`，登记到 `sys_dict`（§九`[12]`）。

```java
// com.erp.admin.wms.service.WmsLocationGenerator
@Transactional(rollbackFor = Exception.class)
public int generateLocations(Long warehouseId) {
    WmsWarehouse wh = warehouseMapper.selectById(warehouseId);
    if (wh.getLocationGenerated() == 1)
        throw new BizException(WmsResultCode.LOCATION_ALREADY_GENERATED);   // 40009
    Long defaultZoneId = zoneMapper.findDefaultStandardZone(warehouseId);    // 须先建标准分区
    String pad = "%0" + wh.getCodePadWidth() + "d";
    List<WmsLocation> batch = new ArrayList<>();
    for (int r = 1; r <= wh.getRackRows(); r++) {
        String rackNo = wh.getRackNoPrefix() + r;                            // A1, A2 …
        for (int l = 1; l <= wh.getRackLayers(); l++)
            for (int c = 1; c <= wh.getRackColumns(); c++) {
                WmsLocation loc = new WmsLocation();
                loc.setWarehouseId(warehouseId); loc.setZoneId(defaultZoneId);
                loc.setRackNo(rackNo); loc.setLayerNo(l); loc.setColumnNo(c);
                loc.setLocationCode(rackNo + "-" + String.format(pad, l) + "-" + String.format(pad, c));
                loc.setLocationType(wh.getDefaultLocationType()); loc.setPickType("PICK");
                batch.add(loc);
            }
    }
    locationService.saveBatch(batch, 1000);
    warehouseMapper.markGenerated(warehouseId);     // location_generated = 1
    return batch.size();
}
```

### 【更新 §4.4】4.4.2 `wms_physical_inventory`：双 ID 不变 + 分区维度 + `uk_batch` 修订（→ 决策 R/Q）

基线双 ID 结构（`wms_tenant_id` 货架归属 + `erp_tenant_id` 货物归属，C5/决策 J）**保持不变**。本次三处增量（DDL §九`[2]`）：

1. **补 `zone_id`**（关联 `wms_zone.id`）：用于分区报表/预览下钻。
2. **冗余 `allocatable TINYINT`**（上架/质检时从 `wms_zone.allocatable` 落值）：FIFO 分配过滤不再 JOIN `wms_zone`，避免对极小维表加排他锁放大锁粒度（修正：原"JOIN wms_zone … FOR UPDATE"会把按批次行锁放大成按分区串行）。`allocatable` 默认 `1`，保证历史/迁移库存在精确回填前仍可分配，不触发假缺货整单挂起。
3. **`uk_batch` 修订**（决策 R）：基线 `uk_batch=(wms_tenant_id,erp_tenant_id,warehouse_id,sku_code,inbound_item_id)` 限定"一入库明细一行"，与业务 §1.2.2"单 SKU 可拆分到多库位"冲突。按改动联动链 (07→08→09→11/12→15) 显式修订为纳入 `location_code`：
   `uk_batch=(wms_tenant_id,erp_tenant_id,warehouse_id,sku_code,inbound_item_id,location_code)`。
   语义澄清："**并入原库位**" = 同 `(inbound_item_id, location_code)` 经 `ON DUPLICATE KEY` 数量累加；"新分配库位" = 同一 `inbound_item` 在不同 `location_code` 插入平行批次行。FIFO 仍按 `inbound_date → pick_order → id` 升序，正确性不变。

**质检状态命名归一（决策 Q）**：退货质检权威字段 = `wms_return_qc_item.qc_status`（附录 A·A4，**不改名**），取值 `QC_PENDING/QC_PASS/QC_FAIL`；`quality_status` 仅作文档同义别名，代码/DTO/VO/Mapper 一律用 `qc_status`。它与 `wms_physical_inventory.quality`(`GOOD/DAMAGED`) 不是同一字段，映射如下：

| `qc_status` | `quality` | 回库分区 `wms_zone.zone_type` | `allocatable` | 可被 FIFO 分配 |
|---|---|---|---|---|
| `QC_PASS` | `GOOD` | `RETURN` 或 `STANDARD` | 1 | ✅ |
| `QC_FAIL` | `DAMAGED` | `DEFECTIVE` | 0 | ❌ |
| `QC_FAIL`(电子类) | `DAMAGED` | `DEFECTIVE` | 0 | ❌ + `photo_url` 强制(BR-04) |

**FIFO 分配查询（§7.2 衔接，去 JOIN、单表行锁）**：

```sql
SELECT * FROM wms_physical_inventory
WHERE wms_tenant_id = #{wmsTenantId}
  AND erp_tenant_id = #{erpTenantId}
  AND warehouse_id  = #{warehouseId}
  AND sku_code      = #{skuCode}
  AND quality       = 'GOOD'            -- C7：仅良品
  AND allocatable   = 1                 -- 标准/退货区=1；不良品/暂存区=0（冗余列，免 JOIN）
ORDER BY inbound_date, pick_order, id   -- C7：FIFO
FOR UPDATE;                             -- 仅锁本表行，不锁维表
```

走新增索引 `idx_fifo_alloc`(§九`[2]`)；基线 `idx_fifo` 保留不动。

### 【更新 §4.4】4.4.3 `wms_location` 容量 + `sku` 仓储属性

业务 §1.1.1"库位类型预设长宽高、上架容量判断"、§1.2.1"称重量方写入 SKU 仓储属性"所需字段：`wms_location` 补 `max_volume_cbm/max_weight_kg`（§九`[7]`）；`sku` 补 `weight_kg/length_cm/width_cm/height_cm/volume_cbm`（§九`[8]`，`volume_cbm` 冗余 = L×W×H/1e6，供容量判断与入库 m³ 计费）。

### 【更新 §4.3/§4.4】4.4.4 单据状态枚举补全

仅 `MODIFY COLUMN … COMMENT`（`status VARCHAR(20)` 不变结构，DDL §九`[9]`），对齐附录 B 状态机：

| 表 | 增量枚举 | 说明 |
|---|---|---|
| `oms_inbound_order` | 补 `DRAFT`，默认改 `DRAFT` | 对齐 §2.2/§2.3"DRAFT→提交→PENDING" |
| `oms_outbound_order` | 补 `BACKORDER`；`ACCEPTED` 保留并入 `SENT` | 缺货整单挂起 BR-01；同进程模型下接单=下发同一次调用 |
| `wms_inbound_order` | 注释补业务标签 | `PENDING待收货/RECEIVING收货中/PUTAWAY待上架/COMPLETED上架完成/CANCELLED`；退货链路 `RETURN_PENDING→QC_PENDING→PUTAWAY→COMPLETED` |
| `wms_outbound_order` | 补 `PACKED` | 对齐附录 B "PACKING→PACKED→SHIPPED" |

### 【新增 §4.8】补建 `wms_stock_flow`（双 ID 库存流水，→ 决策 S）

基线仅在 §3.4/§3.5 提及 `wms_stock_flow`（单 `tenant_id` 索引），**无 CREATE TABLE**；业务 §1.5/§2.7.2 的库存记录只读视图依赖它。按联动链补建，采用与 `wms_physical_inventory` 一致的双 ID（DDL §九`[10]`）。原 §3.5 `idx_tenant_sku(tenant_id, sku_code, create_time)` 的单 `tenant_id` 表述以本节双 ID 建表为准。

---

## 二、§5 多租户核心代码增量

### 【新增 §5.5】统一错误码登记表

基线错误码约定见 §5.2 `BizException(int code, String msg)`（ERP 侧，已有 `50003` 无租户上下文）与 `com.erp.admin.wms.enums.WmsResultCode`（WMS 侧 `ResultCode` 枚举，4xxxx 段，已有 `STOCK_INSUFFICIENT(40010)`）。本次新增码全部续 `WmsResultCode` 4xxxx 段，确认不与既有码冲突：

```java
// com.erp.admin.wms.enums.WmsResultCode（续登记）
LOCATION_ALREADY_GENERATED   (40009, "库位已生成，禁止重复生成"),
// STOCK_INSUFFICIENT        (40010, ...) 既有：缺货整单挂起（BR-01），WMS 侧抛此码
LOCATION_CAPACITY_INSUFFICIENT(40020, "库位容量不足/无可分配库位"),
RACK_NOT_ASSIGNED            (40021, "目标货架未分配给当前 WMS 服务商"),
QC_ILLEGAL_TRANSITION        (40030, "质检状态非法流转"),
QC_PHOTO_REQUIRED            (40031, "次品+电子类必须上传质检照片"),   // BR-04
INVENTORY_WRITE_FORBIDDEN    (40301, "库存由 WMS 统一管理，货主端无库存写入权限"),
STOCKTAKE_MOVED_TO_PLATFORM  (40310, "盘点功能已迁移至平台/WMS 服务商，货主端已下线"),
ADJUSTMENT_MOVED_TO_PLATFORM (40311, "库存调整已迁移至平台/WMS 服务商，货主端已下线");
```

> WMS 路径统一抛 `WmsResultCode`；ERP/OMS 路径沿用 `BizException`。无租户上下文仍为 `50003`（§5.2）。

### 【沿用 §7.2】WMS 上下文与 Mapper 绕过约定

`WmsTenantContext.setCurrentWmsTenant()` 为基线 §7.2 已定义构件（承载 `wms_tenant_id`，因 §5.3 对 WMS 用户 `TenantContext` 返回 `null`），本文 §7 全部沿用。

**关键纠偏**：MyBatis-Plus `@InterceptorIgnore(tenantLine="true")` 只在 **Mapper 接口/方法**上生效，标在 `@Service`/`@Transactional` 方法上是空操作。因此：

- `WmsInboundOrderMapper`、`WmsInboundOrderItemMapper`、`WmsPhysicalInventoryMapper`、`WmsLocationMapper`、`WmsRackPreviewMapper`、`WmsReturnQcItemMapper` 等 WMS 专用 Mapper 一律**类级** `@InterceptorIgnore(tenantLine="true")`。
- 共享的 `SkuMapper`（ERP 侧仍需自动注入 `tenant_id`）只对 WMS 路径使用的方法（如 `selectVolumeCbm`、`isElectronicCategory`，显式传 `erp_tenant_id`）做**方法级** `@InterceptorIgnore`，绝不在类级关闭。
- Service 层不再出现 `@InterceptorIgnore`（删除原伪代码中放错位置的注解，消除"安全错觉"）。

---

## 三、§6 OMS 模块设计增量

### 【新增 §6.4】自定义入库单完整状态流（含取消/异常分支）

自定义入库单与采购入库单共用 `oms_inbound_order`，仅 `source_type`(MANUAL/PURCHASE)+`purchase_order_id`(NULL/非空) 区分；**流程、状态机、同步触发点完全一致**，下游 WMS 不区分来源。

```
OMS oms_inbound_order.status
DRAFT ─货主【提交】─► PENDING ─同事务写 oms_sync_outbox + 同进程 wmsService.createInboundOrder─► SENT(待收货)
  ◄─WMS【标记到货】回调 onInboundReceiving─ RECEIVING(尾程收货中)
  ◄─WMS【确认收货】回写实收数+建SKU仓储属性(主状态不变)─ RECEIVING
  ◄─WMS【确认上架】全部完成 onInboundCompleted─ COMPLETED(已入库)
取消/异常：
  DRAFT      ─删除→ 软删 deleted_at（未入流程，不通知 WMS）
  PENDING    ─撤回→ CANCELLED（Outbox 未消费时本地撤销）
  SENT       ─取消→ CANCELLED（须 wmsService.cancelInboundOrder 成功，即 WMS 仍 PENDING 未到货）
  RECEIVING 之后 不可取消；收货差异(实收≠预期)不阻断，明细留痕；整批次品仍 COMPLETED，上架到不良品区
```

| 业务阶段 | OMS | WMS `wms_inbound_order` | 同步方向/触发点 |
|---|---|---|---|
| 待推送 | `PENDING` | （未建单） | OMS→WMS `createInboundOrder` 建 WMS `PENDING` |
| 待收货 | `SENT` | `PENDING` | OMS 内部置 `SENT` |
| 收货中 | `RECEIVING` | `RECEIVING` | WMS→OMS `onInboundReceiving`（新增 §十） |
| 上架中 | `RECEIVING`(不变) | `PUTAWAY` | WMS 内部流转；回写实收数+建 SKU 仓储属性，OMS 主状态不变（可选 `onInboundReceived` 刷快照） |
| 已完成 | `COMPLETED` | `COMPLETED` | WMS→OMS `onInboundCompleted`（§10.2 已有） |
| 取消 | `CANCELLED` | `CANCELLED` | 双向，须 WMS 仍 `PENDING` |

> 关键不对称点：OMS 无 `PUTAWAY` 态，"确认收货"不推进 OMS 主状态，停留 `RECEIVING` 直到上架完成才置 `COMPLETED`。

### 【新增 §6.5】自定义出库单状态流对照

自定义出库单与销售出库单共用 `oms_outbound_order`，仅 `source_type`(MANUAL/PLATFORM)+`erp_order_id`(NULL/非空) 区分；状态流一致。

| 业务阶段 | OMS | WMS `wms_outbound_order` | 同步方向/触发点 |
|---|---|---|---|
| 待推送 | `PENDING` | （未建单） | OMS→WMS `createOutboundOrder`（选物流产品锁价 §8.4） |
| 待下架 | `SENT` | `PENDING` | OMS 内部置 `SENT`（**此态仍可取消**） |
| 拣货中 | `PICKING` | `ALLOCATING`→`PICKING` | WMS→OMS `onOutboundPicking`（新增）；**回传成功后订单锁定不可取消** |
| 缺货挂起 | `BACKORDER` | `PENDING`(不变，可重试) | WMS→OMS `onStockInsufficient`（§10.2 已有，BR-01 整单挂起） |
| 已打包 | `PACKED` | `PACKING`→`PACKED` | WMS→OMS `onOutboundPacked`（新增） |
| 已发货 | `SHIPPED` | `SHIPPED` | WMS→OMS `onOutboundShipped`（§10.2 已有）：签出扣减 SSOT(`quantity-=`、释放 `reserved_qty`)+生成链路二物流费(§8.4.5 幂等)，回传 `tracking_no` |
| 完成 | `COMPLETED` | `COMPLETED` | WMS→OMS `onOutboundCompleted`（新增） |
| 取消 | `CANCELLED` | `CANCELLED` | 仅 `SENT` 且 WMS 仍 `PENDING` 时可取消；进入 `PICKING` 后取消入口永久关闭（前端禁用 + 服务端二次校验） |

### 【新增 §6.6】货主侧盘点 / 库存写接口下线，库存改只读快照（→ 决策 T）

> 落地业务 §2.7.1/§2.7.2 与 C4。本质 = "货主侧关闭写入口 + 数据源迁到 WMS SSOT"，非删除整个控制器（盘点是 WMS/平台合法的改库存入口，§1.5）。代码位于 `com.erp.admin.wms.controller.*`。

**(1) 写库存/盘点类——对货主关闭**（真实控制器与权限码，权限码用枚举，不用通配，不引入 `hasRole`）：

| Controller(`@RequestMapping`) | 写动作 | 真实权限码 | 直接写 SSOT |
|---|---|---|---|
| `StocktakeController`(`/wms/stocktake`) | `POST /`、`PUT /items`、`PATCH /confirm`、`PATCH /cancel`、`DELETE /`、`POST /add-skus`、`DELETE /items`、`PUT /batch-no-diff` | `wms:stocktake:{add,input,confirm,cancel,del}` | `confirm` 是 |
| `AdjustmentController`(`/wms/adjustment`) | `POST /`、`PUT /`、`PATCH /confirm`、`PATCH /cancel`、`DELETE /` | `wms:adjustment:{add,edit,confirm,cancel,del}` | `confirm` 是 |

处理：① `sys_role_menu` 从 `ROLE_TENANT_*` 撤销上述写权限码（保留 `:read` 给只读，DDL §11.7）；② Service 层纵深防御守卫——主体为货主上下文(`TenantContext.getCurrentTenant()!=null` 且非 WMS/超管)调用写方法即抛 `WmsResultCode.INVENTORY_WRITE_FORBIDDEN(40301)`/`STOCKTAKE_MOVED_TO_PLATFORM(40310)`/`ADJUSTMENT_MOVED_TO_PLATFORM(40311)`，统一异常层映射 HTTP 403；③ 盘点/调整 `confirm` 的库存修改操作 `wms_physical_inventory`(双 ID) + 写 `wms_stock_flow` 留痕；盘点冻结批次在 FIFO 分配时跳过（防与下架并发冲突，C7）。

**(2) 只读查询类——保留，数据源改 WMS 快照**：`InventoryController`(`/wms/inventory/*`)、`StockFlowController`(`/wms/stock-flow/*`) 改由同进程调 WMS 查询（`@InterceptorIgnore` + 显式 `erp_tenant_id` 条件），按 `erp_tenant_id` 过滤 `wms_physical_inventory`/`wms_stock_flow`；VO 不暴露写字段，前端无"调整库存"按钮。`StocktakeController` 的查询接口对货主整体下线（盘点归平台）。

**(3) 配置/预测类——保留给货主**：`InventoryConfigController`、`InventoryForecastController`（改配置非库存；对接 §2.7.3 `oms_forecast_rule`）。

**过渡三步**：阶段1（同迭代）前端移除货主盘点/调整菜单与路由、撤 `sys_role_menu`、Service 守卫生效，货主调用写接口返 403+错误码；阶段2（观察1迭代）按 `tenant_type=ERP_USER` 埋点确认零流量后返 HTTP 410；阶段3（清理）删货主前端 `views/wms/stocktake/*` 等与货主专用 Service 写分支，`StocktakeMapper.xml/AdjustmentMapper.xml` 的 INSERT/UPDATE 保留给 WMS。

**回归测试要点**：货主 token 调 `PATCH /wms/stocktake/confirm`、`PATCH /wms/adjustment/confirm`、`POST /wms/stocktake`、`POST /wms/adjustment` → 403 + 40301/40310/40311 且断言 `wms_physical_inventory` 无变更；只读放行且仅本 `erp_tenant_id`；`ROLE_WMS_*/ROLE_SUPER_ADMIN` 盘点/调整 `confirm` 正常写双 ID + 产 `wms_stock_flow` + 刷 OMS 快照；盘点冻结批次与并发 FIFO 不冲突（沿用 §12.1 `concurrent_allocation_prevents_oversell`）；货主菜单树不含盘点/调整。

---

## 四、§7 WMS 模块设计增量

### 【更新 §7.3】`WmsCallbackService` 纠偏为同进程调用

基线 §7.3 `WmsCallbackService` 注入 `OmsApiClient`(REST) + `WmsCallbackOutbox` 发件箱，与 C3"无 HTTP/MQ、同进程 Bean"及 §10.2 矛盾。**本节纠偏（非照抄）**：`WmsCallbackService` 改为直接注入 `omsService`（同进程 Spring Bean），独立事务、`afterCommit` 触发，失败记 ERROR 待人工/重试；不再走 REST，不再用 `WmsCallbackOutbox`。

```java
@Service
@RequiredArgsConstructor
public class WmsCallbackService {
    private final OmsCallbackService omsService;   // 同进程 Bean（纠偏：原 OmsApiClient REST → 直接注入）
    public void onOutboundShipped(WmsOutboundOrder order) {
        order.setStatus("SHIPPED"); order.setShippedAt(LocalDateTime.now(ZoneOffset.UTC));
        TransactionSyncUtil.afterCommit(() ->
            omsService.onOutboundShipped(order.getOmsOrderNo(), order.getTrackingNo()));
    }
}
```

### 【新增 §7.4】仓库预览接口（`List<RackVO>`，按排聚合）

对应 §1.1.2。后端按排聚合，二维渲染交前端(§11)。查询走 `@InterceptorIgnore(tenantLine="true")`（`wms_location`/`wms_rack_assignment` 无 `tenant_id`，`wms_physical_inventory` 双 ID）。

```java
// com/erp/admin/wms/controller/WarehouseRackController.java
@GetMapping("/admin/warehouses/{warehouseId}/racks")
@PreAuthorize("@per.hasPermission('wms:warehouse:preview')")
public R<List<RackVO>> listRacks(@PathVariable Long warehouseId,
        @RequestParam(required=false) Long wmsTenantId,
        @RequestParam(required=false) String status,
        @RequestParam(required=false) Boolean expiringSoon) {
    return R.ok(rackPreviewService.previewRacks(warehouseId, wmsTenantId, status, expiringSoon));
}
```

`RackVO`：`warehouseId/rackNo/status(IDLE|OCCUPIED)/assignmentId/assignedWmsTenantId/assignedWmsTenantName/monthlyFee/effectiveFrom/effectiveTo/expiringSoon/locationCount/occupiedLocationCount/occupiedQty`。

Service 可见性裁剪：WMS 服务商用户(`WmsTenantContext.getCurrentWmsTenant()!=null`)强制只看本司货架（忽略入参 `wmsTenantId`）；超管入参可选、`null`=全部。`expiringSoon = effectiveTo != null && effectiveTo ≤ today.plusDays(30)`（`today = LocalDate.now(ZoneOffset.UTC)`）。

```java
// WmsRackPreviewMapper（类级 @InterceptorIgnore）
@InterceptorIgnore(tenantLine = "true")
@Select("<script>" +
  "SELECT l.warehouse_id AS warehouseId, l.rack_no AS rackNo, ra.id AS assignmentId, " +
  "  ra.wms_tenant_id AS assignedWmsTenantId, t.tenant_name AS assignedWmsTenantName, " +
  "  ra.monthly_fee AS monthlyFee, ra.effective_from AS effectiveFrom, ra.effective_to AS effectiveTo, " +
  "  COUNT(DISTINCT l.id) AS locationCount, COUNT(DISTINCT pi.location_code) AS occupiedLocationCount, " +
  "  COALESCE(SUM(pi.quantity),0) AS occupiedQty " +
  "FROM wms_location l " +
  "LEFT JOIN wms_rack_assignment ra ON ra.warehouse_id=l.warehouse_id AND ra.rack_no=l.rack_no " +
  "  AND ra.effective_from &lt;= CURDATE() AND (ra.effective_to IS NULL OR ra.effective_to &gt;= CURDATE()) " +
  "LEFT JOIN sys_tenant t ON t.id=ra.wms_tenant_id AND t.deleted_at IS NULL " +
  "LEFT JOIN wms_physical_inventory pi ON pi.warehouse_id=l.warehouse_id AND pi.location_code=l.location_code " +
  "WHERE l.warehouse_id=#{warehouseId} " +
  "<if test='wmsTenantId!=null'> AND ra.wms_tenant_id=#{wmsTenantId} </if>" +
  "GROUP BY l.warehouse_id,l.rack_no,ra.id,ra.wms_tenant_id,t.tenant_name,ra.monthly_fee,ra.effective_from,ra.effective_to " +
  "ORDER BY l.rack_no</script>")
List<RackVO> previewRacks(@Param("warehouseId") Long warehouseId, @Param("wmsTenantId") Long wmsTenantId);
```

### 【新增 §7.5】上架算法 `WmsRackingService`

对应 §1.2.2。事务内写 `wms_physical_inventory`(SSOT, C4)，双 ID + `inbound_date` + `pick_order` + `zone_id` + `allocatable` 一次写齐。**关键修正：拆出纯写库存内部方法 `putawayInventory`（不改单据状态、不发回传），供常规上架与退货质检复用；单据状态流转与 OMS 回传由调用方唯一触发**，避免重复回传与提前 COMPLETED。

```java
public interface WmsRackingService {
    /** 仅写库存：逐行落位（可拆多库位），不改单据状态、不发回传。退货质检也复用此法。 */
    PutawayResult putawayInventory(PutawayRequest req);
    /** 常规入库上架：putawayInventory + 校验状态机 + 置 COMPLETED + 回传 onInboundCompleted。 */
    void completeInboundPutaway(Long inboundOrderId, PutawayRequest req);
}

@Data @Builder public class PutawayRequest {     // 补 @Builder（否则 builder() 不可编译）
    private Long inboundOrderId; private LocalDate inboundDate; private List<PutawayLine> lines;
}
@Data @Builder public class PutawayLine {
    private String skuCode; private Integer qty; private String quality;        // GOOD/DAMAGED
    private String targetZoneType;     // STANDARD/RETURN/DEFECTIVE；null=按quality推导
    private String preferredLocationCode;
}
```

```java
@Service @RequiredArgsConstructor
public class WmsRackingServiceImpl implements WmsRackingService {
    private final WmsInboundOrderMapper inboundOrderMapper;     // 类级 @InterceptorIgnore
    private final WmsInboundOrderItemMapper inboundItemMapper;
    private final WmsLocationMapper locationMapper;
    private final WmsPhysicalInventoryMapper inventoryMapper;
    private final SkuMapper skuMapper;                          // 仅 WMS 方法级 @InterceptorIgnore
    private final WmsZoneMapper zoneMapper;
    private final OmsCallbackService omsCallback;               // 同进程

    @Transactional(rollbackFor = Exception.class)
    public PutawayResult putawayInventory(PutawayRequest req) {
        Long wmsTenantId = WmsTenantContext.getCurrentWmsTenant();          // 货架归属
        WmsInboundOrder order = inboundOrderMapper.selectById(req.getInboundOrderId());
        Long erpTenantId = order.getTenantId();                            // 货物归属
        Long warehouseId = order.getWarehouseId();
        LocalDate inboundDate = req.getInboundDate()!=null ? req.getInboundDate() : LocalDate.now(ZoneOffset.UTC);
        PutawayResult result = new PutawayResult();
        for (PutawayLine line : req.getLines()) {
            BigDecimal unitVol = skuMapper.selectVolumeCbm(erpTenantId, line.getSkuCode());
            String zoneType = line.getTargetZoneType()!=null ? line.getTargetZoneType()
                              : ("GOOD".equals(line.getQuality()) ? "STANDARD" : "DEFECTIVE");
            int allocatable = zoneMapper.allocatableOfType(warehouseId, zoneType);   // 0/1，写入冗余列
            Long inboundItemId = inboundItemMapper.findItemId(order.getId(), line.getSkuCode());
            int remaining = line.getQty();
            // 规则2/3：同 SKU 已有库位剩余足够→并入；锚点就近
            List<LocationCapacityVO> existing = locationMapper.findSameSkuLocations(
                    wmsTenantId, erpTenantId, warehouseId, line.getSkuCode(), line.getQuality(), zoneType);
            String anchorRack = existing.isEmpty() ? null : existing.get(0).getRackNo();
            remaining = placeInto(existing, remaining, unitVol, wmsTenantId, erpTenantId, warehouseId,
                                  line, inboundItemId, inboundDate, allocatable, result);
            // 规则1：不存在/原库位放不下→就近新分配
            if (remaining > 0) {
                List<LocationCapacityVO> candidates = locationMapper.findAllocatableLocations(
                        wmsTenantId, warehouseId, zoneType, anchorRack);
                remaining = placeInto(candidates, remaining, unitVol, wmsTenantId, erpTenantId, warehouseId,
                                      line, inboundItemId, inboundDate, allocatable, result);
            }
            if (remaining > 0)
                throw new BizException(WmsResultCode.LOCATION_CAPACITY_INSUFFICIENT);   // 40020
            inboundItemMapper.addGoodOrDamagedQty(inboundItemId, line.getQuality(), line.getQty());
        }
        return result;   // 不改单据状态、不发回传
    }

    @Transactional(rollbackFor = Exception.class)
    public void completeInboundPutaway(Long inboundOrderId, PutawayRequest req) {
        WmsInboundOrder order = inboundOrderMapper.selectById(inboundOrderId);
        if (!"RECEIVING".equals(order.getStatus()) && !"PUTAWAY".equals(order.getStatus()))
            throw new BizException(WmsResultCode.QC_ILLEGAL_TRANSITION);    // 状态机守卫
        PutawayResult result = putawayInventory(req);
        order.setStatus("COMPLETED"); inboundOrderMapper.updateById(order);
        TransactionSyncUtil.afterCommit(() ->
            omsCallback.onInboundCompleted(order.getOmsOrderNo(), result.getReceivedQtyMap()));
    }

    private int placeInto(List<LocationCapacityVO> locs, int remaining, BigDecimal unitVol,
            Long wmsTenantId, Long erpTenantId, Long warehouseId, PutawayLine line,
            Long inboundItemId, LocalDate inboundDate, int allocatable, PutawayResult result) {
        for (LocationCapacityVO loc : locs) {
            if (remaining <= 0) break;
            locationMapper.lockLocation(loc.getLocationId());              // SELECT id ... FOR UPDATE
            BigDecimal usedVol = inventoryMapper.usedVolumeOfLocation(warehouseId, loc.getLocationCode());
            BigDecimal freeVol = loc.getMaxVolumeCbm().subtract(usedVol);
            int capQty = (unitVol.signum()<=0) ? remaining : freeVol.divide(unitVol,0,RoundingMode.DOWN).intValue();
            if (capQty <= 0) continue;
            int take = Math.min(remaining, capQty);
            int pickOrder = inventoryMapper.nextPickOrder(wmsTenantId, erpTenantId, warehouseId, line.getSkuCode(), inboundDate);
            inventoryMapper.upsertBatch(WmsPhysicalInventory.builder()     // @Builder 必须
                    .wmsTenantId(wmsTenantId).erpTenantId(erpTenantId).warehouseId(warehouseId)
                    .skuCode(line.getSkuCode()).inboundItemId(inboundItemId)
                    .zoneId(loc.getZoneId()).allocatable(allocatable)      // 落分区 + 冗余可分配标志
                    .inboundDate(inboundDate).pickOrder(pickOrder)
                    .quantity(take).reservedQty(0).quality(line.getQuality())
                    .locationCode(loc.getLocationCode()).version(0).build());
            result.addPlacement(line.getSkuCode(), loc.getLocationCode(), take);
            remaining -= take;
        }
        return remaining;
    }
}
```

`upsertBatch` 的 `ON DUPLICATE KEY` 命中修订后的 `uk_batch`(含 `location_code`)：同 `(inbound_item_id, location_code)` 数量累加，不同 `location_code` 插入平行批次行（决策 R 语义）。`WmsLocation`/`LocationCapacityVO` 返回 `zoneId`，`upsertBatch` 写入 `zone_id`/`allocatable`，保证新批次 `zone_id NOT NULL`、`allocatable` 有值（修复 zone_id 回填缺失导致的假缺货）。

> 衔接：容量判断用 §4.4.3 `wms_location.max_volume_cbm` 与 `sku.volume_cbm`；可分配新库位查询须 JOIN `wms_rack_assignment`(有效分配、归属本司)，无候选时抛 `RACK_NOT_ASSIGNED(40021)`。

### 【新增 §7.6】退货质检流程 `ReturnOrderService`

对应 §1.4。复用 `wms_inbound_order`(`source_type=RETURN`) + `wms_return_qc_item`(A4)。状态机 `RETURN_PENDING → QC_PENDING →(QC_PASS/QC_FAIL)→ PUTAWAY → COMPLETED`（附录 B）。**回库走 `putawayInventory`（只写库存），单据状态与 OMS 回传由 `finishIfAllQcDone` 唯一触发**（修复重复回传/提前 COMPLETED/语义错配）。

```java
public interface ReturnOrderService {
    WmsInboundOrder createReturnOrder(ReturnReceiveDTO dto);
    void startQualityCheck(Long inboundOrderId, Long qcUserId);
    void passQualityCheck(QcPassDTO dto);
    void failQualityCheck(QcFailDTO dto);
}
```

```java
@Transactional(rollbackFor = Exception.class)
public WmsInboundOrder createReturnOrder(ReturnReceiveDTO dto) {   // RETURN_PENDING→QC_PENDING，库存无影响
    Long wmsTenantId = WmsTenantContext.getCurrentWmsTenant();
    Long erpTenantId = dto.getErpTenantId();
    WmsInboundOrder o = new WmsInboundOrder();
    o.setTenantId(erpTenantId); o.setWmsOrderNo(WmsNoGen.gen("RTN")); o.setOmsOrderNo(dto.getOmsOrderNo());
    o.setWarehouseId(dto.getWarehouseId()); o.setSourceType("RETURN"); o.setStatus("RETURN_PENDING");
    inboundOrderMapper.insert(o);
    for (ReturnLine l : dto.getLines()) {
        WmsInboundOrderItem it = new WmsInboundOrderItem();
        it.setInboundOrderId(o.getId()); it.setTenantId(erpTenantId); it.setSkuCode(l.getSkuCode());
        it.setExpectedQty(l.getExpectedQty()); it.setReceivedQty(l.getReceivedQty());
        inboundItemMapper.insert(it);
        WmsReturnQcItem qc = new WmsReturnQcItem();
        qc.setWmsTenantId(wmsTenantId); qc.setErpTenantId(erpTenantId); qc.setInboundOrderId(o.getId());
        qc.setSkuCode(l.getSkuCode()); qc.setQcStatus("QC_PENDING"); qc.setQcQty(l.getReceivedQty());
        returnQcItemMapper.insert(qc);
    }
    o.setStatus("QC_PENDING"); inboundOrderMapper.updateById(o);
    TransactionSyncUtil.afterCommit(() -> omsCallback.onReturnStatus(o.getOmsOrderNo(), "QC_PENDING"));
    return o;
}

@Transactional(rollbackFor = Exception.class)
public void startQualityCheck(Long inboundOrderId, Long qcUserId) {
    WmsInboundOrder o = inboundOrderMapper.selectById(inboundOrderId);
    if (!"QC_PENDING".equals(o.getStatus())) throw new BizException(WmsResultCode.QC_ILLEGAL_TRANSITION); // 40030
    returnQcItemMapper.assignQcUser(inboundOrderId, qcUserId, LocalDateTime.now(ZoneOffset.UTC));
}

@Transactional(rollbackFor = Exception.class)
public void passQualityCheck(QcPassDTO dto) {
    WmsInboundOrder o = inboundOrderMapper.selectById(dto.getInboundOrderId());
    LocalDateTime now = LocalDateTime.now(ZoneOffset.UTC);
    List<PutawayLine> lines = new ArrayList<>();
    for (QcPassLine l : dto.getLines()) {
        String zone = l.getTargetZoneType()!=null ? l.getTargetZoneType() : "RETURN";
        returnQcItemMapper.markResult(dto.getInboundOrderId(), l.getSkuCode(), "QC_PASS", l.getQcQty(),
                zone, null, dto.getQcUserId(), now);
        lines.add(PutawayLine.builder().skuCode(l.getSkuCode()).qty(l.getQcQty()).quality("GOOD").targetZoneType(zone).build());
    }
    rackingService.putawayInventory(PutawayRequest.builder()   // 仅写库存，不置 COMPLETED、不回传
            .inboundOrderId(o.getId()).inboundDate(LocalDate.now(ZoneOffset.UTC)).lines(lines).build());
    finishIfAllQcDone(o);
}

@Transactional(rollbackFor = Exception.class)
public void failQualityCheck(QcFailDTO dto) {
    WmsInboundOrder o = inboundOrderMapper.selectById(dto.getInboundOrderId());
    LocalDateTime now = LocalDateTime.now(ZoneOffset.UTC);
    List<PutawayLine> lines = new ArrayList<>();
    for (QcFailLine l : dto.getLines()) {
        boolean electronic = skuMapper.isElectronicCategory(o.getTenantId(), l.getSkuCode());
        if (electronic && StringUtils.isBlank(l.getPhotoUrl()))
            throw new BizException(WmsResultCode.QC_PHOTO_REQUIRED);   // 40031, BR-04
        returnQcItemMapper.markResult(dto.getInboundOrderId(), l.getSkuCode(), "QC_FAIL", l.getQcQty(),
                "DEFECTIVE", l.getPhotoUrl(), dto.getQcUserId(), now);
        lines.add(PutawayLine.builder().skuCode(l.getSkuCode()).qty(l.getQcQty()).quality("DAMAGED").targetZoneType("DEFECTIVE").build());
    }
    rackingService.putawayInventory(PutawayRequest.builder()
            .inboundOrderId(o.getId()).inboundDate(LocalDate.now(ZoneOffset.UTC)).lines(lines).build());
    finishIfAllQcDone(o);
}

private void finishIfAllQcDone(WmsInboundOrder o) {            // 唯一状态流转 + 唯一回传点
    if (returnQcItemMapper.countPending(o.getId()) == 0) {
        if (!"QC_PENDING".equals(o.getStatus()) && !"PUTAWAY".equals(o.getStatus()))
            throw new BizException(WmsResultCode.QC_ILLEGAL_TRANSITION);
        o.setStatus("COMPLETED"); inboundOrderMapper.updateById(o);
        TransactionSyncUtil.afterCommit(() -> omsCallback.onInboundCompleted(o.getOmsOrderNo(), null));
    }
}
```

| 方法 | 状态流转 | `wms_physical_inventory` | OMS 回传(同进程) |
|---|---|---|---|
| createReturnOrder | RETURN_PENDING→QC_PENDING | 无 | onReturnStatus=QC_PENDING |
| startQualityCheck | QC_PENDING(登记质检员) | 无 | 无 |
| passQualityCheck | (全部完结时)→COMPLETED | +GOOD 批次(新 inbound_date，allocatable=1，可分配) | onInboundCompleted（仅全部完结） |
| failQualityCheck | (全部完结时)→COMPLETED | +DAMAGED 批次(DEFECTIVE，allocatable=0) | onInboundCompleted（仅全部完结） |

> 次品天然不可分配：FIFO 查询条件 `quality='GOOD' AND allocatable=1`（C7），DAMAGED/不良品区批次自动排除，无需额外开关。多 SKU 分批质检时不会在第一次回库即提前完结。

---

## 五、§九 数据迁移方案增量

> 前置：附录 A `wms_warehouse/wms_zone/wms_location/wms_return_qc_item/wms_stock_take/oms_forecast_rule` 及 A7(`wms_rack_assignment.contract_file_url`) 已先建表；基线 §4.4 `wms_physical_inventory` 已建。执行前 `mysqldump` 备份（§9.2）。本脚本是**唯一 DDL 事实源**，正文小节只引用。

```sql
-- ========== migration/04_schema_increment.sql ==========

-- [1] 仓库结构参数（驱动库位批量生成）
ALTER TABLE wms_warehouse
  ADD COLUMN rack_rows             INT         NOT NULL DEFAULT 0  COMMENT '排数(rack 数量,结构参数)' AFTER contact_phone,
  ADD COLUMN rack_layers           INT         NOT NULL DEFAULT 0  COMMENT '每排层数'               AFTER rack_rows,
  ADD COLUMN rack_columns          INT         NOT NULL DEFAULT 0  COMMENT '每排列数'               AFTER rack_layers,
  ADD COLUMN rack_no_prefix        VARCHAR(10) NOT NULL DEFAULT '' COMMENT '排号前缀(A→A1/A2;空=纯数字)' AFTER rack_columns,
  ADD COLUMN code_pad_width        TINYINT     NOT NULL DEFAULT 2  COMMENT '层/列补零位宽(2→03)'    AFTER rack_no_prefix,
  ADD COLUMN default_location_type VARCHAR(20)          DEFAULT 'BIG' COMMENT '默认库位类型 BIG/SMALL(同源 sys_dict)' AFTER code_pad_width,
  ADD COLUMN location_generated    TINYINT     NOT NULL DEFAULT 0  COMMENT '库位已生成幂等标记'      AFTER default_location_type;

-- [2] 物理库存：分区维度 + 冗余可分配标志 + uk_batch 修订(决策 R) + FIFO 索引
ALTER TABLE wms_physical_inventory
  ADD COLUMN zone_id     BIGINT  DEFAULT NULL COMMENT '所在分区 wms_zone.id(报表/下钻)' AFTER location_code,
  ADD COLUMN allocatable TINYINT NOT NULL DEFAULT 1 COMMENT '冗余:1=可分配(标准/退货) 0=不可(不良品/暂存),上架/质检落值' AFTER zone_id,
  DROP INDEX uk_batch,
  ADD UNIQUE KEY uk_batch (wms_tenant_id, erp_tenant_id, warehouse_id, sku_code, inbound_item_id, location_code),
  ADD INDEX idx_zone (zone_id),
  ADD INDEX idx_fifo_alloc (wms_tenant_id, erp_tenant_id, warehouse_id, sku_code, quality, allocatable, inbound_date, pick_order, id);

-- [2b] 回填 zone_id / allocatable（防历史库存被排除→假缺货）。allocatable 默认1已保证过渡期可分配
UPDATE wms_physical_inventory i
  JOIN wms_location l ON l.warehouse_id=i.warehouse_id AND l.location_code=i.location_code
  SET i.zone_id = l.zone_id WHERE i.zone_id IS NULL;
UPDATE wms_physical_inventory i
  JOIN wms_zone z ON z.id=i.zone_id
  SET i.allocatable = z.allocatable WHERE i.zone_id IS NOT NULL;

-- [3] 库位容量（上架容量判断）
ALTER TABLE wms_location
  ADD COLUMN max_volume_cbm DECIMAL(10,4) DEFAULT NULL COMMENT '库位容量(m³),由库位类型预设',
  ADD COLUMN max_weight_kg  DECIMAL(10,2) DEFAULT NULL COMMENT '库位承重上限(kg)';

-- [4] SKU 仓储属性（称重量方/容量/入库 m³ 计费）
ALTER TABLE sku
  ADD COLUMN weight_kg  DECIMAL(10,3) DEFAULT NULL COMMENT 'SKU单重kg',
  ADD COLUMN length_cm  DECIMAL(10,2) DEFAULT NULL COMMENT '长cm',
  ADD COLUMN width_cm   DECIMAL(10,2) DEFAULT NULL COMMENT '宽cm',
  ADD COLUMN height_cm  DECIMAL(10,2) DEFAULT NULL COMMENT '高cm',
  ADD COLUMN volume_cbm DECIMAL(10,6) DEFAULT NULL COMMENT '单件体积m³=L*W*H/1e6(冗余)';

-- [5] 退货质检命名归一(决策 Q,仅刷注释)
ALTER TABLE wms_return_qc_item
  MODIFY COLUMN qc_status VARCHAR(20) NOT NULL DEFAULT 'QC_PENDING'
  COMMENT '质检状态(权威字段) QC_PENDING/QC_PASS/QC_FAIL;别名quality_status仅文档用,代码统一qc_status';

-- [6] 自定义出库单:手工收件人(MANUAL 必填,应用层校验)
ALTER TABLE oms_outbound_order
  ADD COLUMN receiver_name     VARCHAR(100) DEFAULT NULL COMMENT '收件人姓名(MANUAL必填)'  AFTER source_type,
  ADD COLUMN receiver_phone    VARCHAR(40)  DEFAULT NULL COMMENT '收件人电话(MANUAL必填)'  AFTER receiver_name,
  ADD COLUMN receiver_country  VARCHAR(50)  DEFAULT NULL COMMENT '收件国家'               AFTER receiver_phone,
  ADD COLUMN receiver_province VARCHAR(50)  DEFAULT NULL COMMENT '收件州/省'              AFTER receiver_country,
  ADD COLUMN receiver_city     VARCHAR(50)  DEFAULT NULL COMMENT '收件城市'              AFTER receiver_province,
  ADD COLUMN receiver_address  VARCHAR(255) DEFAULT NULL COMMENT '收件详细地址(MANUAL必填)' AFTER receiver_city,
  ADD COLUMN receiver_postcode VARCHAR(20)  DEFAULT NULL COMMENT '邮编'                  AFTER receiver_address;

-- [7] WMS 出库单镜像收件字段(自定义单打面单/签出)
ALTER TABLE wms_outbound_order
  ADD COLUMN receiver_name     VARCHAR(100) DEFAULT NULL COMMENT '收件人姓名' AFTER erp_order_id,
  ADD COLUMN receiver_phone    VARCHAR(40)  DEFAULT NULL COMMENT '收件人电话' AFTER receiver_name,
  ADD COLUMN receiver_country  VARCHAR(50)  DEFAULT NULL COMMENT '收件国家'   AFTER receiver_phone,
  ADD COLUMN receiver_province VARCHAR(50)  DEFAULT NULL COMMENT '收件州/省'  AFTER receiver_country,
  ADD COLUMN receiver_city     VARCHAR(50)  DEFAULT NULL COMMENT '收件城市'   AFTER receiver_province,
  ADD COLUMN receiver_address  VARCHAR(255) DEFAULT NULL COMMENT '收件详细地址' AFTER receiver_city,
  ADD COLUMN receiver_postcode VARCHAR(20)  DEFAULT NULL COMMENT '邮编'        AFTER receiver_address;

-- [8] 自定义入库单:头程跟踪号(收货可按跟踪号检索)
ALTER TABLE oms_inbound_order
  ADD COLUMN tracking_no VARCHAR(128) DEFAULT NULL COMMENT '头程跟踪单号' AFTER expected_arrival_date;

-- [9] 单据状态枚举补全(仅刷注释/默认值)
ALTER TABLE oms_inbound_order  MODIFY COLUMN status VARCHAR(20) NOT NULL DEFAULT 'DRAFT'
  COMMENT 'DRAFT/PENDING/SENT/RECEIVING/COMPLETED/CANCELLED';
ALTER TABLE oms_outbound_order MODIFY COLUMN status VARCHAR(20) NOT NULL DEFAULT 'PENDING'
  COMMENT 'PENDING/SENT/PICKING/PACKED/SHIPPED/COMPLETED/CANCELLED/BACKORDER;ACCEPTED=保留(并入SENT)';
ALTER TABLE wms_inbound_order  MODIFY COLUMN status VARCHAR(20) NOT NULL DEFAULT 'PENDING'
  COMMENT 'PENDING待收货/RECEIVING收货中/PUTAWAY待上架/COMPLETED上架完成/CANCELLED;退货:RETURN_PENDING/QC_PENDING/PUTAWAY/COMPLETED';
ALTER TABLE wms_outbound_order MODIFY COLUMN status VARCHAR(20) NOT NULL DEFAULT 'PENDING'
  COMMENT 'PENDING/ALLOCATING/PICKING/PACKING/PACKED/SHIPPED/COMPLETED/CANCELLED';

-- [10] 补建库存流水 wms_stock_flow(决策 S,双 ID,基线缺 DDL)
CREATE TABLE wms_stock_flow (
  id            BIGINT      NOT NULL AUTO_INCREMENT,
  wms_tenant_id BIGINT      NOT NULL COMMENT '货架归属 WMS 服务商(第一层)',
  erp_tenant_id BIGINT      NOT NULL COMMENT '货物归属货主(第二层)',
  warehouse_id  BIGINT      NOT NULL,
  sku_code      VARCHAR(64) NOT NULL,
  change_type   VARCHAR(20) NOT NULL COMMENT 'INBOUND/OUTBOUND/STOCKTAKE/ADJUSTMENT/RETURN',
  quantity_delta INT        NOT NULL COMMENT '变动量(+入/-出)',
  quantity_after INT        NOT NULL COMMENT '变动后结存',
  biz_type      VARCHAR(50) COMMENT '溯源单据类型',
  biz_id        BIGINT      COMMENT '溯源单据 id',
  biz_no        VARCHAR(64) COMMENT '溯源单号',
  location_code VARCHAR(50),
  inbound_item_id BIGINT    COMMENT '批次溯源',
  operator_id   BIGINT,
  occurred_at   DATETIME    NOT NULL COMMENT 'UTC',
  create_time   DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (id),
  KEY idx_erp_sku (erp_tenant_id, sku_code, occurred_at),
  KEY idx_wms_sku (wms_tenant_id, sku_code, occurred_at),
  KEY idx_biz (biz_type, biz_id)
) ENGINE=InnoDB COMMENT='WMS库存变动流水(双ID,SSOT 留痕)';

-- [11] 库存写权限解绑(货主侧盘点/调整下线,决策 T)  见 §11.7
-- [12] 库位类型码值固化(BIG/SMALL,与 wms_warehouse.default_location_type 同源)
INSERT INTO sys_dict (dict_code, title, remark) VALUES ('wms_location_type','库位类型','BIG/SMALL')
  ON DUPLICATE KEY UPDATE title=VALUES(title);
INSERT INTO sys_dict_item (dict_code, value, name, sort) VALUES
  ('wms_location_type','BIG','大库位',1), ('wms_location_type','SMALL','小库位',2)
  ON DUPLICATE KEY UPDATE name=VALUES(name);
-- ========== end ==========
```

**回滚（接 §9.3）**：按相反顺序 `DROP COLUMN`；`wms_physical_inventory` 先 `DROP INDEX idx_fifo_alloc/idx_zone`，再把 `uk_batch` 还原为基线五列键，最后 `DROP COLUMN allocatable, zone_id`；`DROP TABLE wms_stock_flow`；不删除附录 A 既有表。

---

## 六、§十 OMS ↔ WMS 接口契约增量

### 【新增 §10.2】OMS→WMS 补 `cancelInboundOrder`

基线 §10.2 OMS→WMS 仅 `createOutboundOrder/createInboundOrder/cancelOutboundOrder/getInventorySnapshot`，缺入库取消。补：

```
OMS → WMS   wmsService.cancelInboundOrder(omsOrderNo)   取消入库单（仅 WMS 仍 PENDING 未到货时可取消，与 cancelOutboundOrder 对齐）
```

### 【新增 §10.2】WMS→OMS 同进程回调（沿用 §7.3 纠偏后的同进程模型）

在 §10.2 已有 `onOutboundShipped/onInboundCompleted/onStockInsufficient` 基础上补 5 个回调（独立事务、`afterCommit`、失败记 ERROR）：

```
WMS → OMS   omsService.onInboundReceiving(omsOrderNo)              入库【标记到货】    SENT→RECEIVING
WMS → OMS   omsService.onInboundReceived(omsOrderNo, receivedMap)  入库【确认收货】    RECEIVING(不变,回写实收数)
WMS → OMS   omsService.onOutboundPicking(omsOrderNo)               出库【下架】FIFO成功 SENT→PICKING(订单锁定)
WMS → OMS   omsService.onOutboundPacked(omsOrderNo)                出库【打包完成】    PICKING→PACKED
WMS → OMS   omsService.onOutboundCompleted(omsOrderNo)             出库流程结束        SHIPPED→COMPLETED
WMS → OMS   omsService.onReturnStatus(omsOrderNo, qcStatus)        退货质检阶段回传    （退货单状态刷新）
```

---

## 七、§11 前端改造增量

### 【更新 §11.4】三入口完整路由 + BallCat 菜单映射

> `menu type`：`0=目录/1=菜单/2=按钮`；`来源`：基线=原 §11.4 已列、增量=本次新增。数据可见性由后端按角色过滤（§5.2/§5.3），前端路由不做隔离。
> **统一口径（修正跨维度命名冲突）**：盘点真实路径为 `/wms/stocktake`、权限前缀 `wms:stocktake`（依实测控制器，"stocktake"为单一资源词，非 `stock-take`）；调整为 `/wms/adjustment`、`wms:adjustment`。

**平台超管入口（`ROLE_SUPER_ADMIN`）**

| path | 页面 | permission | type | 要点 | 来源 |
|---|---|---|---|---|---|
| `/admin/warehouses` | 仓库管理 | `wms:warehouse:list` | 1 | — | 基线 |
| `/admin/warehouses/create` | 仓库创建(结构参数→批量生成库位) | `wms:warehouse:create` | 1 | hidden;写 `wms_warehouse/wms_zone/wms_location` | 增量 |
| `/admin/warehouses/preview` | 仓库预览(二维) | `wms:warehouse:preview` | 1 | 数据源 §7.4 | 增量 |
| `/admin/racks` | 货架管理 | `wms:rack:list` | 1 | — | 基线 |
| `/admin/racks/assign` | 货架分配(多排+月租+起止+合同) | `wms:rack:assign` | 1 | hidden;写 `wms_rack_assignment`(A7);`uk_rack_period` 冲突校验 | 增量 |
| `/admin/wms-tenants` | WMS 服务商开通 | `admin:wms-tenant:list` | 1 | 写 `sys_tenant(WMS_OPERATOR)`+授 `ROLE_WMS_MANAGER` | 基线 |
| `/wms/stocktake` | 盘点管理(物理盘点,修正 SSOT) | `wms:stocktake:read` | 1 | **唯一一条共享菜单**,绑 `ROLE_SUPER_ADMIN/ROLE_WMS_MANAGER`;写 `wms_stock_take/wms_physical_inventory/wms_stock_flow` | 增量 |
| `/wms/adjustment` | 库存调整 | `wms:adjustment:read` | 1 | 绑 `ROLE_SUPER_ADMIN/ROLE_WMS_MANAGER` | 增量 |
| `/admin/billing/receivable` | 应收账单(链路一) | `wms:receivable:list` | 1 | `wms_monthly_bill`;DRAFT→CONFIRMED→PAID/DISPUTED | 增量 |
| `/admin/fee-rate-cards` | 费率卡 | `wms:fee-rate:list` | 1 | `wms_fee_rate_card` | 增量 |
| `/admin/rate-discounts` | 服务商费率折扣 | `wms:rate-discount:list` | 1 | `wms_tenant_rate_discount`(决策 L) | 增量 |
| `/admin/reports` | 跨服务商报表 | `admin:report:view` | 1 | 无过滤 | 基线 |

**WMS 服务商入口（`ROLE_WMS_MANAGER/OPERATOR`）**

| path | 页面 | permission | type | 要点 | 来源 |
|---|---|---|---|---|---|
| `/wms/inbound-ops/receive` | 收货(单号/箱号/跟踪号) | `wms:inbound:receive` | 1 | 回传 `oms_inbound_order=RECEIVING` | 增量 |
| `/wms/inbound-ops/putaway` | 上架(选库位→批次) | `wms:inbound:putaway` | 1 | 写 SSOT;刷 OMS 快照 | 增量 |
| `/wms/outbound-ops/picking` | 订单下架(三分拣模式) | `wms:outbound:pick` | 1 | FIFO 行锁;BR-01 | 增量 |
| `/wms/outbound-ops/packing` | 打包(四模式) | `wms:outbound:pack` | 1 | — | 增量 |
| `/wms/outbound-ops/checkout` | 订单签出(称重/扣 SSOT) | `wms:outbound:checkout` | 1 | 扣 `quantity` 释放 `reserved_qty`;生 `wms_client_billing_record`;BR-04 | 增量 |
| `/wms/return-qc` | 退货质检 | `wms:return:qc` | 1 | 写 `wms_return_qc_item`+SSOT;BR-04 | 增量 |
| `/wms/inventory` | 库存总览(`wms_tenant_id`) | `wms:inventory:view` | 1 | 只读 SSOT | 基线 |
| `/wms/logistics-products` | 物流产品管理 | `wms:logistics-product:list` | 1 | `wms_logistics_product(_grant)` | 基线 |
| `/wms/my-clients` | 开通货主 | `wms:client:list` | 1 | 写 `sys_tenant(ERP_USER,parent_wms_tenant_id)` | 基线 |
| `/wms/client-bills` | 收入(链路二) | `wms:client-bill:list` | 1 | `wms_client_monthly_bill` | 基线 |
| `/wms/my-bills` | 支出(链路一,只读) | `wms:my-bill:view` | 1 | `wms_monthly_bill` | 基线 |

**货主 ERP 入口（`ROLE_TENANT_ADMIN/USER`）**

| path | 页面 | permission | type | 要点 | 来源 |
|---|---|---|---|---|---|
| `/oms/inbound` | 采购入库单 | `oms:inbound:list` | 1 | `source_type=PURCHASE` | 基线 |
| `/oms/custom-inbound` | 自定义入库单 | `oms:custom-inbound:list` | 1 | `source_type=MANUAL`,`purchase_order_id IS NULL` | 增量 |
| `/oms/outbound` | 销售出库单 | `oms:outbound:list` | 1 | `source_type=PLATFORM`;锁价;BR-03 | 基线 |
| `/oms/custom-outbound` | 自定义出库单 | `oms:custom-outbound:list` | 1 | `source_type=MANUAL`,`erp_order_id IS NULL` | 增量 |
| `/oms/return` | 退货订单 | `oms:return:list` | 1 | `source_type=RETURN`;可见质检结果 | 增量 |
| `/oms/custom-return` | 自定义退货单 | `oms:custom-return:list` | 1 | 手填退货 SKU;复用入库+质检（`:create` 为 type=2 按钮） | 增量 |
| `/oms/inventory/overview` | 库存总览(只读) | `oms:inventory:overview` | 1 | 同进程调 WMS,`erp_tenant_id` 聚合 | 增量 |
| `/oms/inventory/detail` | 库存明细(只读) | `oms:inventory:detail` | 1 | 100% `wms_physical_inventory`;无写按钮 | 增量 |
| `/oms/inventory/record` | 库存记录(只读) | `oms:inventory:record` | 1 | 只读 `wms_stock_flow`(§4.8) | 增量 |
| `/oms/forecast` | 库存预测 | `oms:forecast:view` | 1 | `oms_forecast_rule`(A6) | 增量 |
| `/oms/logistics-products` | 可用物流产品 | `oms:logistics-product:view` | 1 | 只读授权产品 | 基线 |
| `/wms/bills` | 操作计费明细(链路一) | `oms:op-bill:view` | 1 | `wms_billing_record WHERE erp_tenant_id`;决策 N | 基线 |
| `/wms/logistics-bills` | 物流费账单(链路二) | `oms:logistics-bill:view` | 1 | `wms_client_monthly_bill` | 基线 |

> 货主侧 `/wms/stocktake` 盘点**不再单独建货主菜单**——它是上表超管/WMS 的同一条共享菜单，仅对货主角色解绑（§11.7），不软删菜单本体。

### 【新增 §11.4.1】按钮级（type=2）权限点

| 归属菜单 | 按钮 | permission |
|---|---|---|
| 收货 | 标记到货/确认收货/无码生码 | `wms:inbound:receive:arrive` / `:confirm` / `:barcode` |
| 上架 | 确认上架(写 SSOT) | `wms:inbound:putaway:confirm` |
| 订单下架 | 集中/按单/二次分拣 | `wms:outbound:pick:batch` / `:single` / `:second` |
| 打包 | 按SKU/按单/二次分拣/装箱 | `wms:outbound:pack:bysku` / `:byorder` / `:second` / `:carton` |
| 订单签出 | 称重/签出扣减/拍照 | `wms:outbound:checkout:weigh` / `:confirm` / `:photo` |
| 退货质检 | 通过/失败/拍照 | `wms:return:qc:pass` / `:fail` / `:photo` |
| 货架分配 | 合同上传 | `wms:rack:assign:upload` |
| 应收账单 | 出账/确认/标记付款 | `wms:receivable:generate` / `:confirm` / `:pay` |
| 物流产品 | 新建/上下架/授权/专属价 | `wms:logistics-product:create` / `:publish` / `:grant` / `:price` |
| 开通货主 | 开户/启停/配可用仓区 | `wms:client:create` / `:enable` / `:scope` |
| 库存预测 | 固定值/自定义 | `oms:forecast:fixed` / `:custom` |
| 自定义出库单 | 选物流产品/提交推送 | `oms:custom-outbound:select-product` / `:submit` |
| 自定义退货单 | 创建提交 | `oms:custom-return:create` |
| 盘点 | 新建/录入/确认/取消/删除 | `wms:stocktake:add` / `:input` / `:confirm` / `:cancel` / `:del` |
| 库存调整 | 新建/编辑/确认/取消/删除 | `wms:adjustment:add` / `:edit` / `:confirm` / `:cancel` / `:del` |

### 【新增 §11.5】permission 命名规范

格式 `{域}:{资源}:{动作}[:{子动作}]`，全小写，资源内多词 kebab-case（与 BallCat `system:user:read` 一致）。域：`admin`(超管管理)/`wms`(物理执行+计费)/`oms`(货主单据+只读视图)。`type=0` 不设 permission；`type=1` 设 `:list`/`:read`/`:view`；`type=2` 设到动作/子动作。约定例外：`stocktake` 依实测控制器作单一资源词（不拆 `stock-take`），保持与既有代码一致。

### 【新增 §11.6】角色与 `sys_menu` 绑定

BallCat RBAC 数据驱动（`/system/menu/router` 按角色下发，未授权即路由不存在）；`sys_role_menu` 以 `role_code` 关联。系统表 `sys_menu` 用其自带 `deleted` 逻辑删除列（不套 `deleted_at`）。

| 角色 | 授权前缀 |
|---|---|
| `ROLE_SUPER_ADMIN` | `admin:*` + `wms:*`（含 `wms:stocktake:*`、`wms:adjustment:*`、`wms:receivable:*`、费率卡/折扣） |
| `ROLE_WMS_MANAGER` | `wms:inbound:* / outbound:* / return:* / inventory:* / logistics-product:* / client:* / client-bill:* / my-bill:view / stocktake:* / adjustment:*` |
| `ROLE_WMS_OPERATOR` | 仅货物操作（§1.6）：`wms:inbound:receive / inbound:putaway / outbound:pick / outbound:pack / outbound:checkout / return:qc` |
| `ROLE_TENANT_ADMIN` | `oms:*` 全量 + 本租户 `system:*` |
| `ROLE_TENANT_USER` | `oms:*` 业务操作（不含租户内系统管理） |

### 【新增 §11.7】货主侧盘点 / 调整菜单解绑（修正 path 冲突）

落地 §2.7.1 与 C4/决策 T。**关键修正**：盘点/调整菜单是平台/WMS 与货主共享的同一条 BallCat 记录，**不软删菜单本体**（软删会误删超管/WMS 仍在用的活动菜单，且 §11.4 与删除表同 path 自相矛盾）——只解除货主角色绑定。

```sql
-- 仅解除货主角色对盘点/调整菜单(含子按钮)的授权;不动菜单本体、不按 path 误删
DELETE rm FROM sys_role_menu rm
JOIN sys_menu m ON rm.menu_id = m.id
WHERE rm.role_code IN ('ROLE_TENANT_ADMIN','ROLE_TENANT_USER')
  AND (m.permission LIKE 'wms:stocktake:%' OR m.permission LIKE 'wms:adjustment:%');
```

菜单为 RBAC 驱动，解绑后货主入口不再下发该路由；阶段3 再物理删除货主前端 `views/wms/stocktake/*` 等（§6.6 过渡三步）。盘点/调整能力归位至 `/wms/stocktake`(`wms:stocktake:*`)、`/wms/adjustment`(`wms:adjustment:*`)，仅绑 `ROLE_SUPER_ADMIN/ROLE_WMS_MANAGER`，与货主彻底隔离。

---

## 八、§十五 待决策清单增量（续 决策 P）

| # | 决策 | 结论 |
|---|---|---|
| **P** | 自定义出/入库单 共用 vs 独立 | **共用** `oms_outbound_order`/`oms_inbound_order`，靠 `source_type`(+`erp_order_id`/`purchase_order_id` 空判)判别，不独立建表、不加 `order_type`。理由：共用租户隔离/状态机/Outbox/WMS 下游统一处理；独立表无收益反增双表漂移与 UNION 成本。仅"字段集差异极大/状态机完全不同/合规物理分表"才拆，当前均不成立 |
| **Q** | 退货质检字段命名归一 | 权威字段 `wms_return_qc_item.qc_status`(`QC_PENDING/QC_PASS/QC_FAIL`)，`quality_status` 仅文档别名；代码/DTO/VO/Mapper 一律 `qc_status`。它与 `wms_physical_inventory.quality`(GOOD/DAMAGED) 经映射表对应，非同一字段 |
| **R** | `wms_physical_inventory` 批次模型 | 双 ID 不变；`uk_batch` 按联动链(07→08→09→11/12→15)修订纳入 `location_code`(支持单 SKU 多库位拆分,业务 §1.2.2)；补 `zone_id`(报表)+`allocatable`(冗余,免 FIFO JOIN/锁放大)。"并入"=同(inbound_item,location)累加,不同 location=平行批次行;FIFO `inbound_date→pick_order→id` 不变 |
| **S** | 补建 `wms_stock_flow` | 基线缺 DDL,按联动链补建双 ID(`wms_tenant_id`+`erp_tenant_id`)库存流水表,统一 §3.5 单 `tenant_id` 旧表述;入库/出库/盘点/调整/退货全部留痕 |
| **T** | 货主侧库存写入口下线 + §7.3 回调纠偏 | 货主侧盘点/调整下线为只读快照(C4):撤 `sys_role_menu` 授权 + Service 守卫(40301/40310/40311),盘点/调整归平台/WMS;`WmsCallbackService` 由 `OmsApiClient`(REST) 纠偏为直接注入 `omsService` 同进程调用(对齐 C3) |

---

*本增量方案与《设计方案.md》《业务需求说明书.md》配套：数据结构以《设计方案.md》为准，业务流程以《业务需求说明书.md》为准；本文新增/修改章节如与基线冲突，以本文已注明的修订（决策 P～T、§九 唯一 DDL 源、§5.5 错误码登记）为准。*

---

相关文件路径：
- 基线设计：`C:\Users\86178\Desktop\hyldsys-erp-master(1)\hyldsys-erp-master\设计方案.md`
- 增量需求：`C:\Users\86178\Desktop\hyldsys-erp-master(1)\hyldsys-erp-master\业务需求说明书.md`
