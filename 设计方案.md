# ERP 多租户改造 + OMS/WMS 拆分 完整技术设计方案

> 🟥 **勘误（2026-06-28）**：经核对真实代码，本文部分对现状的假设有误，**以《现状-设计差异对照表.md》为准**。要点：① WMS 执行层（入库/出库/退货/盘点/调拓/库存/预测）**已实现**，相关为"改造"非"新建"；② 库存采用**方案②**（新建 `wms_physical_inventory` 批次 SSOT，现有 `wms_inventory` 降级聚合）；③ 软删除全项目用 **`deleted`+`@TableLogic`**（非本文写的 `deleted_at`）；④ `wms_stock_flow`/`wms_warehouse`/`wms_stocktake_order` 已存在。施工请看《改造实施路线图.md》。

> 文档状态：进行中（部分章节已确认）
> 最后更新：2026-06-27
> 基于代码库：`erp-backend`（Spring Boot 2.7.18 + BallCat 2.0.0-SNAPSHOT + Java 8）
>
> **讨论记录**
> - 2026-06-27 ✅ 决策A：WMS 不独立部署，采用模块化单体 + RBAC 隔离
> - 2026-06-27 ✅ 决策B：WMS 共用现有 MySQL 实例（同库，`wms_` 前缀表），无需新容器
> - 2026-06-27 ✅ 租户隔离性能：共享表 + `tenant_id` 方案可行，前提是所有索引以 `tenant_id` 为首列
> - 2026-06-27 ✅ 决策D：租户与 WMS 用户均由系统管理员后台手动开通，无自助注册
> - 2026-06-27 ✅ 决策E（修订）：BallCat 菜单沿用当前粒度；前端登录页提供「货主 ERP」/「WMS 服务商」/「平台超级管理员」**三个入口**，角色不符拒绝进入
> - 2026-06-27 ✅ 决策F：一个用户只属于一个租户，`sys_user_tenant` 对 `user_id` 做唯一约束，不支持跨租户切换
> - 2026-06-27 ✅ 决策G（双层租户架构）：租户体系分为两层——**WMS 服务商**（第一层，3PL 运营方，租用货架、按量计费）和 **ERP 货主**（第二层，由 WMS 服务商开通，管理自己的 SKU/订单）；平台超管管理全部 WMS 服务商；计费以 WMS 服务商为单位出账（汇总其下所有货主的操作量）
> - 2026-06-27 ✅ 决策H：货架按编号（1、2、3…排）由平台超管分配给 WMS 服务商，货架租金按分配排数 × 月单价自动计算

---

## 目录

1. [现状分析](#一现状分析)
2. [整体架构](#二整体架构)
3. [租户隔离策略](#三租户隔离策略)
4. [数据库 Schema 设计](#四数据库-schema-设计)
   - 4.1 [租户核心表（MySQL-A）](#41-租户核心表mysql-a)
   - 4.2 [现有业务表迁移](#42-现有业务表迁移批量添加-tenant_id)
   - 4.3 [OMS 新增表](#43-oms-新增表)
   - 4.4 [WMS 独立数据库（MySQL-B）](#44-wms-独立数据库mysql-b)
5. [多租户核心代码](#五多租户核心代码)
6. [OMS 模块设计](#六oms-模块设计)
7. [WMS 独立服务设计](#七wms-独立服务设计)
8. [计费系统设计](#八计费系统设计)
9. [数据迁移方案](#九数据迁移方案)
10. [OMS ↔ WMS 接口契约](#十oms--wms-接口契约)
11. [前端改造](#十一前端改造)
12. [测试策略](#十二测试策略)
13. [实施路线图](#十三实施路线图)
14. [关键技术难点](#十四关键技术难点)
15. [待决策清单](#十五待决策清单)

---

## 一、现状分析

### 1.1 已有技术栈

| 层 | 技术 | 版本 |
|---|---|---|
| 后端框架 | Spring Boot + BallCat | 2.7.18 + 2.0.0-SNAPSHOT |
| 语言 | Java | 1.8 |
| ORM | MyBatis-Plus | 由 BallCat 管理 |
| 数据库 | MySQL（Docker） | 8.0，端口 3307 |
| 缓存 | Redis（Docker） | 7，端口 6379 |
| 前端 | Vue 3 + Vite + pnpm | Node 24，pnpm 8 |
| 服务端口 | Spring Boot | 8081 |
| 前端端口 | Vite Dev Server | 5173 |

### 1.2 已有模块（54 张表，73 个 Service，36 个 Controller）

```
erp-backend/admin/src/main/java/com/erp/admin/
├── order/       订单管理（Wildberries / Ozon / Yandex 三平台）
├── product/     商品目录（SKU / 品类 / 品牌 / 供应商）
├── shop/        店铺管理（平台凭证）
├── wms/         仓储执行（采购入库 / 销售出库 / 调拨 / 盘点 / 库存）
├── financial/   财务（WB 对账 / 财务报表同步）
├── statistics/  统计看板 / 销售目标
├── sync/        平台数据同步游标
├── system/      BallCat 系统层（用户 / 角色 / 菜单 / 字典）
└── common/      公共工具（多币种 / 邮件 / OSS）
```

### 1.3 关键现状问题

| 问题 | 影响 |
|---|---|
| **所有表无 `tenant_id`** | 无法支持多个客户隔离使用 |
| **WMS 与 OMS 耦合在同一服务** | WMS 用户（仓库员工）与 ERP 用户（货主）混用同一系统 |
| **无计费模块** | 无法对租户收取仓储 / 操作费用 |
| **店铺级隔离≠租户级隔离** | 现有 `shop_id` 只区分店铺，同一系统所有操作数据共用 |

---

## 二、整体架构

> ✅ **已确认（2026-06-27）**：WMS 不独立部署，采用**模块化单体**方案。
> 理由：3PL 自营，WMS 仅内部员工使用，独立部署带来的运维复杂度和分布式事务成本不值得。

**双层租户体系全局视图**

```
【平台超级管理员（你们）】  ← ROLE_SUPER_ADMIN
  管理全部仓库、货架编号（1号架、2号架…）
  将货架分配给 WMS 服务商
  按分配排数向 WMS 服务商出账
        │ 货架分配 / 开通账号
        ▼
【WMS 服务商（3PL 运营方，如客户A）】 ← ROLE_WMS_MANAGER / ROLE_WMS_OPERATOR   第一层租户
  管理自己分配到的货架上的所有货物
  查看自己的月度账单（货架租金 + 操作费）
  为自己的客户开通 ERP+OMS 账号
        │ 开通子账号
        ▼
【ERP 货主（如客户A的客户们）】 ← ROLE_TENANT_ADMIN / ROLE_TENANT_USER          第二层租户
  管理自己的 SKU / 店铺 / 订单
  创建出入库单，查看自己的库存和账单
  看不到其他货主的任何数据
```

**应用架构图**

```
┌──────────────────────────────────────────────────────────────────────┐
│               单一 Spring Boot 应用  erp-backend :8081               │
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │                      ERP + OMS 模块                           │   │
│  │   com.erp.admin.{order, product, shop, financial, oms}        │   │
│  │   ↑ MyBatis-Plus 多租户插件自动注入 WHERE erp_tenant_id       │   │
│  │   ↑ 用户群体：第二层租户（货主/运营人员）                      │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                          │ 同进程方法调用                             │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │                       WMS 模块                                │   │
│  │          com.erp.admin.wms  /  com.erp.admin.billing          │   │
│  │   ↑ @InterceptorIgnore(tenantLine="true")                     │   │
│  │   ↑ 按 wms_tenant_id 隔离：WMS 服务商只看自己货架上的货物     │   │
│  │   ↑ 超管无过滤：可看全部 WMS 服务商数据                       │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  MySQL :3307（同一实例，erp 库）  /  Redis :6379                     │
│  ERP+OMS 表：erp_tenant_id 行级隔离（第二层租户）                    │
│  WMS 表：wms_ 前缀，双 ID（wms_tenant_id + erp_tenant_id）           │
└──────────────────────────────────────────────────────────────────────┘
         │ HTTP
erp-frontend Vue3 :5173
  ├── [货主 ERP 入口]   → ROLE_TENANT_*，只看本货主数据
  ├── [WMS 服务商入口]  → ROLE_WMS_*，只看本 WMS 服务商货架数据
  └── [平台超管入口]    → ROLE_SUPER_ADMIN，看全部数据
      └── RBAC 菜单控制，同一前端，不同角色呈现不同菜单树
```

### 2.1 模块职责边界

| 模块 | 职责 | 用户群体 | 过滤逻辑 |
|---|---|---|---|
| **ERP+OMS 模块** | 商品管理、订单同步、采购单、出入库单创建、财务对账 | 第二层租户（货主/运营人员） | ✅ 自动注入 `WHERE erp_tenant_id`，只看本货主 |
| **WMS 模块** | 物理收货、拣货、发货、库存 SSOT | 第一层租户（WMS 服务商员工） | 按 `wms_tenant_id` 隔离；超管无限制 |
| **Billing 模块** | 费率卡管理、计费流水、月度账单生成 | 超管（全部）/ WMS 服务商（本司账单）/ 货主（下游查账） | 按角色三级可见 |
| **超管模块** | 仓库管理、货架分配、WMS 服务商开通、全局报表 | 平台超级管理员 | 无 tenant 过滤 |

### 2.2 核心架构原则

1. **双层租户**：`sys_tenant.tenant_type` 区分 `WMS_OPERATOR`（第一层）和 `ERP_USER`（第二层），第二层必须关联第一层
2. **库存唯一真源（SSOT）**：只有 WMS 模块修改物理库存；ERP+OMS 模块只读快照（调用 WMS Service 层方法）
3. **模块内通信**：OMS 与 WMS 之间是**同进程 Spring Bean 调用**，不需要 HTTP 或消息队列
4. **两条计费链路**：
   - 链路一 平台 → WMS 服务商：货架租金 + 仓储操作费（`wms_billing_record`）
   - 链路二 WMS 服务商 → 货主：物流产品费，货主下单选产品计费（`wms_client_billing_record`）
5. **界面三分**：同一前端，三个入口，通过 BallCat RBAC 菜单权限呈现不同操作界面

---

## 三、租户隔离策略

### 3.1 策略对比

| 策略 | 隔离性 | 资源成本 | 迁移难度 | 适合场景 |
|---|---|---|---|---|
| 独立数据库 | ★★★★★ | 高 | 很高 | 大客户、强合规要求 |
| 独立 Schema | ★★★★☆ | 中 | 中 | 中型 SaaS |
| **共享表 + `tenant_id`** | ★★★☆☆ | **低** | **低** | **✅ 本项目采用** |

### 3.2 选用理由

- 现有 54 张表均无 `tenant_id`，行级隔离迁移成本最低（ALTER TABLE 加列）
- BallCat 已内置 MyBatis-Plus `TenantLineInnerInterceptor`，配置即用
- 初期租户数量可控，行级过滤性能完全满足要求
- 未来单租户数据量激增时，可平滑升级到独立 Schema（迁移脚本复用）

### 3.3 WMS 模块的特殊处理

WMS 模块跨所有租户操作，**不做行级 tenant_id 过滤**，但所有 `wms_` 表保留 `tenant_id` 字段用于：
- 标记数据归属哪个租户（入库/出库单来自哪个货主）
- 计费时按租户汇总流水
- WMS 管理员查看分租户统计报表

### 3.4 性能分析（✅ 已确认 2026-06-27）

**当前数据规模推算**

```
单租户现状：约 1.2 GB / 6 个月  →  约 2.4 GB / 年

租户数量增长预测：
  5  个租户：约 12 GB / 年
  10 个租户：约 24 GB / 年
  20 个租户：约 48 GB / 年
```

MySQL 8.0 在索引设计合理的前提下，承载数十 TB 无问题。**核心风险不是数据量，而是索引设计。**

**查询性能预期（索引设计正确时）**

```
场景                              平均查询响应时间
────────────────────────────     ──────────────────
单租户（当前）                    < 50 ms
5 个租户 + 索引已优化              < 80 ms
10 个租户 + 索引已优化             < 120 ms
10 个租户 + 索引未优化（⚠️ 危险）  500 ms ~ 全表扫描
```

**原因**：`tenant_id = N` 条件让 MySQL B-tree 只扫描该租户的索引段，其他租户的行在索引层就被跳过，不会因租户增多而线性劣化。

**各表风险等级**

```
表名                    风险等级    原因
─────────────────────   ────────   ─────────────────────────
wb_report_detail        ★★★★★     财务流水，行数最多，字段 80+
erp_order               ★★★★☆     平台订单同步，持续高速增长
wms_stock_flow          ★★★★☆     每次库存变动都记录，累积快
erp_order_item          ★★★☆☆     每单多行
wms_billing_record      ★★★☆☆     每次作业产生一条
sku                     ★★☆☆☆     基本静态，更新少
```

### 3.5 索引三阶段应对策略（✅ 已确认 2026-06-27）

**第一阶段（立即执行，迁移时同步完成）：所有索引 `tenant_id` 置首列**

```sql
-- 核心原则：所有复合索引，tenant_id 必须是第一列
-- 错误示例（原单租户设计）：
KEY idx_create_time (create_time)
-- 正确示例（多租户）：
KEY idx_tenant_status_create (tenant_id, status, create_time)

-- erp_order 常用查询模式重建
ALTER TABLE erp_order
    DROP INDEX IF EXISTS idx_create_time,
    ADD INDEX idx_tenant_status_create  (tenant_id, erp_status, create_time),
    ADD INDEX idx_tenant_shop_create    (tenant_id, shop_id, create_time),
    ADD INDEX idx_tenant_platform_order (tenant_id, platform, platform_order_id);

-- wb_report_detail 财务查询
ALTER TABLE wb_report_detail
    ADD INDEX idx_tenant_shop_period (tenant_id, shop_id, date_from);

-- wms_stock_flow 库存历史
ALTER TABLE wms_stock_flow
    ADD INDEX idx_tenant_sku_create (tenant_id, sku_code, create_time);
```

**第二阶段（租户超过 5 个时）：对热表做分区**

```sql
-- wb_report_detail：按 tenant_id HASH 分区（数据量最大优先）
ALTER TABLE wb_report_detail
    PARTITION BY HASH(tenant_id) PARTITIONS 8;

-- erp_order：按季度 RANGE 分区，便于历史数据归档
ALTER TABLE erp_order
    PARTITION BY RANGE (YEAR(create_time) * 100 + QUARTER(create_time)) (
        PARTITION p2026q1 VALUES LESS THAN (202602),
        PARTITION p2026q2 VALUES LESS THAN (202603),
        PARTITION p2026q3 VALUES LESS THAN (202604),
        PARTITION p2026q4 VALUES LESS THAN (202701),
        PARTITION p_future VALUES LESS THAN MAXVALUE
    );
```

**第三阶段（单个租户数据超过 30 GB 时）：动态数据源路由**

```
方案：将该租户迁移到独立 Schema，其余租户继续共享
      通过 MyBatis-Plus 动态数据源插件（AbstractRoutingDataSource）
      按 tenant_id 路由到不同 DataSource
触发条件：单租户数据 > 30 GB（按当前增速，预计 10 年以上才会触发）
```

### 3.6 待讨论问题

> **【议题 3-A】** 租户的注册/开通流程：由系统管理员后台操作（推荐），还是需要自助注册页面？
>
> **【议题 3-B】** BallCat 菜单权限：所有租户共用同一套 ERP+OMS 菜单，还是每个租户可独立配置可见菜单？

---

## 四、数据库 Schema 设计

### 4.1 租户核心表（MySQL-A）

```sql
-- 租户主表（✅ 双层架构 2026-06-27）
-- tenant_type = WMS_OPERATOR：第一层租户（3PL服务商），由平台超管开通
-- tenant_type = ERP_USER：第二层租户（货主），由 WMS 服务商开通
CREATE TABLE sys_tenant (
    id                   BIGINT       NOT NULL AUTO_INCREMENT COMMENT '租户ID',
    tenant_code          VARCHAR(32)  NOT NULL COMMENT '租户编码（英文短码）',
    tenant_name          VARCHAR(100) NOT NULL COMMENT '租户名称（公司名）',
    tenant_type          VARCHAR(20)  NOT NULL DEFAULT 'ERP_USER'
                         COMMENT 'WMS_OPERATOR=3PL服务商（第一层） / ERP_USER=货主（第二层）',
    parent_wms_tenant_id BIGINT       DEFAULT NULL
                         COMMENT '所属 WMS 服务商 tenant_id（ERP_USER 必填，WMS_OPERATOR 为 NULL）',
    contact_name         VARCHAR(50)  COMMENT '联系人姓名',
    contact_phone        VARCHAR(20)  COMMENT '联系人电话',
    contact_email        VARCHAR(100) COMMENT '联系人邮箱',
    status               TINYINT      NOT NULL DEFAULT 1 COMMENT '1=启用 0=停用',
    remark               VARCHAR(500),
    create_time          DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time          DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted_at           DATETIME     DEFAULT NULL,
    PRIMARY KEY (id),
    UNIQUE KEY uk_tenant_code (tenant_code),
    KEY idx_parent_wms (parent_wms_tenant_id),
    KEY idx_type (tenant_type)
) ENGINE=InnoDB COMMENT='租户表（双层：WMS服务商 > ERP货主）';

-- 初始化：将现有所有数据归属到默认 WMS_OPERATOR 租户
INSERT INTO sys_tenant (id, tenant_code, tenant_name, tenant_type, status)
VALUES (1, 'default_wms', '默认WMS服务商（历史数据）', 'WMS_OPERATOR', 1);

-- 用户与租户的关联（一个用户只属于一个租户，✅ 已确认 2026-06-27）
-- 超管用户不绑定该表，通过 ROLE_SUPER_ADMIN 角色标识
CREATE TABLE sys_user_tenant (
    id          BIGINT   NOT NULL AUTO_INCREMENT,
    user_id     BIGINT   NOT NULL COMMENT 'sys_user.id，唯一约束保证一用户一租户',
    tenant_id   BIGINT   NOT NULL COMMENT 'sys_tenant.id（可为 WMS_OPERATOR 或 ERP_USER）',
    is_admin    TINYINT  NOT NULL DEFAULT 0 COMMENT '是否本租户管理员',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uk_user_id (user_id),
    KEY idx_tenant_id (tenant_id)
) ENGINE=InnoDB COMMENT='用户-租户关联表（一对一）';

-- 货架分配表：超管将具体货架编号分配给 WMS 服务商（✅ 2026-06-27）
-- rack_no 采用业务编码，如 '01'、'02'、'A-03'；分配期间同一货架不可重复分配
CREATE TABLE wms_rack_assignment (
    id             BIGINT        NOT NULL AUTO_INCREMENT,
    wms_tenant_id  BIGINT        NOT NULL COMMENT '被分配的 WMS 服务商 tenant_id',
    warehouse_id   BIGINT        NOT NULL COMMENT '所在仓库',
    rack_no        VARCHAR(20)   NOT NULL COMMENT '货架编号（如 01、02、A-03）',
    monthly_fee    DECIMAL(12,2) NOT NULL COMMENT '该排月租金（CNY）',
    effective_from DATE          NOT NULL COMMENT '分配生效日期',
    effective_to   DATE          DEFAULT NULL COMMENT '分配结束日期（NULL=当前有效）',
    remark         VARCHAR(500),
    created_by     BIGINT        NOT NULL COMMENT '操作超管 user_id',
    create_time    DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time    DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_wms_tenant (wms_tenant_id, effective_from),
    UNIQUE KEY uk_rack_period (rack_no, warehouse_id, effective_from)
) ENGINE=InnoDB COMMENT='货架分配记录（超管分配给WMS服务商）';
```

### 4.2 现有业务表迁移（批量添加 `tenant_id`）

> **执行前必须备份：**
> ```bash
> mysqldump -h127.0.0.1 -P3307 -uroot -p123456 erp > erp_backup_$(date +%Y%m%d).sql
> ```

```sql
-- ===== 订单模块 =====
ALTER TABLE erp_order
    ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 COMMENT '租户ID' AFTER id,
    ADD INDEX idx_tenant_id (tenant_id);

ALTER TABLE erp_order_item
    ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id,
    ADD INDEX idx_tenant_id (tenant_id);

ALTER TABLE erp_label_batch
    ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id,
    ADD INDEX idx_tenant_id (tenant_id);

ALTER TABLE erp_label_batch_file  ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;
ALTER TABLE erp_label_batch_item  ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;

-- ===== 商品模块 =====
ALTER TABLE sku
    ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id,
    ADD INDEX idx_tenant_id (tenant_id);

ALTER TABLE category    ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id, ADD INDEX idx_tenant_id (tenant_id);
ALTER TABLE brand       ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id, ADD INDEX idx_tenant_id (tenant_id);
ALTER TABLE supplier    ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id, ADD INDEX idx_tenant_id (tenant_id);
ALTER TABLE sku_mapping ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id, ADD INDEX idx_tenant_id (tenant_id);
ALTER TABLE sku_files   ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;

-- ===== 店铺模块 =====
ALTER TABLE shop ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id, ADD INDEX idx_tenant_id (tenant_id);

-- ===== 系统辅助表 =====
ALTER TABLE project_group ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;
ALTER TABLE position      ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;
ALTER TABLE sales_target  ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;
ALTER TABLE sync_cursor   ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;
ALTER TABLE exchange_rate ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;

-- ===== 财务模块 =====
ALTER TABLE wb_report_detail
    ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id,
    ADD INDEX idx_tenant_id (tenant_id);

ALTER TABLE wb_financial_sync_job   ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;
ALTER TABLE wb_financial_sync_task  ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;

-- ===== WMS 采购/出库（ERP+OMS 侧的单据） =====
ALTER TABLE wms_purchase_order
    ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id,
    ADD INDEX idx_tenant_id (tenant_id);

ALTER TABLE wms_purchase_order_item     ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;
ALTER TABLE wms_purchase_order_file     ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;
ALTER TABLE wms_purchase_order_qc_item  ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;

ALTER TABLE wms_sales_outbound_order
    ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id,
    ADD INDEX idx_tenant_id (tenant_id);

ALTER TABLE wms_sales_outbound_order_item ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;

-- ===== 重建受影响的唯一索引（加入 tenant_id） =====
-- SKU 编码在各租户内独立，不跨租户唯一
ALTER TABLE sku DROP INDEX IF EXISTS uk_sku_code;
ALTER TABLE sku ADD UNIQUE KEY uk_tenant_sku_code (tenant_id, sku_code);

-- 店铺：同一租户内平台店铺ID唯一
ALTER TABLE shop DROP INDEX IF EXISTS uk_platform_shop;
ALTER TABLE shop ADD UNIQUE KEY uk_tenant_platform_shop (tenant_id, platform, platform_shop_id);
```

### 4.3 OMS 新增表

```sql
-- OMS 出库单（ERP 订单 → WMS 出库）
CREATE TABLE oms_outbound_order (
    id                BIGINT       NOT NULL AUTO_INCREMENT,
    tenant_id         BIGINT       NOT NULL,
    order_no          VARCHAR(64)  NOT NULL COMMENT 'OMS出库单号，格式：OB-yyyyMMdd-xxxx',
    erp_order_id      BIGINT       COMMENT '关联 erp_order.id（平台订单来源时填写）',
    source_type       VARCHAR(20)  NOT NULL COMMENT 'PLATFORM=平台订单 / MANUAL=手工创建',
    warehouse_id      BIGINT       NOT NULL COMMENT '目标仓库',
    logistics_product_id BIGINT    COMMENT '货主选择的物流产品 wms_logistics_product.id（决策O）',
    logistics_fee     DECIMAL(12,2) COMMENT '物流费快照（下单时锁定，防事后改价）',
    logistics_currency VARCHAR(10)  DEFAULT 'RUB' COMMENT '物流费币种',
    status            VARCHAR(20)  NOT NULL DEFAULT 'PENDING'
                      COMMENT 'PENDING / SENT / ACCEPTED / PICKING / PACKED / SHIPPED / COMPLETED / CANCELLED',
    wms_order_no      VARCHAR(64)  COMMENT 'WMS侧出库单号（下发成功后回填）',
    priority          TINYINT      NOT NULL DEFAULT 5 COMMENT '优先级 1-10，值越小越优先',
    remark            VARCHAR(500),
    sent_at           DATETIME     COMMENT '发送给 WMS 的时间',
    completed_at      DATETIME,
    create_time       DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time       DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted_at        DATETIME     DEFAULT NULL,
    PRIMARY KEY (id),
    UNIQUE KEY uk_order_no (tenant_id, order_no),
    KEY idx_tenant_status (tenant_id, status),
    KEY idx_erp_order (tenant_id, erp_order_id)
) ENGINE=InnoDB COMMENT='OMS出库单';

CREATE TABLE oms_outbound_order_item (
    id                  BIGINT      NOT NULL AUTO_INCREMENT,
    tenant_id           BIGINT      NOT NULL,
    outbound_order_id   BIGINT      NOT NULL,
    sku_code            VARCHAR(64) NOT NULL,
    quantity            INT         NOT NULL,
    wms_reserved_qty    INT         NOT NULL DEFAULT 0 COMMENT 'WMS 已锁定数量',
    create_time         DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_order_id (outbound_order_id)
) ENGINE=InnoDB COMMENT='OMS出库单明细';

-- OMS 入库单（采购单 → WMS 入库）
CREATE TABLE oms_inbound_order (
    id                    BIGINT      NOT NULL AUTO_INCREMENT,
    tenant_id             BIGINT      NOT NULL,
    order_no              VARCHAR(64) NOT NULL COMMENT '格式：IB-yyyyMMdd-xxxx',
    purchase_order_id     BIGINT      COMMENT '关联 wms_purchase_order.id',
    source_type           VARCHAR(20) NOT NULL COMMENT 'PURCHASE / MANUAL / RETURN',
    warehouse_id          BIGINT      NOT NULL,
    status                VARCHAR(20) NOT NULL DEFAULT 'PENDING'
                          COMMENT 'PENDING / SENT / RECEIVING / COMPLETED / CANCELLED',
    wms_order_no          VARCHAR(64),
    expected_arrival_date DATE,
    remark                VARCHAR(500),
    sent_at               DATETIME,
    completed_at          DATETIME,
    create_time           DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time           DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted_at            DATETIME    DEFAULT NULL,
    PRIMARY KEY (id),
    UNIQUE KEY uk_order_no (tenant_id, order_no),
    KEY idx_tenant_status (tenant_id, status)
) ENGINE=InnoDB COMMENT='OMS入库单';

CREATE TABLE oms_inbound_order_item (
    id                BIGINT      NOT NULL AUTO_INCREMENT,
    tenant_id         BIGINT      NOT NULL,
    inbound_order_id  BIGINT      NOT NULL,
    sku_code          VARCHAR(64) NOT NULL,
    expected_quantity INT         NOT NULL,
    actual_quantity   INT         NOT NULL DEFAULT 0,
    create_time       DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_order_id (inbound_order_id)
) ENGINE=InnoDB COMMENT='OMS入库单明细';

-- OMS 事件发件箱（Outbox Pattern，防止消息丢失）
CREATE TABLE oms_sync_outbox (
    id          BIGINT      NOT NULL AUTO_INCREMENT,
    tenant_id   BIGINT      NOT NULL,
    event_type  VARCHAR(50) NOT NULL
                COMMENT 'OUTBOUND_CREATED / INBOUND_CREATED / ORDER_CANCELLED',
    biz_id      BIGINT      NOT NULL COMMENT '业务单据 ID',
    biz_no      VARCHAR(64) NOT NULL COMMENT '业务单号',
    payload     JSON        NOT NULL COMMENT '推送给 WMS 的消息体',
    status      VARCHAR(20) NOT NULL DEFAULT 'PENDING' COMMENT 'PENDING / SENT / FAILED',
    retry_count INT         NOT NULL DEFAULT 0,
    last_error  TEXT,
    create_time DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    sent_at     DATETIME,
    PRIMARY KEY (id),
    KEY idx_status_created (status, create_time)
) ENGINE=InnoDB COMMENT='OMS事件发件箱（防丢单）';
```

### 4.4 WMS 表（同库，`wms_` 前缀，✅ 已确认 2026-06-27）

> WMS 表与 ERP+OMS 表共用同一 MySQL 实例（端口 3307，库名 `erp`），无需新容器。
> 通过 `wms_` 表名前缀与 ERP 表区分，应用层通过 `@InterceptorIgnore` 控制过滤逻辑。

```sql
CREATE DATABASE wms DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE wms;

-- ============ 计费子系统 ============

-- 费率卡（服务定价配置）
CREATE TABLE wms_fee_rate_card (
    id           BIGINT        NOT NULL AUTO_INCREMENT,
    tenant_id    BIGINT        NOT NULL DEFAULT 0
                 COMMENT '适用租户 ID（0=所有租户的默认费率）',
    service_type VARCHAR(50)   NOT NULL
                 COMMENT 'INBOUND / OUTBOUND / STORAGE / LABEL / QC / TRANSFER / LOGISTICS',
    fee_name     VARCHAR(100)  NOT NULL COMMENT '费项名称（如"入库操作费"）',
    unit_price   DECIMAL(12,4) NOT NULL,
    currency     VARCHAR(10)   NOT NULL DEFAULT 'CNY',
    unit_type    VARCHAR(20)   NOT NULL
                 COMMENT 'PER_ITEM / PER_ORDER / PER_KG / PER_PALLET / PER_DAY / PER_MONTH',
    min_charge   DECIMAL(12,4) NOT NULL DEFAULT 0 COMMENT '最低收费金额',
    effective_from DATE         NOT NULL,
    effective_to   DATE         COMMENT 'NULL=永久有效',
    is_default     TINYINT      NOT NULL DEFAULT 0,
    remark         VARCHAR(500),
    create_time    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_tenant_type (tenant_id, service_type),
    KEY idx_effective (effective_from, effective_to)
) ENGINE=InnoDB COMMENT='WMS计费费率卡';

-- 计费流水（✅ 双层租户 2026-06-27）
-- wms_tenant_id：账单归属（向哪个 WMS 服务商收费）
-- erp_tenant_id：触发操作的货主（追溯用，NULL=货架租金等非货主操作产生的费用）
-- 规则：货主操作产生的费用归集到其所属 WMS 服务商账单
CREATE TABLE wms_billing_record (
    id             BIGINT        NOT NULL AUTO_INCREMENT,
    wms_tenant_id  BIGINT        NOT NULL COMMENT '账单所属 WMS 服务商（收费对象，第一层）',
    erp_tenant_id  BIGINT        DEFAULT NULL COMMENT '触发操作的货主 tenant_id（追溯，第二层，NULL=非货主操作）',
    bill_period    VARCHAR(7)    NOT NULL COMMENT '账期 yyyy-MM',
    service_type   VARCHAR(50)   NOT NULL
                   COMMENT 'RACK_RENTAL / INBOUND / OUTBOUND / DELIVERY / RETURN / INSPECTION / DRIVER',
    fee_name       VARCHAR(100)  NOT NULL,
    rate_card_id   BIGINT        NOT NULL,
    biz_type       VARCHAR(50)   NOT NULL COMMENT 'RACK_ASSIGNMENT / INBOUND_ORDER / OUTBOUND_ORDER / ...',
    biz_id         BIGINT        DEFAULT NULL,
    biz_no         VARCHAR(64)   DEFAULT NULL,
    quantity       DECIMAL(15,4) NOT NULL COMMENT '计量数量（排数/m³/件/托等）',
    unit_price     DECIMAL(12,4) NOT NULL,
    amount         DECIMAL(15,4) NOT NULL COMMENT '费用金额',
    currency       VARCHAR(10)   NOT NULL DEFAULT 'CNY',
    sku_code       VARCHAR(64)   DEFAULT NULL COMMENT '关联 SKU（出入库时填写）',
    remark         VARCHAR(500),
    occurred_at    DATETIME      NOT NULL,
    create_time    DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_wms_period    (wms_tenant_id, bill_period),
    KEY idx_erp_period    (erp_tenant_id, bill_period),
    KEY idx_biz           (biz_type, biz_id),
    KEY idx_occurred      (occurred_at)
) ENGINE=InnoDB COMMENT='WMS计费流水（账单归属WMS服务商，可追溯货主操作）';

-- 月度账单（以 WMS 服务商为收费单位，✅ 2026-06-27）
-- 货主如需查看明细，通过 wms_billing_record.erp_tenant_id 过滤查询
CREATE TABLE wms_monthly_bill (
    id               BIGINT        NOT NULL AUTO_INCREMENT,
    wms_tenant_id    BIGINT        NOT NULL COMMENT '账单所属 WMS 服务商',
    bill_period      VARCHAR(7)    NOT NULL COMMENT 'yyyy-MM',
    bill_no          VARCHAR(64)   NOT NULL COMMENT '账单号（唯一）',
    rack_rental_amt  DECIMAL(15,4) NOT NULL DEFAULT 0 COMMENT '货架月租金',
    inbound_amount   DECIMAL(15,4) NOT NULL DEFAULT 0 COMMENT '入库操作费',
    outbound_amount  DECIMAL(15,4) NOT NULL DEFAULT 0 COMMENT '出库操作费',
    delivery_amount  DECIMAL(15,4) NOT NULL DEFAULT 0 COMMENT '配送费',
    return_amount    DECIMAL(15,4) NOT NULL DEFAULT 0 COMMENT '退货取件费',
    inspection_amount DECIMAL(15,4) NOT NULL DEFAULT 0 COMMENT '验货费',
    driver_amount    DECIMAL(15,4) NOT NULL DEFAULT 0 COMMENT '司机相关费用',
    total_amount     DECIMAL(15,4) NOT NULL DEFAULT 0,
    currency         VARCHAR(10)   NOT NULL DEFAULT 'CNY',
    status           VARCHAR(20)   NOT NULL DEFAULT 'DRAFT'
                     COMMENT 'DRAFT / CONFIRMED / PAID / DISPUTED',
    confirmed_at     DATETIME      DEFAULT NULL,
    paid_at          DATETIME      DEFAULT NULL,
    remark           VARCHAR(500),
    create_time      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uk_wms_tenant_period (wms_tenant_id, bill_period),
    UNIQUE KEY uk_bill_no (bill_no)
) ENGINE=InnoDB COMMENT='WMS月度账单（以WMS服务商为收费单位）';

-- ============ WMS 业务子系统 ============

-- WMS 入库单（接收自各租户 OMS）
CREATE TABLE wms_inbound_order (
    id                    BIGINT      NOT NULL AUTO_INCREMENT,
    tenant_id             BIGINT      NOT NULL COMMENT '来源租户',
    wms_order_no          VARCHAR(64) NOT NULL COMMENT 'WMS侧单号',
    oms_order_no          VARCHAR(64) NOT NULL COMMENT 'OMS下发单号',
    warehouse_id          BIGINT      NOT NULL,
    source_type           VARCHAR(20) NOT NULL COMMENT 'PURCHASE / MANUAL / RETURN',
    status                VARCHAR(20) NOT NULL DEFAULT 'PENDING'
                          COMMENT 'PENDING / RECEIVING / PUTAWAY / COMPLETED / CANCELLED',
    expected_arrival_date DATE,
    actual_arrival_date   DATE,
    operator_id           BIGINT      COMMENT 'WMS操作员 user_id',
    remark                VARCHAR(500),
    create_time           DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time           DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uk_wms_order_no (wms_order_no),
    KEY idx_oms_order_no (oms_order_no),
    KEY idx_tenant_status (tenant_id, status)
) ENGINE=InnoDB COMMENT='WMS入库单';

CREATE TABLE wms_inbound_order_item (
    id               BIGINT      NOT NULL AUTO_INCREMENT,
    inbound_order_id BIGINT      NOT NULL,
    tenant_id        BIGINT      NOT NULL,
    sku_code         VARCHAR(64) NOT NULL,
    expected_qty     INT         NOT NULL,
    received_qty     INT         NOT NULL DEFAULT 0,
    good_qty         INT         NOT NULL DEFAULT 0 COMMENT '良品数量',
    damaged_qty      INT         NOT NULL DEFAULT 0 COMMENT '次品数量',
    inbound_date     DATE        COMMENT '实际入库日期（FIFO 排序主键）',
    pick_order       INT         NOT NULL DEFAULT 0 COMMENT '同日入库的顺序号',
    batch_no         VARCHAR(64) COMMENT '批次号（可选）',
    create_time      DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time      DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_order_id (inbound_order_id),
    KEY idx_tenant_sku (tenant_id, sku_code)
) ENGINE=InnoDB COMMENT='WMS入库单明细';

-- WMS 出库单（接收自各租户 OMS）
CREATE TABLE wms_outbound_order (
    id                    BIGINT       NOT NULL AUTO_INCREMENT,
    tenant_id             BIGINT       NOT NULL,
    wms_order_no          VARCHAR(64)  NOT NULL,
    oms_order_no          VARCHAR(64)  NOT NULL,
    erp_order_id          BIGINT       COMMENT '原始 ERP 订单 ID（用于状态回传）',
    warehouse_id          BIGINT       NOT NULL,
    status                VARCHAR(20)  NOT NULL DEFAULT 'PENDING'
                          COMMENT 'PENDING / ALLOCATING / PICKING / PACKING / SHIPPED / COMPLETED / CANCELLED',
    priority              TINYINT      NOT NULL DEFAULT 5,
    allocated_at          DATETIME,
    shipped_at            DATETIME,
    tracking_no           VARCHAR(128) COMMENT '物流单号',
    logistics_provider_id BIGINT,
    operator_id           BIGINT,
    remark                VARCHAR(500),
    create_time           DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time           DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uk_wms_order_no (wms_order_no),
    KEY idx_oms_order_no (oms_order_no),
    KEY idx_tenant_status (tenant_id, status)
) ENGINE=InnoDB COMMENT='WMS出库单';

CREATE TABLE wms_outbound_order_item (
    id                BIGINT      NOT NULL AUTO_INCREMENT,
    outbound_order_id BIGINT      NOT NULL,
    tenant_id         BIGINT      NOT NULL,
    sku_code          VARCHAR(64) NOT NULL,
    required_qty      INT         NOT NULL,
    allocated_qty     INT         NOT NULL DEFAULT 0,
    picked_qty        INT         NOT NULL DEFAULT 0,
    inbound_item_id   BIGINT      COMMENT '来自哪个入库批次（FIFO 溯源）',
    create_time       DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time       DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_order_id (outbound_order_id),
    KEY idx_inbound_item (inbound_item_id)
) ENGINE=InnoDB COMMENT='WMS出库单明细';

-- WMS 物理库存（SSOT，按批次追踪，双 tenant_id 设计）
-- wms_tenant_id：货物存放在哪个 WMS 服务商的货架上
-- erp_tenant_id：这批货物属于哪个货主（ERP_USER 租户）
-- WMS 服务商操作：WHERE wms_tenant_id = ?（看自己货架上的所有货）
-- 货主查库存：    WHERE erp_tenant_id = ?（只看自己的货）
-- 超管统计：     不加过滤
CREATE TABLE wms_physical_inventory (
    id              BIGINT      NOT NULL AUTO_INCREMENT,
    wms_tenant_id   BIGINT      NOT NULL COMMENT '货架所属 WMS 服务商 tenant_id（第一层）',
    erp_tenant_id   BIGINT      NOT NULL COMMENT '货物所属货主 tenant_id（第二层）',
    warehouse_id    BIGINT      NOT NULL,
    sku_code        VARCHAR(64) NOT NULL,
    inbound_item_id BIGINT      NOT NULL COMMENT '关联入库明细（批次溯源）',
    inbound_date    DATE        NOT NULL COMMENT 'FIFO 主排序键',
    pick_order      INT         NOT NULL DEFAULT 0 COMMENT 'FIFO 次排序键',
    quantity        INT         NOT NULL DEFAULT 0 COMMENT '当前库存数',
    reserved_qty    INT         NOT NULL DEFAULT 0 COMMENT '已分配锁定数',
    quality         VARCHAR(10) NOT NULL DEFAULT 'GOOD' COMMENT 'GOOD / DAMAGED',
    location_code   VARCHAR(50) COMMENT '库位编码（所在货架排号）',
    version         INT         NOT NULL DEFAULT 0 COMMENT '乐观锁',
    create_time     DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time     DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uk_batch (wms_tenant_id, erp_tenant_id, warehouse_id, sku_code, inbound_item_id),
    KEY idx_fifo    (wms_tenant_id, erp_tenant_id, warehouse_id, sku_code, inbound_date, pick_order, id),
    KEY idx_erp_sku (erp_tenant_id, sku_code)
) ENGINE=InnoDB COMMENT='WMS物理库存（SSOT，含双层租户标识）';
```

### 4.5 费率折扣表（决策 L，✅ 2026-06-27）

```sql
-- WMS 服务商个性化折扣配置（百分比叠加在基础费率上）
-- 不配置 = discount_pct 默认 0，即使用标准费率
CREATE TABLE wms_tenant_rate_discount (
    id             BIGINT       NOT NULL AUTO_INCREMENT,
    wms_tenant_id  BIGINT       NOT NULL COMMENT '目标 WMS 服务商 tenant_id',
    service_type   VARCHAR(50)  NOT NULL
                   COMMENT 'RACK_RENTAL / INBOUND / OUTBOUND / DELIVERY / RETURN / INSPECTION / DRIVER',
    discount_pct   DECIMAL(6,4) NOT NULL DEFAULT 0
                   COMMENT '折扣率：-0.1000=九折，0.0000=标准，+0.2000=加价20%',
    effective_from DATE         NOT NULL,
    effective_to   DATE         DEFAULT NULL COMMENT 'NULL=长期有效',
    remark         VARCHAR(200) COMMENT '折扣原因（如：年度合同）',
    created_by     BIGINT       NOT NULL COMMENT '操作超管 user_id',
    create_time    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_wms_tenant_type (wms_tenant_id, service_type),
    UNIQUE KEY uk_wms_type_from (wms_tenant_id, service_type, effective_from)
) ENGINE=InnoDB COMMENT='WMS服务商费率折扣配置';

-- wms_billing_record 追加两列（对账追溯用）
-- 实收 amount = base_unit_price × (1 + discount_pct) × quantity
ALTER TABLE wms_billing_record
    ADD COLUMN base_unit_price DECIMAL(12,4) COMMENT '标准单价（折扣前）' AFTER unit_price,
    ADD COLUMN discount_pct    DECIMAL(6,4)  COMMENT '实际折扣率（0=未折扣）' AFTER base_unit_price;
```

### 4.6 现有 `wms_*` 表改造（决策 M，✅ 2026-06-27）

分两类处理，不统一对待：

```sql
-- 第一类：ERP 侧业务单据（货主发起），只加 erp_tenant_id
-- 原有 tenant_id 列改名为 erp_tenant_id（或新加列后回填）
ALTER TABLE wms_purchase_order
    ADD COLUMN erp_tenant_id BIGINT NOT NULL DEFAULT 1
    COMMENT 'ERP货主 tenant_id（第二层）' AFTER id;
UPDATE wms_purchase_order SET erp_tenant_id = tenant_id;  -- 回填现有数据

ALTER TABLE wms_purchase_order_item
    ADD COLUMN erp_tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;
UPDATE wms_purchase_order_item SET erp_tenant_id = tenant_id;

ALTER TABLE wms_sales_outbound_order
    ADD COLUMN erp_tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;
UPDATE wms_sales_outbound_order SET erp_tenant_id = tenant_id;

ALTER TABLE wms_sales_outbound_order_item
    ADD COLUMN erp_tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id;
UPDATE wms_sales_outbound_order_item SET erp_tenant_id = tenant_id;

-- 第二类：WMS 物理执行表（已在 §4.4 重新设计为双 ID 结构）
-- 旧的 wms_inventory 表：迁移数据到新 wms_physical_inventory 后废弃
-- 迁移脚本（历史库存归入默认 WMS 服务商 wms_tenant_id=1）
INSERT INTO wms_physical_inventory
    (wms_tenant_id, erp_tenant_id, warehouse_id, sku_code, ...)
SELECT
    1 AS wms_tenant_id,   -- 历史数据归属默认服务商
    tenant_id,            -- 原货主 tenant_id → erp_tenant_id
    warehouse_id, sku_code, ...
FROM wms_inventory;
```

---

## 五、多租户核心代码

### 5.1 租户上下文（ThreadLocal）

```java
// com/erp/admin/common/tenant/TenantContext.java
public final class TenantContext {
    private static final ThreadLocal<Long> HOLDER = new InheritableThreadLocal<>();

    public static void setCurrentTenant(Long tenantId) { HOLDER.set(tenantId); }
    public static Long getCurrentTenant()              { return HOLDER.get(); }
    public static void clear()                         { HOLDER.remove(); }

    /** 临时切换租户执行，用于 WMS 回调等跨租户场景 */
    public static <T> T runAs(Long tenantId, Supplier<T> action) {
        Long previous = getCurrentTenant();
        try {
            setCurrentTenant(tenantId);
            return action.get();
        } finally {
            setCurrentTenant(previous);
        }
    }
}
```

### 5.2 MyBatis-Plus 多租户拦截器

```java
// com/erp/admin/common/tenant/TenantConfig.java
@Configuration
public class TenantConfig {

    /** 不注入 tenant_id 的系统表 */
    private static final Set<String> IGNORE_TABLES = Set.of(
        "sys_tenant", "sys_user", "sys_user_tenant",
        "sys_role", "sys_menu", "sys_organization",
        "sys_dict", "sys_dict_item", "sys_config",
        "log_access_log", "log_login_log", "log_operation_log",
        "exchange_rate", "captcha", "oms_sync_outbox"
    );

    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();

        // 多租户插件（必须在分页插件之前）
        interceptor.addInnerInterceptor(new TenantLineInnerInterceptor(
            new TenantLineHandler() {
                @Override
                public Expression getTenantId() {
                    Long tenantId = TenantContext.getCurrentTenant();
                    if (tenantId == null) {
                        throw new BizException(50003, "无租户上下文，拒绝访问");
                    }
                    return new LongValue(tenantId);
                }

                @Override
                public String getTenantIdColumn() { return "tenant_id"; }

                @Override
                public boolean ignoreTable(String tableName) {
                    return IGNORE_TABLES.contains(tableName.toLowerCase());
                }
            }
        ));

        // 分页插件
        interceptor.addInnerInterceptor(new PaginationInnerInterceptor(DbType.MYSQL));
        return interceptor;
    }
}
```

### 5.3 租户解析过滤器

```java
// com/erp/admin/common/tenant/TenantFilter.java
@Component
@Order(Ordered.HIGHEST_PRECEDENCE + 1)
@Slf4j
public class TenantFilter implements Filter {

    @Autowired
    private SysUserTenantMapper userTenantMapper;

    @Override
    public void doFilter(ServletRequest req, ServletResponse res, FilterChain chain)
            throws IOException, ServletException {
        try {
            Long tenantId = resolveTenantId((HttpServletRequest) req);
            TenantContext.setCurrentTenant(tenantId);
            if (tenantId != null) {
                MDC.put("tenantId", tenantId.toString()); // Logback MDC，日志自动带租户ID
            }
            chain.doFilter(req, res);
        } finally {
            TenantContext.clear();
            MDC.remove("tenantId");
        }
    }

    private Long resolveTenantId(HttpServletRequest request) {
        Authentication auth = SecurityContextHolder.getContext().getAuthentication();
        if (auth == null || !auth.isAuthenticated()) return null;

        // WMS 角色用户：绕过 tenant_id 过滤，返回 null
        // WMS Mapper 用 @InterceptorIgnore(tenantLine="true") 配合使用
        boolean isWmsUser = auth.getAuthorities().stream()
            .anyMatch(a -> a.getAuthority().startsWith("ROLE_WMS"));
        if (isWmsUser) return null;

        // 普通租户用户：查绑定的 tenant_id，自动注入 WHERE tenant_id = ?
        if (auth.getPrincipal() instanceof SysUser) {
            SysUser user = (SysUser) auth.getPrincipal();
            return userTenantMapper.findTenantIdByUserId(user.getId());
        }
        return null;
    }
}
```

### 5.4 日志格式（Logback，含 tenantId）

```xml
<!-- logback-spring.xml -->
<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %level [%thread] [tenant=%X{tenantId}] %logger{36} - %msg%n</pattern>
```

---

## 六、OMS 模块设计

### 6.1 业务流程

```
平台订单同步（Wildberries/Ozon/Yandex）
        │
        ▼
  erp_order（ERP订单层）
        │  确认后创建
        ▼
  oms_outbound_order（OMS出库单）
        │  写入 oms_sync_outbox
        ▼
  定时任务扫描 Outbox → 调用 WMS REST API
        │
        ▼
  wms_outbound_order（WMS执行层）
        │  执行完成后回传事件
        ▼
  OMS 更新状态 → ERP 订单状态更新

手工采购 / 平台采购
        │
        ▼
  wms_purchase_order（采购单）
        │  审核后创建
        ▼
  oms_inbound_order（OMS入库单）
        │  写入 oms_sync_outbox
        ▼
  WMS 收货执行 → 库存增加 → 回传
```

### 6.2 OMS 出库单下发服务（含 Outbox）

```java
// com/erp/admin/oms/service/OmsOutboundService.java
@Service
@Slf4j
@RequiredArgsConstructor
public class OmsOutboundService {

    private final OmsOutboundOrderMapper outboundMapper;
    private final OmsOutboundOrderItemMapper itemMapper;
    private final OmsSyncOutboxMapper outboxMapper;
    private final WmsApiClient wmsApiClient;
    private final TransactionTemplate transactionTemplate;

    /**
     * 创建出库单，业务数据 + Outbox 在同一事务内写入（防丢单核心）
     */
    public OmsOutboundOrder createAndSend(Long tenantId, OmsOutboundCreateDTO dto) {
        return transactionTemplate.execute(status -> {
            String orderNo = generateOrderNo("OB");

            OmsOutboundOrder order = new OmsOutboundOrder();
            order.setTenantId(tenantId);
            order.setOrderNo(orderNo);
            order.setErpOrderId(dto.getErpOrderId());
            order.setWarehouseId(dto.getWarehouseId());
            order.setSourceType(dto.getSourceType());
            order.setStatus("PENDING");
            outboundMapper.insert(order);

            // 明细
            dto.getItems().forEach(item -> {
                OmsOutboundOrderItem i = new OmsOutboundOrderItem();
                i.setTenantId(tenantId);
                i.setOutboundOrderId(order.getId());
                i.setSkuCode(item.getSkuCode());
                i.setQuantity(item.getQuantity());
                itemMapper.insert(i);
            });

            // Outbox（与业务同一事务）
            OmsSyncOutbox outbox = new OmsSyncOutbox();
            outbox.setTenantId(tenantId);
            outbox.setEventType("OUTBOUND_CREATED");
            outbox.setBizId(order.getId());
            outbox.setBizNo(orderNo);
            outbox.setPayload(buildOutboundPayload(order, dto.getItems()));
            outbox.setStatus("PENDING");
            outboxMapper.insert(outbox);

            log.info("[OMS][tenant={}] 出库单创建: {}", tenantId, orderNo);
            return order;
        });
    }

    /** 定时扫描发件箱，推送 WMS */
    @Scheduled(fixedDelay = 5000)
    @Async("outboxExecutor")
    public void processOutbox() {
        List<OmsSyncOutbox> pending = outboxMapper.findPending(20);
        for (OmsSyncOutbox outbox : pending) {
            try {
                TenantContext.setCurrentTenant(outbox.getTenantId());
                WmsOrderResponse resp = wmsApiClient.createOutboundOrder(outbox.getPayload());
                outboundMapper.updateWmsOrderNo(outbox.getBizId(), resp.getWmsOrderNo());
                outboxMapper.markSent(outbox.getId());
                log.info("[OMS][tenant={}] Outbox 推送成功: {}", outbox.getTenantId(), outbox.getBizNo());
            } catch (Exception e) {
                outboxMapper.markFailed(outbox.getId(), e.getMessage());
                log.error("[OMS][tenant={}] Outbox 推送失败: {}", outbox.getTenantId(), outbox.getBizNo(), e);
            } finally {
                TenantContext.clear();
            }
        }
    }
}
```

---

## 七、WMS 模块设计（模块化单体）

> ✅ **已确认（2026-06-27）**：WMS 不独立为微服务，作为 `erp-backend` 内的独立包模块存在。

### 7.1 模块定位

| 维度 | 说明 |
|---|---|
| 代码位置 | `com.erp.admin.wms.*` + `com.erp.admin.billing.*` |
| 数据库 | 同一 MySQL 实例 `:3307`，同一 `erp` 库，`wms_` 前缀表 |
| 用户 | WMS 仓库员工，持有 `ROLE_WMS_*` 角色，与租户用户共用登录入口但菜单不同 |
| 鉴权 | 统一 BallCat JWT 鉴权；WMS 角色在 `TenantFilter` 中跳过 tenant_id 注入 |
| 多租户 | `wms_` 表保留 `tenant_id` 标记数据来源，Mapper 层加 `@InterceptorIgnore` 绕过自动过滤 |

### 7.2 RBAC 角色设计（✅ 双层架构更新 2026-06-27）

```
角色                  归属层级        可访问菜单范围                    数据过滤规则
──────────────────    ──────────────  ──────────────────────────────   ─────────────────────────────
ROLE_SUPER_ADMIN      平台超管         仓库/货架分配/WMS服务商管理/全报表  无过滤（看全部数据）
ROLE_WMS_MANAGER      WMS服务商        WMS操作全部 + 本司账单 + 本司统计  WHERE wms_tenant_id = 本司
ROLE_WMS_OPERATOR     WMS服务商        WMS操作菜单（收货/发货/库存）       WHERE wms_tenant_id = 本司
ROLE_TENANT_ADMIN     ERP货主         ERP+OMS 全部菜单                  WHERE erp_tenant_id = 本租户
ROLE_TENANT_USER      ERP货主         ERP+OMS 只读菜单                  WHERE erp_tenant_id = 本租户
```

**三层 TenantFilter 过滤逻辑**（取代旧版两路分支）

```java
private FilterResult resolveTenantContext(HttpServletRequest request) {
    Authentication auth = SecurityContextHolder.getContext().getAuthentication();
    if (auth == null || !auth.isAuthenticated()) return FilterResult.SKIP;

    Collection<? extends GrantedAuthority> authorities = auth.getAuthorities();

    // 超管：无任何 tenant 过滤
    if (hasRole(authorities, "ROLE_SUPER_ADMIN")) {
        return FilterResult.NO_TENANT;
    }

    Long userId = ((SysUser) auth.getPrincipal()).getId();
    SysUserTenant binding = userTenantMapper.findByUserId(userId);
    if (binding == null) return FilterResult.NO_TENANT;

    SysTenant tenant = tenantMapper.selectById(binding.getTenantId());

    if ("WMS_OPERATOR".equals(tenant.getTenantType())) {
        // WMS 服务商用户：设 wms_tenant_id，ERP 侧拦截器绕过
        WmsTenantContext.setCurrentWmsTenant(tenant.getId());
        return FilterResult.WMS_TENANT;
    } else {
        // ERP 货主用户：设 erp_tenant_id，供 MyBatis-Plus 插件自动注入
        TenantContext.setCurrentTenant(tenant.getId());
        return FilterResult.ERP_TENANT;
    }
}
```

**WMS Mapper 绕过示例**

```java
public interface WmsPhysicalInventoryMapper extends BaseMapper<WmsPhysicalInventory> {

    // WMS 操作需要看全部租户库存，手动传 tenantId 作为 where 条件
    @InterceptorIgnore(tenantLine = "true")
    List<WmsPhysicalInventory> findAvailableForUpdate(
        @Param("tenantId") Long tenantId,       // 明确传入，而非插件自动注入
        @Param("warehouseId") Long warehouseId,
        @Param("skuCode") String skuCode,
        @Param("quality") String quality
    );

    // WMS 管理员查全部租户库存总览（不传 tenantId）
    @InterceptorIgnore(tenantLine = "true")
    List<InventorySummaryVO> summaryByTenant();
}
```

### 7.2 FIFO 库存分配（防超卖核心）

```java
// wms-service/src/.../service/InventoryAllocationService.java
@Service
@Slf4j
@RequiredArgsConstructor
public class InventoryAllocationService {

    private final WmsPhysicalInventoryMapper inventoryMapper;
    private final BillingRecordService billingService;

    /**
     * FIFO 分配：良品 → inbound_date 升序 → pick_order 升序 → id 升序
     * SELECT FOR UPDATE 行锁，事务内完成，防超卖
     */
    @Transactional(rollbackFor = Exception.class)
    public AllocationResult allocate(AllocationRequest req) {
        // 行锁查询可用批次（FIFO 顺序）
        List<WmsPhysicalInventory> batches = inventoryMapper.findAvailableForUpdate(
            req.getTenantId(), req.getWarehouseId(), req.getSkuCode(), "GOOD"
        );

        int remaining = req.getQuantity();
        List<AllocationDetail> allocated = new ArrayList<>();

        for (WmsPhysicalInventory batch : batches) {
            if (remaining <= 0) break;
            int avail = batch.getQuantity() - batch.getReservedQty();
            if (avail <= 0) continue;

            int take = Math.min(avail, remaining);
            batch.setReservedQty(batch.getReservedQty() + take);
            inventoryMapper.updateById(batch);
            allocated.add(new AllocationDetail(batch.getId(), batch.getInboundItemId(), take));
            remaining -= take;
        }

        if (remaining > 0) {
            // 缺货整单挂起（BR-01）
            throw new InsufficientStockException(req.getSkuCode(), req.getQuantity(), req.getQuantity() - remaining);
        }

        // 产生出库计费流水
        billingService.recordOutbound(req.getTenantId(), req.getOutboundOrderId(),
            req.getSkuCode(), req.getQuantity());

        log.info("[WMS][tenant={}] 库存分配成功 sku={} qty={}",
            req.getTenantId(), req.getSkuCode(), req.getQuantity());
        return new AllocationResult(allocated);
    }
}
```

### 7.3 WMS → OMS 状态回传

```java
// WMS 出库完成后回传 OMS
@Service
@RequiredArgsConstructor
public class WmsCallbackService {

    private final OmsApiClient omsApiClient;       // 调用 OMS 的 REST 接口
    private final WmsCallbackOutboxMapper outboxMapper; // WMS 侧也有发件箱

    @Transactional
    public void onOutboundShipped(WmsOutboundOrder order) {
        // 更新 WMS 出库单状态
        order.setStatus("SHIPPED");
        order.setShippedAt(LocalDateTime.now());

        // 写入 WMS 发件箱（回传给 OMS）
        WmsCallbackOutbox outbox = WmsCallbackOutbox.builder()
            .tenantId(order.getTenantId())
            .eventType("OUTBOUND_SHIPPED")
            .bizNo(order.getOmsOrderNo())
            .payload(buildShippedPayload(order))
            .build();
        outboxMapper.insert(outbox);

        log.info("[WMS][tenant={}] 出库单发货: oms={}", order.getTenantId(), order.getOmsOrderNo());
    }
}
```

---

## 八、计费系统设计

### 8.1 计费项目清单（✅ 基于合同确认，2026-06-27）

> **计费对象**：WMS 服务商（第一层租户）。货主操作量归集到所属 WMS 服务商账单。

| 服务类型 | 费项名称 | 计量单位 | 单价（合同） | 触发时机 |
|---|---|---|---|---|
| `RACK_RENTAL` | 货架月租金 | 元/排/月 | 按 `wms_rack_assignment` 分配排数自动计 | 月末按分配记录计算 |
| `INBOUND` | 入库基础费 | 元/m³ | 50 | WMS 入库完成时（按体积） |
| `INBOUND` | 入库加班附加费 | 倍率 | 0.5× 基础费 | 加班操作时 |
| `OUTBOUND` | 托盘出货费 | 元/托 | 80 | WMS 出库完成时 |
| `OUTBOUND` | 散货出货费 | 元/件 | 1 | WMS 出库完成时 |
| `DELIVERY` | 小车配送费 | 元/箱 | 20 | 配送出库时 |
| `RETURN` | 退货取件费 | 元/箱 | 10 | 退货入库时 |
| `INSPECTION` | 普货验货费 | 元/SKU | 5 | QC 完成时 |
| `INSPECTION` | 电子品验货费 | 元/SKU | 10 | QC 完成时（电子类强制拍照） |
| `DRIVER` | 司机激励费 | 卢布/次 | 1000 | 每次出车记录 |
| `DRIVER` | 司机工资分摊 | 卢布/月 | 140000 | 月末自动计入 |

### 8.2 月度账单生成

```java
// wms-service/src/.../service/BillingService.java
@Service
@Slf4j
@RequiredArgsConstructor
public class BillingService {

    private final BillingRecordMapper billingMapper;
    private final MonthlyBillMapper billMapper;
    private final RackRentalMapper rackRentalMapper;
    private final FeeRateCardService rateCardService;

    /** 每月1日凌晨2点自动生成上月账单 */
    @Scheduled(cron = "0 0 2 1 * ?")
    @Transactional
    public void generateMonthlyBills() {
        String period = YearMonth.now().minusMonths(1).toString();
        log.info("[Billing] 开始生成 {} 账单", period);
        List<Long> tenantIds = billingMapper.findTenantsWithRecords(period);
        tenantIds.forEach(tid -> generateForTenant(tid, period));
    }

    /** 按 WMS 服务商（第一层租户）生成账单，汇总其下所有货主的操作量 */
    private void generateForWmsTenant(Long wmsTenantId, String period) {
        // 从计费流水汇总各类操作费
        BillingAggregate agg = billingMapper.aggregateByWmsTenant(wmsTenantId, period);

        // 货架租金：按当月有效分配的排数 × 各排月单价之和
        BigDecimal rackRentalAmt = calculateRackRentalFee(wmsTenantId, period);

        WmsMonthlyBill bill = new WmsMonthlyBill();
        bill.setWmsTenantId(wmsTenantId);
        bill.setBillPeriod(period);
        bill.setBillNo("BILL-" + wmsTenantId + "-" + period.replace("-", ""));
        bill.setRackRentalAmt(rackRentalAmt);
        bill.setInboundAmount(agg.getInbound());
        bill.setOutboundAmount(agg.getOutbound());
        bill.setDeliveryAmount(agg.getDelivery());
        bill.setReturnAmount(agg.getReturnFee());
        bill.setInspectionAmount(agg.getInspection());
        bill.setDriverAmount(agg.getDriver());
        bill.setTotalAmount(
            rackRentalAmt.add(agg.getInbound()).add(agg.getOutbound())
                         .add(agg.getDelivery()).add(agg.getReturnFee())
                         .add(agg.getInspection()).add(agg.getDriver())
        );
        bill.setStatus("DRAFT");
        billMapper.insertOrUpdate(bill);
        log.info("[Billing][wmsTenant={}] {} 账单生成，合计 {} CNY",
            wmsTenantId, period, bill.getTotalAmount());
    }

    /**
     * 货架租金 = Σ( 有效分配的货架 × 该货架月单价 )
     * 数据来源：wms_rack_assignment（月内有重叠则按天精算）
     */
    private BigDecimal calculateRackRentalFee(Long wmsTenantId, String period) {
        YearMonth ym = YearMonth.parse(period);
        List<WmsRackAssignment> racks = rackAssignmentMapper
            .findEffectiveInPeriod(wmsTenantId, ym.atDay(1), ym.atEndOfMonth());
        return racks.stream()
            .map(WmsRackAssignment::getMonthlyFee)
            .reduce(BigDecimal.ZERO, BigDecimal::add);
    }
}
```

### 8.3 计费明细查询权限（决策 N，✅ 2026-06-27）

货主（`ROLE_TENANT_*`）可查看 `wms_billing_record` 中属于自己操作产生的每一条明细：

**货主可见（`erp_tenant_id = 本租户`）：**

| 费项 | 说明 |
|---|---|
| 入库基础费 | 每次入库，按 m³ 计费，显示入库单号、体积、单价、金额 |
| 入库加班附加费 | 加班时产生，显示关联入库单 |
| 托盘/散货出库费 | 每次出库，显示出库单号、数量/托数、单价、金额 |
| 配送费 | 显示配送单号、箱数、金额 |
| 退货取件费 | 显示退货单号、箱数、金额 |
| 验货费 | 显示 SKU、件数、金额 |

**货主不可见（`erp_tenant_id IS NULL`，属于平台与服务商之间）：**

| 费项 | 不可见原因 |
|---|---|
| 货架月租金 | 平台与 WMS 服务商的合同，与货主无关 |
| 司机激励费 | WMS 服务商内部运营成本 |
| 司机工资分摊 | 同上 |

**Controller 层过滤实现（货主调用明细接口时）：**

```java
// 货主查询自己的计费明细
@GetMapping("/billing/my-records")
public PageResult<BillingRecordVO> myRecords(@RequestParam String billPeriod,
                                              @RequestParam(required=false) String serviceType) {
    Long erpTenantId = TenantContext.getCurrentTenant(); // 自动从 JWT 解析
    return billingService.queryByErpTenant(erpTenantId, billPeriod, serviceType);
}

// 对应 SQL 条件（Service 层）
// WHERE erp_tenant_id = #{erpTenantId}
//   AND bill_period = #{period}
//   AND service_type != 'RACK_RENTAL'   -- 租金不透出
//   AND service_type != 'DRIVER'        -- 司机费不透出
```

---

### 8.4 二级计费：物流产品（决策 O，✅ 2026-06-27）

> **这是第二条计费链路：WMS 服务商 → ERP 货主。**
> 与「平台 → WMS 服务商」链路完全独立，收费方/付费方不同，使用独立的表。

**整体两条计费链路**

```
链路一（§8.1–8.3）：平台 → WMS 服务商
   货架租金 + 仓储操作费
   表：wms_billing_record / wms_monthly_bill

链路二（本节）：WMS 服务商 → ERP 货主
   物流产品费（货主下单时选择物流产品 → 按产品定价计费）
   表：wms_logistics_product / wms_client_billing_record / wms_client_monthly_bill
```

**8.4.1 业务流程**

```
WMS 服务商后台配置物流产品（按尺寸/取货/配送等维度组合，定价）→ 上架
        ↓
货主创建 OMS 出库单时，从「本服务商上架的产品」列表中选择一个
        ↓
下单锁定价格快照（oms_outbound_order.logistics_fee）防事后改价
        ↓
WMS 发货完成 → 生成 wms_client_billing_record（向货主计费）
        ↓
月末汇总 → wms_client_monthly_bill（服务商对货主出账）→ 线下对账标记已付
```

**8.4.2 物流产品组合维度**

| 维度 | 字段 | 可选值 |
|---|---|---|
| 尺寸段 | `size_category` | XS / S / M / L / XL / OVERSIZE |
| 重量上限 | `weight_max_kg` | 数值，NULL=不限 |
| 体积上限 | `volume_max_cbm` | 数值，NULL=不限 |
| 取货方式 | `pickup_method` | SELF_DROP 自送到仓 / DOOR_PICKUP 上门取件 |
| 配送方式 | `delivery_mode` | HOME 送货上门 / PICKUP_POINT 自提点 / LOCKER 快递柜 |
| 配送时效 | `delivery_speed` | ECONOMY 经济 / STANDARD 标准 / EXPRESS 加急 |
| 目的地区域 | `destination_zone` | 莫斯科市区 / 莫斯科州 / 周边州 / 偏远地区 |
| 包装服务 | `packaging` | NONE 无 / BASIC 基础 / PREMIUM 精装(木架) |
| 计价单位 | `pricing_unit` | PER_ORDER 按单 / PER_ITEM 按件 / PER_KG 按公斤 |

**8.4.3 数据表**

```sql
-- 物流产品目录（WMS服务商定义，推送给本司下辖货主选择）
CREATE TABLE wms_logistics_product (
    id               BIGINT        NOT NULL AUTO_INCREMENT,
    wms_tenant_id    BIGINT        NOT NULL COMMENT '定义产品的 WMS 服务商',
    product_code     VARCHAR(32)   NOT NULL COMMENT '产品编码',
    product_name     VARCHAR(100)  NOT NULL COMMENT '产品名称（如：莫斯科市区·标准达·小件）',
    -- 组合维度
    size_category    VARCHAR(20)   COMMENT 'XS/S/M/L/XL/OVERSIZE',
    weight_max_kg    DECIMAL(10,2) COMMENT '重量上限kg（NULL=不限）',
    volume_max_cbm   DECIMAL(10,4) COMMENT '体积上限m³（NULL=不限）',
    pickup_method    VARCHAR(20)   COMMENT 'SELF_DROP / DOOR_PICKUP',
    delivery_mode    VARCHAR(20)   COMMENT 'HOME / PICKUP_POINT / LOCKER',
    delivery_speed   VARCHAR(20)   COMMENT 'ECONOMY / STANDARD / EXPRESS',
    destination_zone VARCHAR(50)   COMMENT '目的地区域',
    packaging        VARCHAR(20)   COMMENT 'NONE / BASIC / PREMIUM',
    -- 定价
    price            DECIMAL(12,2) NOT NULL COMMENT '产品价格',
    currency         VARCHAR(10)   NOT NULL DEFAULT 'RUB',
    pricing_unit     VARCHAR(20)   NOT NULL DEFAULT 'PER_ORDER' COMMENT 'PER_ORDER/PER_ITEM/PER_KG',
    -- 可见性（决策 O-2）
    visibility       VARCHAR(20)   NOT NULL DEFAULT 'PUBLIC'
                     COMMENT 'PUBLIC=本司所有货主可见 / RESTRICTED=仅授权货主可见',
    -- 状态
    status           TINYINT       NOT NULL DEFAULT 1 COMMENT '1=上架 0=下架',
    description      VARCHAR(500),
    create_time      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time      DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted_at       DATETIME      DEFAULT NULL,
    PRIMARY KEY (id),
    UNIQUE KEY uk_wms_code (wms_tenant_id, product_code),
    KEY idx_wms_status (wms_tenant_id, status)
) ENGINE=InnoDB COMMENT='物流产品目录（WMS服务商定义）';

-- 物流产品-货主授权表（决策 O-2）
-- 用途一：RESTRICTED 产品的定向开放（有记录=该货主可见）
-- 用途二：对特定货主的专属定价（custom_price 覆盖默认价，NULL=用默认价）
CREATE TABLE wms_logistics_product_grant (
    id                   BIGINT        NOT NULL AUTO_INCREMENT,
    wms_tenant_id        BIGINT        NOT NULL COMMENT '所属 WMS 服务商（冗余，便于过滤）',
    logistics_product_id BIGINT        NOT NULL COMMENT '产品 id',
    erp_tenant_id        BIGINT        NOT NULL COMMENT '被授权/专属定价的货主',
    custom_price         DECIMAL(12,2) DEFAULT NULL COMMENT '专属价（NULL=用产品默认价）',
    custom_currency      VARCHAR(10)   DEFAULT NULL COMMENT '专属价币种（NULL=用产品默认币种）',
    status               TINYINT       NOT NULL DEFAULT 1 COMMENT '1=有效 0=停用',
    remark               VARCHAR(200),
    create_time          DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time          DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uk_product_client (logistics_product_id, erp_tenant_id),
    KEY idx_erp_tenant (erp_tenant_id, status)
) ENGINE=InnoDB COMMENT='物流产品-货主授权与专属定价表';

-- 服务商 → 货主 物流费计费流水（发货完成时产生）
CREATE TABLE wms_client_billing_record (
    id                   BIGINT        NOT NULL AUTO_INCREMENT,
    wms_tenant_id        BIGINT        NOT NULL COMMENT '收费方：WMS 服务商',
    erp_tenant_id        BIGINT        NOT NULL COMMENT '付费方：货主',
    bill_period          VARCHAR(7)    NOT NULL COMMENT 'yyyy-MM',
    logistics_product_id BIGINT        NOT NULL,
    product_name         VARCHAR(100)  NOT NULL COMMENT '产品名称快照',
    biz_type             VARCHAR(50)   NOT NULL DEFAULT 'OUTBOUND_ORDER',
    biz_id               BIGINT        COMMENT '关联出库单 id',
    biz_no               VARCHAR(64)   COMMENT '出库单号',
    quantity             DECIMAL(15,4) NOT NULL DEFAULT 1 COMMENT '计量数量（单/件/kg）',
    unit_price           DECIMAL(12,2) NOT NULL COMMENT '产品单价快照',
    amount               DECIMAL(15,2) NOT NULL COMMENT '费用金额',
    currency             VARCHAR(10)   NOT NULL DEFAULT 'RUB',
    occurred_at          DATETIME      NOT NULL,
    create_time          DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_wms_period (wms_tenant_id, bill_period),
    KEY idx_erp_period (erp_tenant_id, bill_period),
    KEY idx_biz (biz_type, biz_id)
) ENGINE=InnoDB COMMENT='WMS服务商→货主 物流费计费流水';

-- 服务商 → 货主 月度账单（按货主分别出账）
CREATE TABLE wms_client_monthly_bill (
    id            BIGINT        NOT NULL AUTO_INCREMENT,
    wms_tenant_id BIGINT        NOT NULL COMMENT '收费方：WMS 服务商',
    erp_tenant_id BIGINT        NOT NULL COMMENT '付费方：货主',
    bill_period   VARCHAR(7)    NOT NULL COMMENT 'yyyy-MM',
    bill_no       VARCHAR(64)   NOT NULL COMMENT '账单号（唯一）',
    order_count   INT           NOT NULL DEFAULT 0 COMMENT '计费订单数',
    total_amount  DECIMAL(15,2) NOT NULL DEFAULT 0,
    currency      VARCHAR(10)   NOT NULL DEFAULT 'RUB',
    status        VARCHAR(20)   NOT NULL DEFAULT 'DRAFT'
                  COMMENT 'DRAFT / CONFIRMED / PAID / DISPUTED',
    confirmed_at  DATETIME      DEFAULT NULL,
    paid_at       DATETIME      DEFAULT NULL,
    remark        VARCHAR(500),
    create_time   DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time   DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uk_wms_erp_period (wms_tenant_id, erp_tenant_id, bill_period),
    UNIQUE KEY uk_bill_no (bill_no)
) ENGINE=InnoDB COMMENT='WMS服务商→货主 月度账单';
```

**8.4.4 产品可见性与定价规则（决策 O-2）**

货主可见的产品 = 满足以下条件之一，且 `status=1` 上架、所属本货主的 WMS 服务商：

| 产品 visibility | 是否有授权记录 | 货主可见？ | 适用价格 |
|---|---|---|---|
| `PUBLIC` | 无 | ✅ 可见 | 产品默认价 `price` |
| `PUBLIC` | 有（含 custom_price） | ✅ 可见 | 专属价 `custom_price`（覆盖默认） |
| `PUBLIC` | 有（custom_price 为 NULL） | ✅ 可见 | 产品默认价 |
| `RESTRICTED` | 无 | ❌ 不可见 | — |
| `RESTRICTED` | 有 | ✅ 可见 | 专属价或默认价（同上规则） |

**可见产品列表查询（货主侧）：**

```sql
-- erpTenantId = 当前货主，wmsTenantId = 其所属服务商 (parent_wms_tenant_id)
SELECT p.*,
       COALESCE(g.custom_price, p.price)       AS effective_price,
       COALESCE(g.custom_currency, p.currency) AS effective_currency
FROM wms_logistics_product p
LEFT JOIN wms_logistics_product_grant g
       ON g.logistics_product_id = p.id
      AND g.erp_tenant_id = #{erpTenantId}
      AND g.status = 1
WHERE p.wms_tenant_id = #{wmsTenantId}
  AND p.status = 1
  AND p.deleted_at IS NULL
  AND (
        p.visibility = 'PUBLIC'                 -- 公开产品所有货主可见
        OR g.id IS NOT NULL                     -- 定向产品需有授权记录
      );
```

- 货主下单选定产品后，将 `effective_price / effective_currency` 快照写入 `oms_outbound_order.logistics_fee`，后续改价/改授权不影响已下订单
- WMS 发货完成回调时，依据出库单上的快照价生成 `wms_client_billing_record`
- 下单前服务端二次校验：被选产品对该货主确实可见（防止前端绕过提交不可见产品）

**8.4.5 计费时机与幂等**

```java
// WMS 出库发货完成时，产生物流费流水（同 biz_id 幂等，重复回调不重复计费）
@Transactional
public void recordLogisticsFee(OmsOutboundOrder order) {
    if (order.getLogisticsProductId() == null) return; // 未选产品（如自提）不计费
    if (clientBillingMapper.existsByBiz("OUTBOUND_ORDER", order.getId())) return; // 幂等

    WmsClientBillingRecord rec = new WmsClientBillingRecord();
    rec.setWmsTenantId(resolveWmsTenant(order.getTenantId())); // 货主→所属服务商
    rec.setErpTenantId(order.getTenantId());
    rec.setBillPeriod(YearMonth.now().toString());
    rec.setLogisticsProductId(order.getLogisticsProductId());
    rec.setProductName(order.getLogisticsProductName());
    rec.setBizType("OUTBOUND_ORDER");
    rec.setBizId(order.getId());
    rec.setBizNo(order.getOrderNo());
    rec.setQuantity(BigDecimal.ONE);
    rec.setUnitPrice(order.getLogisticsFee());
    rec.setAmount(order.getLogisticsFee());
    rec.setCurrency(order.getLogisticsCurrency());
    rec.setOccurredAt(LocalDateTime.now());
    clientBillingMapper.insert(rec);
}
```

---

## 九、数据迁移方案

### 9.1 迁移策略

```
阶段   操作                                        预计时长   风险
────   ─────────────────────────────────────────   ────────   ────
P0    完整备份当前 erp 数据库                         5 min     无
P1    在测试环境执行完整迁移演练并验证                  1 hour    无
P2    业务低峰期执行生产 ALTER TABLE（加列）           10-30 min  中（短暂锁表）
P3    初始化 sys_tenant、sys_user_tenant            1 min     无
P4    重建受影响的唯一索引                            5-15 min   中
P5    启动新版应用验证（profile=local）               ─          低
P6    手工录入新租户信息，创建用户绑定                  手工        无
```

### 9.2 执行脚本

```bash
#!/bin/bash
# migration/run.sh
set -e

MYSQL_CMD="mysql -h127.0.0.1 -P3307 -uroot -p123456 erp"

echo "[P0] 备份数据库..."
mysqldump -h127.0.0.1 -P3307 -uroot -p123456 erp \
  > "erp_backup_$(date +%Y%m%d_%H%M%S).sql"
echo "  ✓ 备份完成"

echo "[P2] 执行 ALTER TABLE（添加 tenant_id）..."
$MYSQL_CMD < migration/01_add_tenant_id.sql
echo "  ✓ 列添加完成"

echo "[P3] 初始化租户数据..."
$MYSQL_CMD < migration/02_init_tenant.sql
echo "  ✓ 租户初始化完成"

echo "[P4] 重建唯一索引..."
$MYSQL_CMD < migration/03_rebuild_indexes.sql
echo "  ✓ 索引重建完成"

echo "[P5] 迁移完成，请启动应用验证"
```

### 9.3 回滚方案

```sql
-- migration/rollback.sql（若迁移失败，回滚加列操作）
ALTER TABLE erp_order          DROP COLUMN tenant_id;
ALTER TABLE erp_order_item     DROP COLUMN tenant_id;
ALTER TABLE sku                DROP COLUMN tenant_id;
-- ... 其余表同理
-- 恢复原有唯一索引
ALTER TABLE sku ADD UNIQUE KEY uk_sku_code (sku_code);
```

---

## 十、OMS ↔ WMS 调用契约

> ✅ **已确认（2026-06-27）**：OMS 与 WMS 同进程，通信方式为 **Spring Bean 直接调用**，无网络开销，无需消息队列。

### 10.1 OMS 调用 WMS（同进程 Service 调用）

```java
// OMS Service 直接注入 WMS Service，无 HTTP/消息队列
@Service
@RequiredArgsConstructor
public class OmsOutboundService {

    private final WmsInboundOutboundService wmsService;  // 同进程 Spring Bean

    public void sendToWms(OmsOutboundOrder order) {
        // 直接方法调用，在同一事务或独立事务中执行
        WmsOrderResult result = wmsService.createOutboundOrder(
            order.getTenantId(),
            order.getOrderNo(),
            order.getWarehouseId(),
            buildItems(order)
        );
        order.setWmsOrderNo(result.getWmsOrderNo());
        order.setStatus("SENT");
    }
}
```

### 10.2 OMS ↔ WMS 核心接口清单

```
调用方向       方法                                  说明
────────────   ───────────────────────────────────   ─────────────────────────
OMS → WMS      wmsService.createOutboundOrder(...)   创建出库单
OMS → WMS      wmsService.createInboundOrder(...)    创建入库单
OMS → WMS      wmsService.cancelOutboundOrder(...)   取消出库单（PENDING 状态）
OMS → WMS      wmsService.getInventorySnapshot(...)  查询库存快照（只读）
WMS → OMS      omsService.onOutboundShipped(...)     出库发货回调
WMS → OMS      omsService.onInboundCompleted(...)    入库完成回调
WMS → OMS      omsService.onStockInsufficient(...)   库存不足，触发 OMS 挂起
```

### 10.3 事务边界说明

```
场景                         事务策略
──────────────────────────   ────────────────────────────────────────
OMS 创建出库单 → WMS 建单    OMS 事务提交后，WMS 用独立事务执行
                             （WMS 失败不回滚 OMS，靠状态机重试）
WMS 分配库存 → 计费流水      同一事务（必须原子，防止库存扣了但未计费）
WMS 回调 → OMS 更新状态     独立事务，失败记录日志等人工处理
```

---

## 十一、前端改造

### 11.1 Pinia 租户状态

```typescript
// src/stores/tenant.ts
export const useTenantStore = defineStore('tenant', {
  state: () => ({
    tenantId: null as number | null,
    tenantName: '' as string,
    planType: '' as string,
  }),
  actions: {
    async fetchCurrentTenant() {
      const res = await api.get('/system/tenant/current')
      this.tenantId = res.data.id
      this.tenantName = res.data.tenantName
      this.planType = res.data.planType
    }
  }
})
```

### 11.2 Axios 拦截器（自动带租户 Header）

```typescript
// src/utils/request.ts
service.interceptors.request.use(config => {
  const tenantStore = useTenantStore()
  if (tenantStore.tenantId) {
    config.headers['X-Tenant-Id'] = String(tenantStore.tenantId)
  }
  return config
})
```

### 11.3 登录入口设计（✅ 三入口 2026-06-27）

登录页提供三个独立入口，角色与入口不符时服务端拒绝：

```
[货主 ERP 入口]   → 仅 ROLE_TENANT_ADMIN / ROLE_TENANT_USER 可进入
[WMS 服务商入口]  → 仅 ROLE_WMS_MANAGER / ROLE_WMS_OPERATOR 可进入
[平台超管入口]    → 仅 ROLE_SUPER_ADMIN 可进入
```

### 11.4 新增路由模块

```
# 货主 ERP（ROLE_TENANT_*）
/oms/outbound         OMS 出库单管理（创建时选择物流产品）
/oms/inbound          OMS 入库单管理
/oms/logistics-products 可用物流产品浏览（本服务商上架的产品）
/wms/bills            操作计费明细查看（链路一，按 erp_tenant_id 过滤）
/wms/logistics-bills  物流费账单查看（链路二，wms_client_monthly_bill）

# WMS 服务商（ROLE_WMS_*）
/wms/inbound-ops      入库作业（收货/质检/上架）
/wms/outbound-ops     出库作业（拣货/打包/发货）
/wms/inventory        库存总览（wms_tenant_id 过滤）
/wms/logistics-products 物流产品管理（定义组合/定价/上下架）
/wms/client-bills     对货主的物流费账单管理（出账/对账）
/wms/my-bills         本司应付平台账单（链路一）
/wms/my-clients       下辖货主管理（开通 ERP 账号）

# 平台超管（ROLE_SUPER_ADMIN）
/admin/warehouses     仓库管理
/admin/racks          货架管理 & 分配
/admin/wms-tenants    WMS 服务商开通与管理
/admin/billing        全局账单管理
/admin/reports        跨 WMS 服务商统计报表
```

---

## 十二、测试策略

### 12.1 必须覆盖的高风险用例

```java
// 租户数据隔离测试
@Test
void tenantA_cannot_see_tenantB_data() {
    TenantContext.setCurrentTenant(1L);
    skuService.create(buildSku("SKU-A-001"));

    TenantContext.setCurrentTenant(2L);
    List<Sku> result = skuService.list();
    assertThat(result).noneMatch(s -> "SKU-A-001".equals(s.getSkuCode()));
}

// 库存防超卖并发测试
@Test
void concurrent_allocation_prevents_oversell() throws InterruptedException {
    // 初始库存 10，并发 30 个线程各申请 1 件
    int stock = 10, threads = 30;
    ExecutorService pool = Executors.newFixedThreadPool(threads);
    AtomicInteger success = new AtomicInteger();
    AtomicInteger fail = new AtomicInteger();
    CountDownLatch latch = new CountDownLatch(threads);

    for (int i = 0; i < threads; i++) {
        pool.submit(() -> {
            try {
                allocationService.allocate(buildRequest(1L, "SKU-001", 1));
                success.incrementAndGet();
            } catch (InsufficientStockException e) {
                fail.incrementAndGet();
            } finally { latch.countDown(); }
        });
    }
    latch.await();
    // 严格等于库存数，不超卖
    assertThat(success.get()).isEqualTo(stock);
    assertThat(fail.get()).isEqualTo(threads - stock);
}

// 计费流水幂等测试
@Test
void billing_record_is_idempotent() {
    billingService.recordOutbound(1L, 100L, "SKU-001", 5);
    billingService.recordOutbound(1L, 100L, "SKU-001", 5); // 重复调用
    long count = billingRecordMapper.countByBizId("OUTBOUND_ORDER", 100L);
    assertThat(count).isEqualTo(1); // 只产生一条
}
```

### 12.2 覆盖率要求

| 模块 | 最低覆盖率 |
|---|---|
| 多租户隔离逻辑 | 90% |
| WMS 库存分配（FIFO + 防超卖） | 90% |
| 计费流水产生 | 85% |
| OMS Outbox 推送与重试 | 85% |
| 其他业务逻辑 | 70% |

---

## 十三、实施路线图

```
Week 1-2   多租户基础（ERP+OMS）
           ├─ sys_tenant + sys_user_tenant 表创建
           ├─ 数据迁移脚本（ADD COLUMN + 回填）
           ├─ TenantContext + TenantFilter 实现
           ├─ MyBatis-Plus 多租户插件启用
           ├─ 登录流程绑定 tenant_id
           └─ 全量租户隔离测试

Week 3-4   OMS 模块（ERP+OMS）
           ├─ oms_outbound_order / oms_inbound_order 建表
           ├─ Outbox Pattern 实现（OmsSyncOutbox + 定时推送）
           ├─ 现有出库/入库流程接入 OMS 层
           └─ OMS 前端页面（出库单 / 入库单列表）

Week 5-7   WMS 模块（同进程，无需新服务）
           ├─ wms_physical_inventory 等新表建表（同库）
           ├─ WMS 包结构整理（com.erp.admin.wms 重构）
           ├─ FIFO 分配引擎实现（SELECT FOR UPDATE 行锁）
           ├─ OMS → WMS Service 调用接口定义与联调
           ├─ RBAC 角色配置（ROLE_WMS_MANAGER / ROLE_WMS_OPERATOR）
           └─ WMS 菜单权限配置（BallCat sys_menu 数据初始化）

Week 8-10  计费系统
           ├─ 费率卡管理（CRUD + 租户特殊费率）
           ├─ 作业计费流水（入库 / 出库 / 物流 / 增值服务）
           ├─ 仓储费自动计算（货架租用 × 费率 × 天数）
           ├─ 月度账单生成调度任务
           └─ 账单管理前端（管理员 + 货主视图）

Week 11-12 测试 & 上线
           ├─ 租户隔离压测
           ├─ 防超卖并发测试
           ├─ 数据迁移演练（staging 环境全流程）
           ├─ 灰度上线（先切 1 个租户验证）
           └─ 监控告警配置（Prometheus + Logback）
```

---

## 十四、关键技术难点

| 难点 | 解决方案 | 状态 |
|---|---|---|
| `ALTER TABLE` 锁表影响在线服务 | 低峰期执行；大表（`>100万行`）用 `gh-ost` 在线 DDL | 待执行 |
| **多租户查询性能退化** | **迁移时同步重建所有索引，`tenant_id` 必须置首列；超 5 个租户后对热表分区** | ✅ 策略确认 |
| **WMS 与 ERP+OMS 的 tenant_id 过滤隔离** | **WMS Mapper 加 `@InterceptorIgnore(tenantLine="true")`；TenantFilter 按角色决定是否注入** | ✅ 策略确认 |
| 库存防超卖 | `SELECT FOR UPDATE` 行锁 + 乐观锁 `version` 双保险 | 待实现 |
| FIFO 分配性能（海量批次） | 联合索引 `(tenant_id, warehouse_id, sku_code, inbound_date, pick_order, id)` | 待实现 |
| 多租户插件与分页插件冲突 | `TenantLineInnerInterceptor` 必须在 `PaginationInnerInterceptor` 之前注册 | 待实现 |
| WMS 跨租户日志可读性 | MDC 写入 `tenantId`，Logback pattern 强制输出 `[tenant=xxx]` | 待实现 |
| 计费准确性 | 流水在作业事务内**同步写入**，月末幂等汇总 | 待实现 |
| SKU 编码跨租户冲突 | 唯一索引改为 `(tenant_id, sku_code)` | 待执行 |
| BallCat 多租户与 SNAPSHOT 版本兼容 | 验证 `ballcat-business-bom 2.0.0-SNAPSHOT` 是否已内置多租户支持 | 待验证 |

---

## 十五、待决策清单

### ✅ 已确认

| 决策 | 结论 | 确认日期 |
|---|---|---|
| **【决策 A】WMS 是否独立部署** | 不独立。模块化单体，同进程 Spring Bean 调用，RBAC 隔离界面 | 2026-06-27 |
| **【决策 B】WMS 数据库** | 共用现有 MySQL 实例（:3307，erp 库），`wms_` 前缀区分 | 2026-06-27 |
| **【决策 C】OMS→WMS 通信** | 同进程直接方法调用，不需要消息队列 | 2026-06-27 |
| **【索引策略】多租户性能** | 所有索引 `tenant_id` 首列；超 5 租户后对热表分区；极端情况动态数据源 | 2026-06-27 |
| **【决策 D】租户开通方式** | 系统管理员后台手动开通，无自助注册 | 2026-06-27 |
| **【决策 E】登录入口（修订）** | 前端登录页设**三个入口**：「货主 ERP」/「WMS 服务商」/「平台超管」；BallCat 菜单沿用当前粒度 | 2026-06-27 |
| **【决策 F】跨租户用户** | 一个用户只属于一个租户；`sys_user_tenant.user_id` 唯一约束；超管不绑租户，靠角色区分 | 2026-06-27 |
| **【决策 G】双层租户架构** | WMS 也是多租户，分两层：WMS 服务商（第一层，3PL运营方）> ERP 货主（第二层，由WMS服务商开通）；`sys_tenant.tenant_type` + `parent_wms_tenant_id` 区分 | 2026-06-27 |
| **【决策 H】货架分配与租金** | 货架按编号（1号架、2号架…）由平台超管分配给 WMS 服务商；租金按 `wms_rack_assignment` 记录的各排月单价之和自动计算；历史记录归档，不可随意改价 | 2026-06-27 |
| **【决策 I】计费归属** | 计费对象为 WMS 服务商（第一层租户）；货主操作量追溯到所属 WMS 服务商月度账单；`wms_billing_record` 双字段 `wms_tenant_id` + `erp_tenant_id` 同时记录 | 2026-06-27 |
| **【决策 J】物理库存双 ID** | `wms_physical_inventory` 保留 `wms_tenant_id`（货架所属）和 `erp_tenant_id`（货物归属），两个维度均有索引 | 2026-06-27 |

---

### ⏳ 待讨论

> 目前所有已知决策均已确认，暂无待讨论项。

---

### 📋 附：全部决策速查

| # | 决策 | 结论 |
|---|---|---|
| A | WMS 是否独立部署 | 模块化单体，同进程 Bean 调用 |
| B | WMS 数据库 | 共用 MySQL :3307，`wms_` 前缀 |
| C | OMS→WMS 通信 | 同进程方法调用 |
| D | 租户开通方式 | 管理员后台手动开通 |
| E | 登录入口 | 三个入口：货主 / WMS 服务商 / 平台超管 |
| F | 跨租户用户 | 一用户一租户，唯一约束 |
| G | 租户层级 | 双层：WMS 服务商（第一层）> ERP 货主（第二层） |
| H | 货架分配与租金 | 超管按编号分配给 WMS 服务商，月单价记录在分配表 |
| I | 计费归属 | 以 WMS 服务商为单位出账，货主操作量归集 |
| J | 物理库存双 ID | `wms_tenant_id`（货架归属）+ `erp_tenant_id`（货物归属） |
| K | 账单结算流程 | 线下对账，系统手动标记 PAID |
| L | 个性化费率 | 独立折扣表（百分比），不复制整张费率卡；未配置则折扣率=0 |
| M | 现有 `wms_*` 表改造 | 分两类：ERP 侧单据加 `erp_tenant_id`；物理执行表用新 `wms_physical_inventory` 替代 |
| N | 货主可见计费明细 | 可查 `wms_billing_record` 中 `erp_tenant_id` = 本租户的所有操作明细；货架租金/司机费不可见 |
| O | WMS→ERP 计费方式 | WMS 服务商定义「物流产品」（按尺寸/取货/配送等维度组合并定价），上架推送给本司货主；货主下单时选择产品并锁定价格快照，发货后按产品计费；月末按货主分别出账（链路二，独立于链路一） |
| O-2 | 产品定向开放与专属定价 | 产品分 `PUBLIC`（全货主可见）/`RESTRICTED`（仅授权货主可见）；`wms_logistics_product_grant` 表支持对特定货主定向开放 + 专属价覆盖；下单取价 `COALESCE(custom_price, price)`，并锁定快照 |

---

*文档结束 — 请从第一个议题开始逐步讨论*
