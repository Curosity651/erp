# 海外仓平台 · 数据分析页 —— 设计与交接文档

> 面向接手的 AI/开发者（尤其是**后端**）。本页前端已完成，以 mock 数据驱动。
> 本文件说明：这页是什么、展示什么、前端怎么搭的、**后端需要实现的接口契约**、以及如何切到真实数据。
> 截至 2026-07-01。

---

## 0. 背景与定位

本系统是「三个相互隔离的平台/身份」的多租户系统：

- **海外仓平台** `OVERSEAS_PLATFORM`：物理仓库运营方，`sys_tenant.id = -1`。**看全部货主数据**。
- **WMS 服务商** `WMS_OPERATOR`：租货架给货主的中间人。
- **货主** `ERP_USER`：货物主人。

「数据分析」菜单（`statistics` 900000）按身份各看各的看板（前端 `src/utils/menu-filter.ts` 裁剪）：

| 身份 | 菜单项 | 菜单ID | 状态 |
|---|---|---|---|
| 货主 ERP_USER | Dashboard + 销售目标 | 900100 / 900200 | 已实现（销售分析） |
| **海外仓平台 OVERSEAS_PLATFORM** | **平台数据分析** | **900300** | **本文档 —— 前端已建，待后端接口** |
| WMS 服务商 WMS_OPERATOR | 运营数据分析 | 900400 | 仍为占位页（未做） |

**本页 = 900300「平台数据分析」，仓储运营视角，跨全部货主聚合。** 与货主那套「销售额/汇率/SKU排行」完全不同。

---

## 1. 页面展示内容（v1）

四大板块，自上而下：

### A. 运营总览 KPI（顶部四色数字卡）
跨全部货主的即时状态：
- **在库总量**：总件数 + 涉及 SKU 数 + 涉及货主数
- **今日入库**：件数 + 单数
- **今日出库**：件数 + 单数
- **待处理作业积压**：待收货 / 待上架 / 待拣货打包 / 待出库（4 个待办单据数）

### B. 仓容利用率（平台最核心的自有指标）
- **库位占用率**（左，占 14/24）：各仓库「已占用库位 / 总库位」进度条，按占用率降序；≥90% 红、≥75% 橙、其余蓝。
- **分区占用**（右，占 10/24）：标准区/不良品区/退货区/暂存区 的库存件数环形图。四色与库位网格图例一致（标准蓝 `#1890ff` / 不良红 `#ff4d4f` / 退货绿 `#52c41a` / 暂存灰 `#8c8c8c`）。

### C. 吞吐趋势（整行）
- 入库单（蓝柱）/ 出库单（绿柱）/ 退货单（橙线）**按日**趋势。
- **度量 = 单据数**（非件数）。数据点 > 15 天时出现缩放滑块。

### D. 货主排名（两张并排）
- **货主在库占用 TOP10**（横向条形，单位：件）—— 谁占了最多仓容，计费/催单依据。
- **货主吞吐 TOP10**（横向条形，单位：单）。

### 筛选栏
- **时间范围**：今日 / 昨日 / 最近7天 / 本月 / 上月 / 自定义区间（默认最近7天）。
- **仓库**（多选，不选=全部）：数据源 `GET /wms/warehouse/options`。
- **货主**（多选，不选=全部）：数据源 `GET /tenant/erp-tenants`。
- 刷新按钮 + 莫斯科时间 + 最后更新时间。

> 布局示意
> ```
> [时间▾] [仓库▾] [货主▾]                      莫斯科时间 · 更新于 · ⟳
> ── A：4 张 KPI 卡 ─────────────────────────────────────────
> ── B：库位占用率(左) │ 分区占用环形(右) ───────────────────
> ── C：吞吐趋势（整行 柱+线）──────────────────────────────
> ── D：在库占用TOP10(左) │ 吞吐TOP10(右) ─────────────────
> ```

---

## 2. 前端实现（已完成）

技术栈：Vue3 + Vite + TS + ant-design-vue + vue-echarts（`echarts/core` 按需 `use([...])`）。
复用货主 dashboard 的骨架：`容器 + 筛选栏 + a-row/a-col 卡片栅格`，单接口拉一个 VO、每张卡片吃一段数据。

### 新建文件清单

**API 层** `src/api/platform-dashboard/`
- `types.ts` —— **数据契约**（本文件第 3 节即由此而来）。
- `mock.ts` —— mock 数据生成器（后端接通后可整体删除）。
- `index.ts` —— `getPlatformDashboardData(params)`；顶部有 `const USE_MOCK = true` 开关。

**页面 + 组件** `src/views/dashboard/platform/`
- `index.vue` —— 页面骨架（含筛选状态、时间范围派生、loadData）。
- `components/PlatformFilterBar.vue` —— 筛选栏。
- `components/PlatformKpiCards.vue` —— A 运营总览 KPI。
- `components/CapacityUtilizationCard.vue` —— B 库位占用率。
- `components/ZoneOccupancyCard.vue` —— B 分区占用环形图。
- `components/ThroughputTrendCard.vue` —— C 吞吐趋势。
- `components/OwnerRankingCard.vue` —— D 货主排名（复用，props 传 title/unit/color/data）。

**路由** `src/router/dynamic-routes.ts`
- 新增 `COMPONENT_OVERRIDES`（按菜单 path 覆盖组件）。
- 菜单 900300 在 DB(`sys_menu.uri`) 中仍指向占位组件 `common/Placeholder`；前端用 `COMPONENT_OVERRIDES['platform-dashboard']` 覆盖为真实页面，**不改数据库**。
- 若后续想让后端把 `sys_menu.uri` 从 `common/Placeholder` 改指向真实组件路径（如 `dashboard/platform/index`），可移除此 override；二者取其一即可，勿重复。

### 校验状态
- `npx vue-tsc --noEmit` 通过（无类型错误）。
- `npx eslint` 通过（无 error/warning）。
- 前端改动走 Vite dev/HMR，无需重打包。以 `super_admin`（海外仓平台入口）登录 →「数据分析 → 平台数据分析」查看。

---

## 3. 后端需实现的接口契约 ⭐

前端目前 mock 驱动。后端只需实现下面这一个聚合接口，然后前端把 `src/api/platform-dashboard/index.ts` 里 `USE_MOCK` 改成 `false` 即接通，**前端零改动**。

### 接口

```
POST /api/platform-dashboard/data
```

**入参** `PlatformDashboardQueryParams`：
```ts
{
  startDate: string        // YYYY-MM-DD
  endDate: string          // YYYY-MM-DD
  warehouseIds?: number[]  // 仓库过滤，不传=全部
  erpTenantIds?: number[]  // 货主过滤，不传=全部
}
```

**返回** `ApiResult<PlatformDashboardDataVO>`：
```ts
PlatformDashboardDataVO {
  // A 运营总览
  opsOverview: {
    onHandQty: number            // 在库总件数
    skuCount: number             // 涉及 SKU 数
    ownerCount: number           // 涉及货主数
    todayInboundQty: number      // 今日入库件数
    todayInboundOrders: number   // 今日入库单数
    todayOutboundQty: number     // 今日出库件数
    todayOutboundOrders: number  // 今日出库单数
    pending: {                   // 待处理作业积压（单据数）
      receiving: number          // 待收货
      putaway: number            // 待上架
      pickPack: number           // 待拣货打包
      outbound: number           // 待出库
    }
  }

  // B 仓容利用率
  capacity: {
    byWarehouse: Array<{
      warehouseId: number
      warehouseName: string
      used: number               // 已占用库位数
      total: number              // 库位总数
    }>                           // 占用率 used/total 由前端算
    byZone: Array<{
      zone: 'STANDARD' | 'DEFECTIVE' | 'RETURN' | 'TEMP'
      locationCount: number      // 该分区库位数
      invQty: number             // 该分区库存件数（环形图取值）
    }>
  }

  // C 吞吐趋势（度量=单据数），三个数组与 dates 等长、下标对齐
  throughput: {
    dates: string[]              // YYYY-MM-DD，按日
    inbound: number[]            // 每日入库单数
    outbound: number[]           // 每日出库单数
    returns: number[]            // 每日退货单数
  }

  // D 货主排名（各取 TOP10，后端可直接排好序）
  ownerRanking: {
    byStock: Array<{ erpTenantId: number; ownerName: string; qty: number }>       // qty=在库件数
    byThroughput: Array<{ erpTenantId: number; ownerName: string; qty: number }>  // qty=吞吐单数
  }
}
```

> 契约权威在 `src/api/platform-dashboard/types.ts`，与本节一致；若二者冲突以 types.ts 为准。

### 后端数据来源提示（供实现参考，需按实际表结构核对）
- 平台身份 = 看全部货主，**不要**按 `erp_tenant_id` 收窄；仅当入参 `erpTenantIds` 非空时过滤。
- 后台聚合无 `TenantContext` 时注意：平台聚合本就跨租户，走平台可见范围即可（参考现有 `ErpOwnerScopeService`/平台 DataPermission 口径）。
- A 在库/SKU/货主数、B 库位占用、D 在库排名 ← 仓库级库存 `wms_inventory` / 库位 `wms_location` / 落位 `wms_physical_inventory`。
- B 分区占用 ← `wms_zone` + 库位 zone_id + 批次 `zone_id`/件数。
- C 吞吐、A 今日入出库/待处理 ← 各类单据表（采购/退货/销售出库/物流单）+ 作业状态。
- **口径以「底层数据表重构设计.md」为准**，本文档只定前端需要的形状。

---

## 4. 待确认 / 可迭代（v2）

v1 只做 A/B/C/D。以下为已讨论但未纳入 v1，按需迭代：
- **E 库存健康**：库存预警（低于阈值 SKU 数）、呆滞/不良占比。
- **F 平台财务**：仓储费/操作费应收概览、账单状态（接 platform-finance）。
- C 吞吐度量目前=单据数；如需「件数」可加切换开关（mock 已可扩展）。
- 货主/仓库下拉在业务数据为空时为空列表属正常（dev-clean 态）。

---

## 5. 一句话交接
> 前端页面 `src/views/dashboard/platform/` 已完成并用 mock 跑通；后端实现 `POST /api/platform-dashboard/data`（契约见第 3 节 / `types.ts`）后，把 `src/api/platform-dashboard/index.ts` 的 `USE_MOCK` 改 `false` 即上线，前端不用动。
