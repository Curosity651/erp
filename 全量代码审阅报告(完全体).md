# 全量代码审阅报告(完全体)

> **范围**:`erp-backend`(约 1043 个 Java 文件 + XML/SQL 迁移)与 `erp-frontend`(约 613 个 `.vue`/`.ts`)全量深度审阅。
> **性质**:**仅记录、不修改任何代码**。
> **方法**:14 个只读子代理分域并行精读(后端 9 域 + 前端 5 域)+ 5 个只读子代理专项审计 WMS 库存正确性全链路(入库→上架→拉单→出库→退货);结果经交叉验证、去重、按模块+严重级别合并;4 个反复出现的"待确认"项已在源码/迁移脚本/配置中亲自核实定论。
> **日期**:2026-07-06。
> **说明**:本文是《库存正确性审计报告》与《全量代码审阅报告》的合并完全体,库存链路缺陷已完整并入第五章,不再外部引用。

---

## 目录

1. 总体结论(TL;DR)
2. 架构总览与评价
3. 系统性主题(跨模块,优先处理)
4. 本人核实结论(4 项"待确认"定论)
5. **WMS 库存正确性专章**(完整缺陷明细 C/H/M/L)
6. 其余模块 Bug 清单(后端 + 前端)
7. 代理间分歧(需人工裁决)
8. 已核对无问题(正面确认)
9. 修复优先级建议
10. 待确认项汇总
11. 附录:审阅覆盖清单

---

## 1. 总体结论(TL;DR)

系统整体架构**清晰、有纪律**:三平台身份隔离、MyBatis 双拦截器(行级租户 + 数据权限)、过账引擎单一账本、前端统一 `ApiResult` 契约 + fail-closed 菜单裁剪,主干设计正确。缺陷不在"架构错",而在**三条系统性裂缝**沿各模块反复渗漏:

1. **"无租户上下文"语义不统一(头号安全主题)** —— 读 fail-open(看到全部)、写盖默认租户 1、白名单拦截器无上下文时静默跳过注入;叠加异步 `ThreadLocal` 丢失,构成跨租户串数/串写风险。
2. **核心库存双写者冲突** —— `wms_inventory` 被"过账引擎增量"与"聚合器全量覆盖"两个语义互斥写者同改一行(第五章 C-1)。
3. **前端契约错配(v-model 命名 / 分页字段 / 取 id / 导出 blob)** —— 单点即致整块功能静默失效(抽屉打不开、列表恒空、导出不下载、确认流程错单)。

**严重级别汇总:**

| 级别 | WMS 库存链路 | 其余后端 | 前端 | 合计 |
|---|---|---|---|---|
| Critical | 3 | 2 | 3 | 8 |
| High | 7 | 12 | 12 | 31 |
| Medium | 8 | 15 | 22 | 45 |
| Low | 7 | 若干 | 若干 | 50+ |

---

## 2. 架构总览与评价

### 2.1 后端
- **分层**:`controller → service(ExtendServiceImpl) → mapper(MyBatis-Plus + XML)`;`WrappersX.lambdaQueryX`、`selectObjs`、`@Version` 乐观锁、`casOrderStatus` 状态机、`@Transactional`。规范统一。
- **租户隔离三拦截器**(`ErpMybatisPlusConfig`):
  - `TenantLineInnerInterceptor`(`ErpTenantLineHandler`)—— 白名单表自动注入 `WHERE tenant_id`(`erp_order/erp_order_item/wb_report_detail/wb_financial_sync_*/shop/sku/sku_mapping/category/sales_target` 等);**`wms_*` 全部不在白名单**;无上下文时静默跳过注入(fail-open)。
  - `DataPermissionInterceptor`(`WmsOwnerDataPermissionHandler`)—— 仅对 6 张货主单据表 + `wms_region_inventory` 注入 `erp_tenant_id`(`wms_purchase_order/shipping_order/sales_outbound_order/return_inbound_order/logistics_provider/inventory_config/region_inventory`)。
  - **核心库存三表 `wms_inventory/wms_physical_inventory/wms_stock_flow` 不在任何白名单** → 隔离完全依赖 service 显式带 `erp_tenant_id`(系统性风险根源)。
- **上下文**:`TenantContext`(`InheritableThreadLocal`,货主维)+ `WmsTenantContext`(服务商维);`TenantResolveInterceptor` 每请求解析,`runAs` 跨租户;`BLOCK_TENANT_ID=-1`(平台)、`UNBOUND_TENANT_ID=-999`(fail-closed 哨兵)。
- **正面基础设施(已核实,均为非 bug)**:
  - DB 唯一键含租户:`wms_inventory.uk_tenant_wh_sku(wms_tenant_id,erp_tenant_id,warehouse_id,sku_code)`、`wms_physical_inventory.uk_batch`、`wms_monthly_bill.uk_bill_month_tenant`、`wms_region_inventory.uk_tenant_region_sku`(V12/V25/V31)。
  - `DashboardService` 并行查询用 `supplyAsyncWithTenant` 在子线程重建 `erpTenantId/wmsTenantId`,正确规避"异步丢租户上下文"。
  - WB 财务同步执行器 `setCurrentTenant(task.getTenantId())` 显式传播;调度器无上下文读全部、逐任务 runAs,设计正确。
  - 金额 `/100`(戈比→卢布)每条路径恰好一次,逐路径核对**无 100× 错配**。
  - `TenantResolveInterceptor` 对 Web 请求 **fail-closed**:已登录未绑定→`UNBOUND(-999)`(业务表 0 行);租户停用→403。
  - 未显式配置事务隔离级别 → 按 MySQL InnoDB 默认 **REPEATABLE READ(RR)** 分析。

### 2.2 前端
- **契约基线**:axios 拦截器对 JSON 返回 `response.data`(业务拿到一层 `ApiResult{code,data,message}`),`isSuccess(code===200)`;`json-bigint(storeAsString)` 保 ID 精度;`mergePageParam` 把 `current/pageSize→page/size`(后端真实用 `page/size`);ProTable 读 `result.data.records`;`qs arrayFormat:'repeat'`。全量扫描**未见 `.data.data` 双解包/`res.records` 越层的系统性错配**。
- **权限模型** `hasPermission` fail-closed;`menu-filter.ts` 身份菜单裁剪质量最高(未知身份返回 `[]`、成环守卫、系统/统计子树按实例隔离),与"三平台隔离"口径完全一致、**无缺陷**。
- 三平台订单(WB/Ozon/Yandex)逻辑抽到 `hooks/useOrder*` + `components/Order*Cell` 复用,结构良好;`ref/reactive/computed` 等经 `unplugin-auto-import` 自动导入,页面未显式 import 非 bug。

---

## 3. 系统性主题(跨模块,优先处理)

### S1. "无租户上下文"语义三处不一致(安全)
- **读 fail-open**:`ErpOwnerScopeService.readScope()` 无上下文返回 `null` → 查询不收窄 → 看到全部租户。
- **写盖默认租户 1**:`MyMetaObjectHandler` 无上下文时把 `tenant_id` 填默认 1。
- **白名单静默跳过**:`ErpTenantLineHandler` 无上下文时不注入条件(fail-open)。
- **叠加异步 `ThreadLocal` 丢失**(memory `async-loses-tenant-context`):并行/`CompletableFuture` 未显式传播即串数据。
- 命中点:system/tenant(C1)、product/shop(H3)、financial 对账(H2)、warehouse/return(H1)、库存 M-2/L-7 多域独立撞到同一根因。

### S2. `@PreAuthorize` / 越权覆盖不齐(安全,已重新定级)
- **核实结论**:安全白名单 `ignore-urls` 仅含 `/public /actuator /doc.html /swagger /captcha`,**不含** `/system/file`、`/tenant`、`/system/exchange-rate`。故这些接口**并非匿名公网可访问**,而是"**任意已登录身份**均可访问"(缺方法级 `@PreAuthorize` 或对象级 owner 校验的横向越权/IDOR)。据此把 system/tenant 代理原 H3–H6 由"未授权公网访问"**降级为"越权/IDOR"**。
- 命中点:`OzonOrderController`、`LabelBatchController`、`DashboardController`、租户 provisioning、文件下载 IDOR、盘点服务(库存 M-4)。

### S3. 前端契约错配单点致整块失效
- v-model 命名 `visible` vs 仓库约定 `open`(过账详情抽屉、财务对账抽屉、多个财务组件)。
- 分页字段 `list` vs ProTable 契约 `records`(店铺列表)。
- 新建取 id(`ApiResult<void>` 却读 `res.data as number`)。
- 导出普遍缺 `responseType:'blob'` + 不调 `remoteFileDownload`(退货入库、销售出库、采购单路径前缀错)。

### S4. 重复实现与死代码坏味道
- 后端:`TaxRateValidator` 死代码(严格校验被 `GenericTaxRateValidator` 仅判 null 架空)。
- 前端:三套货币格式化(`amount-utils`/`currency-utils`/`financial-utils`)语义各异;两套 `getFileExtension`(带点/不带点,同名反义);`sanitizeFileName` 空实现;`SkuBriefVO` 断裂导入(多文件从未 re-export 的路径引入,`vue-tsc` 报错)。

---

## 4. 本人核实结论(原 4 项"待确认"定论)

| 项 | 核实结果 | 定论 |
|---|---|---|
| SecurityFilterChain 是否 permitAll `/system/file` `/tenant` `/system/exchange-rate` | `ignore-urls` 仅 `/public /actuator /doc.html /swagger /captcha`,**不含**上述路径 | **非公网未授权**;实为"任意已登录身份越权/IDOR",相关项按越权定级(S2) |
| `CurrencyUtils.formatCurrency` 除 100 是否致 CNY 100× | 确认 `divide(100)`;仅 `WbOrderQueryService:228`/`OzonOrderQueryService:208` 导出 CNY 列调用,前后端均按子单位处理 | **非 bug**(除非 `convertedAmount` 存主单位——需后端确认存储单位) |
| DB 唯一约束(库存/物理/账单/配置) | V12/V25/V31 均含租户维度唯一键;但 **V28 拣货分配表 `wms_outbound_pick_allocation` 无唯一键** | 库存/账单有 DB 兜底;**拣货分配确认无兜底 → 证实 wms-outbound H1 / 库存 H-2** |
| traceId 是否仅服务端 | 未在前端消费路径发现回传/日志泄漏 | 低风险,不单列 |

---

## 5. WMS 库存正确性专章(完整缺陷明细)

> 范围:货主端入库 → 仓库上架 → 订单拉取 → 完整订单出库 → 退货 全链路的库存正确性(能否保证**每个货主/租户**库存始终正确)。字段:【级别】· 流程 · 位置 · 描述 · 触发场景 · 影响 · 修复方向 · 来源。
> 统计:Critical 3 · High 7 · Medium 8 · Low 7。

### 5.0 头号结论
**`wms_inventory`(库存快照)存在两个语义互斥的写者,是全局头号缺陷,4 个独立子代理各自撞到同一根因:**
- **写者甲(过账引擎,增量)**:`StockPostingService.post → InventoryPostingEngine`,把订单预占/出库/入库以 delta 增量改 `wms_inventory` 的 available/reserved/in_transit,**从不回写批次表** `wms_physical_inventory`。
- **写者乙(聚合器,全量覆盖)**:`WmsInventoryAggregator.refreshSnapshot` 按 Σ批次 **绝对值整行覆盖** available/reserved/damaged;上架/移库/报废/下架/签出每次都会触发它。

两者写同一行,一个增量一个覆盖,根本不兼容。任何一次 `refreshSnapshot` 都会把预占、已出库扣减凭空抹除/复活 → 超卖、预占丢失、账实不符(见 C-1)。其余缺陷两类:①并发守卫缺失(入库上架/下架/退货质检均无足够 CAS/乐观锁/行锁);②退货链路两套独立入口写同一账本且互不感知(见 C-2)。

### 🔴 CRITICAL

#### C-1 双库存权威冲突:过账增量被聚合器"按批次重算"整行覆盖 → 系统性超卖/预占丢失
- **流程**:贯穿订单拉取/确认预占、出库、入库上架、退货(所有会触发 `refreshSnapshot` 的动作)。
- **位置**:增量写者 `InventoryPostingEngine.java:106-166`(`processWarehouseItems`,`updateWithVersion` 按内存 delta 改 `wms_inventory`),入口 `StockPostingService.post`;预占 `CustomOutboundFacade.java:203-213`、`SalesOutboundFacade.java:317-326`(AVAILABLE−/RESERVED+)。覆盖写者 `WmsInventoryAggregator.refreshSnapshot:95-128`(尤其 L116-117 绝对值覆盖 `available=Σ(quantity−reserved_qty)`、`reserved=Σreserved_qty`);触发点遍布 `WmsPhysicalInventoryService`(putaway/reserve/scrap/move 各 `:91/117/133/158`)、`OutboundPickingService:214-216`、`OutboundShippingService:188-190`。
- **触发场景(纯顺序即可复现,无需并发)**:某货主同仓同 SKU 物理 100。① 上架 100 → 聚合器写 available=100;② 销售单确认预占 30(过账 RESERVED+30/AVAILABLE−30)→ available=70,reserved=30(**批次仍 100/0**);③ 同 SKU 再上架 50 → `refreshSnapshot` 从批次重算 available=150、reserved=0 **覆盖** → 第②步预占彻底消失。出库同理:过账扣了 available 但批次未减,下一次 refresh 让已出库货"复活"。
- **影响**:每个货主 available 被反复复活/高估、reserved 与出库扣减被抹掉 → 跨订单超卖、预占丢失、账实不符。`AVAILABLE` 桶 `allowNegative=true`(见 L-1)使后续扣减无负库存保护,最终批次被扣穿或假缺货 BACKORDER。取消回补(`CustomOutboundFacade.cancel` doReversePosting `:232-273`)在预占已被抹后再加 available,会把 available 抬到超过物理量,进一步污染。
- **修复方向**:统一为**单一库存权威**。二选一:(a) 确认预占/出库也扣减批次层 `reserved_qty`/`quantity`,让聚合器成为唯一写者;(b) `refreshSnapshot` 只重算 available/damaged,reserved/in_transit 交给过账引擎、不做绝对覆盖。
- **来源**:核心账本#1、出库#1、退货#1(背景)、入库(HIGH-2 同机制)—— **4 代理独立收敛,置信度最高**。

#### C-2 退货链路两套独立入口写同一账本;`return_status` 默认值致同单被二次入库
- **流程**:退货。
- **位置**:路径①(货主/ERP 侧一步到位)`ReturnInboundService.create():145-155,200-210` 直接 `StockPostingService.post(RETURN_RECEIVE)`(增量、不建批次);路径②(平台质检)`ReturnQcService.receive/qc:135-231` 走 `physicalInventoryService.putaway()`(建批次 + `refreshSnapshot` 全量重算);二者写同一行 `wms_inventory`。`V30__return_qc.sql:7`:`return_status` 默认 `RETURN_PENDING`。
- **触发场景**:货主在"退货入库"建单(库存已 +N,但 `create()` **从不设 `returnStatus`** → DB 默认 RETURN_PENDING)→ 同单**又出现在平台退货质检工作台**为"待收货"→ 平台收货+质检+上架,再入库一次。
- **影响**:两种坏法(取决于时序):① `create()` 的 delta 无对应批次,后续任何 `refreshSnapshot` 都按 Σ批次全量覆盖抹掉 create() 加的量 → 少计(与 C-1 同根);② 若走 QC 上架 → 双份过账/流水、残品/报废分流口径两路径不一致 → 账实与流水皆不符。
- **修复方向**:退货只保留**单一写库入口**。要么 `create()` 只建单(status=RETURN_PENDING)、由 QC 路径唯一入库;要么 QC 工作台过滤掉已由 create() 入库的单并禁止二次上架。
- **来源**:退货#1。

#### C-3 乐观锁重试在同一事务内重复应用"已成功分组" → 重复过账、流水≠快照
- **流程**:所有多分组过账(多 SKU/多仓单据,常见)。
- **位置**:`InventoryPostingEngine.java:47-60`(`execute` 的 `MAX_RETRY` 循环)+ `62-101/106-166`(`doExecute`/`processWarehouseItems`);外层 `StockPostingService.post` 为单一 `@Transactional`。
- **触发场景**:一次 `post()` 含 ≥2 分组;前一分组已 `updateWithVersion` 成功(未提交),后一分组因并发外部事务改了其行导致 `updateWithVersion` 返回 0(`:162`)抛乐观锁 → `execute` 捕获后整段重跑 `doExecute`。因单事务前面成功写入不回滚,`getOrCreate`(`:123`)在同事务读到自己刚改过(未提交)的行,把已成功分组的 delta 再加一遍。
- **影响**:前序分组数量翻倍写入;流水仅在最终成功那次 `saveBatch`(`:95-97`)写一条 → `wms_stock_flow` 累计 ≠ `wms_inventory` 快照,难以对账。
- **修复方向**:重试粒度改为"仅重试失败分组",或每次尝试用独立新事务(`REQUIRES_NEW`)保证失败回滚;绝不能在同事务里基于已改状态重算增量。
- **来源**:核心账本#2。(对照:`WmsInventoryAggregator` 按绝对值 set 的重试无此问题,佐证本条。)

### 🟠 HIGH

#### H-1 入库上架无 `@Version`/行锁/CAS → 并发重复上架致某货主 available 翻倍
- **位置**:`WmsInboundExecutionService.java:171-245`(状态读 `:173-175`、状态写 `:244-245`,`updateById` 返回值被丢弃);实体 `PurchaseInboundOrder.java` **无 `@Version`**。
- **触发场景**:同一 RECEIVED 入库单被并发提交两次上架(双标签页/重试/双操作员)。两事务在 RC 下都读到 RECEIVED、都过 assert、都各自 `putaway` 插一整套批次、都把状态更新成 COMPLETED(无版本条件均成功)。
- **影响**:`wms_physical_inventory` 出现两套重复批次,`refreshSnapshot` 重算 → 该货主该 SKU 的 available(次品则 damaged)翻倍。
- **补充**:`PurchaseInboundService.updateToSubmitted/update` 的 `if(!updated) throw OptimisticLockException` 是**假乐观锁**——实体无 `@Version`,`updateById` 只要行存在就返回 1。
- **修复方向**:上架读单 `SELECT … FOR UPDATE`;或加 `@Version` 并检查返回值;或 RECEIVED→COMPLETED 用条件 CAS 校验影响行数。
- **来源**:入库 HIGH-1。

#### H-2 下架(pick)缺幂等 CAS → 同单并发双分配/双扣减
- **位置**:`OutboundPickingService.java:161-225`,状态校验仅 Java 侧(`:165-173`),结尾 `salesOutboundMapper.updateById(order)`(`:218-222`)无条件写 PICKING、**无 `casOrderStatus`**。对照签出 `OutboundShippingService.java:160-164` 用 `casOrderStatus(PACKED→SHIPPED)` 抢占。**已核实:分配表 `wms_outbound_pick_allocation` 无任何唯一键(V28),无 DB 兜底。**
- **触发场景**:两个并发下架请求(或双击)同时读到 CONFIRMED 均通过校验;`FOR UPDATE` 只串行化批次访问、不保证订单级幂等。T1 分配+置 PICKING 提交后,T2 基于剩余可用再分配一次,为同一订单插入第二组分配。
- **影响**:签出时 `pickAllocationMapper.selectByOutboundOrderId`(`OutboundShippingService.java:167`)遍历两组分配,`quantity -= take` 执行两遍 → 物理库存超扣一倍订单量;或状态被并发覆盖为 BACKORDER。
- **修复方向**:下架入口用 `casOrderStatus(CONFIRMED/BACKORDER→PICKING)` 抢占;或对 `(outbound_order_id)` 分配写入加唯一约束/存在性检查。
- **来源**:出库#2。

#### H-3 聚合器 `refreshSnapshot` 乐观锁重试不重算批次 → RR 下形同虚设、RC 下丢更新低估
- **位置**:`WmsInventoryAggregator.refreshSnapshot:95-128`——桶值 `b = aggregate(batches)` 在重试循环之外只算一次(`:96-98`),循环内(`:115-125`)只 `selectById` 重读 version、不重算 `b`。
- **触发场景/影响(依隔离级别)**:**默认 RR**:循环内 `selectById` 仍是事务快照 → version 恒旧 → 3 次重试全失败 → 抛 `INVENTORY_VERSION_CONFLICT` 整单回滚(fail-safe 但重试形同虚设,并发同 SKU 频繁误报需人工重试)。**若切 READ_COMMITTED**:重读拿到新 version 成功,但 `b` 仍是旧快照(漏并发已提交批次)→ 用陈旧聚合覆盖 → 快照永久性偏低(丢更新)。
- **修复方向**:把 `aggregate(...)` 移进重试循环内每次重算;或改单条原子 UPSERT(`SUM` 子查询)消除"查-改-写"窗口。
- **代理分歧(已核对)**:入库判"丢更新低估"、核心判"安全"、横切据隔离级别调和(RR fail-safe / RC 丢更新)。**采信横切调和口径;严重级别取决于线上事务隔离级别,需人工确认。**
- **来源**:入库 HIGH-2 · 核心账本(对照正面确认)· 横切 D-1。

#### H-4 退货质检行无数量字段 → 同 SKU 多行重复整批上架(超量)
- **位置**:`ReturnQcService.java:162-224`(尤其 `:194 int qty = nz(item.getReceivedQty())`);`ReturnQcDTO.ReturnQcLineDTO` **无 quantity 字段**。
- **触发场景**:实收 10,前端对同一 SKU 提交两行(良品一行+次品一行,或误提交两次)→ 循环内对同一 SKU 连做两次 `putaway(10)`;两行填不同库位可绕过库位独占守卫。
- **影响**:该货主库存 +20(实退 10),凭空多计。
- **修复方向**:行级增加 quantity;循环前校验同 SKU 汇总=实收数;多行按数量拆分而非各自整批。
- **来源**:退货#2。

#### H-5 退货收货不校验实收 ≤ 应退、可注入任意 SKU → 幽灵库存
- **位置**:`ReturnQcService.java:101-130`(`:124 setReceivedQty(nz(...))` 仅 null 兜底)。
- **触发场景**:退货总量 5,收货填 500;或新增一条与本单无关的高价 SKU 收货行。
- **影响**:质检上架按实收数 putaway → 该货主 available/damaged 凭空多计;任意 SKU 被计入该货主库存。数量守恒被彻底突破。
- **修复方向**:`receivedQty` 上限=应退数;`line.skuCode` 必须属于退货单;`receivedQty ≥ 0` 显式断言。
- **来源**:退货#3。

#### H-6 退货质检状态流转非原子(TOCTOU)、无乐观锁 → 并发/重复提交双份上架
- **位置**:`ReturnQcService.java:137-141, 226-229`;`ReturnInboundOrder.java` **无 `@Version`**;状态 update 不带条件、无锁。
- **触发场景**:操作员双击或两请求并发。两事务都读到 QC_PENDING、都过守卫、都 putaway、都置 COMPLETED;各自事务开始时读的 `occupied` 集合互不可见,库位守卫拦不住。
- **影响**:同一退货上架两次 → 该货主库存翻倍。
- **修复方向**:实体加 `@Version`,或状态推进改条件 CAS `UPDATE … SET status=COMPLETED WHERE id=? AND return_status='QC_PENDING'` 并断言影响行数=1。
- **来源**:退货#4。

#### H-7 出库/待发/调拨的库存**展示**走"无货主维"查询 → 跨货主库存串数
- **位置**:`InventoryService.getStockMapByWarehouseAndSkuCodes`/`getAvailableStockMap`/`pageAvailableStock`(底层 `selectByWarehouseAndSkuCodes`/`selectAvailableStockPage` 仅按 `(warehouseId, skuCode)`,`toMap` 用 `(a,b)->a` 折叠)。调用点:`SalesOutboundService.java:131`、`CustomOutboundService.java:145`、`SalesOutboundController.java:232`(货主待发货列表)、`TransferOrderService.java:253/263`。
- **触发场景**:多个货主在同一「自有仓(共享物理仓)」持有同一 SKU 时,`selectList` 返回多行,`toMap` 任意保留一行 → 货主 A 看到的 available 实为货主 B 的数值。
- **影响**:跨货主库存数字泄露 + 误导性可用量/缺货状态。**边界**:均为展示/状态路径;权威扣减路径(`SalesOutboundFacade.checkStockSufficiency:228` 带 erpTenantId 重载、`TransferOrderService.validateItemQuantities:304`、过账引擎按货主分组)已正确带货主维,**不会超卖/错扣**。
- **修复方向**:展示路径统一改用带 `erpTenantId` 的重载,或删除无货主维方法。
- **来源**:横切 A-1。

### 🟡 MEDIUM

#### M-1 两写者建行键不一致(`wms_tenant_id`)→ 同 `(erp,wh,sku)` 分裂成两行(条件性)
- **位置**:`InventoryService.getOrCreate:77`(新建 `wmsTenantId=0`)vs `WmsInventoryAggregator.java:106`(新建用真实 `wmsTenantId`);读侧 `InventoryMapper.selectByErpWarehouseSku:57-64`(忽略 wms 维、`findFirst`)vs `selectByTenantKey:42-48`(精确匹配)。pick/ship 传 `0L`(`OutboundPickingService:183,215`、`OutboundShippingService:189`)而 `putaway` 用 `dto.getWmsTenantId()`。
- **触发场景**:当 `wms_tenant_id` 被填非 0 时:聚合器 `selectByTenantKey(5,…)` 命不中 → 再插一行 wms=5;`GROUP BY sku_code SUM` 汇总两行相加(重复计数),`findFirst` 取数只取一行(漏另一半);FIFO 按 wms=0 过滤查不到 wms≠0 批次 → 假缺货 BACKORDER。
- **影响**:同一货主库存分裂/重复计数/假缺货。**当前上架口径 wms 恒 0,问题被掩盖**;引入服务商货架维度即触发。
- **待确认**:部署中 `wms_tenant_id` 是否会非 0。
- **来源**:核心账本#3 · 出库#3 · 入库(补充)。

#### M-2 过账幂等靠"先查后插"、无 DB 唯一约束兜底,且幂等查询未按租户收窄
- **位置**:`StockPostingService.post:52-67`(`selectBySourceAndType` 后插入);`createPosting:256-266` 仅对 `posting_no` 处理 `DuplicateKeyException`;`StockPostingMapper.selectBySourceAndType:49-54` 不含租户条件;`wms_stock_posting` 仅普通索引(`V22:29`),未见 `(source_type,source_id,posting_type)` 唯一键。
- **触发场景**:① 并发重复提交两请求都读到 `existing==null` → 都过账 → 库存应用两次;② 若 `source_id` 非全局唯一,A 货主过账误命中 B 货主既有记录 → B 的过账被"幂等"跳过(漏过账)。
- **修复方向**:补 `(source_type,source_id,posting_type[,erp_tenant_id])` 唯一索引,`createPosting` 捕获对应 `DuplicateKeyException` 走幂等返回。
- **待确认**:基线 DDL 是否已建该唯一索引;`source_id` 是否全局唯一。
- **来源**:核心账本#4。

#### M-3 区域库存汇总:可用量按查看者收窄、预占/在途疑似全租户合计(代理分歧,待确认)
- **位置**:`RegionInventoryService.getRegionSummaryList:94-154`。可用量 `:107` 经 `readScope()` 收窄;预占 `selectReservedByRegionIds`、在途 `selectInTransitByRegionIds`。
- **代理分歧(须人工核实)**:核心账本代理判预占/在途查询无租户过滤→汇总全部货主→`regionAvailable=本货主可用−全租户预占`(可为负)、在途偏大→跨租户串数;横切代理判查的是 `wms_region_inventory`(**在数据权限白名单,自动注入 `erp_tenant_id`**)→无问题。
- **处置**:**需人工确认** `selectReservedByRegionIds`/`selectInTransitByRegionIds` 的真实目标表与是否命中 `WmsOwnerDataPermissionHandler` 白名单。
- **来源**:核心账本#5 vs 横切(正面确认)。

#### M-4 盘点服务缺 `assertPlatform()` → 越权可跨货主枚举/调整库存
- **位置**:`StocktakeService`(唯一无 `assertPlatform()` 的库存作业服务);`generateStocktakeItems:225` 用 `getAllByWarehouseId(warehouseId)`(无货主过滤)枚举该仓所有货主库存;`confirm:346` 逐货主过账调整。隔离仅靠菜单权限 `wms:stocktake:*`。
- **触发场景**:若 `wms:stocktake:add/confirm` 被误授予某货主/WMS 服务商,即可查看并调整其他货主在该仓库存。
- **影响**:跨货主库存枚举 + 潜在跨货主篡改。
- **修复方向**:`create`/`confirm`/`saveItems` 入口加 `assertPlatform()`。
- **来源**:横切 A-2。

#### M-5 退货质检遍历"提交行"而非"已收货明细" → 漏行货物永不入库且单据锁死
- **位置**:`ReturnQcService.java:162`(遍历 `dto.getLines()`)+ `226-229`(无条件置 COMPLETED)。
- **触发场景**:收货 2 个 SKU,质检只提交 1 行 → 第 2 个 SKU 的 `receivedQty` 永不进库,单据仍被置 COMPLETED 且状态机不允许再质检。
- **影响**:该货主实收货物少计(库存漏加),无补救路径。
- **修复方向**:质检必须覆盖全部已收货明细(Σ质检=Σ实收)方可 COMPLETED。
- **来源**:退货#5。

#### M-6 收货不校验实收 ≤ 应收(自定义入库链)→ available 超量放大
- **位置**:`WmsInboundExecutionService.receive:97-110`(实收原样写入无上限);`PurchaseInboundService.validateItemQuantities:287-293` 仅建单/编辑调用,收货链路未复用。
- **触发场景**:平台收货录入实收 > 应收。采购链有 `postRegionInTransitOut`→引擎"区域在途不足"兜底报错;但 **MANUAL/CUSTOM_RETURN 自定义链无 in_transit 出账**(`:114` 跳过),超量实收无拦截,经上架 `coversExactly` 全额流入 available。
- **影响**:自定义入库链下该货主 available 可被任意放大。
- **修复方向**:收货对每条明细校验 `actual ≤ expected`(自定义链尤需)。
- **来源**:入库 MEDIUM-2。

#### M-7 非规范 `quality` 字符串 → 批次从 available/damaged 两桶双双静默消失(不守恒)
- **位置**:`WmsInventoryAggregator.aggregate:71-80`(大小写敏感 `"DAMAGED".equals(quality)`,否则走 GOOD 分支且需 `allocatable==1`);上架 `WmsInboundExecutionService.normalizeQuality:375-377`(仅 null/空→GOOD,其余原样透传);`InboundPutawayDTO.java:50-51` 的 `quality` 是自由 `String`、无枚举校验。
- **触发场景**:传入 `"damaged"`(小写)/`"DEFECTIVE"`/`"Bad"`。批次以该字符串落库、`allocatable=0`;聚合时 `"DAMAGED".equals("damaged")` 为 false → 进 GOOD 分支 → `allocatable!=1` → 既不计入 available 也不计入 damaged。
- **影响**:批次实物存在但在货主快照里凭空消失;采购链已扣 in_transit 却没加回 → 不守恒、库存低估。
- **修复方向**:上架入口对 quality 做枚举白名单(仅 GOOD/DAMAGED,统一大写),非法即拒;聚合分桶大小写不敏感或 else 兜底计入 damaged。
- **来源**:入库 MEDIUM-1。

#### M-8 退货合格/不合格分流口径两路径不一致
- **位置**:`ReturnInboundService.java:176-197`(残品→DAMAGED、报废→SCRAP 不入库)vs `ReturnQcService.java:205-208`(FAIL 一律→DAMAGED 批次、无报废/不入库分支)。
- **影响**:同为"退货不合格",经不同入口得到不同的 damaged/scrap 结果,账目口径不统一;与 C-2 叠加时残品桶数值互相打架。
- **修复方向**:统一退货不合格的分流枚举与落桶规则。
- **来源**:退货#6。

### ⚪ LOW / 待确认

- **L-1** `AVAILABLE` 桶允许为负、账本层无超卖硬守卫:`StockBucket.java:14`(allowNegative=true)+ `InventoryPostingEngine.validateConstraint:288-294`(可负桶跳过 <0 校验)。过量出库/预占可把 available 压负且不报错。放大 C-1 后果。**来源**:核心账本#6。
- **L-2** 多处 `updateById` 忽略乐观锁返回值:`OutboundPickingService.java:199-200`(pick 写批次)、`InventoryService.syncFboStock:389` 等未校验影响行数;与 ship(`:181-184` 显式校验)口径不一致。当前靠同事务 `FOR UPDATE` 兜底,前提破坏则静默丢扣减。**来源**:出库#4 · 横切 D-3。
- **L-3** `getOrCreate`/聚合插入/区域库存 check-then-insert 未捕 `DuplicateKeyException`:`InventoryService.getOrCreate:72`、`WmsInventoryAggregator:100-112`、`RegionInventoryService.getOrCreate:48`。并发首建撞唯一键抛异常整单回滚(无脏数据,偶发失败需重试)。建议捕获后回查复用。**来源**:横切 D-2。
- **L-4** 看板 `today-summary`/`trend` 汇总未按租户收窄:`StockFlowMapper.xml selectTodaySummary:6-17`、`selectTrend:20-43` 仅按 `warehouse_id`、无 `erp_tenant_id`。若被货主端复用则串租户(需确认调用方);`StockFlowService.queryPage/getDetail` 本身已按 `readScope()`/`canAccess()` 正确收窄。**来源**:核心账本#7。
- **L-5** FBO 手动同步未 `runAs(货主)`:`OzonFboStockSyncFacade.syncByShopId:101` 未 `runAs`(定时入口 `syncAllShops:86` 有);`syncFboStock:385` 读当前租户作库存归属。现状因 shop 表租户过滤副作用不出事,但归属正确性依赖副作用而非显式 `runAs`。**来源**:横切 A-3。
- **L-6** 并发重复收货重复累加采购/物流"已收量":`WmsInboundExecutionService.receive:114-125` 区域 in_transit OUT 走过账幂等不会重复;但 `increaseReceivedQuantity`(`:117/:120`)非幂等,并发双收货重复累加 → "已入库"数虚高。随 H-1 状态 CAS 一并可解。**来源**:入库 LOW-1。
- **L-7** 建单/上架 `erpTenantId` 缺上下文时兜底为 1:`PurchaseInboundService.resolveOwnerTenantId:527-530`(无上下文→`1L`)、`WmsInboundExecutionService.putaway:227`(`erpTenantId==null?1L`)。货主上下文缺失时归属兜底成默认货主 1 → 跨货主串数据。建议缺失即拒绝而非兜底。**来源**:入库 LOW-2。

---

## 6. 其余模块 Bug 清单(后端 + 前端)

### 6.1 后端

#### 平台运营 / 账单(platform)
- **【High】** 账单生成无 `WMS_OPERATOR` 身份校验:任意身份可触发 `generate`。
- **【Medium】** 计费依赖 `wms_tenant_id` 可靠性(业务表该字段常为 0/脏,应走 `sys_tenant.parent_wms_tenant_id`)。
- **【Medium】** 折扣系数无上下界校验。
- **【Medium】** `DashboardController` 缺 `@PreAuthorize`。
- **范围提示**:memory 记录的"加权平均·分币种·按件分摊(A–E)"月度账务代码在 `platform.finance` 包,**不在本轮 financial 70 文件范围**,financial 代理仅读了 XML,**建议单独排期审计其 Java 实现**(`WmsMonthlyBillMapper` 的 `selectDiscountPct`/`sumRackFee`/`sumFeeByType`、服务商收入只按 `fee_type='SHIPPING'` 统计等口径)。

#### 系统 / 租户(system/tenant)
- **【Critical】** C1 无上下文数据权限 fail-open(见 S1)。
- **【High】** 无上下文写盖默认租户 1(见 S1)。
- **【High】** 白名单拦截器无上下文静默跳过注入(见 S1)。
- **【High】** 租户 provisioning 缺 `@PreAuthorize`/`is_admin` 校验。
- **【Critical→复核】** C2 验证码可重用;实测 `login-captcha:false`,登录未启用验证码,**风险面收窄**(仅其它启用验证码入口受影响)。
- **【High→Medium(重定级)】** 文件下载 IDOR、API Key 落日志、汇率走 HTTP —— 经 S2 核实为"已登录任意身份越权",非公网未授权,按越权定级。

#### 商品 / 店铺 / 同步(product/shop/sync)
- **【High】** 税率校验形同虚设:DTO 只匹配到 `GenericTaxRateValidator`(仅判 null),严格的 `TaxRateValidator` 是死代码 → 可写入 0/负/超精度税率污染定价。
- **【High】** 店铺平台 API Key(Ozon/WB/Yandex)**明文落库** `shop.credential` + 明文入内存 `TEST_CACHE`,无加密。
- **【High】** 无上下文跨租户读写 + SKU 唯一性失效(方案②白名单表无上下文即不注入,见 S1)。
- **【Medium】** `SkuMapping` 不大写归一致孤儿映射;`SyncCursor` 非原子竞态;品类树递归无环路防护(脏数据可死循环挂线程);凭证测试缓存单机 `Map` 集群失效。

#### 财务对账 / 统计(financial/statistics)
- **【High】** H1 财务明细去重键不含 `shop_id`:同租户多 WB 店铺 `rrd_id` 撞号会互相覆盖/丢数,或唯一键冲突整批失败。**待确认现网唯一索引是否含 shop_id**。
- **【High】** H2 对账原生 SQL 含 MySQL JSON `->>` 运算符,租户隔离全靠 `ErpTenantLineHandler` 的 JSqlParser 解析注入;旧版解析失败静默跳过=跨租户串数、抛异常=500。**建议打印最终 SQL 实测**(与 S1 同源)。
- **【Medium】** 去重"读-改-写"非原子并发重复插/整批失败;任务 FAILED 无自动重试(财务明细静默不完整→对账大面积 ANOMALY);统计徽章 ANOMALY 口径与列表过滤不一致;对账实收跨币种直接相加(`SUM(ppvz_for_pay)`+`MAX(currency_name)`);两屏达成率用不同日期列(`platform_created_at_moscow` vs `platform_created_at`)+平台口径;销售目标 `currency` 被忽略(非 RUB 目标对 RUB 销售)。

#### 订单(order)
- **【High】** `OzonOrderController` 缺 `@PreAuthorize`(见 S2)。
- **【High】** `LabelBatchController` 无鉴权。
- **【Medium】** WB 状态转换 fallback 到 `READY_TO_SHIP`(可复活已发货订单);复活订单无重新预占;月度销售统计口径 `!= CANCELED` 与 javadoc 声明的 `DELIVERED` 不符(高估营收)。

#### 入库 / 出库 / 仓库 / 退货(wms 鉴权/隔离补充,库存正确性见第五章)
- **【High】** 入库单写操作(编辑/提交/删除)缺 `assertOwnership` → 跨租户改单。
- **【High】** 入库 mapper 表名 `purchase_inbound_order_item` 缺 `wms_` 前缀 → SQL 错。
- **【High】** 拣货 `pick()` 无 CAS + 拣货分配表无唯一键(已核实 V28)→ 并发重复拣货超扣(=第五章 H-2)。
- **【High】** 批次/快照查询信任前端传入 `erpTenantId` → 跨租户读。
- **【Medium】** 调拨单无鉴权/隔离、信任前端 `erpTenantId`;仓库分页泄漏 FBO 仓;退货单号非集群安全;超退竞态;假乐观锁;`logisticsProductId` 无 owner 校验。

### 6.2 前端

#### 鉴权 / 会话(fe-auth)
- **【High】** 常量路由注册了货主表单页 → 跨身份直接 URL 访问。
- **【Medium】** 菜单加载 `length===0` 无限重定向循环;权限仅 UI 隐藏;403 分支缺可选链。

#### WMS 视图(fe-wms)
- **【Critical】** C1 过账"操作单详情"抽屉打不开:`views/wms/stock-record/views/OrderListView.vue:73` 用 `v-model:open`,`components/StockPostingDetailDrawer.vue:110-118` 声明 `props.visible`/`emit('update:visible')` 且内部同名 `const visible`(:120)遮蔽 → `props.visible` 永不置真、`watch`(:149)不触发 → `loadDetail`/`loadItems` 不执行,详情+SKU 明细分页完全不可用。
- **【Critical】** C2 新建采购单"确认并提交"取 id 错配:`api/wms/purchase-order/index.ts:38-40` `createPurchaseOrder` 返回 `ApiResult<void>`,但 `PurchaseOrderFormPage.vue:562` 新建分支 `const orderId = isCreate ? (res.data as unknown as number) : formModel.id!` 随即 `confirmPurchaseOrder(orderId)` → `orderId===undefined` 确认失败/错单。
- **【High】** H1 采购单导出路径前缀 `/purchase/order/export` 错(同文件其余全为 `/wms/purchase-order/...`,必 404)。
- **【High】** H2 退货入库导出不下载:`api/wms/return-inbound/index.ts:68-72` 无 `responseType:'blob'`;`ReturnInboundPage.vue:195-204` 仅 `message.success` 不调 `remoteFileDownload`。
- **【High】** H3 销售出库导出不下载 + 不带平台筛选(`handleExport` 未合并 `activePlatform`)。
- **【High】** H4 发货单列表消费 VO 不存在字段(`totalShippedQuantity`/`totalReceivedQuantity`)→ 发/入统计与在途进度恒 0。
- **【High】** H5 发货单详情抽屉(`wms:shipping-purchase-order:edit`)与列表页(`wms:shipping-order:edit/del`)权限码不一致 → 一套必错、授权用户功能 fail-closed 消失。
- **【High】** H6 退货入库合格数量可超退货总数:`ReturnInboundModal.vue` 下调 `quantity` 后无 watch 回夹 `qualifiedQuantity`,提交 `quantity<qualifiedQuantity` → 良品超记。
- **【High】** H7 盘点表单页 `StocktakeFormPage.vue` 多处契约错配(守卫 `orderStatus!=='DRAFT'` 而枚举无 DRAFT → 编辑态恒被弹回;提交 `skuIds`/读 `skuId/skuName` VO 无 → 静默丢弃)。疑废弃但路由 `wms/stocktake/form/:mode/:id?` 仍可 URL 直达。
- **【High】** H8 库存"区域下钻"跳转后无法按区域过滤:`InventoryOverviewPage.vue:203-208` 带 `query.regionId`,但 `InventoryDetailSearch.vue` 预填只认 `warehouseId/skuCode`、`InventoryQO` 无 `regionId` → 展示全量库存。
- **【Medium×10】** M1 编辑仓库静默清空 address/remark(VO 不含二字段,PUT 全量覆盖为空);M2 FlowList 筛选参数名与 `StockFlowQO` 全线不匹配(`startTime/endTime`/单选 `postingType`/`flowDirection` vs `createTimeStart/End`/`postingTypes[]`/`direction`)→ 日期/类型/方向/区域全失效;M3 "设置安全库存"快捷弹窗固定 `notifyEnabled:true` 覆盖已有通知配置;M4 销售出库确认表单页不展示缺货明细(与抽屉不一致);M5 采购单付款金额硬编码 `¥` 无视多币种;M6 采购表单页未注册 `useRouteLeaveGuard()`;M7 storage-overview 概览/抽屉缺 `isSuccess` 守卫;M8 破坏性库位/货架操作(`saveStructure`/`doGenerate`/`assignZone`)缺前端权限门,API 注释仍写"仅平台超管";M9 退货质检 FAIL 照片 `before-upload=()=>false` 仅传 count 未上传;M10 采购入库物流明细 `doRequest` 无 `onFail` + `if(res.data)` 对空数组 truthy 误判。

#### 订单 / 商品 / 店铺(fe-order/product/shop)
- **【High】** H1 店铺列表恒空:`api/shop/index.ts:15` `listShops` 返回 `{list; page; size; total}`,`ShopPage.vue:97` 直接交 ProTable(读 `result.data.records`)→ `records` 恒 undefined。
- **【High】** H2 Ozon 商品总价列字段与 VO 不符:`OzonOrderPage.vue:91-105` 渲染 `productTotalAmount/productCurrencyCode/productAmountCny`,但 `OzonOrderPageVO` 未声明(属 `YdOrderPageVO`)→ 恒显 `-`。
- **【High】** H3 SKU 产品名搜索同时写 `chineseName`+`russianName`(AND 语义)→ 一条 SKU 不可能中俄文名同时含同串 → 按产品名搜索几乎恒空。
- **【Medium】** M1 Yandex 打印 `platform="yandex"`(小写)vs Ozon/WB `"Ozon"/"Wildberries"` → 打印历史可能恒空;M2 品类"停用(status=0)"过滤被真值判断 `!status` 短路失效;M3 SKU 表单 `name="supplierCode"` 重复 + 供应商名回填死代码(禁用框绑 `supplierCode`,回填的是 `supplierName`,提交前又 `delete supplierName`);M4 确认发货 doRequest 把关窗+刷新放 `onFinally` → 失败也关窗刷新。

#### 平台财务(fe-platform-finance)
- **【High】** `OrderReconciliationDrawer` 详情永不加载(prop `visible` vs 父 `v-model:open`)。
- **【High】** 3 个财务组件用 `visible/update:visible` 违反仓库 `open/update:open` 约定。
- **【Medium】** WB 同步"近 30 天"永不触发。

#### 组件 / 工具 / 公共层(fe-components)
- **【High】** H1 `components/Lov/LovModal.vue:234 handleClose` 无 `searchOptions` 时 `lovSearchRef.value.clearForm()`(`lov-search` 受 `v-if` 控制、value 为 undefined)→ TypeError(任何无搜索项的 Lov 点确定/取消崩溃)。
- **【High】** H2 `components/Lov/Lov.vue:103 selectOptions` 用 `props.customOptionTitle(x)` 未走 `localCustomOptionTitle` 兜底 → 未传时回显已选项即 `undefined(x)` 崩溃。
- **【Medium×8】** M1 反选池 `LovModal.handleDeselect:239` 只改 keys 不改 rows(父层拿脏行);M2 `WarehouseSelect.vue:129` 过滤条件变更后不清空失效选中(对比 `PlatformOwnerSelect:94` 有级联清空);M3 utils 同名函数语义冲突(`file-utils.getFileExtension` 带点 vs `upload-core.getFileExtension` 不带点,`formatFileSize`/`generateTimeBasedFileName` 亦不一致);M4 `file-utils.ts:155 sanitizeFileName` 空实现(`return fileName`)却被 `generateUniqueFileName`/`generateTimeBasedFileName` 当清洗步骤调用;M5 三套货币格式化并存(`amount-utils`/`currency-utils`(带 `useSubunit` 除 100)/`financial-utils`)+ `currency-utils` 头注称数值码(643)但 `CURRENCY_META` key 是字母码(RUB)→ 若后端传 643 直接把 "643" 当符号输出(待确认);M6 公共 `components/Sku/SkuMappingBadge.vue:66` 反向 import `@/views/product/sku/components/SkuMappingDetailModal.vue`(分层倒置/循环依赖);M7 `api/i18n/i18n-data.ts:39` import 端点 `i18n/i18n-data/import` 缺前导斜杠(待确认 baseURL);M8 `locales/index.ts:15 availableLocales` 含伪语言 `default-local-import`(glob 纳入)。

### 6.3 Low / 坏味道(择要,完整见各子报告)
- **后端**:批次分事务部分提交(`WbReportDetailService` 每 1000 一事务);`job/task.totalRecords` 因含 update 计数虚高;`SalesTargetController.save/updateById` 收裸实体无 targetType/year/month/currency 校验;执行器 `task.getTenantId()` 为 null 时 `setCurrentTenant(null)` → wb_report_detail 租户归属缺失(当前仅 Web 触发,风险低);对账 `totalAmountRub`(戈比)与 `saleAmount/actualIncome`(卢布)同屏并列差 100× 展示误导。
- **前端**:多处 `console.log` 遗留生产高频路径(`financial-utils.ts:62` 货币渲染、`LovModal.vue:148` 分页加载、`file-utils.ts:54` 下载);`Lov.vue:15`/`LovModal.vue:7` 位运算 `placeholder | ''` 应为 `||`(被 `enableI18n:false` 硬编码掩盖,同时该硬编码使两组件 i18n 全失效);`file-utils.ts:119 fileDownload` 同步 `revokeObjectURL` 可中断大文件;`bean-utils.ts:8 overrideProperties` source 缺键覆盖 target 为 undefined;`tree-utils.ts:107 pruneTree` 改入参副作用;`UserSelect` 清空双发 `change`;`SupplierSelect:122/127` onMounted 与 watch(immediate) 竞态;`CategoryTreeSelect:72` 孤儿节点静默丢弃;`TransferItemTable.vue:24-31` 声明 `v-model:items` 却原地改 prop 从不 emit;`ShippingOrderPage.vue:425` blob 取值与其余不一致;`useClipboard` 单例致全表行图标同切;`SalesOutboundDetailDrawer` 草稿态少一列错位;`ReturnInboundModal.open` 未重置分页;预测图表 `chart-builders.ts:13-102 buildZoneMarkAreas/buildStockoutMarkLine` 死代码;`SafetyStockEditModal:63-65` required 与文案矛盾;`ForecastDetailDrawer:259-284` `if(!dailySales)` 致输入 0 被覆盖;`FlowListSearch:56` 用静态选项未用 API 动态;`FlowListView:178`/`OrderListView:165` handleExport 仅 `console.log`;`return-qc/mock.ts` 全文死代码;`warehouse/index.ts:18`/`logistics-provider/index.ts:15` 及 ozon/wb/yd/sku-mapping/category page 接口类型漏 `PageResult` 包装(仅 TS 报错);重复 `getStatusColor` colorMap;`TransferOrderFormPage:334` 缺 `REVOKED` 映射;`emitter.off('refresh-sku-list')` 无 handler 清空全部监听;`SkuPage:373 toFixed()||'-'` 死分支;`LabelBatchList` watch+onMounted 重复请求;WB 金额列缺默认货币符号;`api/tenant/index.ts` 注释仍写"平台超管"与"禁用平台超管"口径不符;`SkuFormPanel` a-form `:rules` 未生效(走自定义校验)、SKU 编码 placeholder"3-50 位"与校验允许 100 位不一致;分页参数双轨(`page`+`size` vs `current`+`size` 混用,待确认后端解析器)。

---

## 7. 代理间分歧(如实标注,需人工裁决)

| # | 主题 | 分歧 | 处置 |
|---|---|---|---|
| 1 | **H-3 聚合器重试** | 入库:丢更新低估;核心:安全;横切:依隔离级别(RR fail-safe / RC 丢更新) | 采信横切调和口径;严重级别取决于线上事务隔离级别,**需确认** |
| 2 | **M-3 区域汇总预占/在途** | 核心:无租户过滤→跨租户串数;横切:查 `wms_region_inventory`(白名单自动注入)→无问题 | **需人工核实** `selectReservedByRegionIds`/`selectInTransitByRegionIds` 的真实目标表与是否命中数据权限白名单 |
| 3 | **M-3(库存补充代理复核)** | 补充代理确认 `wms_region_inventory` 在 OWNER_TABLES 白名单 | **已倾向"非 bug"**(横切正确),仍建议实测最终 SQL 收口 |

---

## 8. 已核对无问题(正面确认)

**库存链路:**
- 过账引擎租户隔离:分组/合并键均含 `erpTenantId`(`buildMergeKey`/`warehouseGroupKey`/`regionGroupKey`/`getOrCreate` 强制 erpTenantId),单笔过账内部不跨货主合并。
- 下架防超卖:`selectFifoAllocatableForUpdate`(`FOR UPDATE` 行锁)+ 两阶段(缺货整单挂起不部分下架)+ 批次 `@Version`。
- 签出幂等:`casOrderStatus(PACKED→SHIPPED)` + 批次 `@Version`(`updated!=1` 抛 409)+ 计费 `bizId` 去重。
- 批次真源:`WmsPhysicalInventoryService` reserve/release/scrap/locationTransfer 全 `@Transactional` + `updateBatchLocked`(`@Version`)+ 同事务聚合刷新 + 流水。
- **异步/并行丢租户上下文**:`wms/` 目录内无 `CompletableFuture`/`parallelStream`/`@Async`/线程池;唯一后台入口 `InventoryAlertTask`、`syncAllShops` 均正确 `runAs(erpTenantId)` 逐货主建上下文。**本次库存审计范围内不存在"异步丢 TenantContext"问题。**
- 流水查询隔离:`StockFlowService.queryPage/getDetail` 用 `readScope()`/`canAccess()` 收窄。
- 软删除:入库单 `@TableLogic`,软删后无法再收货/上架。
- 库位独占/分区↔品质联动:同次上架内库位去重 + 已占用拒绝 + zone_type 强校验,逻辑正确。
- `WmsInventoryAggregator` 自身乐观锁重试(按绝对值 set)安全——与 C-3"按已变状态重算增量"的不安全重试形成对照。

**财务/统计:**
- `DashboardService.supplyAsyncWithTenant` 子线程重建租户上下文、finally 清理 → 规避异步丢上下文。
- WB 同步执行器 `setCurrentTenant(task.getTenantId())` 显式传播;调度器无上下文读全部、逐任务 runAs。
- 金额 `/100` 每路径恰好一次,逐路径核对无 100× 错配。

**框架层:**
- DB 唯一键含租户维度(`wms_inventory`/`wms_physical_inventory`/`wms_monthly_bill`/`wms_region_inventory`)。
- `TenantResolveInterceptor` 对 Web 请求 fail-closed(UNBOUND 哨兵/停用 403)。
- `menu-filter.ts` 身份菜单裁剪 fail-closed、成环守卫、按实例隔离,无缺陷。
- 前端 axios/契约层无 `.data.data` 双解包/越层错配;`json-bigint` 保 ID 精度。

---

## 9. 修复优先级建议

**P0 — 数据正确性 / 资金:**
1. 库存双写者冲突(C-1):统一单一库存权威。
2. 退货双入口二次入库(C-2)、乐观锁重试重复过账(C-3)。
3. 前端两个 Critical:过账详情抽屉(fe-wms C1)、新建采购单取 id 错单(fe-wms C2)。
4. 财务明细去重键补 `shop_id`;拣货分配表补唯一键 + `pick()` 加 CAS(H-2);入库上架加 `@Version`/CAS(H-1)。

**P0 — 安全:**
1. 统一"无上下文"语义(读也 fail-closed);核心库存三表纳入隔离白名单或强制 service 带 `erp_tenant_id`。
2. 补 `OzonOrderController/LabelBatchController/DashboardController/provisioning/StocktakeService` 的 `@PreAuthorize`/`assertPlatform`。
3. 店铺 API Key 加密落库;文件下载补 owner 校验(IDOR)。
4. 退货收货/质检守卫(H-4/H-5/H-6):实收≤应退、SKU 归属、状态 CAS + `@Version`。

**P1 — 功能失效:**
1. 店铺列表 `list→records`;导出统一 blob + `remoteFileDownload` + 修正路径前缀;FlowList/区域下钻/发货单统计字段对齐 QO/VO;财务/过账/组件抽屉 v-model 统一 `open`。
2. `LovModal/Lov` 崩溃兜底;`SkuBriefVO` 导入路径统一;合并三套货币格式化与两套文件工具、实现 `sanitizeFileName`。

**P2:**
- 税率严格校验接回;财务口径统一(ANOMALY/达成率日期列/币种/跨币种汇总);订单状态 fallback 与月度统计口径;库存 M/L 与前端各类 Medium/Low。

---

## 10. 待确认项汇总(交人工核实后再定级/修复)

1. **线上事务隔离级别**(影响 H-3、C-3 实际表现):默认 RR 还是被改成 RC?
2. **M-3**:`selectReservedByRegionIds`/`selectInTransitByRegionIds` 实际读哪张表、是否被数据权限拦截器注入 `erp_tenant_id`(补充代理倾向"非 bug",建议实测 SQL 收口)。
3. **M-1/M-2**:部署中 `wms_tenant_id` 是否会取非 0;`wms_stock_posting` 是否已有 `(source_type,source_id,posting_type)` 唯一索引;`source_id` 是否全局唯一。
4. **L-4**:`today-summary`/`trend` 看板是否被货主端页面复用(决定是否为串租户 bug)。
5. **财务 H1**:`wb_report_detail` 现网唯一索引是否含 `shop_id`;WB `rrd_id` 是否跨该租户各店铺可撞号。
6. **财务 H2**:含 JSON `->>` 的对账 SQL 经 JSqlParser 后每张白名单表是否真带 `tenant_id`(打印最终 SQL)。
7. **CurrencyUtils / 前端货币**:各平台 `totalAmount/convertedAmount/product*` 存主单位还是子单位(戈比/分);`currency-utils` 后端传字母码还是数值码。
8. **fe-wms 待确认**:`createPurchaseOrder` 后端是否回 id;发货单未声明数量字段是否实际返回;`ShippingOrderDetail` 命名路由是否注册;分页 `page/size` vs `current/size` 后端是否同时接受。

---

## 11. 附录:审阅覆盖清单

**后端 9 域 + 库存专项 5 代理:**
- platform 运营/账单、system/tenant 隔离基础设施、product/shop/sync、financial 对账 + statistics 统计、order 三平台、wms 入库、wms 出库、wms 仓库/退货、wms 库存过账盘点调整。
- 库存专项覆盖(逐方法):StockPostingService、InventoryPostingEngine、WmsInventoryAggregator、InventoryService、WmsPhysicalInventoryService、StockFlowService、RegionInventoryService、PurchaseInboundService、WmsInboundExecutionService、SalesOutboundService、CustomOutboundService、OutboundPickingService、OutboundShippingService、ReturnInboundService、ReturnQcService、StocktakeService、TransferOrderService、AdjustmentService、ErpOwnerScopeService;Facade(SalesOutbound/CustomOutbound/OzonFboStockSync);Mapper 接口+XML;实体/枚举;基础设施(ErpMybatisPlusConfig/ErpTenantLineHandler/WmsOwnerDataPermissionHandler/TenantContext/OptimisticLockerInnerInterceptor);DDL(V12/V22/V25/V28/V30/V31)。

**前端 5 域:**
- auth/会话、wms 视图(约 180 文件)、order/product/shop、平台财务、组件/工具/公共层。

**本人核实(源码/迁移/配置)**:`CurrencyUtils.java`、`application.yml`(`ignore-urls`)、V12/V25/V28/V31 唯一键、`StockFlowController`/`StockFlowDrawer` 契约。

**未覆盖 / 建议续审:**
- `platform.finance` 月度账务 Java 实现(加权平均·分币种·A–E 分摊)。
- pro-components 第三方库层;`components` 中 Button/File/Upload/ImageUpload/Notify/Editor/Icon/Captcha/CropperModal/Operation 等基础件;SKU 文件上传两组件(OSS key 处理)。

---

> 本报告仅记录,未改动任何代码。修复实施建议按第九节优先级分批进行,每批改动后回归对应身份链路(货主 ERP_USER / 服务商 WMS_OPERATOR / 海外仓平台 OVERSEAS_PLATFORM)。如需针对某条(尤其 C-1/C-2/C-3 或前端两个 Critical)展开可执行修复方案,请指定条目——仍按"先出方案待确认再改"流程。
