# 修复记录

> 依据《全量代码审阅报告(完全体)》逐条修复。流程:先出方案 → 用户确认 → 修改 → 本文件登记。
> 每条记录:编号 · 来源条目 · 根因 · 改动文件/行 · 验证方式 · 状态。

---

## #1 · fe-wms C1 · 过账"操作单详情"抽屉打不开 ✅ 已修复

- **日期**:2026-07-06
- **来源**:完全体报告 §6.2 fe-wms C1(Critical)
- **根因**:父 `OrderListView.vue:73` 用 `v-model:open`(传 prop `open`、听 `update:open`),子 `StockPostingDetailDrawer.vue` 却声明 prop `visible`/emit `update:visible` → 父传的 `open` 绑不到子 `visible` prop → `props.visible` 恒 false → `watch(()=>props.visible)` 不触发 → `loadDetail`/`loadItems` 永不执行,详情+SKU 明细分页全废。
- **改动**(仅 `StockPostingDetailDrawer.vue` 一个文件,4 处):
  - prop `visible: boolean` → `open: boolean`
  - emit `update:visible` → `update:open`
  - `watch(() => props.visible …)` → `watch(() => props.open …)`
  - `emit('update:visible', val)` → `emit('update:open', val)`
  - 内部 `const visible = ref(false)`(给 `<a-drawer v-model:open>`)保留,此时与 prop 不再同名,遮蔽问题一并消除。
- **影响面**:仅一个调用方(`OrderListView.vue`),其本已是正确的 `v-model:open`,无需改动;无其它调用方(已 grep 确认)。
- **验证**:货主端/平台端 → 库存记录 → 操作单列表 → 点『详情』,抽屉应打开并加载概况 + SKU 明细分页。前端 Vite HMR,无需重启后端。
- **状态**:✅ 已改,待用户界面回归确认。

---

## #2 · fe-wms C2 · 新建采购单"确认并提交"取 id(重定级 Critical→Low)✅ 已修复

- **日期**:2026-07-06
- **是什么错误**:前端 `createPurchaseOrder` 类型声明为 `ApiResult<void>`(index.ts:39),但后端实际是 `ApiResult<Long> create(){return ApiResult.ok(id)}`(PurchaseOrderController.java:86-88,已核实真回 id)。前端用 `res.data as unknown as number`(PurchaseOrderFormPage.vue:562)双重强转硬取新单 id 传给确认接口。
- **影响**:运行时**不坏**(后端回了 id 能取到,新建+确认当前可用);隐患是"类型说谎"——类型标 void 却有值靠强转掩盖,若有人照类型清理或后端改成不回 id 即静默错单且无编译报错。故 **Critical 下调为 Low(类型卫生)**。
- **改动**(2 文件 2 行,纯类型对齐,不改运行时行为):
  - `api/wms/purchase-order/index.ts:39`:`ApiResult<void>` → `ApiResult<number>`
  - `PurchaseOrderFormPage.vue:562`:`res.data as unknown as number` → `res.data as number`
- **验证**:货主端新建采购单勾选"确认并提交",行为与现状一致(正常建单+确认)。
- **状态**:✅ 已改,待用户界面回归确认。
- **备注**:同文件 index.ts:99 的导出路径 `/purchase/order/export` 前缀错(fe-wms H1)未在本条处理,留待单独修复。→ 已在 #3 修复。

---

## #4 · fe-wms H2 · 退货入库导出(补成真 Excel,方案 A + 中文标签)✅ 已修复(含后端重建)

- **日期**:2026-07-06
- **是什么错误(比报告更深)**:后端 `ReturnInboundController.export` 缺 `@ResponseExcel`,返回 `ApiResult<List<ReturnInboundPageVO>>`(一串 JSON)而非 Excel 文件流;前端拿到 JSON 后只弹"导出成功"、不产生文件。即"导出"功能前后端都没做完——光改前端加 blob 会下载一坨 JSON。
- **改动**(后端 5 + 前端 2,共 7 文件):
  - 后端:①新建 `ReturnInboundExportVO.java`(FastExcel `@ExcelProperty` 注解,仅含 XML 实际 select 的列:退货单号/平台订单号/入库仓库/退货日期/退货原因/退货总数量/合格入库数量/不合格数量/备注/创建人/创建时间;id `@ExcelIgnore`;日期用 String 由 MyBatis getString 输出);②`ReturnInboundMapper.java:65` 返回 `List<ReturnInboundExportVO>`(+import);③`ReturnInboundMapper.xml:113` resultType 改 ExportVO;④`ReturnInboundService.java:226` 返回类型改 ExportVO,新增 `translateReturnReason()` 把退货原因 code→中文(用 `ReturnReason` 枚举 description),+import;⑤`ReturnInboundController.java` 加 `@ResponseExcel(name="退货入库单-#{currentDateTime()}")`(+import `org.ballcat.fastexcel.annotation.ResponseExcel`),签名改 `List<ReturnInboundExportVO> export(...)` 去 ApiResult 包装。
  - 前端:⑥`api/wms/return-inbound/index.ts` `exportReturnInbound` 去 `ApiResult<>` 泛型 + `responseType:'blob'`;⑦`ReturnInboundPage.vue` `handleExport` 改用 `remoteFileDownload(response)`(镜像 purchase-inbound 等 4 个已工作页面),import 换 `isSuccess`→`remoteFileDownload`。
- **退货原因中文**:`NOT_WANTED→客户不想要了 / DAMAGED→商品破损 / WRONG_ITEM→发错货 / QUALITY_ISSUE→质量问题 / OTHER→其他`。
- **重建**:kill java → `mvn -q -pl admin -am package -DskipTests`(JAVA_HOME=jdk8)→ 起 jar `--spring.profiles.active=local`。构建成功、后端 `Started AdminApplication in 26s` 无异常。
- **验证**:退货入库列表点导出 → 下载 `退货入库单-时间.xlsx`,列齐全、退货原因显示中文。**⚠️ 后端已重启,浏览器会话可能掉线需重新登录。**
- **状态**:✅ 已改并重建,待用户界面回归确认。

---

## Wave 1（前端，无需重建）批量执行 · 2026-07-06

> 授权:用户同意一次性执行 Wave1+Wave2,默认项按我判断走。逐条如下。

### #6 · fe-order/product H1（重定级 High→Low）✅
- 错误:`listShops` 前端类型声明 `{list;page;size;total}`,但后端 `ShopController.page` 返回 `ApiResult<PageResult<ShopPageVO>>`(即 `records`)。核实后端 = **运行时不空**,仅类型说谎。
- 改:`api/shop/index.ts` `listShops` 返回类型改 `ApiResult<PageResult<ShopVO>>`,+import PageResult。

### #7 · fe-order H2 —— ❌ 非 bug（核实后撤销）
- 报告称 Ozon 商品总价列字段抄自 Yandex。核实后端 `OzonOrderPageVO` **确有** `productTotalAmount/productCurrencyCode/productAmountCny`(:101/104/107),列正常。**不改。**

### #8 · fe-order H3 ✅
- 错误:`SkuPageSearch` 把 `skuName` 拆成 `chineseName`+`russianName`(后端 QO 无此二字段→被忽略)并删掉 skuName → 名称搜索完全失效。后端 `SkuMapper.getSkuLambdaQueryWrapperX:58-60` 本就对 `skuName` 做「中文名 OR 俄文名」。
- 改:删掉拆分逻辑,直接透传 `skuName`。纯前端。

### #9 · fe-order M1 —— ❌ 非 bug（核实后撤销）
- `PlatformEnum.code()` 全小写(`wildberries/ozon/yandex`),Yandex 页用 `"yandex"` **正是规范值**;WB/Ozon 用大写反而不规范,但因 MySQL `_ci` 排序不敏感照常工作。Yandex 历史未坏,改大写是错方向。**不改。**

### #10 · fe-order M2 ✅
- 错误:品类 `status=0`(停用)被 `!searchParams.status` 短路 → 停用筛选失效。
- 改:`CategoryPage.vue` 三处(filterTreeData 190/198、getExpandedKeys 230、高亮 172)改为显式判空 `!== undefined && !== null && !== ''`。

### #11 · fe-order M3 ✅
- 错误:`SkuFormPanel` 两个 `a-form-item` 都 `name="supplierCode"`(antd 字段注册冲突),禁用框标签"编码"却提示"名称自动填充"矛盾。
- 改:禁用展示框移除重复 `name`(展示型不需注册),提示改"供应商编码（自动填充）"。

### #12 · fe-order M4 ✅
- 错误:`useOrderConfirm` WB/Ozon 分支把关窗+刷新放 `onFinally`,失败也关窗刷新。
- 改:关窗/刷新移到 `onSuccess`,`onFinally` 仅重置 loading。

### #27 · fe-platform-finance（对账抽屉详情永不加载）✅
- 错误:父 `index.vue:292` `v-model:open`,子 `OrderReconciliationDrawer` 声明 `props.visible`/`emit update:visible` → watch 不触发,详情不加载(同 #1)。
- 改:子组件 5 处 `visible`→`open`(template `:open`、props、emit 声明、watch 源、handleClose emit)。
- 注:报告提到的"另 3 个财务组件用 visible/open 约定"若父子一致则仅约定问题、不影响功能,未动。

### #29 · fe-components H1 ✅
- `LovModal.handleClose` `lovSearchRef?.value.clearForm()`(`?.` 永不短路)→ 无 searchOptions 的 Lov 崩溃。改 `lovSearchRef.value?.clearForm()`。

### #30 · fe-components H2 ✅
- `Lov.selectOptions` 用 `props.customOptionTitle(x)` 未走兜底 → 未传时回显崩溃。改 `localCustomOptionTitle.value(x)`。

### #31 · fe-components M1 ✅
- 反选池 `LovModal.handleDeselect` 只改 keys 不改 rows → emit 脏行。改:按 keySet 过滤 `selectedRows`。

### #32 · fe-components M2 ✅
- `WarehouseSelect` 过滤条件变更后不清失效选中。改:`loadOptions` 末尾若 `props.value` 不在新选项则 `handleChange(undefined)`。

### #33 · fe-components M4 ✅
- `sanitizeFileName` 空实现却被当清洗步骤。改:实现移除非法字符 `\/:*?"<>|`、控制字符、首尾空白/点。

### #35 · fe-components M7 ✅
- `api/i18n/i18n-data.ts:39` import 端点缺前导斜杠。改 `/i18n/i18n-data/import`。

### #36 · fe-components M8 ✅
- `availableLocales` 含伪语言 `default-local-import`(核实 lang 目录确有该文件)。改:`.filter(l => l !== 'default-local-import')`。

### Low 批 · Lov 位运算 ✅
- `Lov.vue:15`、`LovModal.vue:7` 的 `placeholder | ''` / `modalTitle | ''` 位或误用 → 改 `||`。

### 延后 · fe-components M6
- 公共 `SkuMappingBadge` 反向依赖 `views/`(分层倒置)属结构重构,移入 Wave 3 结构项,本波不动。

### #14 · fe-wms H6 ✅（退货合格数超退货总数）
- 错误:`ReturnInboundModal` 下调 `quantity` 后无 watch 回夹 `qualifiedQuantity`,`handleSubmit` 只 `validate()` 不校验分配合计 → 良品可超记。
- 改:加 `watch(()=>formModel.quantity)` 夹紧 `qualifiedQuantity`;`handleSubmit` 加 `isAllocationValid` 一致性校验。

### #17 · fe-wms M2 ✅（FlowList 筛选参数全线不匹配）
- 错误:`FlowListSearch` emit `startTime/endTime/postingType(单)/flowDirection`,后端 QO 用 `createTimeStart/End`、`postingTypes[]`、`direction` → 日期/类型/方向筛选全失效。
- 改:`FlowListView.tableRequest` 做字段映射(`startTime→createTimeStart`、`flowDirection→direction`、`postingType→[postingType]`)。

### #20 · fe-wms M5 ✅（付款金额硬编码 ¥）
- 错误:`PaymentInfoSection` 两处 `¥` 硬编码,无视多币种。
- 改:加 `currencyCode` prop + `getCurrencySymbol` computed,父 `PurchaseOrderFormPage` 传 `:currency-code="formModel.currencyCode"`。

### #22 · fe-wms M7 ✅（storage-overview 缺守卫）
- 错误:`storage-overview/index.vue loadSummary` 无 `isSuccess` 守卫、无 catch,`Object.assign(overview, res.data)` 失败可赋脏值。
- 改:加 `isSuccess(res)&&res.data` 守卫 + catch + import。抽屉 `StorageWarehouseDrawer` 用 `res.data||[]` 有兜底非崩溃,未动。

### #26 · fe-auth L2 ✅（403 缺可选链）
- 错误:`axios/index.ts` 403 分支 `description: data.message`,`data` 可能为空 → 抛错(default 分支用 `data?.message`)。
- 改:`data?.message`。

---

## Wave 2（后端，本波三项攒一起，已重建）· 2026-07-06

### #38 · wms H2（SQL 表名缺 wms_ 前缀）✅
- 错误:`PurchaseInboundMapper.xml` :34/:167 两处 `FROM purchase_inbound_order_item`(缺 `wms_`),仅在按采购单号筛选时触发 → SQL "表不存在" 报错。正确表名 `wms_purchase_inbound_order_item`(同文件 :76/:196/:236 及 DDL 均用)。
- 改:replace_all 修两处为 `wms_purchase_inbound_order_item`。

### #39 · order H1（OzonOrderController 缺鉴权）✅
- 错误:`OzonOrderController` 全部 9 个接口无 `@PreAuthorize`(任意已登录身份可确认/同步/锁单/导出)。对照 `WbOrderController`(同 `erp_order` 实体)有完整鉴权。
- 改:+import `PreAuthorize`;按 Wb 口径加注解——page/print-labels/export=`order:erp-order:read`,confirm/sync/sync-shop/lock/unlock/sync-all=`order:erp-order:edit`。这些码 Ozon 用户已具备,不会误锁。

### #40 · order H2（LabelBatchController 无鉴权）✅
- 错误:`LabelBatchController` 两个只读接口(page/detail)无 `@PreAuthorize`。
- 改:+import;两方法加 `order:erp-order:read`(与打印面单同域、订单用户已具备)。

### #41 · platform M3 —— ❌ 非问题（核实后撤销）
- 报告称 `DashboardController` 缺 `@PreAuthorize`。核实:**已有** `statistics:dashboard:read/export` 注解。不改。

### Wave 2 重建
- kill java → mvn 打包 EXITCODE=0 → 起 jar，`Started AdminApplication in 83s` 无异常。**⚠️ 后端已重启,需重新登录。**

---

## 本轮尚未完成（诚实清单，留待下一批）

**Wave 1 前端剩余**：
- #13 fe-wms H5（发货单权限码抽屉/列表不一致)——需先核实后端 `@PreAuthorize` 哪个是权威码再对齐前端。
- #15 fe-wms H8（库存区域下钻筛选)——需确认后端 StockFlow/Inventory QO 是否支持 regionId。
- #16 fe-wms M1（编辑仓库清空 address/remark)——需后端详情 VO 是否返回该二字段。
- #18 fe-wms M3（安全库存弹窗覆盖配置)、#19 fe-wms M4（销售出库确认页缺货明细)、#21 fe-wms M6（采购表单离开守卫,用的是 useUnsavedChangesGuard 需对比)、#23 fe-wms M10（采购入库物流明细空判)。
- #24 fe-auth H1（常量路由货主表单页,较敏感需核实)、#25 fe-auth M1（菜单无限重定向)、#28 fe-platform-finance（WB 同步近30天)。
- fe-components M3/M5（getFileExtension 双实现、三套货币格式化合并)——多调用点,单独处理。

**Wave 2 后端剩余**（每项需深入调查+带风险,下一批逐个来）：
- #42 system H4（provisioning 缺 @PreAuthorize/is_admin)、#43 wms H1（入库写操作缺 assertOwnership)、#44 product/shop H1（税率死校验器)、#45 platform H1（账单生成缺 WMS_OPERATOR 校验)、#46 order M3（月度营收口径,默认按 DELIVERED)、#47 financial M3（ANOMALY 徽章/列表口径统一)。

**Wave 3 架构级**（未动,按约定单独逐条）：库存 C-1/C-2/C-3、H-1/H-2、S1 无上下文 fail-open、店铺 API Key 明文、fe-components M6 分层倒置。

---

## Wave 2 续（后端剩余，第二次重建）· 2026-07-06

### #42 · system H4 —— ❌ 非问题（核实后撤销）
- `TenantProvisionController` 无 `@PreAuthorize`,但 `TenantProvisionService` 每个方法都按双身份模型断言:`openWmsOperator` 非平台→`OPEN_TENANT_FORBIDDEN`,`openErpTenant` 非服务商→拒,set*Status 同理(`canOpenWmsOperator` 要求 `IDENTITY_OVERSEAS_PLATFORM`)。控制器加静态 `@PreAuthorize` 无法表达"平台开服务商/服务商开货主"双身份且可能误锁。**服务层已正确防护,不改。**

### #43 · wms H1（入库单写操作缺归属校验）✅
- 错误:`PurchaseInboundService` 的 `update`/`updateToCancelled`/`updateToSubmitted`/`deleteOrder` 仅按 id 操作,未调 `assertOwnership` → 跨租户改/删单(`deleteOrder` 最严重,连查都不查直接按 id 删明细+主单)。已有 `assertOwnership`(:535,平台 scope==null 放行)仅用于 manual/customReturn。
- 改:四方法补 `Assert.notNull(order) + assertOwnership(order)`;`deleteOrder` 先 getById 再校验后删。

### #44 · product/shop H1（税率死校验器）✅
- 错误:活跃的 `GenericTaxRateValidator` javadoc 称"大于0"但只判 `taxRate == null`,可写入 0/负税率;严格的 `TaxRateValidator`(有 `<= 0` 判断)是死代码。
- 改:`GenericTaxRateValidator` 条件补 `|| taxRate.compareTo(BigDecimal.ZERO) <= 0`,与死校验器/文档一致。

### #45 · platform H1 —— ❌ 非问题（核实后撤销）
- `MonthlyBillService.generate:72` 及全部账单操作都调 `assertPlatform()`(:208 校验身份=OVERSEAS_PLATFORM 否则 403),controller 另有 `@PreAuthorize('platform-finance:bill:oper')`。鉴权充分,报告有误。**不改。**

### #46 · order M3（月度营收口径）✅
- 错误:`ErpOrderMapper.xml calculateMonthlyCompletedOrdersAmount:12` 用 `erp_status != 'CANCELED'`,但同方法 javadoc(:99)明确写 `erpStatus = 'DELIVERED'` → SQL 违背文档、高估营收。
- 改:按 javadoc + 方法名 + 用户默认,`!= 'CANCELED'` → `= 'DELIVERED'`。

### #47 · financial M3（对账 ANOMALY 徽章/列表口径不一致）✅
- 错误:统计 `selectOrderStats` 的 `reconciliation_status` 用 `ELSE 'ANOMALY'`(含 NEW/PROCESSING 等所有未列状态);列表/导出的 ANOMALY 过滤(`OrderReconciliationMapper.xml` page:166、export:288)只匹配 `DELIVERED 无销售 OR RETURNED 无记录` → 徽章数 > 列表行。
- 改:两处 ANOMALY 过滤改为等价 ELSE 的否定 `NOT(READY_TO_SHIP OR SHIPPED/ARRIVED OR CANCELED OR DELIVERED且有销售 OR RETURNED且有记录)`,用 `COALESCE(...,0)>0` 正确处理 NULL,精确对齐徽章口径。

### Wave 2 续 重建
- kill java → mvn EXITCODE=0 → 起 jar `Started in 32s` 无异常(无 Invalid bound statement)。**⚠️ 后端已重启,需重新登录。**

---

## 阶段小结（截至本轮）

- **已修**:前端 20 项(Wave1)+ 后端 7 项(#38/#39/#40/#43/#44/#46/#47)= **27 项**。
- **核实为非 bug**:#7 Ozon 列、#9 Yandex platform、#41 Dashboard、#42 provisioning、#45 账单 = **5 项**。
- **尚未完成**:Wave1 前端剩余 ~10 项(多需先核实后端)、Wave3 架构级(库存 C-1/C-2/C-3 等)。 · fe-wms H3 · 销售出库导出(补真 Excel + 平台筛选 + 中文状态)✅ 已修复(含后端重建)

- **日期**:2026-07-06
- **是什么错误**:①后端 `SalesOutboundController.export` 缺 `@ResponseExcel`、返回 `ApiResult<List<PageVO>>`(JSON)非 Excel;②前端 `handleExport` 用裸 `searchParams`,未并入 `activePlatform` → 选平台 Tab 后导出全平台。
- **改动**(后端 5 + 前端 2):①新建 `SalesOutboundExportVO.java`;②`SalesOutboundMapper.java:53` 返回 ExportVO;③`SalesOutboundMapper.xml:75` resultType 改 ExportVO + `LEFT JOIN sys_user` 取 `create_by_name`;④`SalesOutboundService.java:262` 返回类型改 ExportVO、保留仓库名回填、新增 `translateOrderStatus` 状态 code→中文(`OutboundOrderStatus`);⑤`SalesOutboundController.java:268` 加 `@ResponseExcel(name="销售出库单-#{currentDateTime()}")` 去 ApiResult;⑥`api/wms/sales-outbound/index.ts:85` 去泛型 + blob;⑦`SalesOutboundPage.vue` handleExport 并入 `activePlatform`→`platform` + `remoteFileDownload`,import 换 `isSuccess`→`remoteFileDownload`。
- **平台**:按用户要求保持原样(小写 wildberries/ozon/yandex)。
- **重建**:构建 EXITCODE=0,后端 `Started in 55s` 无异常。
- **验证**:选某平台 Tab → 导出 → `销售出库单-时间.xlsx`,仅含该平台、状态中文、含创建人。**⚠️ 后端已重启,需重新登录。**
- **状态**:✅ 已改并重建,待用户界面回归确认。

---

## #3 · fe-wms H1 · 采购单导出路径前缀错(必 404)✅ 已修复

- **日期**:2026-07-06
- **是什么错误**:`api/wms/purchase-order/index.ts:99` 导出路径写成 `'/purchase/order/export'`,而同文件其余端点前缀均为 `'/wms/purchase-order/...'`,漏 `/wms` 段且用 `/` 而非 `-`,系笔误。
- **影响**:后端无此路由 → **必然 404**,采购单导出完全不可用。后端真实路由已核实为 `/wms/purchase-order/export`(类映射 `/wms/purchase-order` + `@GetMapping("/export")`,PurchaseOrderController.java:167)。
- **改动**(1 文件 1 行):`'/purchase/order/export'` → `'/wms/purchase-order/export'`;`responseType:'blob'`/`params:qo` 本就正确,不动。
- **验证**:采购单列表点"导出",应正常下载 Excel(`@ResponseExcel` 名"采购单-时间")而非 404。
- **状态**:✅ 已改,待用户界面回归确认。

---

## Wave 1 剩余批（前端为主，逐条补录）

### #13 · wms 物流单编辑权限码错 ✅
- 错误:`ShippingOrderDetailDrawer.vue` 编辑按钮权限码写成 `wms:shipping-purchase-order:edit`,后端权威码为 `wms:shipping-order:edit` → 有权用户按钮不显示/无权者判断错位。
- 改:权限码 `wms:shipping-purchase-order:edit` → `wms:shipping-order:edit`。

### #15 · wms 库存区域下钻丢 regionId ✅
- 错误:库存明细按区域下钻时,`InventoryQO` 无 `regionId` 字段、`InventoryDetailSearch.vue` 未读 `query.regionId` → 从区域卡片点进明细仍查全部区域。后端 `InventoryMapper.xml:23-24` 早已支持 `regionId` 过滤。
- 改:`api/wms/inventory/types.ts` `InventoryQO` 补 `regionId`;`InventoryDetailSearch.vue` formModel + watch 读 `query.regionId` 回填并查询。

### #16 · wms 仓库编辑清空地址/备注 ✅（后端+前端类型，含重建）
- 错误:仓库详情 `WarehouseService.getDetail` 用 `getById`(全实体含 address/remark)→ `WarehouseConverter.poToPageVo` 转 VO,但 `WarehousePageVO` 无 address/remark 两字段 → MapStruct 丢弃 → 前端 `WarehouseFormModal` 回填时这俩为空 → 保存把已有地址/备注清空。
- 改:①后端 `WarehousePageVO.java` 补 `address`/`remark` 字段(MapStruct 按名自动映射,converter/查询无需改);②前端 `api/wms/warehouse/types.ts` `WarehousePageVO` 同步补两字段。
- 重建:随本批最终后端重建生效。

### #18 · wms 安全库存快捷编辑覆盖通知配置 ✅
- 错误:后端 `saveSkuConfig` 是全量 upsert(一次覆盖 safetyStock/notifyEnabled/notifyThresholdDays 三字段);`SafetyStockEditModal` 仅提交 safetyStock,另两字段丢空 → 快捷改安全库存把已配的预警通知清掉。
- 改:`SafetyStockEditModal.vue` 增 `currentNotifyEnabled`/`currentNotifyThresholdDays` 两 prop,payload 原样带回;`ForecastDetailDrawer.vue` 传入 `detail.notifyEnabled`/`detail.notifyThresholdDays`。

### #19 · wms 销售出库表单缺货未走缺货弹窗 ✅
- 错误:`SalesOutboundFormPage.vue` 的 `handleConfirmOutbound` 遇库存不足直接失败,未像抽屉那样弹 `StockShortageModal` 让用户处理。
- 改:引入 `StockShortageModal` + `stockShortageModalRef` + `<stock-shortage-modal>`,重写 `handleConfirmOutbound` 镜像抽屉的缺货分支处理;移除未用 `doRequest` import。

### #21 · wms 采购单表单未接离开守卫 ✅
- 错误:`PurchaseOrderFormPage.vue` 引入了 `useUnsavedChangesGuard` 但未解构/调用 `useRouteLeaveGuard()` → 有未保存改动直接离开无拦截。
- 改:解构出 `useRouteLeaveGuard` 并在 setup 内调用 `useRouteLeaveGuard()`。

### #23 · wms 采购入库表单空数组误判 ✅
- 错误:`PurchaseInboundFormPage.vue` 用 `if (res.data)`,空数组 `[]` 为 truthy → 无明细时仍当有数据继续。
- 改:`if (res.data)` → `if (res.data && res.data.length > 0)`。

### #25 · auth 空菜单死循环重导航 ✅
- 错误:`guards.ts` 用 `if (userMenus)`,空数组 truthy → 每次导航都重建动态路由并 `return to.fullPath` 重导航 → 换身份/无菜单时死循环。
- 改:`if (userMenus)` → `if (userMenus && userMenus.length > 0)`,空菜单 fail-closed 放行(空侧栏),不再死循环。

### #27 · financial 对账抽屉 visible/open 不匹配 ✅
- 错误:`OrderReconciliationDrawer.vue` 子组件用 `props.visible`/`emit('update:visible')`,父用 `v-model:open` → `props.visible` 恒 undefined → watch 不触发 → 详情不加载。
- 改:visible → open(prop/emit/watch/emit 调用共 5 处)。

### #28 · financial WB 财报同步弹窗 visible/open 不匹配 ✅
- 错误:同上模式,`WbFinancialReportSyncModal.vue` 用 visible、父用 `v-model:open` → 弹窗打不开 + 30 天默认日期 watch 不触发。
- 改:visible → open(5 处)。

### #24 · auth H1 常量路由跨身份 URL 直达 —— ⏸ 归入 Wave 3（架构级，暂缓）
- 错误:`constant-routes.ts` 把全部业务表单页(SKU/采购单/物流单/采购入库/自定义入库/自定义退货/调拨/盘点/销售出库/自定义出库等)注册为**常量路由**,与菜单权限无关、始终可达。任一身份用户可直接输 URL 到达任意表单页外壳。
- 影响评估:**非数据越权**。表单页内所有 API 调用受后端身份校验(`assertPlatform`/`canOpenWmsOperator`)+ 租户拦截,货主直达服务商表单页拉不到数据、提交被拒。属 UI 外壳可达的纵深防御瑕疵。
- 为何暂缓:正确修法需给每条表单路由挂 `meta.permission` 并在全局 `guards.ts` 内按当前身份菜单校验,动到全局守卫属敏感架构改动,已随 Wave 3 fe-auth 一并推迟。**本批不改,单独评估。**

---

## 阶段小结（Wave 1 收尾）

- **本批新补前端**:#13/#15/#18/#19/#21/#23/#25/#27/#28 = 9 项;**后端+前端类型**:#16(随本批重建)。
- **暂缓(归 Wave 3)**:#24 常量路由跨身份直达(纵深防御,后端已挡数据)。
- **累计已修**:前端约 29 项 + 后端 8 项(含 #16)。
- **待办**:Wave 3 架构级 —— 库存 C-1/C-2/C-3(双写/退货双记账)、H-1/H-2(CAS)、S1(无上下文 fail-open 安全)、店铺 API Key 明文加密、fe-auth #24、fe-components M6。

---

## Wave 1 遗漏项补齐批（前端 5 项核实）

### #48 · fe-wms H4 发货单列表统计"恒 0" —— ✅ 实为误报，仅补前端类型
- 核实:报告称"消费 VO 不存在字段 totalShippedQuantity/totalReceivedQuantity → 恒 0"。**实测后端 `ShippingOrderPageVO` 有这两字段(:59/:62),`ShippingOrderService.queryPage:136-137` 经 `getQuantityStatsMapByShippingOrderIds` 正确填充** → 运行时不为 0,报告误判(同 #7 情形)。
- 唯一真实缺口:前端 `api/wms/shipping-order/types.ts` 的 `ShippingOrderPageVO` 类型漏声明这两字段(模板在用,运行时无碍,仅 TS 类型缺口)。
- 改:前端类型补 `totalShippedQuantity?`/`totalReceivedQuantity?`。

### #49 · fe-finance 第3个财务组件 visible/open 错配 ✅
- 错误:`WbFinancialReportDetailModal.vue` 声明 `props.visible`/`emit('update:visible')`,父 `WbFinancialReportPage.vue:111` 用 `v-model:open` → `props.visible` 恒 undefined,`:open="visible"` 恒 undefined → **明细弹窗永不打开**(点行无反应)。与 #27/#28 同模式,补齐"3 个财务组件"最后一个。
- 改:visible → open(template `:open`、Props、emits、handleClose,共 4 处)。

### #50 · fe-wms H7 盘点表单页契约错配 → ✅ 移除废弃孤儿页 + 闭合 URL 直达
- 核实:`StocktakeFormPage.vue` 已被改为"仅新建"(`isUpdateForm` 恒 false),但仍残留:①死 edit 路径 `loadStocktakeDetail` 用 `orderStatus!=='DRAFT'`(枚举 `StocktakeStatus` 无 DRAFT → 恒被弹回);②PARTIAL 收集 `skuIds` 提交,但后端 `StocktakeDTO` 无 skuIds 字段、`create()` 对 PARTIAL 只建空单(由录入页加 SKU)→ 选择被静默丢弃。
- **根因**:此页是**废弃孤儿**——列表页 `StocktakePage.vue:132/293` 实际用 `StocktakeFormModal` 建单(在用流程正确:`createStocktake(formModel)` 不发 skuIds)、录入页 `StocktakeInputPage` 加 SKU;`StocktakeFormPage.vue` 全局仅被 `constant-routes.ts:130` 引用,只能 URL 直达。
- 改:①`constant-routes.ts` 移除 `StocktakeForm` 孤儿路由(闭合"废弃页 URL 直达");②删除 `StocktakeFormPage.vue`(复核全局无残留引用)。**在用的 Modal/录入页流程不动。**

### #51 · fe-wms M8 破坏性库位/货架操作缺前端权限门 ✅
- 错误:`WarehouseLocationDrawer.vue` 的保存结构/生成·重新生成库位/库位设置(→分区 assignZone)按钮**无任何前端权限门**;后端 `WmsLocationManageController` 全部方法要求 `@per.hasPermission('wms:warehouse:edit')` → 无权用户点了只得 403,属"按钮可见但无效"纵深防御瑕疵 + API 注释"仅平台超管"误导。
- 改:引入 `useAuthorize`,`canEdit = hasPermission('wms:warehouse:edit')`(对齐后端权威码);门控三入口——保存结构+生成库位 `a-space v-if="canEdit"`、库位设置按钮 `v-if="canEdit && ..."`(assignZone 仅在 settingMode 内可达,随此按钮一并门控)。

### #52 · fe-wms M9 退货质检 FAIL 照片未真上传 → ✅ 复用阿里云 OSS 直传，完整实现留证（DB+后端+前端+重建）
- 错误:`ReturnQcModal.vue` `a-upload :before-upload="()=>false"` 阻止真实上传(照片仅存本地 fileList),提交只发 `photoCount`(数量);后端 `ReturnQcDTO.ReturnQcLineDTO` 只有 `photoCount(:46)`、`ReturnQcService:177` 仅用它做"附了≥1张"的校验门,**无任何存照片的字段/表** → 真实照片从头到尾没上传,凭证丢失。
- 复用基建:项目已有阿里云 OSS 前端直传(`SysFileController`:取签名 `/system/file/upload-signature`→前端直传 OSS→存元数据 `POST /system/file` 返回 `fileId`;下载签名URL / 批量下载URL;前端 `useSysFileUpload().uploadFile` 封装整套)。**无需新建桶/接口/SDK**,照片存 `private-files` 私有桶,只加一个持久化字段。用户决策:private 桶 + 同意 ALTER 运行库。
- **DB**:①存档 `erp-backend/sql/migration/V49__return_qc_photos.sql`;②`ALTER TABLE wms_return_qc_item ADD COLUMN qc_photos VARCHAR(1000)`(存 sys_file.id 逗号分隔),已 ALTER 到 docker `erp-mysql` 运行库并 `SHOW COLUMNS` 验证通过。
- **后端**(4 文件):①`WmsReturnQcItem` 加 `qcPhotos`;②`ReturnQcDTO.ReturnQcLineDTO` 加 `List<Long> photoFileIds`(保留 photoCount 兼容);③`ReturnQcService` 校验改用 `photoFileIds`(空则 fallback photoCount,FAIL 电子类必须≥1)+ 落库 `item.setQcPhotos(joinPhotoIds)`,`buildItems` 回显 `qcPhotoFileIds`,新增 CSV join/parse 辅助;④`ReturnOrderItemVO` 加 `List<Long> qcPhotoFileIds`。MyBatis-Plus 自动映射,mapper 无需改。
- **前端**(2 文件):①`api/wms/return-qc/types.ts` `photoCount`→`photoFileIds?:number[]`、`ReturnOrderItemVO` 加 `qcPhotoFileIds?`;②`ReturnQcModal.vue` `a-upload` 改 `:custom-request` 走 `useSysFileUpload().uploadFile(file,{bucketKey:'private-files',allowedTypes:图片})`→收集 `fileId`→提交 `photoFileIds`;上传后 `getFileDownloadUrl` 取预览、缩略图带删除角标;只读态 `batchGetDownloadUrls` 批量取签名URL 用 `a-image` 展示;校验/提交全部改 `photoFileIds`。
- **重建**:随本次后端重建生效。**⚠️ 后端已重启,需重新登录。**
- **验证**:FAIL+电子类 SKU 点"上传照片"→选图直传 OSS→出缩略图;未传则提交被拦;完成后重开只读详情应见照片缩略图(签名URL可点开预览)。

---

## Wave 3 · C-1 双库存权威冲突（销售出库）→ ✅ 方案A：确认时批次预留 + 取消释放（含重建）

### 是什么错误
`wms_inventory`(快照)有两个互斥写者:①过账引擎在**确认出库**时对快照 `available` 做 delta 扣减(`SalesOutboundFacade.doStockPosting` 的仓库 AVAILABLE- 项),**不碰批次**;②聚合器 `refreshSnapshot` 按批次绝对值覆盖 available/reserved。确认只改快照不改批次,任何一次 refreshSnapshot(上架/报废/移库/退货)都会照满批次重算,把确认的扣减**抹掉** → 系统性超卖/预占丢失。根因:真正的"锁货"发生在下架(pick 的 `reserved_qty +=`),而确认抢先在快照上单方面扣了 available。

### 方案A（用户拍板，确认即锁货 + 下架前可取消释放）
把"锁货"从下架**提前到确认**,可用数确认时就从批次算着下降;下架前可取消并释放。

**后端（2 文件）**
- `OutboundPickingService`：抽出可复用的
  - `reserveForOrder(order, items)`：FIFO 锁批次(`selectFifoAllocatableForUpdate` FOR UPDATE)+ 全量分配;任一 SKU 缺口>0 返回全部缺货明细、不应用任何预留;够则逐批次 `reserved_qty += take` + 建拣货分配 + refreshSnapshot。
  - `releaseForOrder(order)`：按拣货分配逐批次 `reserved_qty -= take` + 删分配 + refreshSnapshot(@Version 冲突抛 409)。
  - `pick` 简化：去掉批次预留(已挪到确认),仅推进 CONFIRMED→PICKING + 指派拣货员;**兜底**——若无分配(历史/异常单)则补预留、缺货挂 BACKORDER。
- `SalesOutboundFacade`：
  - `confirm`：删仓库 AVAILABLE- delta(`doStockPosting` 只留区域 RESERVED-);新增调 `reserveForOrder`,缺货返回明细、单留 DRAFT、事务回滚。
  - `cancel`：放开到允许 **CONFIRMED**(下架前);已确认单执行 `releaseForOrder` + `reverseRegionReservation`(区域 RESERVED IN,用 `PostingType.SALES_RELEASE` 与确认的 SALES_SHIP 幂等键区分,避免被去重跳过)+ 释放 ERP 占用 + 置 CANCELLED。截止点:PICKING 及之后拒绝取消。

**前端（2 文件）**
- `api/wms/sales-outbound/index.ts`：加 `deleteSalesOutbound(ids)`(DELETE /wms/sales-outbound,body=ids)。
- `SalesOutboundDetailDrawer.vue`：按状态机重排底部按钮——**草稿**=[编辑][删除][确认出库];**已确认**=[取消出库](弹"取消将释放已占用库存");加 `canCancel` computed + `handleDelete`。

### 行为变化
1. ✅ 可用数在**确认时**下降,之后再上架不再被抹回。
2. 缺货判断提前到确认时(前端 StockShortageModal 已支持);原下架挂 BACKORDER 改为确认失败。
3. 批次在确认时锁定;确认→签出间隔久且批次有变时分配或需人工微调(操作层面)。
4. **取消**:下架前可取消,批次预留释放、可用恢复、区域预占加回、单变 CANCELLED。

### 重建 & 验证
- kill java → mvn EXITCODE=0 → 起 jar `Started in 29s`,无循环依赖/无异常。**⚠️ 后端已重启,需重新登录。**
- 待界面回归:①上架100→确认卖30→可用**70**;②再上架50→可用**120**(不被抹回);③取消该确认单→可用**150**、单 CANCELLED;④草稿:提交→可用↓ 或 删除→单没了;⑤已下架单取消→被拒。

### 范围
本次**仅销售出库**。自定义出库 `CustomOutboundFacade`、调拨 `TransferOrderService` 同口径待后续单独处理。

---

## Wave 3 · C-1 收口（自定义出库）→ ✅ 复用批次预留/释放（含重建）

### 是什么错误
`CustomOutboundFacade.submit` 提交时 `doStockPosting` 对仓库 `AVAILABLE-` 做快照 delta(不碰批次)、`cancel` 的 `doReversePosting` 做 `AVAILABLE+` 冲正——与销售出库同一 C-1 病灶,会被 `refreshSnapshot` 抹掉。自定义出库本就是 `SalesOutboundOrder` 行、走同一套 pick/ship。

### 方案（复用销售出库已建的批次预留/释放）
- `CustomOutboundFacade` 注入 `OutboundPickingService`。
- `submit`：保留 `checkStockSufficiency` 预检;`doStockPosting` → `outboundPickingService.reserveForOrder`(FIFO 锁批次+建分配+刷快照,可用当场↓);`updateToConfirmed(id, null)`(无过账)。
- `cancel`(已允许 DRAFT|CONFIRMED)：已确认单 `doReversePosting` → `outboundPickingService.releaseForOrder`(释放批次+删分配+刷快照,可用↑)。
- 旧私有 `doStockPosting`/`doReversePosting`/`resolvePostingType` 保留但不再调用(死方法,编译无碍),避免多块删除风险。
- 自定义出库无区域、无 ERP 订单,故无区域冲正、无订单占用释放;前端契约不变(submit 仍返回 shortages、cancel 仍 void)故前端无需改。

### 调拨 TransferOrderService —— 非同类，暂不动
核实 `TransferOrderService` 无 AVAILABLE/RESERVED/post 这类"确认改快照"动作(只有 CRUD),其库存流转不是 C-1 结构,单独留待后续核实。

### 重建 & 验证
- kill java → mvn EXITCODE=0 → 起 jar `Started in 35s`,无循环依赖/异常。**⚠️ 后端已重启,需重新登录。**
- 待验证:自定义出库 提交→可用↓;再上架同 SKU→可用不被抹回;取消(下架前)→可用恢复。

---

## Wave 3 · C-3 乐观锁重试重复过账 → ✅ 重试下沉到分组级（含重建）

### 是什么错误
`InventoryPostingEngine.execute` 乐观锁失败时**整段重试** `doExecute`。多分组过账(如调拨源仓+目标仓两组)时:第1组成功写库(同事务内已生效)→第2组并发冲突抛异常→整段重跑→第1组 `getOrCreate` 读到自己刚加过的值、把增量**再加一遍** → 数量翻倍;流水只记最终一次 → 快照≠流水。

### 方案
改 `InventoryPostingEngine` 一个文件:
- `execute`:去掉外层整段重试,只调一次 `doExecute`。
- `processWarehouseItems`/`processRegionItems`:改为**每个分组独立乐观锁重试**——每次尝试重新 `getOrCreate` 读最新版本、从新值重算增量、构建本组流水,`updateWithVersion` 成功才把本组流水并入总表并 break;耗尽 MAX_RETRY 抛 OptimisticLockException。业务不足(afterQty<0/validateConstraint)直接抛出、不参与重试。
- 效果:某组失败重试不牵连已成功组,增量绝不重复;无论 RR/RC 都不会重复过账,最坏 fail-safe 回滚。

### 附带发现（记录待办）
- **调拨库存逻辑在 `TransferOrderFacade`**(非 TransferOrderService),用过账引擎做仓库 `AVAILABLE-`/`IN_TRANSIT+` delta → 与 C-1 同类病灶(且天然多分组)。**待后续按方案A口径处理。**
- `ReturnInboundService.create` 走 `RETURN_RECEIVE` 对仓库 `AVAILABLE+`/`DAMAGED+` 快照 delta(退货入库),同属两写者范畴(C-2)。待后续。

### 重建
kill java → mvn EXITCODE=0 → 起 jar `Started in 35.8s` 无异常。**⚠️ 后端已重启,需重新登录。**

---

## BUG-PendingOrders500 —「添加订单」500 报错(QO 字段与 Mapper 不一致)

### 现象
销售出库单新建表单点「添加订单」→ `GET /wms/sales-outbound/pending-orders` 返回 **500**,待出库订单弹窗永远打不开(显示暂无数据/报错)。

### 根因
`ErpOrderMapper.xml` 的 `queryPendingOutboundPage`(销售出库待出库列表专用,绑定 `PendingOrderQO`)动态 SQL 里引用了 `qo.platformItemIds`,但 `PendingOrderQO` 只有 `skuCodes`(List<String>),**没有 platformItemIds 字段**。MyBatis 用 OGNL 求 `<if test="qo.platformItemIds != null...">` 时访问不存在的属性 → `org.apache.ibatis.ognl.NoSuchPropertyException: com.erp.admin.order.model.qo.PendingOrderQO.platformItemIds` → 500。前端 `SelectOrderModal.vue` 发的也是 `skuCodes`,双方本就该按 SKU 编码过滤。
> 注:同名 `platformItemIds` 还出现在 `<sql id="orderFilterClause">`(第243行),但那段被 `queryPageByQo`/`selectExportBatch` 复用,用的是 `ErpOrderQO`(确实有该字段)——**不能动**,只改 `queryPendingOutboundPage`。

### 影响
待出库订单列表打不开,销售出库单无法添加订单(该链路完全不可用)。非 C-1/C-3 引入,是既有历史缺陷。

### 修复
`queryPendingOutboundPage`:把 `qo.platformItemIds`/`oi.platform_item_id IN(...)` 改为按 `qo.skuCodes` 过滤,`LEFT JOIN sku_mapping sm ON oi.platform_item_id = sm.platform_item_id`,`sm.sku_code IN (...)`——对齐同文件 keyword 子查询(第235-239行)的 SKU 关联写法,也对齐前端入参。仅改崩溃点,未新增 warehouseId 过滤(避免行为变更导致列表空)。

### 验证
kill java → mvn EXITCODE=0 → jar `Started in 35.3s`。浏览器登录货主 JHIN → 新建销售出库单 → 点「添加订单」:**console 不再出现 pending-orders 500**,弹窗正常加载待出库订单并成功加入明细。✅

---

## BUG-EmptySku —「SKU编码不能为空」(待出库订单 SKU 取值走错来源)

### 现象
添加订单进明细后,SKU 列显示「-」,保存草稿/确认出库时后端报「SKU编码不能为空」。

### 根因(同根三处)
`erp_order_item` 表**本身就有 `sku_code` 列且已填**(DJZ-018 等),而 `platform_item_id` 全表为 NULL(20/20)。但代码全程按「platform_item_id → sku_mapping.sku_code」反查 SKU,该路径对当前数据整体失效:
1. `ErpOrderItem` 实体**根本没映射 `sku_code` 字段**(只有 platformItemId),故 `SELECT *` 拿不到 sku。
2. `SalesOutboundController.getPendingOrders` 用 `skuMappingService.getSkuCodeMapByPlatformItemIds` 取 skuCode,platform_item_id 为空 → 查不到 → VO.skuCode=null → 前端「-」→ 保存校验报空。
3. `ErpOrderMapper.xml queryPendingOutboundPage` 的 SKU 过滤也 JOIN sku_mapping(承接 BUG-PendingOrders500 的临时写法),按 SKU 搜索搜不到。

### 影响
待出库订单 SKU 全部显示不出、无法保存/出库(整链路不可用)。

### 修复(三处统一改为「订单明细自带 sku_code 优先」)
1. `ErpOrderItem` 实体新增 `private String skuCode;`(MyBatis-Plus 自动映射 `sku_code`)。
2. `SalesOutboundController` 新增 `resolveSkuCode(item, skuCodeMap)`:优先 `item.getSkuCode()`,为空才回退 sku_mapping 映射;VO 构建与 skuBrief 批量查询均改用它;platformItemIds 为空时跳过 sku_mapping 查询。
3. `queryPendingOutboundPage` 的 SKU 过滤改为直接 `oi.sku_code IN (...)`,不再 JOIN sku_mapping。

### 验证
mvn EXITCODE=0 → jar `Started in 33.5s`。刷新「添加订单」弹窗:**0 console 错误**,16 条待出库订单 SKU 编码/商品名/可用库存全部正确显示(FTBFVT-4022、DJZ-025、DJZ-018…),不再有空 SKU。✅

---

## BUG-RegionReserve —「库存不足,无法出库(区域预占不足)」(区域预占链路从未成功)

### 现象
出库确认时,明细行库存状态明明「充足(50)」,却弹「库存不足,无法出库:DJZ-025(区域预占不足) 需求1/可用0/缺口1」。

### 根因
出库 confirm 除校验仓库可用外,还硬校验「区域预占」`wms_region_inventory.reserved_quantity ≥ 出库量`。这笔预占本应由平台 FBS 订单进入活跃态(SHIPPED/READY_TO_SHIP)时 `OrderReservationService.doReserve` 写入区域 `RESERVED+`。但**全系统 0 条 SALES_RESERVE 过账、所有 reserved_quantity=0**,预占从未成功,两个真 bug 叠加:
1. **SKU 解析走坏路径**(同 BUG-EmptySku):`doReserve/doRelease` 用 `platform_item_id → sku_mapping` 取 SKU,platform_item_id 全空 → 「无 SKU 映射,跳过预占」。
2. **区域过账缺货主**:`doReserve` 构建过账时**没传 erpTenantId**,而区域过账 `RegionInventoryService.getOrCreate` 强制要求非空(区域库存按货主隔离),直接抛 `区域库存必须指定货主 erpTenantId`。confirm 侧的区域过账则传了 `order.getErpTenantId()`——两边不对称,预占侧从没跑通。
   - 补充:`ErpOrder` 实体不映射 `tenant_id`(MP 多租户隔离列),故 doReserve 无 `getErpTenantId()`,须从 `TenantContext.getCurrentTenant()` 取(同步/状态变更都跑在 `runAs(shop.tenantId)` 内)。
（另:测试数据是直接塞库、未走订单同步 `OrderLifecycleService`,即便代码正确也没触发预占。）

### 影响
只要区域预占未建立,**所有销售出库都被「区域预占不足」卡死**,无法确认出库。

### 决策
用户选「**保留区域预占硬卡 + 修好预占链路**」(平台订单先预占、出库再消耗的闭环)。

### 修复
1. `OrderReservationService.doReserve/doRelease`:SKU 解析改「明细自带 `sku_code` 优先」(新增 `resolveItemSkuCode`);区域过账补 `.erpTenantId(resolveErpTenantId(order))`,货主取 `TenantContext.getCurrentTenant()`(null/平台-1 则跳过并告警);`resolveSkuCodes` 仅为缺 sku_code 的行查 sku_mapping 兜底。
2. **一次性数据回填**(`ReservationBackfillRunner`,`@Profile("local")`,验证后已删除):启动时对现有活跃 FBS 订单在其货主 `runAs` 上下文内调真实 `reserveIfNeeded`,走正规过账入账(引擎按 (ORDER,orderId,SALES_RESERVE) 去重,幂等)。结果:**16 单全部补建预占,0 失败,16 条 SALES_RESERVE 入账**。

### 验证(方案A 全链路走通)
浏览器货主 JHIN 确认出库 SO202607070003(DJZ-025×1)→ 状态变「已出库」,详情出现「取消出库」按钮。DB 核对:
- 仓库快照 available 50→**49**(确认即降,方案A);批次 `reserved_qty` 0→**1**(FIFO 锁最早批次);
- 区域预占 reserved 1→**0**(确认消耗)。✅
删除回填 Runner → 重新打包 EXIT=0 → 干净重启无异常。

---

## 退货流程改造 — 货主「只申报」，质检入库交海外仓平台（含SKU根因第4处）

### 背景/现象
1. 货主「新建退货入库单」第2步有「入库分配(合格入库/转残品/报废)」，货主越权做质检；且 `ReturnInboundService.create` 建单即 `RETURN_RECEIVE` 过账入库。而 `return_status` 默认 `RETURN_PENDING`，单子又进平台质检队列 → **潜在双重入库**。
2. 用户在海外仓平台「退货质检」点「收货」→ **什么也没有**（收货弹窗空表）。

### 根因
- 设计上退货有两套流程共用 `wms_return_inbound_order`：货主 create（旧，建单即过账）vs 平台 `ReturnQcService`（RETURN_PENDING→收货→QC_PENDING→质检 putaway 真正入库）。平台链路才是正规质检路径。
- 「收货什么也没有」：`ReturnQcService.buildItems` 对未收货单靠退货单头合成明细，条件 `order.getSkuCode()!=null`。而退货单 `sku_code=NULL`——**SKU根因第4处**：`ReturnInboundMapper.selectReturnableOrders` 用 `sm.sku_code`（JOIN sku_mapping ON platform_item_id，platform_item_id 全空）→ skuCode 为空 → 建单 sku_code 空 → 合成不出明细 → 收货弹窗空。

### 决策（用户确认）
保留平台质检硬路径；货主侧退化为「只申报」；「已退数量」在**平台质检完成**时才扣减。

### 修复
后端：
- `ReturnInboundService.create`：删除 `RETURN_RECEIVE` 过账与合格/残品/报废计算；只存申报（`return_status` 默认 RETURN_PENDING）；落 `order_item_id`；质检数量置 0；新增在途去重 `sumInFlightReturnQty`（RETURN_PENDING/QC_PENDING 占用计入可退量，防重复申报）；不再扣已退数量。
- `ReturnQcService.qc`：COMPLETED 时按实收(合格+不合格)累加回写 `erp_order_item`/`erp_order` 的 returned_quantity（注入两 mapper；orderItemId 为空则跳过并告警）。
- `ReturnInboundMapper.selectReturnableOrders`：`sm.sku_code`→`oi.sku_code`（去掉 sku_mapping JOIN），keyword 同改 `oi.sku_code`。
- `ReturnInboundDTO`：`qualifiedQuantity`/`toDamagedQuantity` 去 `@NotNull`（标 @Deprecated 保留兼容）——否则申报提交报「合格入库/转残品数量不能为空」400。
- 实体 `ReturnInboundOrder` +`orderItemId`；迁移 `V50__return_inbound_order_item_id.sql`（已应用运行库）。
- 测试 `ReturnQcServiceTest` 构造器补两 mapper。
前端 `ReturnInboundModal.vue`：删「入库分配」整块及相关 model/校验/watch/computed；按钮「确认入库」→「提交退货」；提交文案「退货申请已提交，待海外仓平台质检」。（用户要求不加蓝色提示条，已去除。）DTO 类型 `qualifiedQuantity?`/`toDamagedQuantity?` 改可选。

### 数据修复
现有退货单 `RI202607070001` 的 sku_code 由 NULL 回填为 DJZ-025（取自 order_item 70019）。

### 验证
货主提交退货申报成功（无入库分配、无过账、状态 RETURN_PENDING）。平台收货弹窗根因已修（sku_code 补齐后 buildItems 可合成明细）。多次 mvn EXIT=0、重启无异常。

---

## 退货质检弹窗改造 — 分区自动/库位可选/通用留证

### 需求（用户）
海外仓平台退货质检：①库位改为「该区可用库位」下拉（可选而非手填）；②质检通过→退货区(RETURN)、失败→次品区(DEFECTIVE)，分区自动定不手选；③照片上传通用化（留证据）；④保留备注。

### 系统分区（仓库1）
STANDARD 标准区 / DEFECTIVE 次品区(zone2,16库位) / RETURN 退货区(zone3,16库位) / TEMP 暂存区。

### 改动
后端：
- `ReturnQcService.listAvailableLocations(warehouseId, zoneType)`：按仓库+分区返回「未被占用」库位码（`wmsLocationService.listByWarehouse` 过滤 zoneId + 排除 `physicalInventoryService.occupiedLocationCodes`），assertPlatform 仅平台可查。
- `ReturnQcController` +`GET /wms/return-qc/available-locations?warehouseId=&zone=`。
前端 `ReturnQcModal.vue`：
- 回库分区：删下拉，PASS→绿标「退货区」、FAIL→红标「次品区」自动定；`onResultChange` 设 zone 并清空已选库位。
- 库位：`a-input` 手填 → `a-select`（show-search），选项来自该行分区的可用库位；结果未判定时禁用。
- 照片：上传按钮对所有行开放（原仅「FAIL+电子类」显示）；电子类失败必传的校验保留；缩略图+删除保留。
- 备注：保留。
- `api/wms/return-qc/index.ts` +`getAvailableLocations`；提示文案更新；移除未用 `PASS_ZONE_OPTIONS` 引用。

### 验证
mvn EXIT=0、重启无异常；退货区/次品区各 16 可用库位。待平台端界面回归（收货→质检：判定→分区自动→库位下拉→上传照片→备注→确认上架）。

### 补充：库位隔离加固
用户指出「可选区域应只能是该退货单所属货主仓库的区域」。原 `available-locations` 收前端传的 `warehouseId`（可被平台操作员篡改看别的仓库）。改为收 `returnOrderId`，后端从退货单取 `warehouse_id`（该货主申报时选定的仓库）再列库位——平台操作员只能选到本单对应仓库的库位，无法越权到其它仓库/货主。（注：`wms_warehouse` 无 erp_tenant_id 列，物理层仓库不按货主分区，货主归属体现在退货单选定的 warehouse_id。）前端改传 `order.id`。

### 补充2：库位限制到货主租用的货架排 + 备注移位
用户指出「可选库位应是该货主租用的排，不能是全部排」。沿用入库上架同口径：
- `ReturnQcService` 注入 `SysTenantMapper`+`WmsRackAssignmentService`，新增 `resolveAllowedRacks(erpTenantId, warehouseId)`：货主→`SysTenant.parentWmsTenantId`(父服务商)→`activeRackNos(warehouseId, 服务商)`(该仓当前有效租用排)。
- `listAvailableLocations` 库位过滤加 `allowedRacks.contains(rackNo)`；`qc()` 提交再加一道 `allowedRacks` 守卫（防绕过前端直接提交到未租用排）。
- 前端 `ReturnQcModal.vue`：备注输入框从「照片下方」改到「照片右侧」（`.qc-cell` flex 横排，列宽 230→320）。
数据实证：退货单1 货主6→父服务商5→租 A10 排；退货区可用 A10-18(空)，次品区 A10-17 被 tenant7 占用故空（正确）。测试 FAIL 需 A10 排有空闲次品区库位。

---

## 移除调拨的「FBO入库」类型，只保留普通调拨（2026-07-07）

### 现象/背景
调拨管理原有两种调拨类型：普通调拨(NORMAL)、FBO入库(FBO_INBOUND)。用户要求去除 FBO 入库，仅保留普通调拨（自有仓之间调拨），并因只剩一种类型而去掉类型筛选与类型展示栏。经确认：**保持现有"仓库→仓库"语义不变，不改为库位级调拨**。

### 根因/现状梳理
- 两种类型的唯一实质差别只在 `TransferOrderService.validateWarehouses`：NORMAL 目标仓须为自有仓(OWN)，FBO_INBOUND 目标仓须为 FBO 仓。
- 出库/入库/撤回过账逻辑（TransferOrderFacade）完全不分类型。
- 调拨为仓库级库存桶过账（AVAILABLE/IN_TRANSIT），明细仅 SKU+数量，不涉及库位。

### 改动
后端：
- `TransferType.java`：删除 `FBO_INBOUND`，仅留 `NORMAL`。
- `TransferOrderService.validateWarehouses`：去掉按类型分支，目标仓固定校验为自有仓(OWN)；移除无用 `TransferType` import。
- `TransferOrderDTO.java`：transferType 的 Schema 注释去掉 FBO 字样（字段保留、恒 NORMAL，不删列，避免数据迁移风险）。

前端：
- `types.ts`：`TransferType` 枚举只留 NORMAL，删除 `TransferTypeMap`、`TransferTypeColorMap`。
- `TransferOrderPageSearch.vue`：删除「调拨类型」筛选项、相关 import、formModel.transferType。
- `TransferOrderPage.vue`：列表「调拨单信息」单元格去掉类型 Tag，删除相关 import。
- `TransferOrderDetailDrawer.vue`：详情去掉「调拨类型」描述项、getTypeColor、相关 import。
- `TransferOrderFormPage.vue`：删除类型 Radio 区块、FBO 提示 alert、targetWarehouseType 联动（目标仓固定 OWN）、handleTransferTypeChange；formModel.transferType 保持常量 'NORMAL' 用于提交。

### 验证
- 后端 `mvn -pl admin -am package -DskipTests` 构建通过，jar 重启就绪（health 200）。
- 前端（Vite HMR）：调拨管理筛选栏已无「调拨类型」；新建表单调拨路径区仅源仓库→目标仓库+货主，无类型 Radio/无 FBO 提示；列表页/详情页无类型栏。控制台 0 报错。
- 全链路 create（transferType=NORMAL）逻辑未变（仅移除 FBO 分支），前端仍提交 NORMAL，后端 @NotBlank 通过。

---

## Wave 3 · 调拨 C-1 收口 —— ⏸ 用户搁置（2026-07-07）
用户表示调拨单的具体业务形态尚未确定，`TransferOrderFacade` 的 C-1 同类病灶（确认出库/入库做仓库 AVAILABLE-/IN_TRANSIT delta，会被 refreshSnapshot 抹掉）暂不修，待业务形态定稿后再评估。**本项挂起，不动代码。**

补充核实：C-2 退货双写在本会话退货链路重构后已基本闭合——`ReturnInboundService.create` 已改为"仅申报"、不再做 RETURN_RECEIVE 过账；实际入库改由平台质检完成时的批次级 putaway 承担（refreshSnapshot 可正确聚合）。

---

## Wave 3 · 并发守卫 H-1/H-2/H-6（CAS 抢占，含重建）· 2026-07-07

### 是什么错误（三条，均为并发/重复提交下重复写库）
- **H-1 入库上架**：`WmsInboundExecutionService.putaway` 读 RECEIVED 后末尾 `setOrderStatus(COMPLETED)+updateById` 无条件、丢返回值，`PurchaseInboundOrder` 无 @Version。并发/重试双提交两事务都过 assert、各插一整套批次、都置 COMPLETED → 该货主该 SKU available/damaged 翻倍。
- **H-2 下架 pick**：`OutboundPickingService.pick` 状态写 `setOrderStatus(PICKING)+updateById` 无 CAS（仅 Java 侧校验）。并发双下架都读到 CONFIRMED 都通过，兜底补预留分支还会双分配 → 签出遍历两组分配、物理库存超扣一倍。
- **H-6 退货质检**：`ReturnQcService.receive`(RETURN_PENDING→QC_PENDING)、`qc`(QC_PENDING→COMPLETED) 状态写无条件，`ReturnInboundOrder` 无 @Version。双击/并发：receive 双建收货行、qc 双 putaway → 该货主库存翻倍。

### 方案（照搬已验证的条件 CAS 抢占，零 DB 迁移）
统一采用签出同款「早抢占」范式（`SalesOutboundMapper.casOrderStatus` / `OutboundShippingService`）：在做写库动作前，用 `UPDATE ... SET status=next WHERE id=? AND status=expect` 原子抢占，返回值!=1 即抛「已处理/请勿重复操作」。写库动作与状态推进同处一个 @Transactional，落败事务在抢占处即被挡下、不做重复 putaway；即便已做也随回滚一并撤销。无需加 @Version 列、无 DB 迁移、无实体/前端改动。

改动文件：
- `PurchaseInboundMapper`：新增 `casOrderStatus(id, expect, next)` default 方法（WrappersX.lambdaUpdate）；import WrappersX。
- `WmsInboundExecutionService.putaway`：开头 `casOrderStatus(RECEIVED→COMPLETED)` 抢占 + 判!=1 抛错；删末尾无条件状态写。
- `OutboundPickingService.pick`：复用 `salesOutboundMapper.casOrderStatus(st→PICKING)` 抢占 + 判!=1 抛错（st=启动时 CONFIRMED/BACKORDER）；兜底缺货/成功路径末尾 setOrderStatus+updateById 保留（抢占只负责抢跑）。
- `ReturnInboundMapper`：新增 `casReturnStatus(id, expect, next)` default 方法。
- `ReturnQcService.receive`：`casReturnStatus(RETURN_PENDING→QC_PENDING)` 抢占 + 删末尾状态写；`qc`：`casReturnStatus(QC_PENDING→COMPLETED)` 抢占（末尾 updateById 保留以写 qualified/unqualified 数量）。

### 影响面
纯并发防护，单线正常流程行为不变（成功路径 rows==1 照常推进）。无 DB 迁移、无实体改动、无前端改动。

### 重建 & 验证
- kill java → mvn EXIT=0 → 起 jar，health 200、单进程新实例，Spring 上下文正常装配（新增 mapper default 方法与各 service 均无问题）。**⚠️ 后端已重启，需重新登录。**
- 待人工回归：对入库上架/下架/退货收货/退货质检各做「双击/重复提交」——应只成功一次，第二次报「已处理/请勿重复操作」，无重复批次/分配/库存翻倍；单线正常流程照常。

---

## Wave 3 · 退货数量守恒 H-4/H-5（含顺带收口 M-5）· 2026-07-07

### 是什么错误
- **H-5 退货收货幽灵库存**：`ReturnQcService.receive` 建质检明细时 `receivedQty` 仅 null 兜底，不校验：①SKU 是否属于本退货单 ②实收≤应退 ③实收≥0。收货填 500(应退5)或塞无关高价 SKU，质检上架按实收 putaway → 该货主库存凭空放大、数量守恒突破。
- **H-4 退货质检超量**：`ReturnQcService.qc` 遍历提交 lines、每行按 `item.receivedQty` 整批 putaway，行级无数量字段且不去重。同 SKU 两行(良/次或误提交两次、填不同库位绕过独占)→ putaway 两次、库存翻倍。
- **M-5（顺带）**：qc 遍历"提交行"而非"已收货明细"，漏判的收货 SKU 永不入库、单据卡 QC_PENDING。

### 方案（单 SKU 模型下的守恒守卫，非破坏性）
- `receive`(H-5)：`expectedQty=单头 totalQuantity`；每行断言 `skuCode==单头SKU`(防注入)、`0≤received≤expectedQty`、同 SKU 收货行不重复。
- `qc`(H-4)：`Set<String> judgedSku` 去重——同一 SKU 重复质检直接抛错；putaway 口径不变(整批 item.receivedQty)。
- `qc`(M-5)：循环后断言 `judgedSku.size()==itemBySku.size()`（判定集合==已收货集合），确保每个已收货 SKU 都被质检覆盖。与前端"所有行必须判定"一致，不误伤正常流程。

改动文件：`ReturnQcService.java`（receive + qc 两处）。无 DTO/前端/DB 改动。

### 重建 & 验证
- kill java → mvn EXIT=0 → 起 jar health 200。**⚠️ 后端已重启，需重新登录。**
- 待人工回归：①收货实收>应退→报"实收数不能超过应退数"；②收货塞非本单 SKU→报"不属于本退货单"；③质检同 SKU 两行→报"不能重复质检"；④正常单行收货+质检照常通过、库存按实收一次入账。

---

## Wave 3 · H-7 出库库存展示跨货主串数（含重建）· 2026-07-07

### 是什么错误
`InventoryService` 的无货主维查询 `getStockMapByWarehouseAndSkuCodes(warehouseId, skuCodes)` 底层仅按 (warehouseId, skuCode) 查、`toMap((a,b)->a)` 折叠。多个货主在同一「自有仓(共享物理仓)」持有同一 SKU 时 selectList 返回多行、任意保留一行 → 货主 A 看到的 available 实为货主 B 的数值（跨货主库存泄露 + 误导可用量/缺货）。边界：均为展示/状态路径，权威扣减路径已正确带货主维、不会超卖。

### 方案（仅后端 3 文件，改调已存在的带货主维重载 getStockMapByErpWarehouseAndSkuCodes）
- `SalesOutboundService.enrichItemsWithStockStatus`：加 erpTenantId 参数；getDetail 用 `this.getById(id).getErpTenantId()`（订单头货主，货主在 SalesOutboundOrder 上，item 无该字段）取数。
- `CustomOutboundService.enrichItemsWithStockStatus`：同上（自定义出库同样管理 SalesOutboundOrder 实体）。
- `SalesOutboundController.enrichStockStatus`（货主待发列表，视角=当前货主）：用 `TenantContext.getCurrentTenant()` 取数。

### 范围/取舍
调拨的两个无货主维点（`TransferOrderService.pageAvailableStock` :252 / `batchQueryStock`→`getAvailableStockMap` :262）**本次跳过**：需改前端契约(传选中货主)，且调拨业务形态未定、C-1 已挂起，一并留待调拨定稿后处理。无货主维方法（getStockMapByWarehouseAndSkuCodes/getAvailableStockMap/pageAvailableStock）因此保留，仅剩调拨在用。

### 重建 & 验证
- 首次编译失败：误用 `SalesOutboundOrderItem.getErpTenantId()`（该实体无货主字段）→ 改为订单头 `SalesOutboundOrder.getErpTenantId()`（this.getById(id)）。
- 修正后 mvn EXIT=0 → 起 jar health 200。**⚠️ 后端已重启，需重新登录。**
- 待人工回归：同一自有仓下两货主持有同 SKU 时，各货主的出库详情/待发列表只显示自己那份 available，不再串数。

---

## Wave 3 · H-3 聚合器重试不重算批次（含重建）· 2026-07-07

### 是什么错误
`WmsInventoryAggregator.refreshSnapshot`：三桶 `b=aggregate(batches)` 在重试循环外只算一次，循环内版本冲突只 `selectById` 重读 version、不重新聚合。
- RC 隔离：重读拿新 version 更新成功，但 b 仍是旧快照(漏并发已提交批次)→ 用陈旧聚合覆盖 → 快照永久性偏低(丢更新)。
- RR 隔离：重读仍事务快照、version 恒旧 → 重试全失败抛冲突整单回滚(fail-safe，但重试形同虚设)。

### 方案（单文件 WmsInventoryAggregator）
重试循环内在重读 version 之后，追加 `b = aggregate(listForAggregate(...))` 重新聚合最新批次再尝试写。RC 下每次重试基于最新批次、消除丢更新；RR 下仍 fail-safe。初次聚合保留用于 insert 与首次尝试。

### 重建 & 验证
- mvn EXIT=0 → 起 jar health 200。**⚠️ 后端已重启，需重新登录。**
- 逻辑回归：并发同 (货主,仓,SKU) 刷新时，最终快照反映所有已提交批次、不再偏低。

### 进度
Critical 3 条已清(调拨 C-1 半条挂起)；**High 7 条全部完成**(H-1/2/3/4/5/6/7)。剩余：Medium(M-1/2/3/4/6/7/8)、S1 安全、L-1 超卖硬守卫、店铺 API Key 明文、fe M6。

---

## Wave 3 · M-4 盘点越权守卫（含重建）· 2026-07-07

### 是什么错误
`StocktakeService` 是唯一无 `assertPlatform()` 的库存作业服务：`create`→`generateStocktakeItems` 用 `getAllByWarehouseId`(无货主过滤)枚举该仓所有货主库存；`confirm` 逐货主过账调整；隔离仅靠菜单权限 `wms:stocktake:*`。若该权限被误授予某货主/WMS 服务商，即可查看并调整其他货主在该仓库存（跨货主枚举+篡改）。

### 方案（单文件 StocktakeService）
盘点为海外仓平台专属作业。照搬 ReturnQcService 的 `assertPlatform()`（`tenantIdentityService.currentIdentity(null)` 非 IDENTITY_OVERSEAS_PLATFORM 抛 403），加到全部 15 个公有入口首行：queryPage/getDetail/getItems/getProgress/create/saveItems/confirm/cancel/delete/getDiffPreview/getAvailableSkuPreview/batchMarkNoDiff/addSkus/removeItems/getSelectableSkus。补 import BusinessException。`generateStocktakeNo`(仅生成单号)不守。

### 重建 & 验证
- mvn EXIT=0 → 起 jar health 200。**⚠️ 后端已重启，需重新登录。**
- 待人工回归：平台身份进入盘点管理各功能正常；货主/服务商即便有菜单权限，调用盘点接口一律 403。

### 进度
Critical 3 已清(调拨 C-1 半条挂起)；High 7 全清；Medium 已修 M-4、M-5(随 H-4 一并)。剩余 Medium：M-1/M-2/M-3/M-6/M-7/M-8；另 S1、L-1、店铺 API Key 明文、fe M6。

---

## Wave 3 · 批次1+2 统一执行（M-6/M-7/S2部分/M6 + M-2，含重建）· 2026-07-07

> 5 组只读代理核实后，用户批准执行批次1+2。M-8/M-3/S3 经核实已收敛无需改。

### M-6 自定义入库收货不校验实收≤应收
`WmsInboundExecutionService.receive`：写 actualQuantity 前加断言 `actual ≤ expectedQuantity`（仅 expected 非空时约束，不误伤无应收基准单）。1 文件。

### M-7 非规范 quality 致批次双桶静默消失
`WmsInboundExecutionService.normalizeQuality`：改 trim+大写归一 + 白名单（非 GOOD/DAMAGED 抛 400）；`WmsInventoryAggregator.aggregate`：`"DAMAGED".equalsIgnoreCase` 兜底历史脏数据（不动 allocatable 逻辑）。2 文件。

### S2 补 @PreAuthorize（仅安全子集）
经核实 ballcat 下平台管理员只拥角色被授菜单权限、给不存在的权限码加注解会 403 挡平台。故**只处理 DB 中有真实码的两个控制器**：`InventoryConfigController`（GET→wms:inventory-config:read；PUT/global、PUT/sku、DELETE/sku→wms:inventory-config:edit，正好覆盖 agent 点名的写端点）、`InventoryForecastController`（3 GET→wms:inventory-forecast:read）。
**未处理(需先播种权限码+授权平台角色，属独立DB任务，盲加会挡平台)**：WmsLogisticsProductController、AssetFinanceController、OperatorFinanceController、OperatorDashboardController、ShipProdCalcController。

### M-2 过账幂等 DB 兜底 + 查询补租户
- 排查：wms_stock_posting 26 行、0 重复组、17 行 erp_tenant_id NULL（NULL 不阻断唯一键）。
- 迁移 `V51__stock_posting_idempotent_uk.sql`：`ADD UNIQUE KEY uk_source_posting_tenant (source_type, source_id, posting_type, erp_tenant_id)`，已应用到运行库（Non_unique=0 四列）。
- `StockPostingMapper`：加 4 参重载 `selectBySourceAndType(...,erpTenantId)`（eqIfPresent 租户，null 退化兼容 transfer 等无货主过账）。
- `StockPostingService.post`：幂等查询改带租户；createPosting 命中来源唯一冲突(DuplicateKeyException)时回查返回 existed=true（并发安全闭环，不重复应用库存）。

### M6 前端分层倒置（反转依赖）
`components/Sku/SkuMappingBadge.vue`：删内嵌 SkuMappingDetailModal + 其 import，点击改 `emit('open')`，Badge 变纯展示（只依赖 vue）。`views/product/sku/SkuPage.vue`：改由页面持有 SkuMappingDetailModal（同层），加 mappingModal 状态 + openMappingModal(record)（闭包捕获行上下文），Badge 渲染 `onSuccess→onOpen`。2 文件。断掉 components→views 反向依赖。（第二处 ShopSelectInput→@/views/components/Platform 未动，可另处理。）

### 重建 & 验证
- mvn EXIT=0 → 起 jar health 200。**⚠️ 后端已重启，需重新登录。** 前端 Vite HMR。
- 待人工回归：收货超收→报错；上架非法品质→报错；库存配置/预测 货主/服务商无权限→403、平台正常；过账并发/重复→幂等只应用一次；SKU 页点映射徽章→弹窗正常、成功刷新。

### M-1 两写者 wms_tenant_id 建行键不一致致快照分裂
**是什么错误**：wms_inventory 快照有两个写入者，各用不同键定位/建行——
- 过账引擎 `InventoryService.getOrCreate`：建行 `wms_tenant_id=0`，读用 `selectByErpWarehouseSku`（三键 erp×仓×SKU，忽略 wms）；
- 聚合器 `WmsInventoryAggregator.refreshSnapshot`：建行 `wms_tenant_id=入参`，读用 `selectByTenantKey`（四键含 wms）、聚合只取 `listForAggregate`（按 wms 切分的批次子集）。

唯一键是 `uk_tenant_wh_sku(wms_tenant_id, erp_tenant_id, warehouse_id, sku_code)`（四键含 wms）。一旦聚合器拿到非 0 的 wms 维，同一 (erp,仓,SKU) 会被分裂成两行（wms=0 与 wms=N 各一），且聚合器只统计自己 wms 分片的批次 → 快照按 wms 切片、与过账引擎的 0 行互相覆盖不到，货主看到的可用量长期偏低/翻倍。
**影响**：当前潜伏——排查运行库 5 条快照 wms_tenant_id 全为 0、零分裂，未触发；属未来引入非 0 wms 批次即爆的隐患。

**方案（统一到过账引擎口径：快照按货主收敛、wms 恒 0）**：
- `WmsPhysicalInventoryMapper` 新增 `listForAggregateByErp(erpTenantId, warehouseId, skuCode)`：聚合该货主该 SKU **跨全部货架/wms 的批次**（不按 wms 切分）。
- `WmsInventoryAggregator.refreshSnapshot`：读改 `selectByErpWarehouseSku`（三键）、聚合改 `listForAggregateByErp`（含 H-3 重试内那处）、建行 `setWmsTenantId(0L)`。入参 `wmsTenantId` 保留仅兼容调用方签名，不再参与快照行键。
- 2 文件，无数据迁移（现存 5 行已是 wms=0，无需合并）。
**遗留（不在 M-1 范围，独立缺口）**：服务商侧汇总 `selectByWmsTenant/listByCurrentOperator` 因快照恒 wms=0 而仍返回空——此为既有缺口（非本次回归引入），服务商库存汇总口径需另设计（改走批次表 wms 维聚合），本次不动。

### 重建 & 验证（M-1）
- kill java → mvn EXIT=0（jar 19:13:23 重生）→ 起 jar，`Started AdminApplication`（8081），login/root 返回 401（安全层生效、容器正常）。**⚠️ 后端已重启，需重新登录。**
- 待人工回归：货主库存快照可用/破损/预留三桶与批次实况一致；反复过账/上下架后同 (货主,仓,SKU) 始终单行 wms=0、无分裂。

### S1 无租户上下文 fail-open 收敛（仅①+②，位置③留后）
**是什么错误**：后端三处在拿不到租户上下文时选择"放行"，与前端菜单 fail-closed 相反——漏设上下文即静默跨货主串数据：
- ① `ErpOwnerScopeService.readScope`：`erp==null`(无上下文) 与 `-1`(平台) 合并 `return null`(看全部)；
- ② `MyMetaObjectHandler.insertFill` + `ErpOwnerScopeService.writeOwner`：无上下文/平台插入带 erp_tenant_id 单据一律兜底盖货主 1；
- ③ `ErpTenantLineHandler.ignoreTable`：白名单表仅在有上下文时注入 tenant，无上下文跳过=读全部租户。

**为何本次只做 ①+②**（用户 2026-07-07 拍板"只做①+②"）：位置③高危——排查确认改 fail-closed 会击中约 12 个后台入口（8 订单同步/1 FBO/1 WB财务调度在"逐店 runAs 循环前"先用无上下文线程拉全量 `shop`/`wb_financial_sync_task`；2 日志落库 null 租户分支），且 `ShopMapper.listEnabledByPlatform` 等**均无** `@InterceptorIgnore`。前置必须先给这些合法跨租户平台级查询加放行 + 处理日志 null 分支，才能翻③，否则由"串数据"变"定时任务全崩"。→ ③保持现状+文档标注，留后单独排期。

**方案（①+②，只影响请求路径）**：
- ① `readScope`：拆分——仅 `BLOCK_TENANT_ID(-1)`(平台) 返 null(看全部)；`erp==null`(无上下文) 返 `singletonList(BLOCK_ID=-999)` 命中 0 行。
- ②A `writeOwner`（实际仅 `CustomOutboundController.batchQueryStock` 1 处用作按货主取数）：无正值货主上下文（null 或 ≤0）抛 `TenantBusinessException(NO_TENANT_CONTEXT)`，不再返 1。
- ②B `MyMetaObjectHandler.insertFill`：带 erpTenantId 且未显式赋值的插入，仅正值货主上下文盖当前货主；null/平台(-1) 抛 `NO_TENANT_CONTEXT`，不再盖 1。（WMS 入库/库存/批次/流水均显式赋值→strictInsertFill 跳过，平台物理作业显式赋值→不触发兜底，不受影响。）
- 3 文件（ErpOwnerScopeService、MyMetaObjectHandler）。无 DB 迁移。

**重建 & 验证**：kill java → mvn EXIT=0 → 起 jar，`Started AdminApplication`(8081, 26s)。**⚠️ 后端已重启，需重新登录。**
**待人工回归**：货主登录查自定义出库可售库存/货主单据列表正常；插入货主单据 erp_tenant_id 正确=当前货主；平台身份不再误挂/误看货主1的数据。**重点验证后台同步任务仍正常**（本次未动③，理论不受影响，但②B 是全局插入拦截，须确认无后台插入 erpTenantId 实体时漏设上下文——如有会由静默挂1 变为抛错，需 runAs 补上下文）。

### 【统一执行批次·A组+B1】2026-07-07（用户审批：A组+B1，L-1聚合下限=截0+WARN）

#### A1　M6 第二处前端分层倒置（ShopSelectInput→views）
- 是什么错误：公共组件 `components/ShopSelectInput.vue:78` import `@/views/components/Platform` 的 `PlatformSelect`——公共层反向依赖业务视图层（与已修的 SkuMappingBadge 同类越层）。
- 根因：Platform 控件组（Select/RadioGroup/Segmented/Tag 4 个通用控件 + barrel，仅再导出 `@/constants/platform`）本是共享控件，却错放在 `views/components/`。全仓 21 处引用它，其中 20 处在 views/（合法），仅 ShopSelectInput 1 处越层。
- 方案：把整组 `views/components/Platform` 迁到 `components/Platform`（共享层归位），批量改全部 21 处 `@/views/components/Platform`→`@/components/Platform`，删空目录 `views/components`。控件内部仅依赖 `@/constants/platform`（绝对）+ barrel 内相对引用，迁移无断裂。前端 Vite HMR 无需重建；残留检查 0。

#### A2　S4 死代码清理（自定义出库废弃过账路径）
- 是什么错误：`CustomOutboundFacade.doStockPosting`/`doReversePosting` 两个 private 方法**全仓无调用者**（自定义出库 submit/cancel 早已改走批次预留 `reserveForOrder`/`releaseForOrder`），是残留死代码；且它们正是对 AVAILABLE 桶做 OUT/IN 过账的写法，留着有被误复活成错误可用量扣减路径的风险（B1 扫描点名）。
- 方案：删两方法 + 仅被其调用的 `resolvePostingType`；连带清理随之失效的 8 个 import（StockPostingDTO/StockPostingItemDTO/CustomOutboundType/PostingType/SourceType/StockBucket/StockDirection/StockPostingResult）与未再用的 `stockPostingService` 注入字段。已核实 8 import 与字段仅出现在死代码区间。构建无 checkstyle/spotless，编译通过。

#### A3　M-3 加固（区域预占/在途聚合显式传 readScope）
- 是什么错误：`selectReservedByRegionIds`/`selectInTransitByRegionIds` 两条区域聚合只按 region_id 分组、无 erp 过滤，**单靠数据权限拦截器自动注入** erp_tenant_id（`wms_region_inventory` 在白名单）。当前正确，但一旦该表被移出白名单即静默串货主（脆）。
- 方案：对齐同服务里 `selectAggregateByRegionIds(regionIds, readScope())` 已有范式——两方法加 `@Param("erpTenantIds")`，SQL 加 `<if erpTenantIds非空> AND erp_tenant_id IN (foreach)</if>`（null=平台看全部；`[-999]`=空作用域命中 0 行）；`RegionInventoryService` 两调用点传 `erpOwnerScopeService.readScope()`。XML(selectReserved) + 内联@Select(selectInTransit) 各一处。**验证**：对 DB 跑等价 SQL（含 AND erp_tenant_id IN）语法有效、正常返回。

#### B1　L-1 AVAILABLE 桶超卖硬守卫
- 是什么错误：`StockBucket.AVAILABLE` allowNegative=true，`InventoryPostingEngine.validateConstraint` 对可负桶短路 → 过量出库/预占可静默把 available 压负，无硬守卫。
- 前置排查（只读代理全局扫）：LIVE 的 AVAILABLE OUT 过账仅 2 处——`TransferOrderFacade:83`(调拨出库,有Assert预检,仅TOCTOU竞态)、`StocktakeService:372`(盘亏,无预检)；**无任何以"合法压负"为设计的过账路径**（报废走批次级带守卫；销售/自定义出库已改批次预留不再走 AVAILABLE OUT）。故改 fail-closed 只会把竞态/脏数据从"静默压负"变"抛错回滚"，属正向。
- 方案：① `StockBucket.AVAILABLE` allowNegative 改 `false`（过账引擎自动对 OUT 超量抛 `IllegalStateException`）。② 聚合直写快照 `WmsInventoryAggregator.refreshSnapshot` 绕过 validateConstraint、`aggregate` 在 res>qty 脏数据下可算出负 available——按用户决策**截0+WARN 日志**：新增 `clampAvailable(b,erp,wh,sku)`（available<0 时 log.warn 并返 0），insert 与 updateWithVersion 两处写库点接入；`aggregate` 保持纯函数不动。2 文件（StockBucket、WmsInventoryAggregator）。

#### 重建 & 验证（A组+B1）
- kill java → mvn EXIT=0 → 起 jar，`Started AdminApplication`(8081, 27s)，无 mapper 绑定异常。A3 两聚合 SQL 已在 DB 直跑验证有效。前端 Platform 迁移走 Vite HMR。**⚠️ 后端已重启，需重新登录。**
- 待人工回归：区域概览预占/在途按货主正确（服务商看名下、平台看全部、货主看自己）；调拨/盘亏在余量不足时报错回滚而非压负；自定义出库 submit/cancel 仍正常（死代码删除不影响）；平台各处 PlatformSelect 下拉正常显示。

### 【C1】S2 剩余 5 控制器补 @PreAuthorize（越权/越平台）2026-07-08
**是什么错误**：`WmsLogisticsProduct/AssetFinance/OperatorFinance/OperatorDashboard/ShipProdCalc` 五个控制器无方法级 @PreAuthorize，任意登录身份可越权/越平台调用（如货主直调服务商财务）。本系统 `@per.hasPermission` **无超管绕过**，权限码必须经 `sys_role_menu` 授给角色才生效；而这 5 个的权限码在 DB 里根本不存在——故上次 S2 只做了有真实码的 2 个、把这 5 个留下（盲加注解会连合法用户一起 403）。
**归属（由现有页面菜单授权口径坐实，非猜测）**：
- 货主页 asset-finance(150200)/ship-prod-calc(165100) → ROLE_ADMIN_T6/T7/T6_ROLE_MANAGER
- 服务商页 logistics-product(180100)/income(180200)/expense(180300)/新建 operator-dashboard → ROLE_ADMIN_T5/T8
**排查关键发现**：5 页里 **4 页页面菜单已存在**（income/expense 我初次搜错 path，实际在 180000 服务商组下），只缺按钮权限码；**仅"服务商看板"(dashboard/operator) 完全无菜单**（建好没接线的孤儿页）。
**方案（做法甲：连入口一起补，用户 2026-07-08 拍板）**：
- 迁移 `V52__seed_wms_permission_codes.sql`（幂等：主键 ON DUPLICATE KEY + INSERT IGNORE）：① 4 页下挂按钮型权限码 type=2（logistics-product 读/增/改/删；asset-finance/ship-prod-calc/operator-finance 读）；② 新建服务商看板页面菜单 180500(uri=dashboard/operator)+按钮 180501；③ sys_role_menu 按归属授权。已应用运行库、验证 10 行权限码授权全对。
- 5 控制器加注解：4 个纯读控制器用**类级** @PreAuthorize；WmsLogisticsProduct 管理端 4 接口按读/增/改/删加服务商码。
- **⚠️ 避坑**：`WmsLogisticsProductController.options` **故意不加注解**——它由货主建单调用拉父服务商产品，加服务商码会 403 挡住货主建单（service.listOptionsForOwner 对非货主返空兜底）；已加注释防后人误加。
**重建&验证**：mvn EXIT=0 → `Started AdminApplication`(8081)。**⚠️ 后端已重启，需重新登录。**
**待人工回归**：货主登录看资产账务/发货测算正常、看不到服务商财务(403)；服务商登录看物流产品/财务/看板正常、看不到货主页；货主建单能正常拉物流产品选项(options 不被挡)。

### 未执行（留后单独排期）
**C2** S1 位置③(ignoreTable fail-closed)——需先给 12 个后台入口加 @InterceptorIgnore/runAs 放行。
**C3** M-1 服务商侧库存汇总迁批次表 wms 维聚合。
**挂起**：调拨 C-1 / 调拨 H-7 两点 / fe-auth #24。**不做**：店铺 API Key 明文加密（用户 2026-07-07 指示）。
