# 海外仓平台 · 海外仓作业 · 下架(拣货) —— 设计与交接文档

> 面向接手的 AI/开发者（尤其是**后端**）。本页前端已完成，以 mock（含内存态）驱动。
> 说明：这页是什么、展示什么、前端怎么搭的、**后端需要实现的接口契约**、以及如何切到真实数据。
> 截至 2026-07-01。

---

## 0. 背景与定位

**归属身份 = 海外仓平台 `OVERSEAS_PLATFORM`** → 海外仓作业(ops) → **下架(拣货)**（menu id **170503**，path **`picking`**，parent ops 170500）。当前 DB `sys_menu.uri` 仍为占位组件 `common/Placeholder`。用户角色 = WMS 拣货员/管理员。

- 平台接收货主推送的**销售出库单 / 自定义出库单**（`oms_outbound_order` → `wms_outbound_order = PENDING 待下架`）。
- 本页做**下架**：选下架模式 + 指定拣货员 → **FIFO 分配（锁定库存）** → 生成拣货单 → 状态转 `PICKING`，回传 OMS（`SENT`，订单不可再取消）。
- **打包 + 签出**是下一个模块（menu 170504，1.3.2），本页**不含**；下架只锁定 `reserved_qty`，**签出时才真正扣库存**。
- **后端现状**：出库执行/拣货 controller **尚未实现**（无 picker 实体）。→ 前端 mock 先行，后端按第 3 节契约实现。

> 参照实现：入库侧 `InboundOpsPage.vue`（收货+上架），本页范式与之一致。

---

## 1. 业务口径（业务需求 1.3.1）

**状态机（+ OMS 同步）**
```
OMS oms_outbound_order          WMS wms_outbound_order
PENDING → SENT(已下发)  ───►     PENDING(待下架)
                                   │【下架】选模式+指定拣货员
                                   │ 触发 FIFO 分配(SELECT FOR UPDATE)
                                   ▼
                                ALLOCATING → PICKING(拣货中)
SENT ◄──(下架成功回传，订单不可再取消)──┘
```

**下架模式（三选一）**
| 模式 | 适用 | 逻辑 |
|---|---|---|
| 集中分拣 CENTRALIZED | 一票一件(1单1SKU) | 批量拣回打包台配货，同款 SKU≥3 批量打面单 |
| 按单分拣 BY_ORDER | 高货值/爆款/重货大货 | 按单找货，逐单核验 |
| 二次分拣 SECONDARY | 一票多件(1单≥2SKU) | 拣到打包台后按篮子分配，篮子集齐打面单 |

**关键业务规则（铁律，必须体现在 UI 与后端）**
- **FIFO 分配（防超卖，C7）**：仅良品 `quality=GOOD`、单仓履约、按 `inbound_date → pick_order → id` 升序锁定批次，`reserved_qty += take`，行锁 `SELECT ... FOR UPDATE`。
- **缺货整单挂起（BR-01）**：任一 SKU 可用不足 → **整单 backorder，不部分下架**，回传 OMS 触发挂起。
- **不拆单不合单（BR-02）**：一单一拣货单一包裹。
- **下架后不可取消**：状态进入 `PICKING` 后 OMS 端取消入口关闭。

**依赖数据表**：`wms_outbound_order`、`wms_outbound_order_item`、`wms_physical_inventory`、`oms_outbound_order`。

---

## 2. 页面展示（v1）

参照 `InboundOpsPage`：搜索栏 + ProTable + 作业弹窗 + 抽屉。

### 搜索栏
- 出库单号 / 状态（待下架/拣货中/缺货挂起）/ 货主（`ErpTenantSelect`）

### 列表（ProTable）
| 出库单号 | 货主 | 仓库 | SKU/件数 | 下架模式 | 拣货员 | 状态徽标 | 创建时间 | 操作 |

- **操作列跟状态**：`PENDING`→[下架]；`PICKING`→[拣货单]；`BACKORDER`→[缺货详情]（红）；其余→—。

### 下架作业弹窗（PickModal）
- **出库明细表**：SKU / 需求数 / 可用良品 / 缺口（缺货整行标红）。
- **FIFO 分配预览**：库位 / SKU / 批次 / 入库日 / 取货数（按 inbound_date 升序，按库位排序）。
- **下架模式**（三选一，带场景说明）+ **拣货员**（必选下拉）。
- **缺货**：顶部红色告警 + 明细整行标红 + **[确认下架] 禁用**（整单挂起）。
- [确认下架] → FIFO 锁定 / 生成拣货单 / 转 PICKING。

### 拣货单抽屉（PickListDrawer）
- 头：出库单号 / 货主 / 仓库 / 下架模式 / 拣货员。
- 取货清单：序号 / 库位(tag) / SKU / 批次 / 取货数，按库位排序；底部合计。

---

## 3. 后端需实现的接口契约 ⭐

前端 mock 驱动。后端实现下列接口并对齐 VO 后，把 `src/api/wms/outbound-picking/index.ts` 的 `USE_MOCK` 改 `false` 即接通，**前端零改动**。

**接口前缀建议**：`/wms/outbound-picking`

| 方法 & 路径 | 作用 | 入参 | 返回 |
|---|---|---|---|
| `GET /page` | 分页待下架/拣货中订单 | query=`PageParam`(size,page,sort)+`OutboundPickingQO` | `PageResult<OutboundOrderVO>`（列表不带 items） |
| `GET /{id}` | 出库单详情（含 items） | — | `OutboundOrderVO` |
| `GET /{id}/preview` | FIFO 分配预览 | — | `PickAllocationVO[]` |
| `POST /pick` | 确认下架（FIFO 锁定/生成拣货单/转 PICKING） | body=`PickDTO` | void |
| `GET /{id}/pick-list` | 拣货单 | — | `PickListVO` |
| `GET /pickers` | 拣货员选项 | — | `PickerVO[]` |

> 统一包 `ApiResult<T>`（`{ code, data, message }`，成功 `code=200`）。

### VO / DTO（权威在 `src/api/wms/outbound-picking/types.ts`）

```ts
type OutboundStatus = 'PENDING'|'PICKING'|'BACKORDER'|'PACKED'|'SHIPPED'|'COMPLETED'
type PickMode = 'CENTRALIZED'|'BY_ORDER'|'SECONDARY'

OutboundOrderVO {
  id: number
  outboundNo: string
  erpTenantId: number
  ownerName: string
  warehouseId: number
  warehouseName: string
  skuKinds: number          // SKU 种类数
  totalQty: number          // 总件数
  pickMode?: PickMode        // 下架后回填
  pickerId?: number
  pickerName?: string
  status: OutboundStatus
  createTime: string
  items?: OutboundOrderItemVO[]   // 详情才带
}

OutboundOrderItemVO {
  skuCode: string
  skuName?: string
  requiredQty: number       // 需求数
  availableQty: number      // 可用良品库存(GOOD 且 allocatable)
  shortage: boolean         // availableQty < requiredQty
}

PickAllocationVO {          // FIFO 分配行（预览 / 拣货单）
  skuCode: string
  skuName?: string
  locationCode: string      // 库位
  batchNo: string           // 批次(inbound_date + pick_order)
  inboundDate: string
  takeQty: number           // 从该批次取货数
}

OutboundPickingQO { outboundNo?: string; status?: OutboundStatus; erpTenantId?: number; warehouseId?: number }

PickDTO { outboundOrderId: number; pickMode: PickMode; pickerId: number }

PickListVO { outboundNo: string; ownerName: string; warehouseName: string; pickMode: PickMode; pickerName: string; allocations: PickAllocationVO[] }

PickerVO { id: number; name: string }
```

### 后端实现提示
- **平台看全部货主**；`erpTenantId`/`warehouseId` 非空时过滤。
- `/preview` 与 `/pick` 的分配算法一致（FIFO），`/preview` 只算不锁，`/pick` 加行锁真正 `reserved_qty += take`。
- `/pick` 必须做**幂等 + 前置校验**：仅 `PENDING` 可下架；下架前重新核对可用量，**任一 SKU 不足则整单转 `BACKORDER` 并返回错误**（不可部分下架）。
- 下架成功回传 OMS（`oms_outbound_order` 同步），关闭取消入口。
- `pickers` 先返回仓库员工/`sys_user`（角色=拣货员）。
- **口径以《业务需求说明书》1.3.1 与《底层数据表重构设计.md》为准**。

---

## 4. 前端文件清单（已完成）

**API 层** `src/api/wms/outbound-picking/`
- `types.ts` / `mock.ts`（mock + 内存态：下架流转、FIFO 预览、拣货单）/ `index.ts`（6 接口 + `USE_MOCK` 开关 + 接口清单注释）。

**页面 + 组件** `src/views/platform/outbound-ops/`
- `OutboundPickingPage.vue`（主页）/ `PickModal.vue`（下架作业）/ `PickListDrawer.vue`（拣货单）/ `constants.ts`（状态·模式 元数据）。

**路由** `src/router/dynamic-routes.ts`
- `COMPONENT_OVERRIDES['picking']` → 本页，绕过占位组件 `common/Placeholder`，**不改数据库**。

**校验**：`npx vue-tsc --noEmit` 通过；`npx eslint` 无 error/warning。前端走 Vite HMR，无需重打包。以 `super_admin`（海外仓平台入口）登录 →「海外仓作业 → 下架(拣货)」查看。

---

## 5. 待确认 / 可迭代
- **打包 + 签出（1.3.2，menu 170504）**：本页不含，另做；签出时才扣物理库存 + 生成链路二物流费。
- **拣货完成确认**：PICKING → 进入打包 的"拣货员标记已拣完"动作，当前放在打包页/未做；如需在下架页加，可迭代。
- **打印拣货单**：当前拣货单为屏幕查看，打印（库位路径小票）v2。
- **拣货员数据源**：mock 一组（张三/李四/王五）；后端接 `sys_user`。

---

## 6. 一句话交接
> 前端页面 `src/views/platform/outbound-ops/` 已完成并用 mock（含内存态）跑通；后端实现 `/wms/outbound-picking/*`（6 接口，契约见第 3 节 / `types.ts`）后，把 `src/api/wms/outbound-picking/index.ts` 的 `USE_MOCK` 改 `false` 即上线，前端不用动。**FIFO 锁定、缺货整单挂起、下架不可取消 三条铁律务必在后端实现。**
