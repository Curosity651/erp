# 改造实施路线图（最小步 · 可验证 · Claude 提示词）

> 配套文档：《设计方案.md》《业务需求说明书.md》《完整技术设计方案.md》。
> 架构口径：**模块化单体**（WMS = 同一 Java 应用内模块，同进程 Bean 调用，同库 `wms_` 前缀）。

> 🟥 **施工基准（必读，优先级最高）：《现状-设计差异对照表.md》**。本路线图与该表冲突时以该表为准。三条贯穿全程的硬约束：
> 1. **WMS 执行层已实现**（入库/出库/退货/盘点/调拨/库存/预测/物流商 已有表+控制器+前端页）。下面带 🔧 的步骤是**改造现有**，不是新建——动手前先 `grep @TableName` 与对应 Controller/前端页确认现状。
> 2. **库存方案②**：新建 `wms_physical_inventory`(批次级 SSOT)，现有 `wms_inventory` 降级为只读聚合快照（每次批次变动同事务聚合刷新）。
> 3. **软删除用 `deleted`(BIGINT)+`@TableLogic`**（不是 `deleted_at`）；台账/流水表不软删；审计字段 `create_by/create_time/update_by/update_time`+`@TableField(fill)`。所有新表 DDL 一律遵此。

---

## 一、怎么用这份文档

每一步是一个**独立、可运行、可验证**的纵切片。固定节奏：

```
① 把该步的【Claude 提示词】整段粘给一个新的 Claude（指向本仓库）
② Claude 改完后：跑该步的【自动化测试】→ 必须绿
③ 你按【人工验证】亲自点一遍 → 看到预期效果
④ 通过后再进入下一步；不通过就把现象回贴给 Claude 修
```

**铁律（每个提示词都已内置，验收时一并检查）**：双层租户、库存 SSOT 仅 WMS 可写、OMS↔WMS 同进程调用（禁 HTTP/MQ）、FIFO 防超卖、不用外键、软删 `deleted_at`、时间 UTC、密钥走环境变量。

---

## 二、环境与回滚（每步开工前）

```bash
# 后端 :8081   前端 :5173   MySQL :3307(erp)   Redis :6379
# 后端 Swagger:  http://127.0.0.1:8081/swagger-ui.html  (或 /v3/api-docs)
# 前端:          http://127.0.0.1:5173
# 连库:          mysql -h127.0.0.1 -P3307 -uroot -p123456 erp

# 1) 每步独立分支
git checkout -b step-XX-<name>
# 2) 改库前必备份
mysqldump -h127.0.0.1 -P3307 -uroot -p123456 erp > backup_stepXX_$(date +%Y%m%d_%H%M).sql
# 3) 回滚：还原分支 + 还原备份
git checkout . && mysql -h127.0.0.1 -P3307 -uroot -p123456 erp < backup_stepXX_*.sql
```

**关键安全策略（贯穿 A 阶段）**：多租户拦截器采用**白名单**——只对"已迁移完成"的表注入 `WHERE tenant_id`，未迁移的表一律 `ignoreTable=true`。这样每步只影响本步改的表，绝不波及现有功能。

---

## 三、阶段总览（14 步）

| 步 | 名称 | 后端 | 前端 | 一句话可验证产出 |
|---|---|---|---|---|
| **A1** | 租户地基（不改业务） | 租户表+上下文+拦截器(空挂载) | — | 旧功能照常，新表就位 |
| **A2** | 商品模块租户隔离样板 | sku 系列加 tenant_id | — | 两租户互相看不到对方 SKU |
| **A3** | 全量业务表租户化 | 其余表加 tenant_id+重建唯一索引 | — | 各模块仍正常，跨租户隔离 |
| **B1** | 三角色 + 双层租户解析 | 角色+TenantFilter按角色 | — | 三类账号能登录 |
| **B2** | 三入口登录 + 菜单裁剪 | 登录校验角色 | 三入口登录页+路由守卫 | 每入口进对应界面 |
| **B3** | 双层租户开通 | 超管开WMS商/WMS开货主 | 两个开通页 | 超管→WMS商→货主 三级打通 |
| **C1** | 🔧仓库扩展+库位生成 | ALTER wms_warehouse加结构参数;🆕wms_zone/location | 仓库页加结构参数 | 建仓库自动生成库位 |
| **C2** | 🆕货架分配+二维预览 | rack_assignment+RackVO | 预览二维图+分配页 | 看到每排归属色块 |
| **D1** | 库存方案②+只读化 | 🆕physical_inventory(SSOT);🔧wms_inventory降级+stock_flow加租户;🔧盘点对货主隐藏 | 库存页改只读 | 货主库存只读无写入口 |
| **D2** | 🔧入库链路对接 | 🆕oms_inbound_order→🔗对接现有wms_purchase_inbound_order收货上架 | 自定义入库单页 | 建单→收货上架→批次库存增加 |
| **E1** | 🔧出库链路对接 | 🆕oms_outbound_order→🔗对接现有wms_sales_outbound_order下架签出(FIFO) | 自定义出库单页 | 建单→下架签出→库存减少 |
| **E2** | 🔧退货质检扩展 | 🔧扩展现有wms_return_inbound_order加质检三态/拍照/批次回库 | 退货质检页(已存在) | 质检通过/失败→良品/次品区 |
| **F1** | 计费链路一（平台→WMS商） | 费率卡+流水+月账单 | 应收账单页 | 出账→看到 WMS 商账单 |
| **F2** | 计费链路二（WMS商→货主） | 物流产品+签出计费+对货主账单 | 产品管理+货主账单页 | 选产品下单→签出→生成物流费账单 |

> 依赖链：A1→A2→A3→B1→B2→B3→C1→C2→D1→D2→E1→E2→F1→F2。每步只依赖前一步，不可跳。

---

# 四、分步详述（含 Claude 提示词）

---

## A1 · 租户地基（不改任何业务功能）

**目标**：建租户核心表、租户上下文、MyBatis-Plus 多租户拦截器（先空挂载，对所有表 ignore），现有单租户功能零影响。

**后端**：`com.erp.admin.common.tenant` 下新增 `TenantContext`、`WmsTenantContext`、`TenantConfig`（拦截器，`ignoreTable` 暂时对所有表返回 true）；建 `sys_tenant`、`sys_user_tenant`；默认租户 id=1；存量用户绑定默认租户。

**测试**：拦截器单测（注入/清理）；启动冒烟。

**人工验证**：旧功能（登录、商品列表、订单列表）完全照常；`SELECT * FROM sys_tenant;` 有默认租户；`sys_user_tenant` 行数 = 存量用户数。

```text
【任务】为 erp-backend 增加多租户地基，遵循《设计方案.md》§4.1、§5.1–5.3 与《完整技术设计方案.md》。本步绝不改动任何现有业务功能。
1. 新建包 com.erp.admin.common.tenant：
   - TenantContext（ThreadLocal<Long>，存 ERP 货主 tenant_id，含 runAs）
   - WmsTenantContext（ThreadLocal<Long>，存 WMS 服务商 tenant_id）
   - TenantConfig：注册 MybatisPlusInterceptor，先加 TenantLineInnerInterceptor 再加 PaginationInnerInterceptor；
     getTenantId() 读 TenantContext，为空抛 BizException(50003)；getTenantIdColumn()="tenant_id"；
     【关键】ignoreTable(name) 当前对所有表返回 true（白名单为空），保证现有功能零影响。
2. 建表（写 Flyway/SQL 迁移脚本，放迁移目录）：sys_tenant、sys_user_tenant，DDL 完全按《设计方案.md》§4.1（含 tenant_type、parent_wms_tenant_id）；插入默认租户 (1,'default_wms','默认WMS服务商','WMS_OPERATOR',1)；用 INSERT...SELECT 把存量 sys_user 绑定到默认租户。
3. 不要给任何业务表加 tenant_id（留到 A2/A3）。不要改 TenantFilter（留到 B1）。
4. 加单测验证 TenantContext set/get/clear 与 runAs 嵌套恢复。
完成后：mvn -q -pl admin test 通过；本地启动后旧接口行为不变。列出你改动/新增的文件清单。
```

---

## A2 · 商品模块租户隔离（端到端第一个样板）

**目标**：把商品族表（`sku`/`category`/`brand`/`supplier`/`sku_mapping`）纳入租户隔离，跑通"两租户互不可见"的完整闭环，作为后续模块的模板。

**后端**：上述表 `ADD COLUMN tenant_id`；`sku` 唯一键改 `(tenant_id, sku_code)`；拦截器白名单加入这 5 张表；登录后把当前用户的 `tenant_id` 写入 `TenantContext`（临时在拦截器/过滤器里按 `sys_user_tenant` 解析，B1 会正式化）。

**测试**：租户隔离用例——租户1建SKU，切租户2查不到（参照《设计方案.md》§12.1）。

**人工验证**：手工在 `sys_tenant` 加一个租户2 + 一个绑定到租户2的用户；分别登录，各自只看到自己的 SKU。

```text
【任务】把商品模块接入多租户隔离，作为隔离样板。依据《设计方案.md》§4.2、§12.1。
1. 迁移脚本：sku/category/brand/supplier/sku_mapping/sku_files 各 ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 1 AFTER id，并 ADD INDEX idx_tenant_id；
   sku 删除原 uk_sku_code，新增 UNIQUE KEY uk_tenant_sku_code(tenant_id, sku_code)。
2. TenantConfig.ignoreTable 改为白名单：仅对 {sku,category,brand,supplier,sku_mapping,sku_files} 返回 false（注入 tenant_id），其余仍 true。
3. 临时租户解析：在一个 Filter（或现有安全链）里，登录用户经 sys_user_tenant 查 tenant_id 写入 TenantContext，请求结束 clear()；超管/无绑定则不设（此时商品接口要求有租户上下文）。
4. 加集成测试 TenantIsolationTest：TenantContext=1 建 SKU-A；TenantContext=2 list 不含 SKU-A（用 @SpringBootTest + 真实 mapper 或 H2）。
完成后：mvn -q -pl admin test 通过。给我一段 SQL，用于手工插入"租户2 + 租户2用户(密码可复用现有加密方式)"，方便我登录验证两租户隔离。
```

---

## A3 · 全量业务表租户化

**目标**：把剩余业务表（订单/店铺/财务/统计/WMS 现有单据等）全部加 `tenant_id` 并重建受影响唯一索引，拦截器白名单全开。

**后端**：按《设计方案.md》§4.2 批量 `ALTER`；`shop` 唯一键改 `(tenant_id, platform, platform_shop_id)`；`wms_*` 现有单据表加 `erp_tenant_id`（按《完整技术设计方案.md》§4.3 决策 P 口径）；白名单纳入所有业务表。

**测试**：抽 2–3 个模块各加一条隔离用例；全量回归启动。

**人工验证**：两租户分别登录，订单/店铺/财务页都只看到自己的数据；旧单租户数据全部归属默认租户、可见。

```text
【任务】把其余业务表全部接入多租户隔离。依据《设计方案.md》§4.2 与《完整技术设计方案.md》§4.3。
1. 迁移脚本按 §4.2 给这些表加 tenant_id+idx：erp_order(_item)、erp_label_*、shop、project_group、position、sales_target、sync_cursor、exchange_rate、wb_report_detail、wb_financial_sync_*；
   重建 shop 唯一键为 uk_tenant_platform_shop(tenant_id,platform,platform_shop_id)。
2. WMS 现有单据表(wms_purchase_order(_item/_file/_qc_item)、wms_sales_outbound_order(_item)) 按决策 P 加 erp_tenant_id 并用旧数据回填（这些是货主单据，加 erp_tenant_id 即可）。
3. TenantConfig 白名单全开：除系统表(sys_*、log_*、oms_sync_outbox、exchange_rate 视情况)外，业务表全部注入。给出最终 ignore 清单。
4. 给 order 与 financial 各加 1 条隔离集成测试。
5. 全量自检：确认所有现有 Mapper 查询在有租户上下文时不报错（注意手写 SQL 是否被插件正确改写）。
完成后：mvn -q -pl admin test 通过；本地启动，用 A2 的两个租户账号回归订单/店铺/财务页。列出全部改动文件与最终白名单。
```

---

## B1 · 三角色 + 双层租户解析

**目标**：落地 `ROLE_SUPER_ADMIN / ROLE_WMS_MANAGER / ROLE_WMS_OPERATOR / ROLE_TENANT_ADMIN / ROLE_TENANT_USER`，`TenantFilter` 按角色分流（超管不过滤；WMS 商设 `WmsTenantContext`；货主设 `TenantContext`），对齐《完整技术设计方案.md》§7.2 三路逻辑。

**后端**：正式化 `TenantFilter`（替换 A2 的临时解析）；角色常量与 `sys_role` 初始化数据。

**测试**：三类用户解析单测（超管→无过滤；WMS商→WmsTenantContext；货主→TenantContext）。

**人工验证**：分别用三类账号调一个受租户约束的接口，观察日志 `[tenant=...]` / 数据范围符合预期。

```text
【任务】实现三角色体系与双层租户解析过滤器。依据《完整技术设计方案.md》§7.2 的三路 resolveTenantContext 逻辑、《设计方案.md》§5.3。
1. 定义角色常量 ROLE_SUPER_ADMIN / ROLE_WMS_MANAGER / ROLE_WMS_OPERATOR / ROLE_TENANT_ADMIN / ROLE_TENANT_USER，写 sys_role 初始化脚本。
2. TenantFilter（替换 A2 临时解析，HIGHEST_PRECEDENCE+1）：
   - ROLE_SUPER_ADMIN → 不设任何上下文（全量可见）；
   - sys_tenant.tenant_type=WMS_OPERATOR 的用户 → WmsTenantContext.set(tenantId)；
   - tenant_type=ERP_USER 的用户 → TenantContext.set(tenantId)；
   - finally 清理两个 ThreadLocal，并写 MDC[tenantId]。
3. 给现有几个 WMS Mapper 加 @InterceptorIgnore(tenantLine="true") 的位置预留说明（实际 WMS 查询在 C/D 步用）。
4. 单测覆盖三种角色的上下文分流。
完成后：mvn -q -pl admin test 通过。给我三条 SQL：建超管、建WMS商管理员、建货主管理员各一个账号（货主 parent_wms_tenant_id 指向该 WMS 商），供我登录验证。
```

---

## B2 · 三入口登录 + 菜单裁剪（前端首次上场）

**目标**：前端登录页提供「货主 ERP / WMS 服务商 / 平台超管」三入口，服务端按角色校验入口匹配；路由/菜单按角色裁剪。

**后端**：登录响应返回角色；入口-角色不匹配返回明确错误码。

**前端**：`src/views/login` 三入口；路由守卫按角色过滤菜单（BallCat `/system/menu/router`）。

**测试**：后端入口校验单测。

**人工验证**：用错入口登录被拒；用对入口登录进入对应首页，菜单只剩本角色的。

```text
【任务】实现三入口登录与按角色菜单裁剪。依据《完整技术设计方案.md》§11.4、《设计方案.md》§11、决策 E。
后端：登录接口在校验通过后，校验"前端选择的入口类型"与用户角色是否匹配（货主入口仅 ROLE_TENANT_*；WMS入口仅 ROLE_WMS_*；超管入口仅 ROLE_SUPER_ADMIN），不匹配返回业务错误码并提示"请使用正确入口登录"；登录响应带回角色与 tenantType。
前端(erp-frontend)：
1. src/views/login 增加三入口切换（Tab 或三个按钮），登录请求带 entryType。
2. 路由/菜单：拉取 /system/menu/router 后按角色渲染；未授权路由不可达。
3. 顶部显示当前身份(超管/WMS服务商名/货主名)。
测试：后端加入口-角色匹配校验单测。
完成后：前端 pnpm lint 通过、可 dev 启动。告诉我三个账号分别该用哪个入口登录、预期看到的菜单差异，我来逐一验证。
```

---

## B3 · 双层租户开通

**目标**：超管能"开通 WMS 服务商"，WMS 服务商能"开通货主(ERP+OMS 账号)"，双层租户数据闭环。

**后端**：超管侧 `sys_tenant(WMS_OPERATOR)`+管理员账号+`ROLE_WMS_MANAGER`；WMS 侧 `sys_tenant(ERP_USER, parent_wms_tenant_id=本商)`+管理员+`ROLE_TENANT_ADMIN`，且只能开在自己名下。

**前端**：超管 `/admin/wms-tenants`，WMS 商 `/wms/my-clients`。

**测试**：开通后父子关系、角色绑定正确；WMS 商不能给别的服务商开货主。

**人工验证**：超管开一个 WMS 商→用其账号登录→开一个货主→用货主账号登录看到空的 ERP 工作台。

```text
【任务】实现双层租户开通。依据《业务需求说明书.md》§1.1.3、§3.1、《完整技术设计方案.md》§11.4，决策 D/G。
后端：
1. 超管"WMS服务商开通"：创建 sys_tenant(tenant_type=WMS_OPERATOR,parent_wms_tenant_id=NULL)+管理员 sys_user+sys_user_tenant(is_admin=1)+授 ROLE_WMS_MANAGER。
2. WMS商"货主开通"：创建 sys_tenant(tenant_type=ERP_USER,parent_wms_tenant_id=当前WMS商)+管理员+授 ROLE_TENANT_ADMIN；服务端强制 parent=当前登录WMS商，禁止跨商开通。
3. 停用 WMS 商时级联拒绝其下货主登录。
前端：超管页 /admin/wms-tenants（列表+开通弹窗）；WMS商页 /wms/my-clients（列表+开通弹窗）。
测试：开通后父子/角色正确；越权开通被拒（返回错误码）。
完成后：mvn test + 前端 lint 通过。给我操作脚本：超管开WMS商→WMS商开货主→货主登录，我端到端验证三级打通。
```

---

## C1 · 🔧 仓库扩展 + 🆕 库位批量生成

> 🟥 `wms_warehouse` **已存在**（OWN/FBO 平台仓模型：`regionId/platform/platformWarehouseId/shopId`，无排/层/列/库位）。本步是 **ALTER 扩展**它 + **新建** `wms_zone`/`wms_location`。`wms_region`(业务大区) 与 `wms_zone`(仓内品质分区) 是不同维度，勿混。

**目标**：在现有仓库上补结构参数并按"排×层×列"自动生成库位；建品质分区（标准/不良品/退货/暂存）。

**后端**：ALTER `wms_warehouse` 加结构参数；🆕 `wms_zone`、`wms_location`；`WmsLocationGenerator`（《完整技术设计方案.md》§4.4.1）。

**前端**：超管仓库页（现有 `views/wms/warehouse`）增加结构参数 + 【生成库位】。

**测试**：生成库位数 = 排×层×列；重复生成幂等被拒；编码规则 `A1-03-03`。

**人工验证**：对一个仓库填 2排×3层×4列 → 生成库位 24 条、编码符合规则。

```text
【任务】扩展现有仓库并新建库位体系。先读《现状-设计差异对照表.md》§3.1 与现有 com.erp.admin.wms.model.entity.Warehouse / WarehouseController / 前端 views/wms/warehouse。依据《业务需求说明书.md》§1.1.1、《完整技术设计方案.md》§4.4.1。
后端：
1. 迁移脚本【ALTER 现有 wms_warehouse】加列：rack_rows/rack_layers/rack_columns/rack_no_prefix/code_pad_width/default_location_type/location_generated；不动现有 FBO/platform 字段。
2. 【新建】wms_zone(warehouse_id,zone_type:STANDARD/DEFECTIVE/RETURN/TEMP,zone_name,allocatable)、wms_location(warehouse_id,zone_id,rack_no,layer_no,column_no,location_code,location_type,pick_type,max_volume_cbm,max_weight_kg)。新表遵循现有约定：审计 create_by/create_time/update_by/update_time；wms_zone 用 @TableLogic Long deleted，wms_location 同（参照 Warehouse 实体写法）。禁用 deleted_at。
3. WmsLocationGenerator.generateLocations(warehouseId)：校验 location_generated=0 否则抛 WmsResultCode.LOCATION_ALREADY_GENERATED；要求已建默认 STANDARD 分区；按 §4.4.1 saveBatch 生成 location_code={rackNo}-{补零层}-{补零列}，完成置 location_generated=1；事务包裹。
4. 新表 Mapper 加 @InterceptorIgnore(tenantLine="true")。
前端：在现有 warehouse 页加结构参数表单 + 分区维护 + 【生成库位】按钮。
测试：2×3×4 生成 24 库位、编码正确、二次生成被拒。
完成后：mvn -q -pl admin test 通过。告诉我操作步骤，我对一个仓库验证库位自动生成与编码。
```

---

## C2 · 货架分配 + 二维平面预览

**目标**：超管把"某仓库某几排"分配给 WMS 服务商（含月租金/起止/合同），二维预览按排显示归属色块。

**后端**：`wms_rack_assignment`(+合同字段)；`RackVO` 聚合接口（《完整技术设计方案.md》§7.4）。

**前端**：货架分配页 + 二维预览页（按排色块、临期黄框、点击下钻库位）。

**测试**：同排同期重复分配被拒（`uk_rack_period`）；RackVO 状态(空闲/占用)正确。

**人工验证**：给 WMS 商分排1-3→预览看到排1-3 该商颜色、排4+ 灰色空闲。

```text
【任务】实现货架分配与仓库二维预览。依据《业务需求说明书.md》§1.1.2、§1.1.4，《完整技术设计方案.md》§7.4、附录A(A7)。
后端：
1. 迁移：建 wms_rack_assignment（按《设计方案.md》§4.1 + 附录A 合同字段 contract_file_url），唯一键 uk_rack_period(rack_no,warehouse_id,effective_from)。
2. 分配接口：校验所选排在时间段未被占用，否则抛冲突错误码；rack_no 取值必须来自该仓库已生成的 DISTINCT rack_no。
3. 仓库预览接口 GET /admin/warehouses/{id}/racks 返回 List<RackVO>{rackNo,assignedWmsTenantId,assignedWmsTenantName,status(IDLE/OCCUPIED),effectiveTo,locationCount,occupiedQty}，SQL 按 §7.4（distinct rack_no LEFT JOIN 当前有效 assignment LEFT JOIN sys_tenant，可选聚合 physical_inventory），Mapper @InterceptorIgnore。
前端：/admin/racks/assign（多选排+月租+起止+合同上传）；/admin/warehouses/preview（按排色块，同商同色、临期黄框、点击排下钻库位网格）。
测试：重复分配被拒；RackVO status 正确。
完成后：mvn test + 前端 lint 通过。给我步骤：分配排1-3给某WMS商，我在预览页核对色块与归属。
```

---

## D1 · 库存方案② + ERP 侧只读化

> 🟥 现状：`wms_inventory`(桶聚合+version)、`wms_stock_flow`(桶/方向/过账)、`wms_stocktake_order(_item)`(盘点) **均已存在**。本步：🆕 批次级 SSOT；🔧 现有库存降级聚合 + 流水加租户列；🔧 盘点对货主隐藏（决策 S"补建 stock_flow"作废）。

**目标**：新建批次级 SSOT 并把现有库存改为其聚合快照；ERP+OMS 侧库存只读、货主侧盘点/库存写下线。

**后端**：🆕 `wms_physical_inventory`(方案②，§二)；🔧 ALTER `wms_inventory`/`wms_stock_flow` 加 `wms_tenant_id/erp_tenant_id`；自有仓写路径改走 physical_inventory→同事务聚合刷新 `wms_inventory`+写 `wms_stock_flow`；货主库存查询同进程读 WMS。

**前端**：货主库存总览/明细/记录改纯只读；货主盘点/调整菜单解绑授权（§11.7，不软删菜单本体）。

**测试**：批次写→聚合数对得上；货主无任何写库存路径；只读接口按 `erp_tenant_id` 过滤。

**人工验证**：货主库存页只读无写按钮；盘点菜单对货主消失；超管/WMS 仍可盘点。

```text
【任务】落地库存方案② + ERP 侧只读化。先读《现状-设计差异对照表.md》§二、§3.2、§四，以及现有 Inventory/StockFlow/StocktakeOrder 实体与 InventoryController/StocktakeController。依据《完整技术设计方案.md》§4.4.2、§6.6、§11.7，铁律 C4。
后端：
1. 【新建】wms_physical_inventory：双ID(wms_tenant_id+erp_tenant_id)、warehouse_id、sku_code、inbound_item_id、inbound_date、pick_order、quantity、reserved_qty、quality(GOOD/DAMAGED)、location_code、zone_id、allocatable、version；uk_batch 含 location_code；idx_fifo_alloc。台账表【不加 deleted】，审计 create_by/create_time/update_by/update_time。
2. 【ALTER 现有，勿新建】wms_inventory 加 wms_tenant_id/erp_tenant_id 并降级为只读聚合；wms_stock_flow 加 wms_tenant_id/erp_tenant_id（决策 S 作废，不新建流水表）。
3. 自有仓库存写路径改造：所有写自有仓库存的地方改为写 wms_physical_inventory，再同事务按 §2.3 规则聚合刷新 wms_inventory(乐观锁 version) + 写 wms_stock_flow；FBO 同步路径(FboStockSyncController)不动。
4. 货主只读：ERP/OMS 库存总览/明细/记录改为同进程调 WMS 查询服务，按 erp_tenant_id 过滤；移除/@Deprecated 货主侧库存写接口。
5. 盘点对货主隐藏：DELETE sys_role_menu 解除 ROLE_TENANT_* 对 wms:stocktake:%/wms:adjustment:% 授权（菜单本体不删，超管/WMS 保留）。
测试：批次写后聚合 available/damaged/reserved 与明细一致；货主无写库存路径；只读按 erp_tenant_id 过滤。
前端：货主库存三页纯只读；货主盘点/调整菜单消失。
完成后：mvn -q -pl admin test + 前端 lint 通过。告诉我核对步骤：货主库存只读、盘点对货主消失但超管可用。
```

---

## D2 · 🔧 入库链路对接（采购 + 自定义）

> 🟥 入库执行**已存在**：`wms_purchase_inbound_order(_item)`(`inboundNo/shippingOrderId/orderStatus DRAFT-CONFIRMED-CANCELLED/stockPostingId`)，上游 `wms_purchase_order`+`wms_shipping_order`(头程物流)，确认入库走 `wms_stock_posting` 过账。本步**新建 OMS 下发层 `oms_inbound_order`**，下发后**对接驱动现有采购入库执行**完成收货上架——勿另造执行表。

**目标**：货主建入库单(采购/自定义)→Outbox 推送→驱动现有 WMS 收货→上架(产生批次写 physical_inventory)→回传，库存增加。

**后端**：🆕 `oms_inbound_order(_item)`(source_type 区分，决策 P)、`oms_sync_outbox`；🔗 对接 `wms_purchase_inbound_order`；🆕 `WmsRackingService.putaway`(§7.5，写批次)；OMS↔WMS 同进程回调。

**前端**：货主自定义入库单页(§2.3)；WMS 收货/上架（现有 `views/wms/purchase-inbound` 扩展扫描+选库位）。

**测试**：上架算法（同SKU并库位/新库位拆分）；状态机流转；收货上架后批次+聚合+流水一致。

**人工验证**：货主建自定义入库单→WMS 收货上架→货主库存页对应批次增加。

```text
【任务】打通入库链路：新建 OMS 入库下发层，对接现有采购入库执行。先读《现状-设计差异对照表.md》§3.3 与现有 PurchaseInboundOrder/PurchaseInboundController/前端 purchase-inbound、StockPosting 过账逻辑。依据《业务需求说明书.md》§1.2/§2.2/§2.3，《完整技术设计方案.md》§4.3/§6.4/§7.5/§十，决策 P。
后端：
1. 【新建】oms_inbound_order(source_type:PURCHASE/MANUAL/RETURN，purchase_order_id 可空，自定义跟踪号字段)、oms_inbound_order_item、oms_sync_outbox。软删除遵现有约定(deleted+@TableLogic)；Outbox 流水表不软删。
2. OMS：createInboundOrder（采购=PURCHASE关联 purchase_order_id；自定义=MANUAL且 purchase_order_id 为空，§4.3 口径）；业务+Outbox 同事务；定时扫 Outbox 同进程调 WMS（禁 HTTP/MQ，C3）。
3. WMS 对接现有：收货/上架复用并扩展现有 PurchaseInbound 流程——收货【标记到货】回调 onInboundReceiving；【确认收货】录实收数、无码SKU生码；上架 WmsRackingService.putaway 按 §7.5 找同SKU库位→容量判断→并入(同 inbound_item+location 累加)/新分配→每次写 wms_physical_inventory 批次→同事务聚合刷新 wms_inventory + 写 wms_stock_flow→全部上架后 onInboundCompleted 回传 OMS=COMPLETED。保留现有确认入库的 stock_posting 过账与新批次写入的一致性。
4. 新表 Mapper @InterceptorIgnore；状态机按附录B。
前端：货主 /oms/custom-inbound(§2.3)；在现有 purchase-inbound 页加收货/上架(扫描+选库位)。
测试：putaway 同SKU并库位/不足新库位拆分各一例；状态机流转；批次+聚合+流水一致。
完成后：mvn -q -pl admin test + 前端 lint 通过。给我端到端步骤：货主建自定义入库单→WMS收货上架→货主库存增加。
```

---

## E1 · 🔧 出库链路对接（销售 + 自定义）

> 🟥 出库执行**已存在**：`wms_sales_outbound_order(_item)` + `SalesOutboundController` + 前端 `sales-outbound`。本步**新建 OMS 下发层 `oms_outbound_order`**，对接现有销售出库执行做下架/拣货/发货；**不新建 `wms_outbound_order` 并行表**，在现有出库表上加租户+批次分配字段。（注意：`wms_shipping_order` 是头程入库物流，与客户出库无关，勿误用。）

**目标**：货主建出库单(销售/自定义)→下架(FIFO 防超卖、缺货整单挂起)→打包→签出(扣批次 SSOT)→回传，库存减少。

**后端**：🆕 `oms_outbound_order(_item)`(收件人字段，决策 P)；🔧 现有 `wms_sales_outbound_order` 加租户+批次分配；FIFO 分配(§4.4.2 单表行锁)；签出扣 physical_inventory→聚合刷新。

**前端**：货主自定义出库单页(§2.5)；WMS 下架/打包/签出（现有 `sales-outbound` 扩展，§1.3）。

**测试**：并发防超卖(30抢10)；缺货整单挂起 BR-01；签出后批次+聚合+流水一致。

**人工验证**：货主建自定义出库单→WMS 下架签出→库存减少；缺货时整单挂起。

```text
【任务】打通出库链路：新建 OMS 出库下发层，对接现有销售出库执行。先读《现状-设计差异对照表.md》§3.4 与现有 SalesOutboundOrder/SalesOutboundController/前端 sales-outbound。依据《业务需求说明书.md》§1.3/§2.4/§2.5，《完整技术设计方案.md》§4.3/§4.4.2/§6.5/§7.2，决策 P，铁律 C7、BR-01/02/05。
后端：
1. 【新建】oms_outbound_order(_item)：已规划 logistics_product_id/logistics_fee；补手工收件人 receiver_name/phone/address（自定义单非空校验，PLATFORM 可空）。软删 deleted+@TableLogic。
2. 【改造现有】wms_sales_outbound_order(_item) 加 wms_tenant_id/erp_tenant_id 与批次分配字段(allocated_qty/inbound_item 溯源)；不新建并行出库表。
3. OMS：createOutboundOrder（销售=PLATFORM关联erp_order_id；自定义=MANUAL且erp_order_id空）+Outbox+同进程下发。
4. WMS 下架：FIFO 按 §4.4.2（WHERE quality=GOOD AND allocatable=1 ORDER BY inbound_date,pick_order,id FOR UPDATE 单表行锁），reserved_qty+=take；任一SKU不足→整单 BACKORDER 回传(BR-01)不部分下架；成功 onOutboundPicking=PICKING（下架后不可取消）。
5. 打包 onOutboundPacked=PACKED；签出：扣 physical_inventory.quantity、清 reserved_qty、同事务聚合刷新 wms_inventory + 写 wms_stock_flow、onOutboundCompleted=SHIPPED→COMPLETED 回传 tracking_no。
测试：并发30抢10严格不超卖；缺货整单挂起；签出后批次/聚合/流水一致。
前端：货主 /oms/custom-outbound(§2.5)；在现有 sales-outbound 加下架/打包/签出(§1.3，三分拣先做集中分拣)。
完成后：mvn -q -pl admin test + 前端 lint 通过。给我端到端步骤：建自定义出库单→下架→签出→库存减少；并构造一次缺货看整单挂起。
```

---

## E2 · 🔧 退货质检扩展

> 🟥 退货质检**大体已实现**：`wms_return_inbound_order` 已有 `returnReason / qualifiedQuantity(合格) / unqualifiedQuantity(不合格) / toDamagedQuantity(转残品) / scrapQuantity(报废)` + `ReturnInboundController` + 前端 `return-inbound`。本步是**扩展**：补质检状态机三态、电子类拍照、回库写到 physical_inventory 批次+分区；**不要新建 ReturnOrderService 替代现有**。

**目标**：在现有退货入库上补质检三态(QC_PENDING/QC_PASS/QC_FAIL)；通过→良品(标准/退货区,可分配)、失败→次品(不良品区,不可分配,电子类强制拍照)，回库生成批次。

**后端**：🔧 扩展 `wms_return_inbound_order`(加 qc 状态/拍照，加双租户)；如需明细级质检，🆕 `wms_return_qc_item` 作为其**子表**(qc_status，决策 Q)；质检通过/失败写 `wms_physical_inventory` 批次 + 聚合刷新。

**前端**：现有 `return-inbound` 页加质检操作(通过/失败/拍照)；货主退货单/自定义退货单(§2.6)。

**测试**：质检状态机守卫；通过/失败回库 `quality`/`allocatable`/分区正确；电子类失败缺图被拒(BR-04)。

**人工验证**：退货→质检通过→良品可被出库；质检失败→次品不可被分配。

```text
【任务】扩展现有退货入库，补质检状态机与批次回库。先读《现状-设计差异对照表.md》§3.5 与现有 ReturnInboundOrder/ReturnInboundController/前端 return-inbound（已有 qualified/unqualified/toDamaged/scrap 字段）。依据《业务需求说明书.md》§1.4/§2.6，《完整技术设计方案.md》§4.4.2/§7.6，决策 Q，BR-04。
后端：
1. 【扩展现有】wms_return_inbound_order 加 qc_status(QC_PENDING/QC_PASS/QC_FAIL)、photo_url、wms_tenant_id/erp_tenant_id；保留并复用现有 qualified/unqualified/toDamaged/scrap 语义。如需 SKU 明细级质检，新建子表 wms_return_qc_item(关联退货单, qc_status, target_zone_type, photo_url)。代码一律用 qc_status(非 quality_status)。
2. 在现有退货 Service 上加质检状态机(非法跃迁抛错)：
   - 收货 RETURN_PENDING→QC_PENDING；startQualityCheck 进入质检；
   - passQualityCheck：quality=GOOD 入 STANDARD/RETURN 区 allocatable=1，写 wms_physical_inventory 新批次 + 聚合刷新 + stock_flow，qc_status=QC_PASS；
   - failQualityCheck：quality=DAMAGED 入 DEFECTIVE 区 allocatable=0，qc_status=QC_FAIL；电子类必须 photo_url 否则抛 BizException(BR-04)；
   - 各阶段同进程 onReturnStatus 回传 OMS。
前端：现有 return-inbound 页加质检(通过/失败/拍照)；货主 /oms/return、/oms/custom-return。
测试：状态机守卫；通过/失败的 quality+allocatable+分区正确；电子类失败缺图被拒。
完成后：mvn -q -pl admin test + 前端 lint 通过。给我步骤：退货→质检通过(良品可出库) 与 质检失败(次品不可分配) 两条路径验证。
```

---

## F1 · 计费链路一（平台 → WMS 服务商）

**目标**：费率卡 + 作业计费流水 + 货架租金 + 月度账单(DRAFT→CONFIRMED→PAID)，平台对 WMS 服务商出账。

**后端**：`wms_fee_rate_card`、`wms_tenant_rate_discount`(决策 L 折扣)、`wms_billing_record`(双字段+base_unit_price+discount_pct)、`wms_monthly_bill`；作业触发计费(入库/出库等)、`BillingService.generateForWmsTenant`(§8.2)。

**前端**：超管费率卡/折扣页、应收账单页(出账/确认/标记付款)。

**测试**：货架租金=Σ有效分配月租；折扣公式 `单价×(1+pct)×量`；计费幂等(同 biz_id 不重复)。

**人工验证**：配费率→产生几笔作业→月末出账→看到 WMS 商账单各项金额正确。

```text
【任务】实现计费链路一（平台→WMS服务商）。依据《设计方案.md》§4.4、§八，《完整技术设计方案.md》§8，决策 K/L/I。
后端：
1. 迁移：wms_fee_rate_card、wms_tenant_rate_discount(百分比折扣)、wms_billing_record(wms_tenant_id+erp_tenant_id+base_unit_price+discount_pct)、wms_monthly_bill；初始化合同费率(货架租金/入库m³/出库托·件/配送/退货/验货/司机 等)。
2. 作业计费：入库上架、出库签出等动作内同事务写 wms_billing_record（计价=COALESCE基础单价×(1+折扣)×量，记 base_unit_price+discount_pct），按 biz_id 幂等。
3. BillingService.generateForWmsTenant（§8.2）：月度按 wms_tenant_id 汇总各项 + 货架租金=Σ当月有效 wms_rack_assignment.monthly_fee，生成 wms_monthly_bill(DRAFT)；状态 DRAFT→CONFIRMED→PAID/DISPUTED（线下对账手动标记，决策K）。
前端：超管 /admin/fee-rate-cards、/admin/rate-discounts、/admin/billing/receivable（出账/确认/标记付款）。
测试：货架租金汇总、折扣公式、计费幂等。
完成后：mvn test + 前端 lint 通过。给我步骤：配费率→跑几笔入出库→触发出账→核对 WMS 商账单金额。
```

---

## F2 · 计费链路二（WMS 服务商 → 货主）

**目标**：WMS 商定义物流产品(组合定价+可见性+专属价)→货主下单选品锁价→签出按产品计费→对货主月度出账。

**后端**：`wms_logistics_product`(+visibility)、`wms_logistics_product_grant`(授权/专属价，决策 O-2)、`wms_client_billing_record`、`wms_client_monthly_bill`；签出时 `recordLogisticsFee`(§8.4.5 幂等)。

**前端**：WMS 商物流产品管理页；货主可用产品浏览 + 下单选品(挂到 E1 出库单)；货主物流费账单页 + WMS 商收入页。

**测试**：可见性(PUBLIC/RESTRICTED)+取价 `COALESCE(custom_price,price)`；下单锁价快照；签出计费幂等；下单前服务端二次校验可见性。

**人工验证**：WMS 商配产品(含给某货主专属价)→货主下单看到价并选→签出→货主物流费账单出现该笔。

```text
【任务】实现计费链路二（WMS服务商→货主）。依据《业务需求说明书.md》§3.2、§3.3，《设计方案.md》§8.4，《完整技术设计方案.md》，决策 O/O-2/N。
后端：
1. 迁移：wms_logistics_product(含 visibility:PUBLIC/RESTRICTED 与组合维度+定价)、wms_logistics_product_grant(custom_price/custom_currency)、wms_client_billing_record、wms_client_monthly_bill。
2. 货主可用产品查询：按 §8.4.4 SQL（PUBLIC 或有授权记录可见，价取 COALESCE(custom_price,price)）。
3. 下单(衔接 E1 出库单)：货主选 logistics_product_id→服务端二次校验该产品对本货主可见→把 effective_price/currency 快照写 oms_outbound_order.logistics_fee/logistics_currency。
4. 签出时 recordLogisticsFee（§8.4.5）：按出库单快照价生成 wms_client_billing_record，同 biz_id 幂等；月末按货主汇总 wms_client_monthly_bill。
5. 货主可见明细：决策 N（链路二物流费可见；链路一货架租金/司机费不透出）。
前端：WMS商 /wms/logistics-products（建/上下架/授权/专属价）、/wms/client-bills(收入)；货主 /oms/logistics-products(浏览)、出库单下单选品、/wms/logistics-bills(物流费账单)。
测试：可见性+取价、锁价快照、计费幂等、不可见产品提交被拒。
完成后：mvn test + 前端 lint 通过。给我步骤：配产品(含专属价)→货主选品下单→签出→货主物流费账单出现该笔，端到端验证。
```

---

## 五、里程碑（阶段性可演示）

| 完成到 | 能演示什么 |
|---|---|
| A3 | 多租户隔离：两个货主各看各的商品/订单 |
| B3 | 双层租户：超管→WMS商→货主 三级账号体系 |
| C2 | 仓库可视化：建仓库、分配货架、二维预览归属 |
| D2 | 入库闭环：货主下单→仓库收货上架→库存增加 |
| E2 | 出入库+退货全闭环：含 FIFO 防超卖、退货质检 |
| F2 | 双计费闭环：平台向 WMS 商、WMS 商向货主分别出账 |

> 建议每个里程碑打 tag（如 `milestone-A3`），便于演示与回退。

---

*本路线图与《设计方案.md》《业务需求说明书.md》《完整技术设计方案.md》配套；每步提示词已引用对应章节与决策编号，可直接粘贴执行。*
