# 订单/库存同步问题 · 修复方案（待审查，未改代码）

> 本文只给方案，等你审查通过后再实施。每条含:已核实根因 → 建议改法(具体到文件/方法)→ 风险与决策点 → 验证方式。
> 说明:E1(调拨撤回)已按你指示修复,附录记录其真实根因更正。E2–E5 为本次待审查方案。

---

## E2 — Ozon FBO 手动单店同步未切换店铺租户上下文

**根因(已核实)**
`wms/facade/OzonFboStockSyncFacade.java:109` — `syncByShopId()` 直接 `syncByShop(shop, MANUAL)`,无 `TenantContext.runAs`;而全量入口 `:86` 是 `runAs(shop.getTenantId(), () -> syncByShop(...))`。下游 `InventoryService.syncFboStock` 以 `TenantContext.getCurrentTenant()` 作为库存归属货主写 `wms_inventory`。

**影响**:手动同步用**触发者**的租户上下文写库 —— 平台身份(tenant ≤ 0)会触发 `getOrCreate` 断言失败;若为其他货主则把库存串写到错误货主。

**建议改法(最小、对齐全量入口)**
`OzonFboStockSyncFacade.syncByShopId` 把最后一行包进 `runAs`:
```java
public FboSyncResultVO syncByShopId(Long shopId) {
    Shop shop = shopService.getById(shopId);
    if (shop == null) throw new IllegalArgumentException("店铺不存在: " + shopId);
    if (!PLATFORM.equalsIgnoreCase(shop.getPlatform())) throw new IllegalArgumentException("仅支持 Ozon 店铺");
    // 多租户:按店铺所属货主建立上下文,确保 FBO 库存落到正确货主
    return TenantContext.runAs(shop.getTenantId(), () -> syncByShop(shop, FboSyncType.MANUAL.getCode()));
}
```
- 文件:`wms/facade/OzonFboStockSyncFacade.java`(1 处)
- 风险:极低。与全量入口口径一致;`shop.getTenantId()` 已是店铺真实货主。

**验证**:以平台身份手动触发某店 FBO 同步 → 不再断言失败;`wms_inventory` 落库的 `erp_tenant_id` = 店铺所属货主。

---

## E3 — Ozon 订单手工按店同步未切换租户上下文

**根因(已核实)**
`order/controller/OzonOrderController.java:145` — `syncShop(shopId)` 直接调 `ozonOrderSyncService.syncShopPostings(shopId, since, to)`,controller 内无 `runAs`。定时链有:`syncAllEnabledShops`(`:93 runAs`)→ `syncShopOrders`(`:109`)→ `syncShopPostings`(`:122`)。`erp_order` 在租户白名单,插入时按当前上下文注入 `tenant_id`。

**影响**:手工同步时新单落到**触发者**租户而非店铺真实租户;无上下文时租户过滤失效 → 跨租户串单。

**建议改法(推荐:让 `syncShopPostings` 自洽,一次修复所有手工/直接调用方)**
在 `OzonOrderSyncService.syncShopPostings` 方法体最外层加载店铺并 `runAs`(服务已注入 `shopService`):
```java
public void syncShopPostings(Long shopId, LocalDateTime since, LocalDateTime to) {
    Shop shop = shopService.getById(shopId);
    Assert.notNull(shop, "店铺不存在: " + shopId);
    TenantContext.runAs(shop.getTenantId(), () -> {
        doSyncShopPostings(shopId, since, to);   // 原方法体抽成私有 doSyncShopPostings
        return null;
    });
}
```
- 定时链已在 `runAs(同一货主)` 内,此处为**同租户嵌套 runAs**,`TenantContext` 保存/恢复语义下安全无副作用。
- 覆盖同样问题的 `:230`(未完结状态同步里调 `syncShopPostings`)与任何未来直接调用方。
- 文件:`order/service/ozon/OzonOrderSyncService.java`(抽方法 + 包 runAs)。

**备选(更小但只堵一个口)**:仅在 `OzonOrderController.syncShop` 里 `runAs`。缺点:其它直接调 `syncShopPostings` 的入口仍裸奔。**不推荐**。

**决策点**:确认手工同步的权限边界 —— 谁能点"按店同步"?若平台超管可点,`runAs(店铺货主)` 是正确归属;需确保 controller 有权限校验(避免越权同步他人店铺)。

**验证**:手工触发某店同步 → 新 `erp_order.tenant_id` = 店铺货主;换一个货主的店铺同步,数据互不串。

---

## E4 — Ozon 订单 Upsert 查旧单不带 shopId(与 WB/Yandex 不一致)

**根因(已核实)**
`order/service/ozon/OzonOrderUpsertService.java:52-54` 只按 `platform=Ozon + platformOrderId` 查旧单,随后 `:73 setShopId(shopId)`。而 `WbOrderUpsertService.java:50`、`YdOrderUpsertService.java:77` 均用现成的 `ErpOrderMapper.selectOneByShopPlatformPlatformOrderId(shopId, platform, platformOrderId)`(含 shop)。

**影响**:同租户多 Ozon 店出现同号 posting 时,旧单查询会命中**别的店**的订单并把其 `shopId` 改写(误"迁店");叠加 E3(无租户上下文)时更易跨租户命中/串写。

**建议改法(与 WB/Yandex 对齐,复用现有方法)**
```java
ErpOrder existing = orderMapper.selectOneByShopPlatformPlatformOrderId(
        shopId, PlatformEnum.Ozon.code(), dto.getPostingNumber());
```
- 文件:`order/service/ozon/OzonOrderUpsertService.java`(1 处查询)。
- 风险:低,方法已被 WB/Yandex 使用、行为成熟。

**决策点(需你拍板)**:当前"按 posting 号命中即改 shopId"是否为**有意的迁店功能**?
- 若**不是**(判断:Ozon posting 号在店内唯一、不应跨店迁移)→ 直接加 shopId,推荐。
- 若**是**(极少见)→ 保留但需显式的迁店逻辑与租户校验,另议。
我的判断:posting 号是店铺维度的,跨店同号应视为两张单,应加 shopId。

**验证**:构造同租户两店同号 posting,分别同步 → 两条独立 `erp_order`,`shopId` 各自正确,不再互相覆盖。

---

## E5 — 增量同步失败仍推进游标,漏单不可恢复(WB / Ozon / Yandex 三平台)

**根因(已核实)**
- WB `WbOrderSyncService.java:194-206` 平台/普通异常 `break` 分页循环 → `:210/213-214` **无条件** `advanceCursor(segmentEnd)`。失败窗口剩余页被永久跳过。
- Ozon `OzonOrderSyncService.java:352-360` 单单 upsert 失败记日志跳过 → `:270-271` 段结束**无条件**推进游标。被跳过的订单永久漏入库。
- Yandex `YdOrderSyncService.java:148-151` 单单失败跳过 → `:157-158` 推进游标。同构。

**影响**:任何瞬时故障(限流/网络/上游 5xx/单单映射异常/DB 抖动)所在的时间窗被标记"已同步",其中未成功的订单不再重试 → **静默漏单,不可恢复**。此条数据丢失影响最大。

**建议改法** —— 分两类,建议**先做最小正确性修复,再视需要上健壮版**:

**(1) 抓取/分页失败(整窗失败) → 不推进游标,终止本轮**
把"是否整窗成功"作为推进前提。WB 为例:
```java
boolean segmentOk = true;
try {
    // ...分页拉取...
} catch (PlatformApiException e) {
    log.warn(...); segmentOk = false;
} catch (Exception e) {
    log.error(...); segmentOk = false;
}
if (!segmentOk) {
    log.warn("[WB] 段 {}~{} 拉取失败,保留游标,下轮重试", cursor, segmentEnd);
    break;                     // 不推进游标,退出段循环;下轮从原 cursor 续跑
}
cursor = segmentEnd.plusSeconds(1);
syncCursorService.advanceCursor(shop.getId(), ..., segmentEnd);
```
- 因 upsert 幂等(按 shop+platform+orderId),重拉整窗不会造成重复单。
- Ozon `syncShopPostings` 的 90 天分段循环同理:段内 FBS/FBO 拉取失败则不推进该段游标、`break`。

**(2) 单订单 upsert 失败 → 不静默丢,二选一:**
- **方案 A(最小,推荐先上)**:段内只要有**任一**单失败,则该段**不推进游标**(记 `hasFailure`,失败即 `break` 外层、保留游标),下轮重跑整段。
  - 优点:零新表、实现小、不漏单。
  - 缺点:**队头阻塞** —— 一张"永久坏单"(如数据非法)会卡住该店游标。需配套:失败告警 + 人工介入/或对确定性坏单做显式跳过白名单。
- **方案 B(健壮,建议作为后续)**:引入**失败订单死信表**(shopId, platform, platformOrderId, 原始报文, 失败原因, 重试次数);单单失败落死信、游标照常推进;独立重试任务定期重放死信,超阈值告警。
  - 优点:不阻塞游标、不漏单、可观测。
  - 缺点:需新增表 + 重试任务 + 幂等重放,工作量较大。

**推荐落地顺序**:先做 (1) + (2)方案A(纯逻辑改动、无 schema),把"漏单"堵死;死信表(2B)列为后续增强。

- 文件:`WbOrderSyncService.java`、`OzonOrderSyncService.java`(`syncShopPostings`/`syncShopFbsPostings`/`syncShopFboPostings`)、`YdOrderSyncService.java`。
- 风险:中。改动同步主循环,需回归"正常全量/增量、限流重试、封禁中断"路径;务必确认 upsert 幂等(现状:按 shop+platform+orderId 命中即更新,幂等成立;E4 修复后 Ozon 亦成立)。

**决策点**:队头阻塞(方案A)能否接受?若你的业务对"个别坏单卡住整店"零容忍,则直接上死信表(方案B)。

**验证**:
- 注入一次拉取异常(mock 限流)→ 游标不推进,下轮从原点重拉,订单不丢。
- 注入单单 upsert 异常 → 方案A:该段游标不进、下轮补齐;方案B:落死信 + 重试任务补齐。

---

## 汇总:改动面 / 风险 / 建议顺序

| 编号 | 严重级别 | 改动文件数 | 需重构建 | 风险 | 建议顺序 |
|---|---|---|---|---|---|
| E5 | 高(静默漏单) | 3 | 是 | 中 | ① 先修(1)+(2A) |
| E3 | 高(串租户) | 1 | 是 | 低 | ② |
| E2 | 中–高(串/失败) | 1 | 是 | 低 | ③ |
| E4 | 中(同租户串店) | 1 | 是 | 低 | ④ |

> 四条都是后端 Java 改动,需 `mvn package` 重构建 + 重启。E2/E3/E4 是"补上下文/加条件"的小手术;E5 是同步主循环的逻辑调整,需重点回归。

---

## 附:E1 修复记录 + 审计原结论更正

**E1(调拨撤回失败)已修复。** 但原审计对根因的判断**有误**,更正如下:
- 原审计说"revoke 过账明细漏传 erpTenantId 导致断言失败" —— **不成立**:`TransferOrderFacade.revoke()` 第 189 行 `.erpTenantId(item.getErpTenantId())`、`postInTransit` 头(228 行)均已传。
- **真实根因**:撤回用 `putaway`(无条件 INSERT 新批次)恢复源库存,与仍存在的源批次行撞 `uk_batch(wms_tenant_id, erp_tenant_id, warehouse_id, sku_code, inbound_item_id, location_code)` 唯一键 → `DuplicateKeyException`,即前端"SKU编码已经存在"。
- **已实施修复**:新增 `WmsPhysicalInventoryService.restoreForTransferRevoke(batchId, qty, sourceNo)`(对原批次行加回数量 + 刷新快照 + 写 `TRANSFER_CANCEL` 入库流水),`revoke()` 改用它替代 `putaway`。

> 提醒:此前 5 个子代理产出的**库存流程审计**(头号 Critical:`wms_inventory` 存在"过账增量"与"批次全量重算覆盖 refreshSnapshot"两个互斥写者,导致超卖/预占丢失等)尚未生成解决方案。那属于账本架构级问题,方案更重,建议单独出一份《库存账本一致性重构方案》。需要我接着写,告诉我即可。
