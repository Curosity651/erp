# 海外仓平台 · 海外仓作业 · 打包签出 —— 设计与交接文档

> 面向接手的 AI/开发者（尤其是**后端**）。本页前端已完成，以 mock（含内存态）驱动。
> 说明：这页是什么、展示什么、前端怎么搭的、**后端需要实现的接口契约**、以及如何切到真实数据。
> 截至 2026-07-01。

---

## 0. 背景与定位

**归属身份 = 海外仓平台 `OVERSEAS_PLATFORM`** → 海外仓作业(ops) → **打包签出**（menu id **170504**，path **`packing`**，parent ops 170500）。当前 DB `sys_menu.uri` 仍为占位组件 `common/Placeholder`。用户角色 = WMS 配货员/打包员/签出员。

- **接续下架(拣货)之后**：`PICKING 拣货中 → 打包 → PACKED 已打包 → 签出 → SHIPPED 已发货 → COMPLETED`。
- **签出时才真正扣减物理库存 + 释放 reserved_qty**（下架只是锁定），并生成**链路二物流费**（`wms_client_billing_record`）。
- **后端现状**：出库执行 controller **尚未实现**。→ 前端 mock 先行，后端按第 3 节契约实现。

> 出库链三页：[下架拣货](下架拣货-设计与交接.md) → 打包签出（本页）。与下架页同目录 `src/views/platform/outbound-ops/`。

---

## 1. 业务口径（业务需求 1.3.2）

**状态机**
```
PICKING(拣货中)
   │ 打包：按SKU/按单/二次分拣/订单装箱
   │   配货核验 → 打印物流标签 → 装袋/装箱贴标
   ▼
PACKING → PACKED(已打包)
   │ 订单签出：扫面单跟踪号 → 称重 →【订单签出】
   │   物理库存正式扣减(quantity -= 实发；释放 reserved_qty)
   │   生成链路二物流费流水(按所选物流产品计价)
   ▼
SHIPPED(已发货) ──同步──► OMS=SHIPPED → ERP 订单状态更新；回传 tracking_no
   ▼
COMPLETED(完成)
```

**关键业务规则**
- **库存扣减时机**：签出（SHIPPED）时才真正 `quantity -=` 并清对应 `reserved_qty`（下架时只锁定）。
- **次品+电子类强制拍照（BR-04）**：涉及不良品出库或电子类，签出环节强制上传照片。
- **物流费计费（链路二，C6）**：签出成功且出库单关联 `logistics_product_id` 时，生成 `wms_client_billing_record`（幂等：同 `biz_id` 不重复计费）。
- 签出支持按渠道签出或自动选择渠道；称重数据写回用于体积重核算。

**依赖数据表**：`wms_outbound_order`、`wms_physical_inventory`、`wms_client_billing_record`、`oms_outbound_order`、`erp_order`。

---

## 2. 页面展示（v1）

参照下架/入库作业范式：搜索栏 + ProTable + 作业弹窗。

### 搜索栏
- 出库单号 / 状态（拣货中(待打包)/已打包(待签出)/已发货）/ 货主（`ErpTenantSelect`）

### 列表（ProTable）
| 出库单号 | 货主 | 仓库 | SKU/件数 | 物流产品 | 状态徽标 | 跟踪号 | 创建时间 | 操作 |

- **操作列跟状态**：`PICKING`→[打包]；`PACKED`→[签出]；`SHIPPED`→只读（列表展示跟踪号 tag + 渠道）。

### 打包弹窗（PackModal）
- 配货明细：SKU / 数量 / 品质(良/次 tag)。
- **打包模式（四选一）**：按SKU打包 / 按单打包 / 二次分拣 / 订单装箱（带场景说明）+ 打包员（选填）。
- 确认 → 打印物流标签 → 转 `PACKED`。

### 签出弹窗（ShipModal）
- **物流渠道**（下拉，含"自动选择渠道"）+ **面单跟踪号**（扫/录入）+ **称重(kg)**。
- **次品/电子类（needPhoto）→ 出现签出照片上传，必传**（BR-04），否则拦截。
- 确认 → 物理库存扣减 + 释放锁定；关联物流产品的**回显生成的物流费（₽）** → 转 `SHIPPED`。

---

## 3. 后端需实现的接口契约 ⭐

前端 mock 驱动。后端实现下列接口并对齐 VO 后，把 `src/api/wms/outbound-shipping/index.ts` 的 `USE_MOCK` 改 `false` 即接通，**前端零改动**。

**接口前缀建议**：`/wms/outbound-shipping`

| 方法 & 路径 | 作用 | 入参 | 返回 |
|---|---|---|---|
| `GET /page` | 分页 拣货中/已打包/已发货 订单 | query=`PageParam`(size,page,sort)+`PackShipQO` | `PageResult<PackShipOrderVO>`（列表不带 items） |
| `GET /{id}` | 订单详情（含 items） | — | `PackShipOrderVO` |
| `POST /pack` | 打包 PICKING→PACKED（打印标签） | body=`PackDTO` | void |
| `POST /ship` | 签出 PACKED→SHIPPED | body=`ShipDTO` | `ShipResultVO` |
| `GET /channels` | 物流渠道选项 | — | `LogisticsChannelVO[]` |

> 统一包 `ApiResult<T>`（`{ code, data, message }`，成功 `code=200`）。
> `OutboundStatus` 复用 `@/api/wms/outbound-picking/types`（下架页定义）。

### VO / DTO（权威在 `src/api/wms/outbound-shipping/types.ts`）

```ts
type OutboundStatus = 'PENDING'|'PICKING'|'BACKORDER'|'PACKED'|'SHIPPED'|'COMPLETED'
type PackMode = 'BY_SKU'|'BY_ORDER'|'SECONDARY'|'CARTON'  // 按SKU/按单/二次分拣/订单装箱

PackShipOrderVO {
  id: number
  outboundNo: string
  erpTenantId: number
  ownerName: string
  warehouseName: string
  skuKinds: number
  totalQty: number
  status: OutboundStatus         // 主要 PICKING/PACKED/SHIPPED/COMPLETED
  pickerName?: string
  packMode?: PackMode            // 打包后回填
  packerName?: string
  logisticsProductName?: string  // 关联物流产品(链路二计费依据)
  needPhoto: boolean             // 次品/电子类 → 签出强制拍照(BR-04)
  channelName?: string           // 签出后回填
  trackingNo?: string
  weight?: number                // kg
  shippingFee?: number           // 链路二物流费
  createTime: string
  items?: PackShipItemVO[]        // 详情才带
}

PackShipItemVO { skuCode: string; skuName?: string; qty: number; quality: 'GOOD'|'DAMAGED' }

PackShipQO { outboundNo?: string; status?: OutboundStatus; erpTenantId?: number }

PackDTO { outboundOrderId: number; packMode: PackMode; packerName?: string }

ShipDTO {
  outboundOrderId: number
  channel: string        // 渠道代码；'AUTO'=自动选择渠道
  trackingNo: string
  weight: number
  photoCount?: number    // needPhoto 时必传(真实为文件 id 列表)
}

ShipResultVO { trackingNo: string; channelName: string; shippingFee: number; billingRecordId?: number }

LogisticsChannelVO { code: string; name: string }
```

### 后端实现提示
- **平台看全部货主**；`erpTenantId`/`status` 非空时过滤。
- `/pack`：仅 `PICKING` 可打包；写 `packMode`/`packerName`；打印标签；转 `PACKED`。
- `/ship`：仅 `PACKED` 可签出；**幂等 + 前置校验**；
  - `needPhoto` 为真且无照片 → 报错（BR-04）；
  - **扣减物理库存 `quantity -= 实发` 并释放对应 `reserved_qty`**（此刻才动库存）；
  - 关联 `logistics_product_id` → 生成 `wms_client_billing_record`（幂等，同 `biz_id` 不重复），`shippingFee` 回传；
  - 同步 OMS=SHIPPED，回传 `tracking_no`；称重写回体积重核算。
- **口径以《业务需求说明书》1.3.2 与《底层数据表重构设计.md》为准**。

---

## 4. 前端文件清单（已完成）

**API 层** `src/api/wms/outbound-shipping/`
- `types.ts` / `mock.ts`（mock+内存态：打包/签出流转、物流费生成、渠道）/ `index.ts`（5 接口 + `USE_MOCK` + 接口清单注释）。

**页面 + 组件** `src/views/platform/outbound-ops/`（与下架页同目录）
- `PackShipPage.vue`（主页）/ `PackModal.vue`（打包）/ `ShipModal.vue`（签出，含条件拍照）/ `packship-constants.ts`（打包模式·状态·币种·金额格式化）。

**路由** `src/router/dynamic-routes.ts`
- `COMPONENT_OVERRIDES['packing']` → 本页，绕过占位组件 `common/Placeholder`，**不改数据库**。

**校验**：`npx vue-tsc --noEmit` 通过；`npx eslint` 无 error/warning。前端走 Vite HMR。以 `super_admin`（海外仓平台入口）登录 →「海外仓作业 → 打包签出」查看。

---

## 5. 待确认 / 可迭代
- **照片存储**：mock 仅记录张数（`photoCount`）；后端接文件上传服务，DTO 改为文件 id 列表。
- **币种**：物流费展示暂用 `₽`（`packship-constants.ts` 的 `CURRENCY_SYMBOL`），与应收账单一致；如实际不同改此常量。
- **拣货完成确认**：PICKING→打包 是否需要"拣货员标记已拣完"前置动作，见下架页交接第 5 节；当前直接从 PICKING 进打包。
- **面单打印**：当前打包仅提示"已打印物流标签"，真实打印对接 v2。

---

## 6. 一句话交接
> 前端页面 `src/views/platform/outbound-ops/`（PackShipPage/PackModal/ShipModal）已完成并用 mock（含内存态）跑通；后端实现 `/wms/outbound-shipping/*`（5 接口，契约见第 3 节 / `types.ts`）后，把 `src/api/wms/outbound-shipping/index.ts` 的 `USE_MOCK` 改 `false` 即上线，前端不用动。**签出才扣库存+释放锁定、次品/电子类强制拍照、关联物流产品生成链路二物流费(幂等) 务必在后端实现。**
