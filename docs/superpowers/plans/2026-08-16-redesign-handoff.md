# ERP/WMS 简化改造交接记录

## 仓库与分支

- 仓库：`C:\Users\86178\Desktop\hyldsys-erp-master(1)\hyldsys-erp-master`
- 分支：`codex/logical-location-fulfillment`
- 工作区包含用户此前留下的大量未提交改动，不得整体暂存、回滚或清理。
- 每次提交只能显式暂存本任务涉及的文件，禁止使用 `git add .`。

## 已完成提交

1. `ca1ad01`：WMS 新旧内核切换开关。
2. `ebb0850`：逻辑库位库存与预占账本数据库结构。
3. `6aefa26`：动态排与逻辑库位管理。
4. `04ac62b`：库位容量计算与装箱推荐。
5. `32c034b`：逻辑库位库存事务服务。
6. `dfc8a8d`：SKU 外箱尺寸与单箱毛重必填。
7. `1aa029f`：库位库存网格、树形和详情视图。

## 当前完成情况

- 正在完成计划任务 8：取消托盘上架，改为“SKU → 逻辑库位”的人工分配。
- 已新增逻辑上架计划与执行接口：
  - `GET /wms/inbound-execution/logical-putaway-plan`
  - `POST /wms/inbound-execution/logical-putaway`
- 上架数量必须与实收数量完全一致。
- 同 SKU、同品质、同库位的重复行在服务端合并。
- 推荐库位受服务商货架范围及公共暂存区规则约束。
- 良品不能进入不良品区；不良品只能进入不良品区。
- 体积超过推荐允许人工覆盖，但必须填写原因；承重和 SKU 种类限制不能覆盖。
- 新前端按 SKU 展示尺寸、重量和图片，支持拆分到多个库位，不再选择托盘或托位。
- 上架完成只生成和打印“上架单”，不再打印托盘标签。

## 已验证

- 后端完整打包：`mvn -pl admin -am -DskipTests package` 通过。
- 逻辑上架纯逻辑测试 4/4 通过。
- 前端：`pnpm type-check` 通过。

## 紧接着要做

1. 检查并提交任务 8 的精确文件范围。
2. 为入库收货增加现场照片凭证并做前后端校验。
3. 将库位调整、盘点、退货质检、报废、出库预占逐步切换到逻辑库位库存内核。
4. 按已确认方案取消销售出库单聚合主流程，改成订单级待下架、拣货、面单和签出。
5. 清理仓库业务数据后重建逻辑库存账，并切换 `wms.core-mode`。
6. 完成整库编译、数据库迁移、业务回归、Git 提交和前后端启动。

## 重要业务约束

- 商品不再使用批次、托盘、托位。
- 每箱必须贴内部 SKU 标签。
- 上架和移库直接选择逻辑库位。
- 已被销售订单预占的数量禁止人工移库，只能移动可用数量。
- 暂存区允许共同使用并允许直接参与拣货。
- 一个自定义出库申请只能对应一个物理包裹。
- ERP 订单确认后进入内部“待下架”，海外仓下架确认后再同步电商平台。
