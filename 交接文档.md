# 交接文档 —— 海外仓 OMS+WMS 多租户改造（截至 2026-07-01）

> 面向接手的 AI/开发者。本文件是「本轮会话做了什么 + 如何继续」的索引。
> **权威设计**见项目根 `底层数据表重构设计.md`；本文件侧重当前状态、操作方式、已交付功能、遗留项。

---

## 0. 工程与操作

- **项目根**：`C:\Users\86178\Desktop\hyldsys-erp-master(1)\hyldsys-erp-master`
- **后端** `erp-backend`：Spring Boot 2.7.18 + BallCat 2.0.0-SNAPSHOT + Java 8 + MyBatis-Plus + MySQL；主模块 `admin`。
- **前端** `erp-frontend`：Vue3 + Vite + TypeScript + ant-design-vue；别名 `@`=`src`，`#`=`pro-components`（ProTable 在 `#/table`）。
- **数据库**：MySQL `127.0.0.1:3307`，库 `erp`，user `root` / pw `123456`。

### 构建 / 运行（Git Bash）
```bash
# 后端编译/测试（务必跑 test，别只 compile——增量 compile 会缓存掩盖缺 import 的报错）
cd erp-backend && export JAVA_HOME=/c/tools/jdk8u492-b09 && \
  /c/tools/apache-maven-3.9.16/bin/mvn -pl admin -am test -o

# 打包（先停 8081，否则 jar 被占用 clean 失败）
/c/tools/apache-maven-3.9.16/bin/mvn -pl admin -am clean package -o -q -DskipTests

# 运行（必须用 java 全路径，直接 `java` 会 exit 127）
/c/tools/jdk8u492-b09/bin/java.exe -jar admin/target/admin-2.0.0-SNAPSHOT.jar   # 端口 8081

# 停 8081（PowerShell）
Get-NetTCPConnection -LocalPort 8081 -State Listen | %{ Stop-Process -Id $_.OwningProcess -Force }
```

### 前端校验
```bash
cd erp-frontend
npx vue-tsc --noEmit                                   # 类型检查
npx eslint --fix src/views/wms/location-mgmt/xxx.vue   # 只 fix 格式（warning 可接受，error 必须清）
```
前端不需手动构建部署：改动走用户侧 Vite dev/HMR 生效。**只有后端改动才需重新打包+重启 8081。**

### 数据库迁移
```bash
mysql --default-character-set=utf8mb4 -h127.0.0.1 -P3307 -uroot -p123456 erp < erp-backend/sql/migration/Vxx__*.sql
```
- 已应用：**V22**（基础字段/双维列/删 bak）、**V23**（删 sys_user_tenant）、**V24**（删 rack_layers/layer_no，库位二维化）、**V25**（wms_inventory_config 唯一键改 uk_tenant_region_sku）。
- **下一个迁移版本号 = V26。**
- 约定：软删=`@TableLogic private Long deleted`；台账/库存/流水表**无 deleted**；审计=create_by/create_time/update_by/update_time。DB 已备份，勿再备份。

---

## 1. 架构口径（务必遵守）

**三个隔离平台（单体多租户+多门户）**，不是权限高低而是三个独立后台：
- **海外仓平台** `OVERSEAS_PLATFORM`：单实例，`sys_tenant.id = -1`（即 BLOCK 哨兵）。物理仓库运营方。
- **WMS 服务商** `WMS_OPERATOR`：多租户，租货架给货主。
- **货主** `ERP_USER`：多租户，货物主人。`sys_tenant.parent_wms_tenant_id` → 其父服务商（1 货主→1 父服务商）。

> 术语纪律：**不要用「平台超管」**。第三身份=海外仓平台，非权限超管。

**三层隔离 Tier**：
- **G 全局**：无租户列（字典/菜单等）。
- **T 单租户**：`tenant_id` + `ErpTenantLineHandler`（MyBatis 拦截器，SQL 级自动填/过滤；无上下文的后台任务跳过注入）。白名单含 sys_role/sys_user/sys_organization/sys_file/export_data/三日志/product/order/wb 等。
- **W 货主货物**：`erp_tenant_id`（+`wms_tenant_id`）+ `WmsOwnerDataPermissionHandler`（DataPermission 拦截器，SELECT/UPDATE/DELETE 注入 erp_tenant_id 范围）。平台看全部，货主看自己，服务商看名下货主（只读）。

**关键类**：
- `TenantContext`（ThreadLocal，`BLOCK_TENANT_ID=-1L`，`runAs(tenantId, Supplier)`）；`WmsTenantContext`（服务商维）。
- `ErpOwnerScopeService.readScope()`：返回 `null`=平台看全部；`[自身]`=货主；`[名下货主]` 或空时 `[-999]`（失败闭合）=服务商。写归属 `writeOwner()`；可见校验 `canAccess(ownerErpTenantId)`。
- 两个正交货物维：`erp_tenant_id`=货主（货物主人），`wms_tenant_id`=服务商（货架承租方）。

---

## 2. 本轮会话已交付（按功能）

### 2.1 P5/P7 核心 —— 区域库存 + 过账按货主（财务）
- DTO/实体 `StockPostingItemDTO`/`StockPostingDTO`/`StockPostingItem`/`StockPosting`/`RegionInventory` 全加 `erpTenantId`(+`wmsTenantId`，wms 维多留空)。
- `RegionInventoryMapper.selectByTenantRegionSku(erp,region,sku)`；`RegionInventoryService.getOrCreate/getReservedQuantity` 强制传 erp（null 抛错）。
- `InventoryPostingEngine`：区域分组键 `(erp,region,sku)`（public static `regionGroupKey`），flow 填 erp/wms。
- `StockPostingService.post`：明细/头盖 erp/wms；`buildMergeKey`（public static）+ 冲突校验键含 erp/wms。
- facade 按单据货主填：`SalesOutboundFacade`、`ShippingOrderFacade`、`WmsInboundExecutionService`（取物流单货主冲减同一区域库存行）。
- `wms_region_inventory` 纳入 `WmsOwnerDataPermissionHandler.OWNER_TABLES`。
- 单测 `admin/src/test/.../wms/StockPostingTenantIsolationTest`。

### 2.2 P5+ A —— 库存配置按货主
- V25 把 `wms_inventory_config` 唯一键 `uk_region_sku` → `uk_tenant_region_sku(erp_tenant_id,region_id,sku_code)`（原来第二个货主无法对同 region/sku 存配置）。
- `InventoryConfigMapper.selectByRegionAndSku` 改 selectList+findFirst 防 selectOne 抛错。读取本就在 OWNER_TABLES 按货主过滤。

### 2.3 P5+ B —— 仓库级库存按货主（财务核心）
- `wms_inventory` 唯一键是 4 键 `(wms_tenant_id, erp_tenant_id, warehouse_id, sku_code)`（聚合器 `WmsInventoryAggregator` 按此键建行）。
- **修复**：`InventoryService.getOrCreate` 原 2 键盲查会 selectOne 抛 TooManyResults / 跨货主混算。已改签名 `getOrCreate(erpTenantId, warehouseId, skuCode)` → `InventoryMapper.selectByErpWarehouseSku`（忽略 wms 维命中聚合器同行，建行 wms=0，Assert erp 非空）。
- `InventoryPostingEngine.processWarehouseItems` 按 `(erp,仓,sku)` 分组（`warehouseGroupKey`）。
- 所有仓库级读取改 erp 精确版：`getByErpWarehouseAndSku`/`getStockMapByErpWarehouseAndSkuCodes`；`getDetail` 按 currentErpScope 精确取。
- **5 条流程盖货主**：销售出库、退货入库（取单据货主）；**盘点**（全量盘点从库存行自动带 erp，追加 SKU 用 `StocktakeAddSkuDTO.erpTenantId`）；**调整 / 调拨**（**单头选一个货主**：`AdjustmentDTO`/`TransferOrderDTO` 加 `erpTenantId`，`batchSave` 盖到各明细）；FBO 同步取上下文货主。
- 前端：新增 `@/components/Lov/ErpTenantSelect.vue`（数据源 `listErpTenants`=当前服务商名下货主）；调整/调拨表单头 + 盘点追加 SKU 选货主。
- **遗留小 gap**：编辑草稿态调整/调拨单时货主需重选（后端 detail VO 未回填 erpTenantId）。

### 2.4 P5+ C —— 台账读端点按货主收窄
- `StockPostingQO`/`StockPostingItemQO`/`StockFlowQO` 加隐藏 `erpTenantIds`。
- `StockPostingQueryService`(queryPage/queryItemPage)、`StockFlowService`(queryPage) 注入 `ErpOwnerScopeService`，查询前 `qo.setErpTenantIds(readScope())`；三 mapper queryPage Wrapper 加 `.inIfPresent(getErpTenantId, erpTenantIds)`；`getDetail` 加 `canAccess(head.erpTenantId)`。
- 调整/调拨 confirm 的过账头补 `erpTenantId=items.get(0).getErpTenantId()`（供 getDetail 认领）。
- **重要**：没有把台账表加进全局 handler —— 那会波及过账幂等查询 `StockPostingService.selectBySourceAndType`，导致同单据重复过账的财务风险。
- 起因：货主有「仓储 160000」菜单→`wms:stock-flow:read`(161101)，原本可看到所有货主的过账/流水台账。

### 2.5 库位管理 二维化 + 页面重建
- **二维化**：去掉「层」。V24 删 `wms_warehouse.rack_layers` + `wms_location.layer_no`。库位编码由 `A1-03-03` 改为 `A1-03`（排号-列号）。`WmsLocationGenerator` 二维笛卡尔积；`buildLocationCode(prefix,row,column,pad)`、`countLocations(rows,columns)`。
- **允许重新生成**：`generateLocations` 已生成时先查 `WmsPhysicalInventoryMapper.countByWarehouse`（有货物落位则抛 `LOCATION_OCCUPIED 40013`），无占用则 `deleteByWarehouse` 清旧重建。
- **前端三段式**（`erp-frontend/guidelines/页面布局规范.md`）：`views/wms/location-mgmt/index.vue`=搜索栏 `LocationMgmtSearch.vue` + `ProTable` 仓库列表（`listStructureWarehouses` 前端筛选分页）+ `WarehouseLocationDrawer.vue`（单仓抽屉）。**列表页只对已有仓库设计库位，无"新建仓库"**。

### 2.6 货架分配 布局一致化
- `views/platform/rack-assignment/index.vue` 重构为同款三段式（**复用** `LocationMgmtSearch`）+ 新建 `RackAssignmentDrawer.vue`（2D 排色块网格 + 分配弹窗 + 点排下钻/解除，逻辑从旧 index 迁入）。后端 rack API 未动。

### 2.8 服务商「货主管理」三段式（与 platform/wms-operators 对齐）
- `views/wms/my-clients/` 从「手写标题+全量 a-table+开通弹窗」重构为三段式：`MyClientsSearch.vue`（货主编码/名称/状态）+ `index.vue`(ProTable，列 货主信息/联系方式/状态/创建时间/操作，工具栏开通货主) + `ClientFormModal.vue`（开通，`openErpTenant`）+ `ClientDetailDrawer.vue`（货主详情）。操作列「详情 / 启用·停用」。
- **新增后端**：`GET /tenant/erp-tenants/page`（`TenantProvisionService.pageErpTenants`，限当前服务商名下）+ `PUT /tenant/erp-tenants/{id}/status`（`setErpTenantStatus`，**校验该货主 parent=当前服务商**，仅控账号启停不删租户）。前端 api `pageErpTenants`/`setErpTenantStatus`。`SysTenantMapper.pageErpTenants(pageParam,qo,parentWmsTenantId)`。
- 与「平台管服务商」`platform/wms-operators` 完全同构（后者是参照模板）。

### 2.7 库位管理详情抽屉（WarehouseLocationDrawer.vue）多轮细化
- **结构设计**：inline 表单→`a-row/a-col` 栅格；「保存结构 / 生成·重新生成」按钮移进结构第 2 排；「已生成·N」收进标题 tag。
- **去除库位类型（BIG/SMALL）**：结构表单字段 + 列表列 + 常量/form 字段清掉（后端 `locationType` 列留着不再用）。
- **品质分区**：固定四类，只显示 `标准区/不良品区/退货区/暂存区` 四词条；打开抽屉若未初始化则**静默** `initDefaultZones`；四色同时作为网格配色图例（标准蓝/不良红/退货绿/暂存灰）。
- **库位网格**：格子去掉重复列号，改统一小方块，按分区上色，悬停显示编码/分区。
- **库位设分区**（网格选格子）：标题「库位设置」按钮进入设置模式；设置模式下**拖拽框选一片**（矩形）/ 单击切换 / 点排号列号表头整排整列选 / 全选 / 清除；工具条「设为 标准区/不良品区/退货区/暂存区」。
  - 后端 `POST /wms/location-mgmt/locations/move-zone`（`MoveLocationZoneDTO`）→ `WmsZoneService.moveLocationsToZone`：**联动口径**（用户拍板）——更新库位 zone_id + 同步该批库位下现有批次 `zone_id`/`allocatable` + 按 `(服务商×货主×仓×SKU)` 调 `WmsInventoryAggregator.refreshSnapshot` 重算可用库存。整段事务。
- **列表视图**：分页可调每页条数，选项 `10/20/50/100`，默认 10，显示「共 N 个」；加「分区」列。

---

## 3. 品质分区语义（供理解 move-zone 的财务联动）
- `wms_zone` 四类：标准区 STANDARD(可分配1) / 不良品区 DEFECTIVE(0) / 退货区 RETURN(1) / 暂存区 TEMP(0)。
- **`allocatable` 决定"可用库存"**：`WmsInventoryAggregator` 算 available 只计 `quality=GOOD 且批次 allocatable=1` 的量。
- **批次 `allocatable` 是上架时快照**存在 `wms_physical_inventory` 上，不随分区/库位实时联动 → 所以改分区/移库位必须显式联动更新批次 + 重算（见 2.7）。
- 生成库位要求先有 STANDARD 区（`WmsLocationGenerator` 校验 `STANDARD_ZONE_REQUIRED`）。

---

## 4. 当前部署状态
- 后端已打包部署在 **8081**（含 P5/P7、P5+ A/B/C、库位管理二维化+重新生成、move-zone 联动重算）。启动干净（~22s，无循环依赖/异常）。
- DB 处于 **dev-clean 态**：仅 `super_admin`（tenant_id=-1，密码 123123，海外仓平台入口）+ 全量 sys_menu(238) + sys_dict/config；业务数据为空。用 super_admin 重新开通租户测试。
- 前端改动通过用户侧 Vite 生效。

---

## 5. 已知遗留 / 可继续项
1. **编辑回填货主**：调整/调拨草稿编辑时货主需重选（后端 `AdjustmentDetailVO`/`TransferOrderDetailVO` 未回填 `erpTenantId`）。补法：detail 服务从 items[0] 或订单头带出 erpTenantId → 前端 loadDetail 回填。
2. **库存预警/预测按货主**：`InventoryAlertTask` 无上下文（`getSummary` 走区域聚合、per-货主 未接），预警对多货主基本空转。属预存在缺口，需按货主 runAs 迭代 + 每货主通知配置（较大，独立排期）。
3. **仓库级库存读的展示折叠**：`getStockMapByWarehouseAndSkuCodes`/`getAvailableStockMap` 仍有个别纯展示调用未按货主收窄（无崩溃、List 查询）；核心写路径与确认门控已收窄。
4. **locationType(BIG/SMALL)**：后端字段留存但前端已移除、不再下发；如需彻底删可清实体/DTO/VO/生成器。
5. **品质分区深操作**：目前只做「固定四类 + 库位改分区」；未做分区手动增删改/自定义类型（用户当前不需要）。

---

## 6. 权威文档 / 记忆
- **`底层数据表重构设计.md`**（项目根）：模型、命名、逐表决策、PLAN P1–P9、进度、铁律 —— **设计权威**。
- **`erp-frontend/guidelines/页面布局规范.md`**、`UX 规范.md`：前端三段式规范。
- Claude 记忆（`~/.claude/.../memory/`）：`oms-wms-transformation.md`（改造总账）、`three-isolated-platforms.md`（三平台口径）、`auth-session-model.md`（鉴权会话）、`warehouse-location-mgmt.md`（库位管理/货架分配设计）。

---

## 7. 铁律速记
- 三平台是隔离身份层，业务物理层不动；勿用"平台超管"表述。
- 财务/库存改动**稳着来**：加事务 + 单测；勿在末段半改财务引擎。
- `wms_inventory` 4 键、`wms_region_inventory`/`wms_inventory_config` 按 (erp,...) 唯一 —— 任何按 (warehouse/region, sku) 的 2 键 selectOne 在多货主下都会崩，必须带 erp。
- 台账/库存读端点按货主收窄走 **query service + ErpOwnerScopeService**，**勿动全局 handler**（会破坏过账幂等）。
- 后台定时任务无 TenantContext → 需 `runAs(货主/店铺租户)` 包裹，否则数据错归。
- 跑后端改动务必 `mvn test`（非仅 compile）；打包前停 8081。
