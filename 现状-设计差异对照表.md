# 现状 ↔ 设计 差异对照表（施工基准 · 权威）

> 用途：校正《设计方案.md》《业务需求说明书.md》《完整技术设计方案.md》《改造实施路线图.md》对真实代码的错误假设。
> **效力**：凡四份设计文档与本表冲突，**以本表为准**（本表基于真实实体类/控制器/前端目录核对得出）。
> 核对范围：`C:\Users\86178\Desktop\hyldsys-erp-master(1)\hyldsys-erp-master`（原始项目 + 设计文档）。
> 关键大前提：**WMS 执行层已基本完整实现**（17 控制器 / 17 前端页 / ~30 张 `wms_*` 表）；全项目**零 `tenant_id`**。本次改造是"在既有 WMS 上叠加多租户/双层/计费/货架库位/物流产品"，不是从零做 WMS。

---

## 一、已确认的全局事实（实证）

| 事实 | 证据 | 对设计的影响 |
|---|---|---|
| 共 54 个实体（@TableName） | 全包 grep | 设计 §1.2"54 表"数字对，但 WMS 执行层完备这点被低估 |
| **零 tenant_id** | 全包 grep `tenant` 命中 0 | 多租户/双层/计费/货架/库位/物流产品/`oms_*`/`sys_tenant` = 真新建，设计有效 |
| **软删除 = `@TableLogic private Long deleted`**（列 `deleted`，BIGINT 逻辑删除标志） | Warehouse/Region/ReturnInbound/PurchaseInbound/ShippingOrder 等 | ⚠️ 设计铁律与全部新表 DDL 写的 `deleted_at DATETIME` **错误**，新表须改用 `deleted`+`@TableLogic` |
| 审计字段 = `create_by/create_time/update_by/update_time` + `@TableField(fill=INSERT / INSERT_UPDATE)` | 同上 | 新表 DDL 缺 `create_by/update_by` 与填充注解，须补齐 |
| 流水/库存/配置表**不软删**（无 `@TableLogic`） | Inventory/StockFlow/InventoryConfig | 台账类新表（physical_inventory/计费流水）同样**不加** `deleted` |
| 无独立 SQL 文件、无 mapper XML | Glob 无命中 | 表结构在实体注解里；迁移脚本需新建，建议 Flyway |

---

## 二、库存模型 —— 方案②规范（已定，2026-06-28）

**决策**：新建 `wms_physical_inventory` 作为**批次级唯一真源（SSOT）**；现有 `wms_inventory` **降级为只读聚合快照**。

### 2.1 职责边界

| 表 | 角色 | 粒度 | 谁写 | 谁读 |
|---|---|---|---|---|
| `wms_physical_inventory`（**新建**） | 自有仓批次级 SSOT（FIFO/分配/锁定/品质/库位发生地） | 批次：仓×SKU×入库明细×库位 | 仅 WMS（上架/分配/签出/质检/盘点/调整） | WMS 内部 + 聚合刷新 |
| `wms_inventory`（**改造·降级**） | 桶聚合只读快照（看板、库存预测输入、FBO 同步落点） | 仓×SKU（桶：available/reserved/inTransit/damaged） | **仅由 physical_inventory 聚合刷新**（自有仓）；FBO 仓继续由 `FboStockSyncController` 写 | ERP/OMS 只读、看板 |
| `wms_stock_flow`（**改造·复用**） | 库存变动流水（保留现有桶+过账模型） | 每次变动一条 | WMS 各作业 | 库存记录页 |

### 2.2 `wms_physical_inventory` 字段（方案②最终版，覆盖《完整技术设计方案》§4.4.2）

- 双 ID：`wms_tenant_id`(货架归属) + `erp_tenant_id`(货物归属)。
- `warehouse_id, sku_code, inbound_item_id, inbound_date, pick_order, quantity, reserved_qty, quality(GOOD/DAMAGED), location_code, zone_id, allocatable(冗余), version(乐观锁)`。
- 审计：`create_by/create_time/update_by/update_time`。**不加 `deleted`**（台账表，与现有 Inventory/StockFlow 一致）。
- 唯一键：`uk_batch(wms_tenant_id, erp_tenant_id, warehouse_id, sku_code, inbound_item_id, location_code)`；索引 `idx_fifo_alloc(wms_tenant_id, erp_tenant_id, warehouse_id, sku_code, quality, allocatable, inbound_date, pick_order, id)`。

### 2.3 聚合刷新规则（同事务）

每次写 `wms_physical_inventory`（上架/分配/签出/质检/盘点/调整）→ 同事务内：
1. 写 `wms_stock_flow`（带 `wms_tenant_id/erp_tenant_id`，沿用其 bucket/direction/before/after/posting 字段）；
2. 刷新 `wms_inventory` 聚合（乐观锁 `version`）：
   - `available_quantity = Σ(quantity - reserved_qty)`，条件 `quality=GOOD AND allocatable=1`；
   - `damaged_quantity = Σ quantity`，条件 `quality=DAMAGED`；
   - `reserved_quantity = Σ reserved_qty`；`in_transit` 维持现状（在途由入库在途逻辑维护）。

### 2.4 `wms_inventory` 改造（降级 + 多租户）

- 加列：`wms_tenant_id, erp_tenant_id`（聚合现在是"每货主×每服务商×仓×SKU"）。
- 原直接写 `wms_inventory` 的自有仓写路径**全部改为**走 `wms_physical_inventory` 再聚合（FBO 同步路径不变）。
- 该表对 ERP/OMS **只读**（C4）。

---

## 三、逐表/逐模块对照（动作：🆕新建 / 🔧改造 / 🔗对接 / ✏️改名）

### 3.1 仓储基础

| 设计中的表/概念 | 真实现状 | 动作 | 要点 |
|---|---|---|---|
| `wms_warehouse`（附录A 当新建） | **已存在**：OWN/FBO 平台仓模型（`regionId/platform/platformWarehouseId/shopId/warehouseType`），**无排/层/列/库位** | 🔧 改造 | ALTER 加结构参数 `rack_rows/rack_layers/rack_columns/rack_no_prefix/code_pad_width/default_location_type/location_generated`；"出租排给服务商"语义新增，不动 FBO 字段 |
| `wms_zone`（新建） | 无直接对应；但有 `wms_region`（区域：regionCode/regionName） | 🆕 + 🔗 | `wms_zone`(标准/不良品/退货/暂存) 为**新建逻辑分区**；明确：`wms_region` = 业务大区(东/西线、目的地)，`wms_zone` = 仓内品质分区，二者**不同维度**，不可混用 |
| `wms_location`（新建库位） | **完全没有库位概念** | 🆕 新建 | 库位/排/层/列全新；批量生成逻辑见技术方案 §4.4.1 |
| `wms_rack_assignment`（货架分配） | 无 | 🆕 新建 | 真新建 |
| `wms_region` / `wms_region_inventory` / `wms_platform_region_mapping` | 已存在 | 🔧 加租户 | 加 `erp_tenant_id/wms_tenant_id`；保留区域库存逻辑 |

### 3.2 库存

| 设计 | 现状 | 动作 | 要点 |
|---|---|---|---|
| `wms_physical_inventory`（新 SSOT） | 无 | 🆕 新建 | 见 §二（方案②） |
| `wms_inventory` | 已存在（桶+version） | 🔧 降级+加租户 | 见 §2.4 |
| `wms_stock_flow`（决策 S 称"补建"） | **已存在**（桶/方向/过账模型） | 🔧 改造（决策 S 作废） | 仅 ALTER 加 `wms_tenant_id/erp_tenant_id`；**不要新建** |
| `wms_stock_posting(_item)`（过账） | 已存在 | 🔧 加租户 | 入库/出库确认时的过账机制复用 |
| `wms_inventory_config`（库存配置） | 已存在（regionId/skuCode/safetyStock/notifyThresholdDays） | 🔧 扩展 | BRD §2.7.3"两种预测模式"在此基础扩展，非新建 |
| `oms_forecast_rule`（附录A 新建预测规则） | 有 `InventoryForecastController` + `wms_inventory_config` + 前端 `inventory-forecast` | 🔗 改造 | 预测已存在；"自定义模式"作为**新增模式**接入现有预测，`oms_forecast_rule` 仅承载自定义规则表达式 |

### 3.3 入库链路

| 设计 | 现状 | 动作 | 要点 |
|---|---|---|---|
| `oms_inbound_order`（OMS 入库单, source_type 区分采购/自定义/退货） | 入库执行已存在：`wms_purchase_inbound_order(_item)`（`inboundNo/shippingOrderId/orderStatus DRAFT-CONFIRMED-CANCELLED/stockPostingId`）；上游 `wms_purchase_order(_item/_file/_qc_item)` + `wms_shipping_order`（头程物流单） | 🆕 OMS层 + 🔗 对接 | `oms_inbound_order` 作为**货主下发层**新建；下发后对接/驱动现有 `wms_purchase_inbound_order` 执行收货上架；**采购入库单改造前流程已是"采购单→物流单→采购入库单→确认过账"**（与系统文档 §2.2 一致），不要另造执行表 |
| 自定义入库单 | 无 | 🆕 | `oms_inbound_order.source_type=MANUAL`，但其 WMS 执行仍走 purchase_inbound 同一套收货上架 |

### 3.4 出库链路

| 设计 | 现状 | 动作 | 要点 |
|---|---|---|---|
| `oms_outbound_order`（OMS 出库单） | 出库执行已存在：`wms_sales_outbound_order(_item)`；另有 `wms_shipping_order`=**头程入库物流**（非客户出库，勿混） | 🆕 OMS层 + 🔗 对接 | `oms_outbound_order` 货主下发层新建；对接现有 `wms_sales_outbound_order` 做下架/拣货/发货；物流产品/计费(链路二)挂 OMS 出库单 |
| `wms_outbound_order`（技术方案曾拟新建） | 用 `wms_sales_outbound_order` | 🔗 复用 | 不新建并行出库执行表；在现有表加租户+批次分配字段 |

### 3.5 退货（⚠️ 已实现，重点纠正）

| 设计 | 现状 | 动作 | 要点 |
|---|---|---|---|
| `ReturnOrderService` + `wms_return_qc_item`（E2 当新建质检） | **已存在** `wms_return_inbound_order` + `ReturnInboundController` + 前端 `return-inbound`，且**已含质检结果字段**：`returnReason / qualifiedQuantity(合格) / unqualifiedQuantity(不合格) / toDamagedQuantity(转残品) / scrapQuantity(报废)` | 🔧 扩展（不新建） | 退货质检大体已实现；本次只需**补质检状态机三态(`QC_PENDING/QC_PASS/QC_FAIL`)、电子类强制拍照(BR-04)、回库到 physical_inventory 批次 + 分区**；`wms_return_qc_item` 若需明细级质检可作为现有单的**子表补充**，不替代现有退货入库单 |

### 3.6 其他已存在、设计未提及的表（仅需加租户）

`wms_transfer_order(_item)`（调拨）、`wms_adjustment_order(_item)`（库存调整）、`wms_stocktake_order(_item)`（盘点，✏️ 真名——附录A `wms_stock_take` 是错名）、`wms_logistics_provider`（物流商，BRD"不变"正确，勿与新"物流产品 `wms_logistics_product`"混淆）。动作：🔧 加 `erp_tenant_id/wms_tenant_id`；盘点/调整按 §6.6 仅平台/WMS 可见、对货主隐藏。

### 3.7 真·新建（设计成立，无现状冲突）

`sys_tenant` / `sys_user_tenant` / `wms_rack_assignment` / `wms_physical_inventory` / `wms_location` / `wms_zone` / `oms_inbound_order(_item)` / `oms_outbound_order(_item)` / `oms_sync_outbox` / `wms_fee_rate_card` / `wms_tenant_rate_discount` / `wms_billing_record` / `wms_monthly_bill` / `wms_logistics_product(_grant)` / `wms_client_billing_record` / `wms_client_monthly_bill`。
→ 这些新表一律按 §一 约定：软删除用 `deleted`+`@TableLogic`（台账/流水类不加），审计 `create_by/create_time/update_by/update_time`。

---

## 四、对设计决策的修订

| 决策/章节 | 原内容 | 修订 |
|---|---|---|
| 铁律"软删除 `deleted_at`" | 全文 + 所有新表 DDL | **改为 `deleted`(BIGINT)+`@TableLogic`**；台账/流水表不软删 |
| 决策 S（补建 `wms_stock_flow`） | 称基线缺失需新建 | **作废**：已存在，改为 ALTER 加双租户列 |
| 决策 R（physical_inventory） | 批次模型 | 保留，且明确为**方案②**：新 SSOT + `wms_inventory` 降级聚合（§二） |
| 附录A `wms_stock_take` | 新建盘点表 | ✏️ **改名**对接现有 `wms_stocktake_order(_item)` |
| 附录A `wms_warehouse` 新建 | — | 🔧 改 ALTER 现有 |
| §6.6/§11.7 盘点"下线" | 隐含盘点是新功能 | 重述：盘点**已实现**，改为"仅平台/WMS 可见、对货主角色解绑授权" |
| E2 退货质检"新建 ReturnOrderService" | — | 🔧 改"扩展现有 ReturnInbound 加质检三态/拍照/批次回库" |
| oms_forecast_rule 新建预测 | — | 🔗 接入现有 InventoryForecast，仅承载自定义规则 |
| B1「超管不设上下文 → 全量可见」 | 路线图 B1 | **反转**：超管在货主业务白名单表上强制 `tenant_id=-1`（哨兵）→ 0 行，屏蔽货主订单/商品/店铺等经营数据；仅凭专用授权读 SKU/货物用于作业（见 §六） |

---

## 五、数据级可见性（已定，2026-06-29）

> 原则：**菜单/按钮裁剪仅是 UI，不是数据安全**；隔离一律在后端 SQL 层强制。下表为各身份「能看到什么数据」的权威口径，已落地。

### 5.1 身份 × 数据域 可见性矩阵

| 数据域 | 货主(ERP_USER) | WMS 服务商(WMS_OPERATOR) | 平台超管(SUPER_ADMIN) |
|---|---|---|---|
| OMS 经营数据（订单/店铺/商品族/统计…白名单表） | 仅自己（`TenantContext`=自身 id） | 0 行（`WmsTenantContext` 不命中 OMS 白名单 → 无 `TenantContext`，方案②跳过=空） | **0 行**（`TenantContext`=`BLOCK_TENANT_ID(-1)` 哨兵） |
| WMS 库存明细 `wms_inventory`（含库位） | 仅自己（`erp_tenant_id`=自身） | 仅汇总、按 `wms_tenant_id`、**无库位明细**（`/wms/inventory/by-operator`） | 全部（作业所需） |
| 作业用 SKU 查询 `/wms/sku-lookup` | 仅自己 | 名下货主子树 | 全部货主（`@InterceptorIgnore` + 显式按身份授权） |
| 日志（登录/操作/访问） | **仅自己**（按用户名） | **仅自己** | **仅自己** |

### 5.2 落地机制（四件，编号 ①②③④）

| # | 机制 | 关键实现 |
|---|---|---|
| ① 屏蔽超管越权读货主经营数据 | 哨兵租户 | `TenantContext.BLOCK_TENANT_ID=-1`；`TenantResolveInterceptor` 对已登录未绑定租户者注入 -1 |
| ② 作业用 SKU 跨租户查询 | 绕过租户注入 + 按身份授权 | `WmsSkuLookupMapper`(`@InterceptorIgnore`)+`WmsSkuLookupService`（超管全量/服务商子树/货主自身）+`/wms/sku-lookup` |
| ③ 库存按货物归属可见 | `erp_tenant_id` 作用域 | `InventoryService.currentErpScope()` 注入所有汇总/明细查询；`getDetail` 校验归属；服务商汇总走 `selectByWmsTenant`+`/wms/inventory/by-operator` |
| ④ 日志仅自己 | 用户名数据权限 | `LogDataPermissionHandler`(`MultiDataPermissionHandler`)+`DataPermissionInterceptor`：对 `log_login_log.username`/`log_operation_log.operator`/`log_access_log.username` 的 SELECT 注入 `=当前用户`；无用户兜底 `1=2`。**未加 tenant_id 列**——三表均已记用户名，登录日志在租户上下文建立前写入，按用户名过滤更可靠且精确到「仅本人」。历史日志已全量清空 |

---

## 六、四份文档的处置

| 文档 | 处置 |
|---|---|
| 本表《现状-设计差异对照表.md》 | **权威施工基准**，冲突时以此为准 |
| 《改造实施路线图.md》 | 已据本表打补丁（顶部基准块 + C1/D1/D2/E1/E2 改"改造现有"+真实表名+方案②+`deleted` 约定）。施工直接看它 |
| 《完整技术设计方案.md》 | 顶部加勘误指针；决策 S/附录A 命名/软删除以本表为准 |
| 《设计方案.md》《业务需求说明书.md》 | 顶部加勘误指针，指向本表；其余文字保留作背景 |

---

*核对依据：实体类 @TableName×54、wms/controller×17、frontend/src/views/wms×17、关键实体字段级阅读（Warehouse/Inventory/StockFlow/Region/ReturnInboundOrder/PurchaseInboundOrder/ShippingOrder/InventoryConfig）。*
