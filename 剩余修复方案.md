# 剩余缺陷修复方案（待审批统一执行）· 2026-07-07

> 经 5 组只读调查代理核实。挂起项（调拨 C-1、调拨 H-7 两点、前端 #24）不在本清单。

## 零、经核实已解决，无需改
- **M-8** 退货合格/不合格口径两路径不一致 → 已随退货重构收敛：`create` 仅申报不落桶，`qc` 是唯一落桶点、FAIL 统一 DAMAGED。
- **M-3** 区域库存汇总货主维不一致 → 已解决：`wms_region_inventory` 在数据权限白名单，reserved/in_transit 由拦截器自动注入 `erp_tenant_id`，与 available 同一 readScope 作用域。（可选加固：给两条聚合 SQL 显式传 readScope 防白名单被误删。）
- **S3** 前端契约错配 → 4 项均已修复（open 契约、PageResult.records、createShop 返回 id、导出 blob）。

---

## 批次 1 — 低风险（纯校验/注解/前端，可直接批量执行）

### M-6 自定义入库收货不校验实收≤应收
- **错误**：`WmsInboundExecutionService.receive`（约101-122行）把货主录入实收原样写入 `actualQuantity`，不校验 ≤ 应收；自定义链（shippingOrderId==null）又不走区域在途兜底 → 超量经上架全额流入 available。
- **改法**：写 `actualQuantity` 前加断言 `actual ≤ item.getExpectedQuantity()`（复用 `PurchaseInboundService.validateItemQuantities` 语义）。1 文件。
- **风险**：需处理 expected 为 null/0 的自定义链（无应收基准）——保守：expected 为空视为需建单填写、不静默当 0。

### M-7 非规范 quality 致批次双桶静默消失
- **错误**：quality 是无白名单自由字符串；传非规范值（小写 damaged / DEFECTIVE 等）→ 批次以该值 + allocatable=0 落库；聚合 `"DAMAGED".equals` 为 false 走 GOOD 分支但 allocatable≠1 又不计 available → 既不进 available 也不进 damaged，实物在快照里凭空消失、不守恒。
- **改法**：① `WmsInboundExecutionService.normalizeQuality`（约382行）改为 trim+大写归一 + 白名单（非 GOOD/DAMAGED 抛错）；② `WmsInventoryAggregator.aggregate`（约71行）`"DAMAGED".equalsIgnoreCase(...)` 兜底历史脏数据。2 文件。
- **风险**：聚合端只做 equalsIgnoreCase，**不动 allocatable 逻辑**（避免把"良品但暂不可分配/待检"批次误算进 damaged）。

### S2 补 @PreAuthorize（越权/IDOR）　〔✅ 全部完成：2 控制器(批次1) + 剩余5控制器 C1(2026-07-08，含 V52 权限码播种)〕
- **错误**：7 个控制器无方法级 `@PreAuthorize`，任意已登录身份可调（尤其写端点）。对照 `LogisticsProviderController`(6/6 全覆盖) 与 `WmsLogisticsProductController`(0) 明显不一致。
- **改法**：按现有约定 `@PreAuthorize("@per.hasPermission('wms:xxx:read|add|edit|del')")` 补注解。**写端点优先**：`InventoryConfigController`(PUT/global、PUT/sku、DELETE/sku)、`WmsLogisticsProductController`(POST、PUT/status、DELETE)。其余读端点：AssetFinance/OperatorFinance/InventoryForecast/OperatorDashboard/ShipProdCalc。7 文件。
- **风险**：注解只解决功能权限；对象级 IDOR（按 id 直取他人单据）还需 service 层 `canAccess`（依赖 S1 修复才可靠）——本批只补功能权限注解。需确认各端点对应的权限码已在菜单/角色体系存在，否则平台自身会被挡。

### M6 前端分层倒置（SkuMappingBadge 反向依赖 views）
- **错误**：公共组件 `components/Sku/SkuMappingBadge.vue:66` import 了 `views/product/sku/components/SkuMappingDetailModal.vue`（公共层依赖业务视图层，方向倒置）。
- **改法**：反转依赖——Badge 删掉内嵌 Modal、改 `emit('open')` 变纯展示；由唯一消费者 `views/product/sku/SkuPage.vue`（同层）持有并渲染该 Modal，监听 `@open`（闭包捕获行 record 带出 sku 上下文）。2 文件。
- **风险**：`SkuPage.vue:298` 是表格列 render 批量生成 Badge，`onOpen` 必须闭包捕获该行、不能共享单例 state。另发现同类第二处 `ShopSelectInput.vue:78` → `@/views/components/Platform`，可一并处理或单列。

---

## 批次 2 — 需 DB 迁移（中风险，含历史数据处理）

### M-2 过账幂等无 DB 唯一约束 + 幂等查询无租户维
- **错误**：`StockPostingService.post`（约52行）幂等仅靠应用层"先查后插"（`selectBySourceAndType` 无租户维、无锁），RC 并发下双读 null→双插双应用库存；跨租户 source_id 撞号还会误命中他人记录被"幂等"跳过（漏过账）。DB 层 `wms_stock_posting` 仅普通索引、无 `(source_type,source_id,posting_type)` 唯一键。
- **改法**：① 新增迁移 `V51__stock_posting_idempotent_uk.sql` 加唯一键 `(source_type, source_id, posting_type, erp_tenant_id)`；② `StockPostingMapper.selectBySourceAndType` 增 `erpTenantId` 入参并加条件，`post` 传 `dto.getErpTenantId()`；③ `createPosting` 捕获该唯一键的 `DuplicateKeyException` 时回查返回 existed=true（并发安全闭环）。
- **风险**：**建唯一键前必须先扫描/合并历史重复过账行**（含其已应用的库存增量），否则 ALTER 失败；`erp_tenant_id` 为 NULL 的历史行需先回填非空（NULL 不参与唯一比较）。需先跑一个排查 SQL 确认现网有无重复/NULL。

---

## 批次 3 — 高风险/大改（建议逐条单独确认，不宜盲目统一执行）

### L-1 AVAILABLE 桶允许为负、无超卖硬守卫　〔✅ 已完成 2026-07-07（A组+B1批次）〕
- **错误**：`StockBucket.AVAILABLE` allowNegative=true，`InventoryPostingEngine.validateConstraint` 对可负桶短路 → 过量出库/预占可静默把 available 压负。
- **改法**：`StockBucket` 将 AVAILABLE 改 `allowNegative=false`（validateConstraint 自动生效）。
- **风险（为何列高危）**：需**先全局排查所有对 AVAILABLE 桶 StockDirection.OUT 的过账调用点**，确认本会话改造后确无残留合法 AVAILABLE- 过账（否则正常业务被打成异常回滚）；且**聚合直写 `refreshSnapshot.updateWithVersion` 绕过 validateConstraint**，available 仍可能因 qty-res<0 写负 → 要真正堵死需在聚合端另加下限守卫（截 0 还是报错需业务定）。→ 执行前需一轮排查。

### M-1 两写者 wms_tenant_id 建行键不一致
- **错误**：过账引擎 `getOrCreate` 写死 wms=0；聚合器 `refreshSnapshot` 用批次真实 wms。当前所有写入路径 wms 恒 0，**缺陷被掩盖**（latent，未在发生）；一旦经服务商货架维度（WmsPhysicalInventoryService/WmsZoneService）写入非 0 批次即触发同 (erp,wh,sku) 分裂两行。
- **改法（推荐方案①统一为 0）**：`wms_inventory` 只按货主聚合、wms 维恒 0；聚合器建/查行改 `selectByErpWarehouseSku`+`setWmsTenantId(0L)`。
- **风险（为何列高危）**：服务商只读汇总 `InventoryService.listByCurrentOperator`→`selectByWmsTenant` 依赖 `wms_inventory.wms_tenant_id≠0`，统一为 0 后会失效，**须一并把服务商库存视图迁到批次表 `wms_physical_inventory` 聚合**。是一处结构调整，非一行改动。且当前是 latent（不影响现网），优先级可后置。

### S1 无租户上下文 fail-open（三处）　〔✅ ①+② 已完成 2026-07-07；❗位置③ 留后，见下〕
- **错误**：`ErpOwnerScopeService.readScope`（无上下文返回 null=看全部）、`MyMetaObjectHandler.insertFill`/`writeOwner`（无上下文盖成货主 1）、`ErpTenantLineHandler.ignoreTable`（无上下文跳过 tenant 注入=读全部）三处 fail-open，与前端菜单 fail-closed 相反。
- **改法**：统一收敛为"无上下文=命中 0 行/拒绝"：readScope 区分平台(-1 返 null)与无上下文(返 BLOCK 哨兵 0 行)；MetaObjectHandler 无上下文不盖真实租户（抛错或盖 -999）；ignoreTable 白名单表始终注入、由 getTenantId fail-closed 决策。
- **风险（为何列高危）**：位置 3 影响**所有白名单业务表 + admin 表**，改 fail-closed 后**所有后台定时/同步任务若未 runAs 建立上下文会批量抛异常**。→ 执行前必须先排查全部后台任务(scheduler/异步)是否都已 `runAs`，否则由"串数据"变"任务全崩"。

### 店铺 API Key 明文存储加密　〔❌ 不处理 — 用户 2026-07-07 明确指示"店铺API明文存储不要管，不需要处理"，本项从待办移除〕
- **错误**：`Shop.credential`（json 列）明文存 api_key/client_secret，且明文入内存 `ShopFacade.TEST_CACHE`；全链路无 at-rest 加密。
- **改法**：新增 `EncryptedStringTypeHandler`（Hutool AES，密钥走环境变量/KMS，**不复用登录密钥**）；`Shop.credential` 标 `@TableField(typeHandler=...)` 写时加密读时解密；列类型 `json→varchar/text`（DDL）；存量明文一次性迁移（加 `enc:v1:` 前缀双读兼容）；TEST_CACHE 改存密文或缩 TTL。
- **风险（为何列高危）**：涉及**列类型 DDL + 存量数据加密迁移 + 密钥管理**，且需核对 `ShopMapper.xml` 有无绕过实体映射直取 credential 的自定义查询（否则 TypeHandler 不触发）。本地备份是脱敏值，真实迁移须在生产库配密钥后执行。→ 是独立小项目，强烈建议单独排期，不与其他项一起"统一执行"。

---

## 可选（非功能，暂不列入主执行）
- **S4** 死代码　〔✅ 已完成 2026-07-07：删 CustomOutboundFacade 废弃过账死代码 doStockPosting/doReversePosting/resolvePostingType + 连带 import/字段。注：`ErpTenantLineHandler.getTenantId` 的 null 抛错**保留**——它是将来位置③ fail-closed 的挂钩，非真死代码，不删。〕
- **M-3 加固**　〔✅ 已完成 2026-07-07：区域 reserved/in_transit 聚合显式传 readScope。〕

---

## 执行建议
- **批次 1** 立即可执行（低风险、无迁移）。
- **批次 2 (M-2)** 执行前先跑历史重复/NULL 排查 SQL，确认干净或先清理，再建唯一键。
- **批次 3** 每条执行前都有一轮"前置排查"（L-1 查残留 AVAILABLE- 过账；M-1 迁服务商视图；S1 查后台任务 runAs；API Key 配密钥+迁移）。建议逐条单独确认，不建议盲目一次性执行。
