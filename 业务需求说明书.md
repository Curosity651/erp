# 多租户海外仓系统业务需求说明书（完整版）

> 🟥 **勘误（2026-06-28）**：附录 A 把 `wms_warehouse`/`wms_stock_flow`/盘点表当"新建"，实际**已存在**（盘点真名 `wms_stocktake_order`，非 `wms_stock_take`）；退货质检在 `wms_return_inbound_order` 中**大体已实现**。库存采用**方案②**。冲突处**以《现状-设计差异对照表.md》为准**，施工见《改造实施路线图.md》。

> 版本：v1.0（2026-06-28）
> 编写依据：
> - 《系统文档.md》—— 功能骨架与业务诉求来源
> - 《设计方案.md》—— 已确认的技术决策与数据库 Schema（权威）
> - 《易仓一件代发 WMS 操作指南》—— 行业标准流程参考（收货 / 上架 / 下架 / 打包 / 签出）
>
> **本说明书内容深度以"可直接指导开发"为目标。** 凡涉及数据结构，均对齐《设计方案.md》表定义；凡《设计方案.md》尚未覆盖的新表，本文在 [附录 A](#附录-a数据模型补充设计方案待补建表) 统一补充。

---

## 0. 全局架构约束（开发必读）

以下约束来自《设计方案.md》已确认决策，**所有模块设计不得违反**：

| # | 约束 | 说明 |
|---|---|---|
| C1 | **双层租户** | `sys_tenant.tenant_type`：`WMS_OPERATOR`（第一层，3PL 服务商）/ `ERP_USER`（第二层，货主）；货主通过 `parent_wms_tenant_id` 关联其所属 WMS 服务商 |
| C2 | **三类登录入口/角色** | `ROLE_SUPER_ADMIN`（平台超管）/ `ROLE_WMS_*`（WMS 服务商）/ `ROLE_TENANT_*`（货主） |
| C3 | **WMS 模块化单体** | WMS 不独立部署，`com.erp.admin.wms` 与 OMS 同进程；OMS↔WMS 走 **Spring Bean 直接调用**，无 HTTP/MQ |
| C4 | **库存 SSOT 归 WMS** | 仅 WMS 模块可写 `wms_physical_inventory`；ERP+OMS 只读快照，**不得本地修改库存** |
| C5 | **库存双 ID** | `wms_physical_inventory.wms_tenant_id`（货架归属）+ `erp_tenant_id`（货物归属），两个维度分别加索引 |
| C6 | **计费链路分离** | 链路一 平台→WMS 服务商（货架租金+操作费）；链路二 WMS 服务商→货主（物流产品费）。两套表互不混用 |
| C7 | **FIFO 防超卖** | 分配 = 单仓履约 + 仅良品 + 批次 FIFO（`inbound_date → pick_order → id`）+ `SELECT ... FOR UPDATE` 行锁 |
| C8 | **已定稿业务规则** | BR-01 缺货整单挂起；BR-02 不拆单不合单、单仓履约；BR-03 平台单按店铺 auto_confirm；BR-04 良品/次品（次品+电子类强制拍照）；BR-05 允许跨仓存放但单订单单仓履约；BR-06 库存同步默认 30 分钟可配 |

**物理仓储层级**（贯穿全文）：

```
仓库 warehouse
  └─ 排 rack（编号 1/2/3…，计费单位，整排分配给某个 WMS 服务商）
       └─ 层 layer × 列 column → 库位 location（如 A1-03-03）
分区 zone（逻辑覆盖在库位上）：标准区 / 不良品区 / 退货区 / 暂存区
  规则：不良品区、暂存区不可分配库存；退货区、标准区可分配库存
```

---

## 目录

- [第一部分 海外仓平台（超级管理员）](#第一部分-海外仓平台超级管理员)
  - [1.1 仓库管理](#11-仓库管理)
  - [1.2 入库管理](#12-入库管理)
  - [1.3 出库管理](#13-出库管理)
  - [1.4 退货管理](#14-退货管理)
  - [1.5 盘点管理](#15-盘点管理平台专属)
  - [1.6 权限管理](#16-权限管理)
  - [1.7 财务系统（应收）](#17-财务系统应收)
- [第二部分 ERP+OMS 用户（货主）](#第二部分-erpoms-用户货主)
  - [2.1 基本不变模块](#21-基本不变模块说明)
  - [2.2 采购入库单（改造）](#22-采购入库单改造)
  - [2.3 自定义入库单（新增）](#23-自定义入库单新增)
  - [2.4 销售出库单（改造）](#24-销售出库单改造)
  - [2.5 自定义出库单（新增）](#25-自定义出库单新增)
  - [2.6 退货订单 / 自定义退货单](#26-退货订单管理--自定义退货单)
  - [2.7 库存模块调整（删除盘点 + 只读快照）](#27-库存模块调整核心)
- [第三部分 WMS（服务商运营端）](#第三部分-wms服务商运营端)
  - [3.1 账户管理（开通货主）](#31-账户管理开通货主)
  - [3.2 物流产品管理](#32-物流产品管理链路二计费)
  - [3.3 财务系统（收入/支出）](#33-财务系统收入支出)
- [附录 A：数据模型补充](#附录-a数据模型补充设计方案待补建表)
- [附录 B：OMS↔WMS 状态机全景](#附录-boms-wms-状态机全景)

---

# 第一部分 海外仓平台（超级管理员）

> 用户群体：平台运营方（你们自己），角色 `ROLE_SUPER_ADMIN`。
> 数据可见性：**无租户过滤**，可查看所有 WMS 服务商、所有货主、所有仓库数据。

## 1.1 仓库管理

### 1.1.1 创建仓库

**模块目标**：将物理仓库的结构（排/层/列）数字化建模，自动生成库位，为后续货架分配、上架、拣货提供位置基础。

**用户角色**：仅平台超管。

**页面核心字段**

| 字段 | 说明 | 对应数据 |
|---|---|---|
| 仓库编码 | 唯一，如 `MOW-01` | `wms_warehouse.warehouse_code` |
| 仓库名称 | 如"莫斯科一号仓" | `wms_warehouse.warehouse_name` |
| 仓库类型 | 海外仓 / 中转仓 | `wms_warehouse.warehouse_type` |
| 所属区域 | 国家/城市/地址 | `country/city/address` |
| 联系人 / 联系电话 | | `contact_name/contact_phone` |
| 仓库参数 | 排数、每排层数、每排列数 | 用于批量生成库位 |

**核心业务流程**

```
新建仓库基本信息
   ↓
录入结构参数（N 排 × M 层 × K 列）
   ↓
系统批量生成库位 wms_location（编码规则见下）
   ↓
为库位划分分区 zone（标准/不良品/退货/暂存）
   ↓
仓库状态 = ENABLED，可用于货架分配
```

**关键业务规则**

- **库位编码规则**（参考易仓）：`{排}-{层}-{列}`，例 `A1-03-03` 表示 A1 排第 3 层第 3 列。批量生成时按参数笛卡尔积自动产出。
- **库位类型**：每个库位绑定类型（大库位/小库位，预设长宽高），用于上架时容量判断。
- **拣货类型**：库位分"拣货库位/存储库位"，默认 FIFO 策略不区分；如需"先拣货后存储"策略需单独配置（本期默认 FIFO，见 C7）。
- 库位创建后支持单个/批量打印库位条码。

**依赖数据表**：`wms_warehouse`、`wms_zone`、`wms_location`（均见附录 A）。

---

### 1.1.2 仓库预览（二维平面）★ 模糊点专项

**模块目标**：以二维平面图直观呈现"每一排当前归属于哪个 WMS 服务商"，让平台快速掌握仓库出租率与空置情况。

**用户角色**：平台超管。

**页面布局**

```
┌──────────────────────────────────────────────┐
│ [搜索栏]  仓库下拉▼   服务商筛选▼   到期状态▼   │
├──────────────────────────────────────────────┤
│  仓库 MOW-01 二维平面（按排渲染）              │
│  ┌────┬────┬────┬────┬────┬────┐              │
│  │排1 │排2 │排3 │排4 │排5 │排6 │              │
│  │客A │客A │客A │客B │客B │空置│              │
│  │绿  │绿  │绿  │蓝  │蓝  │灰  │              │
│  └────┴────┴────┴────┴────┴────┘              │
│  点击某排 → 弹出该排库位明细 + 占用率           │
└──────────────────────────────────────────────┘
```

**前端渲染逻辑（关键）**

1. 选定仓库后，拉取该仓库全部排：`SELECT DISTINCT rack_no FROM wms_location WHERE warehouse_id = ?`。
2. 拉取当前有效的货架分配：
   ```sql
   SELECT rack_no, wms_tenant_id
   FROM wms_rack_assignment
   WHERE warehouse_id = ?
     AND effective_from <= CURDATE()
     AND (effective_to IS NULL OR effective_to >= CURDATE());
   ```
3. 前端按 `rack_no` 建立色块：
   - 有分配记录 → 取 `wms_tenant_id` 映射到一个**固定色值**（同一服务商同色），格子显示服务商简称。
   - 无分配记录 → 灰色"空置"。
   - 即将到期（`effective_to` 距今 ≤ 30 天）→ 加黄色边框告警。
4. 点击某排 → 下钻：展示该排各库位（层×列网格），并叠加分区颜色（标准/不良品/退货/暂存），格内显示当前库存占用件数（来自 `wms_physical_inventory` 按 `location_code` 聚合）。

**关键业务规则**

- 一个排在同一时间只能归属一个 WMS 服务商（`wms_rack_assignment` 的 `uk_rack_period` 保证）。
- 历史归属可追溯（`effective_to` 不为空的过期记录保留）。

**依赖数据表**：`wms_rack_assignment`、`wms_location`、`wms_physical_inventory`、`sys_tenant`。

---

### 1.1.3 账户开通（开通 WMS 服务商）

**模块目标**：平台为一个 3PL 服务商（第一层租户）开通 WMS 运营账号。

**用户角色**：平台超管。

**页面核心字段**：公司名称、联系方式、地址、状态。

**核心业务流程（状态机）**

```
[新建] → 填写服务商公司信息
   ↓ 系统创建 sys_tenant(tenant_type=WMS_OPERATOR, parent_wms_tenant_id=NULL)
   ↓ 创建管理员 sys_user + sys_user_tenant 绑定 + 授予 ROLE_WMS_MANAGER
[ENABLED 启用] ⇄ [DISABLED 停用]
```

**关键业务规则**

- 开通即生成该服务商的管理员账号（`is_admin=1`），后续该服务商可自行在 WMS 端开通下属员工/货主。
- 停用服务商时，其下所有货主（`ERP_USER`）级联禁止登录（校验 `parent_wms_tenant_id` 链）。

**依赖数据表**：`sys_tenant`、`sys_user`、`sys_user_tenant`。

---

### 1.1.4 仓库开通（货架分配）★ 计费起点

**模块目标**：把"某仓库的某几排"租给某个 WMS 服务商，设定到期时间与月租金，并存档合同——这是链路一计费（货架租金）的数据源。

**用户角色**：平台超管。

**页面核心字段**

| 字段 | 对应 `wms_rack_assignment` |
|---|---|
| WMS 服务商 | `wms_tenant_id` |
| 仓库 | `warehouse_id` |
| 货架排号（可多选） | `rack_no`（每排一条记录） |
| 月租金（元/排/月） | `monthly_fee` |
| 生效日期 / 到期日期 | `effective_from / effective_to` |
| 合同附件 | 新增 `contract_file_url`（见附录 A 扩展） |

**核心业务流程**

```
选择服务商 → 选择仓库 → 勾选若干排 → 设月租金/起止日期 → 上传合同
   ↓ 校验：所选排在该时间段未被他人占用
   ↓ 为每个排插入一条 wms_rack_assignment
   ↓ 仓库预览中这些排即变色归属该服务商
```

**关键业务规则**

- **冲突校验**：同一 `rack_no + warehouse_id + 时间段` 不可重复分配（`uk_rack_period`）。
- **到期处理**：到期后该排在仓库预览置灰；月度账单按当月实际有效天数/有效记录计租金（见 [1.7](#17-财务系统应收)）。
- 合同附件必传（线下签署后上传扫描件）。

**依赖数据表**：`wms_rack_assignment`、`wms_warehouse`、`sys_tenant`。

---

## 1.2 入库管理

> 平台接收 ERP+OMS 用户（货主）下发的**采购入库单 / 自定义入库单**，执行物理收货 + 上架。
> 收货、上架的操作主体是 **WMS 服务商的仓库员工**，数据写入 WMS 侧表，并回传状态给 OMS。

### 1.2.1 收货

**模块目标**：清点货主下发入库单的实到货物数量与品质，登记实收数，触发 SKU 仓储属性创建，状态回传 OMS。

**用户角色**：WMS 服务商收货员（`ROLE_WMS_OPERATOR`）。

**页面布局**（两部分，对齐《系统文档》与易仓）

```
[第一部分 搜索栏] 收货方式：入库单号(默认)/箱号/跟踪号  + 状态筛选
[第二部分 列表]   展示推送到本服务商的待收货入库单
```

**页面核心字段**：入库单号、来源类型（采购/自定义/退货）、货主、目的仓库、预计到达、SKU 列表（预期数/实收数/单重/长宽高）、状态。

**核心业务流程（状态机 + OMS 同步）**

```
WMS 侧 wms_inbound_order.status          OMS 侧 oms_inbound_order.status
──────────────────────────────          ──────────────────────────────
PENDING（待收货）
   │ 收货员【标记到货】(异步脚本)
   │  ───────────────────────────────►  RECEIVING（尾程收货中）
RECEIVING（收货中）
   │ 录入实收数 →【确认收货】
   │ 收货完成自动创建 SKU 仓储属性
   ▼  ───────────────────────────────►  （上架完成后才置 COMPLETED）
PUTAWAY（待上架）
```

**关键业务规则**

- **收货方式三选一**（参考易仓）：按入库单号（默认）/ 按箱（扫 Packing List 箱唛）/ 按跟踪号（仅"自发头程"类型可用，"中转代发"不可用跟踪号）。
- **无条码 SKU 自动生成条码**：若 SKU 无条码，系统按规则生成产品代码条码供打印粘贴。生成规则建议：`{货主tenant_code}-{sku_code}` 或系统流水码，保证**同货主内唯一**（对齐 `sku` 表 `uk_tenant_sku_code`）。
- **称重量方**：收货时可录入/引用 SKU 单重与长宽高，写入 SKU 仓储属性（供物流产品按尺寸匹配、体积计费用）。
- 实收数允许 ≠ 预期数（多收/少收），差异在单据上留痕。
- 状态回传走 **OMS↔WMS 同进程调用**（C3）：WMS 收货 Service 直接调用 OMS 回调方法更新 `oms_inbound_order` 状态。

**依赖数据表**：`wms_inbound_order`、`wms_inbound_order_item`、`oms_inbound_order`、`sku`。

---

### 1.2.2 上架

**模块目标**：把已收货货物按 SKU 归位到具体库位，**产生批次库存**（SSOT 写入），并同步库存快照回 OMS。

**用户角色**：WMS 服务商上架员（`ROLE_WMS_OPERATOR`）。

**页面布局**：搜索栏（上架方式：默认/按箱；上架搜索：入库单号默认）+ 待上架入库单列表。

**核心业务流程**

```
PUTAWAY（待上架）
   │ 扫描入库单号 → 系统带出待上架 SKU 及待上架数
   │ 上架员录入实际上架数 + 选择库位（可拆分到多库位）
   │【确认上架】
   ▼ 每次上架 → INSERT wms_physical_inventory（一条批次）
            → 同步库存快照回 OMS【库存管理】
   待全部 SKU 上架完毕
   ▼
COMPLETED（上架完成） ──同步──► OMS oms_inbound_order = COMPLETED（收货完成/已入库）
```

**关键业务规则（上架库位分配逻辑，对齐《系统文档》§上架）**

1. 该 SKU 仓库中**不存在** → 整体放入同一空库位；超出单库位容量则就近再选一个库位继续整体存放。
2. 该 SKU **已存在** 且原库位剩余空间足够 → 并入原库位。
3. 该 SKU 已存在但原库位空间不足 → 新分配一个库位。
4. **批次生成**：每次上架生成一条 `wms_physical_inventory`，写入：
   - `wms_tenant_id`（当前服务商）、`erp_tenant_id`（入库单所属货主）
   - `inbound_item_id`（批次溯源）、`inbound_date`（实际上架日 = FIFO 主键）、`pick_order`（同日顺序）
   - `quantity`、`quality=GOOD`、`location_code`
5. **分区约束**：良品上架到标准区；不良品上架到不良品区（不良品区不可被分配出库，见 C8/分区规则）。

**依赖数据表**：`wms_physical_inventory`（SSOT，C4/C5）、`wms_inbound_order_item`、`wms_location`、`oms_*` 库存快照视图。

---

## 1.3 出库管理

> 货主在 OMS 推送**销售出库单/自定义出库单**到平台；平台执行下架（拣货）→ 打包 → 签出。
> 流程标准对齐易仓"出库操作"。

### 1.3.1 订单下架（拣货）

**模块目标**：把待出库订单分配库存（FIFO 锁定）并生成拣货单，指派拣货员到库位取货。

**用户角色**：WMS 拣货员/管理员。

**核心业务流程（状态机 + OMS 同步）**

```
OMS oms_outbound_order            WMS wms_outbound_order
─────────────────────             ──────────────────────
PENDING → SENT（已下发）  ───►     PENDING（待下架）
                                     │【下架】选下架模式 + 指定拣货员
                                     │ 触发 FIFO 分配（SELECT FOR UPDATE）
                                     ▼
                                  ALLOCATING → PICKING（拣货中）
SENT ◄────（下架成功回传，订单不可再取消）────┘
```

**下架模式（三选一，参考易仓"操作场景"）**

| 模式 | 适用 | 逻辑 |
|---|---|---|
| **集中分拣** | 一票一件（1 单 1 SKU） | 批量拣回打包台，配货员扫 SKU 配货，同款 SKU≥3 支持批量打面单 |
| **按单分拣** | 高货值/爆款/重货大货 | 按单找货，扫订单号→扫 SKU→打面单，逐单核验 |
| **二次分拣** | 一票多件（1 单 ≥2 SKU） | 全部拣到打包台，扫 SKU 分配到篮子号，篮子集齐自动打面单 |

**关键业务规则**

- **FIFO 分配（防超卖，C7）**：仅良品（`quality=GOOD`）、单仓履约（BR-02/05）、按 `inbound_date → pick_order → id` 升序锁定批次，`reserved_qty += take`，行锁 `SELECT ... FOR UPDATE`。
- **缺货整单挂起（BR-01）**：任一 SKU 不足 → 整单 backorder，不部分下架，回传 OMS 触发挂起。
- **不拆单不合单（BR-02）**：一单一拣货单一包裹。
- **下架后不可取消**（对齐《系统文档》§销售出库单）：状态进入 PICKING 后 OMS 端取消入口关闭。

**依赖数据表**：`wms_outbound_order`、`wms_outbound_order_item`、`wms_physical_inventory`、`oms_outbound_order`。

---

### 1.3.2 订单出库（打包 + 签出）

**模块目标**：完成配货核验、贴物流标签、称重、按渠道签出，扣减物理库存，回传发货状态与跟踪号。

**用户角色**：WMS 配货员/打包员/签出员。

**核心业务流程（状态机）**

```
PICKING（拣货中）
   │ 打包：按SKU打包 / 按单打包 / 二次分拣 / 订单装箱
   │   扫 SKU 配货成功 → 打印物流标签 → 装袋/装箱贴标
   ▼
PACKING → PACKED（已打包）
   │ 订单签出：扫面单跟踪号 → 称重 →【订单签出】
   │   物理库存正式扣减（quantity -= 实发；释放 reserved_qty）
   │   生成链路二物流费流水（按所选物流产品计价）
   ▼
SHIPPED（已发货） ──同步──► OMS=SHIPPED → ERP 订单状态更新；回传 tracking_no
   ▼
COMPLETED（完成）
```

**关键业务规则**

- **库存扣减时机**：签出（SHIPPED）时才真正 `quantity -=`，并清掉对应 `reserved_qty`（下架时只是锁定）。
- **次品+电子类强制拍照（BR-04）**：若涉及不良品出库或电子类，签出环节强制上传照片。
- **物流费计费（链路二，C6）**：签出成功且出库单关联了 `logistics_product_id` 时，生成 `wms_client_billing_record`（幂等：同 `biz_id` 不重复计费）。
- 签出支持按渠道签出或自动选择渠道；称重数据写回用于体积重核算。

**依赖数据表**：`wms_outbound_order`、`wms_physical_inventory`、`wms_client_billing_record`、`oms_outbound_order`、`erp_order`。

---

## 1.4 退货管理 ★ 模糊点专项（质检环节）

**模块目标**：处理退回货物的重新入库，**增加质检环节**判定良品/次品，决定回库分区，保证库存品质可控。

**用户角色**：WMS 退货收货员 + 质检员。

**设计定位**：退货 = "带质检的重新入库"（对齐《系统文档》§退货管理）。复用入库单结构，`source_type=RETURN`，但状态机插入质检阶段。

**核心业务流程（状态机，含质检三态）**

```
RETURN_PENDING（待退货收货）
   │ 收货员清点退回货物
   ▼
QC_PENDING（待质检）
   │ 质检员逐 SKU 判定
   ├──► QC_PASS（质检通过）──► 上架到【退货区/标准区】，quality=GOOD（可再分配）
   └──► QC_FAIL（质检失败）──► 上架到【不良品区】，quality=DAMAGED（不可分配）
   ▼
PUTAWAY → COMPLETED（退货入库完成）──同步──► OMS 退货单状态更新
```

**质检与库存处理规则（关键）**

| 质检结果 | 回库分区 | `wms_physical_inventory.quality` | 是否可被出库分配 |
|---|---|---|---|
| **质检通过** | 退货区 或 标准区 | `GOOD` | ✅ 可（退货区/标准区均可分配，见分区规则） |
| **质检失败** | 不良品区 | `DAMAGED` | ❌ 不可（不良品区/暂存区不可分配） |

- **次品 + 电子类强制拍照（BR-04）**：质检失败且为电子类时，必须上传质检照片留证。
- 质检通过的退货货物生成新批次 `wms_physical_inventory`（新 `inbound_date`，FIFO 重新排队）。
- 质检结果写入退货单明细，作为后续与货主对账/理赔依据。

**依赖数据表**：`wms_inbound_order`（`source_type=RETURN`）+ 质检扩展字段（见附录 A `wms_return_qc_item`）、`wms_physical_inventory`、`wms_zone`。

---

## 1.5 盘点管理（平台专属）

**模块目标**：库存盘点是物理动作，**仅平台/WMS 服务商可执行**（货主侧已删除，见 [2.7](#27-库存模块调整核心)）。盘点调整直接修正 SSOT。

**用户角色**：WMS 服务商管理员/盘点员。

**核心业务流程**

```
新建盘点任务（按仓库/排/库位/SKU 范围）
   ↓ 冻结相关批次（暂停分配）
   ↓ 录入实盘数 → 对比账面数 → 生成盈亏差异
   ↓ 复核确认 → 调整 wms_physical_inventory.quantity（唯一可改库存的合法入口之一）
   ↓ 同步库存快照回 OMS；产生盘盈/盘亏流水
```

**关键业务规则**

- 盘点是 ERP+OMS **不可触碰**的操作（C4）；货主端无任何写库存入口。
- 盘点调整须留盈亏台账，便于审计。

**依赖数据表**：`wms_physical_inventory`、`wms_stock_flow`（库存流水）、盘点任务表（附录 A `wms_stock_take`）。

---

## 1.6 权限管理

**模块目标**：区分平台内两类角色的可见范围。

| 角色 | 权限范围 |
|---|---|
| **平台管理员** | 全部界面：仓库创建/预览、账户开通、货架分配、角色管理、财务应收 |
| **平台普通员工** | 仅货物操作：收货、上架、下架、打包、签出、退货质检 |

**实现**：基于 BallCat RBAC（`sys_role`/`sys_menu`/`sys_role_menu`）。平台超管角色 `ROLE_SUPER_ADMIN`；普通操作员 `ROLE_WMS_OPERATOR`。菜单按角色裁剪（C2）。

**依赖数据表**：`sys_role`、`sys_menu`、`sys_role_menu`、`sys_user`。

---

## 1.7 财务系统（应收）

**模块目标**：管理**链路一**（平台→WMS 服务商）的应收账单。

**用户角色**：平台超管/财务。

**页面核心字段**（对齐 `wms_monthly_bill`）：账期、WMS 服务商、货架租金、入库费、出库费、配送费、退货费、验货费、司机费、合计、状态、确认时间、付款时间。

**核心业务流程（账单状态机，决策 K）**

```
月初定时任务（cron 0 0 2 1 * ?）汇总上月
   ↓ 货架租金 = Σ(当月有效 wms_rack_assignment.monthly_fee)
   ↓ 操作费   = 汇总 wms_billing_record（按 wms_tenant_id）
   ▼
DRAFT（草稿）→ 平台核对 → CONFIRMED（已确认）→ 线下对账收款 → 手动标记 PAID（已付款）
                                              └→ DISPUTED（争议）
```

**关键业务规则**

- 计费对象 = WMS 服务商；货主的操作量通过 `wms_billing_record.erp_tenant_id` 归集到其所属服务商（C6/决策 I）。
- 个性化费率：通过 `wms_tenant_rate_discount` 按百分比折扣叠加在基础费率（决策 L）：`实收 = 基础单价 × (1+discount_pct) × 数量`。
- 结算线下对账，系统仅手动标记付款状态（决策 K）。

**依赖数据表**：`wms_monthly_bill`、`wms_billing_record`、`wms_rack_assignment`、`wms_fee_rate_card`、`wms_tenant_rate_discount`。

---

# 第二部分 ERP+OMS 用户（货主）

> 用户群体：货主（第二层租户 `ERP_USER`），角色 `ROLE_TENANT_ADMIN/USER`。
> 数据可见性：MyBatis-Plus 多租户插件自动注入 `WHERE tenant_id = 本货主`（C2）。

## 2.1 基本不变模块（说明）

以下模块**界面与交互不变**，但全部纳入多租户隔离（自动注入 `tenant_id`），开发时仅需确认查询走插件、索引含 `tenant_id` 首列：

| 模块 | 不变说明 | 多租户适配点 |
|---|---|---|
| 数据分析 | 业务内容不变 | 统计查询自动按 `tenant_id` 过滤 |
| 系统管理 | 不变 | 用户/角色限定本租户 |
| 日志管理 | 不变 | 日志带 `tenant=` MDC |
| 订单管理 | 不变 | `erp_order` 加 `tenant_id` 隔离 |
| 财务管理 | 不变 | `wb_report_detail` 等加 `tenant_id` |
| 物流商管理 / 采购单管理 / 物流单管理 | 不变 | 对应表加 `tenant_id` |

---

## 2.2 采购入库单（改造）

**模块目标**：保留采购→物流→入库单生成链路，但**入库执行权移交平台**——产生入库单后推送海外仓平台收货上架，本端只跟踪状态。

**用户角色**：货主采购/仓管。

**改造前后对比（关键）**

```
改造前：创建采购单 → 创建物流单 → 由物流单创建入库单 → 本端审批入库通过（本端改库存）
改造后：创建采购单 → 创建物流单 → 生成入库单 → 推送平台
        平台收货+上架（写 SSOT）→ 回传状态 → 本端入库单状态自动流转（本端不改库存）
```

**核心业务流程（OMS 状态机）**

```
DRAFT（草稿）→ 提交 → PENDING → 写 oms_sync_outbox → 推送平台
   ▼
SENT（已下发）→ RECEIVING（尾程收货中）→ COMPLETED（收货完成/已入库）
   状态由 WMS 收货/上架回传驱动（C3 同进程回调）
```

**关键业务规则**

- 采购入库单**必须关联采购来源**（`source_type=PURCHASE`，关联 `wms_purchase_order_id`）——这是与自定义入库单的核心区别（见 2.3）。
- 推送用 Outbox 防丢单（`oms_sync_outbox`，同事务写入）。
- 本端**不再有"审批入库"改库存**动作；库存以平台上架回传为准（C4）。

**依赖数据表**：`wms_purchase_order`、`oms_inbound_order`（`source_type=PURCHASE`）、`oms_sync_outbox`。

---

## 2.3 自定义入库单（新增）★ 模糊点专项

**模块目标**：允许货主在**没有采购单来源**的情况下，手工创建入库单（如样品、补货、第三方调入）并推送平台。

**用户角色**：货主仓管。

**页面布局**（与采购入库单界面一致：搜索栏 + 列表 + 新建）。

**新建表单（两部分，对齐《系统文档》）**

1. **入库信息**：目的仓库、预计到达信息、跟踪单号。
2. **货品选择**：搜索 SKU 选择货品 + 录入数量。

**与采购入库单的数据模型区别（核心）**

| 维度 | 采购入库单 | 自定义入库单 |
|---|---|---|
| `source_type` | `PURCHASE` | `MANUAL` |
| 是否关联 ERP 采购订单 | **必须**（`purchase_order_id` 非空） | **不关联**（`purchase_order_id` 为 NULL） |
| 数据来源 | 由采购单/物流单带出 SKU 与数量 | 货主手工搜索 SKU、手填数量 |
| 其余流程 | **完全一致**：都推送平台、平台收货上架、状态回传 | 同左 |

> 即：二者共用 `oms_inbound_order` 表，仅靠 `source_type` 与 `purchase_order_id` 是否为空区分。下游 WMS 处理逻辑统一。

**依赖数据表**：`oms_inbound_order`（`source_type=MANUAL`，`purchase_order_id IS NULL`）、`oms_inbound_order_item`、`oms_sync_outbox`。

---

## 2.4 销售出库单（改造）

**模块目标**：货主拉取平台订单生成销售出库单，推送海外仓平台执行下架→拣货→发货，本端跟踪状态。

**用户角色**：货主运营。

**核心业务流程**

```
拉取平台销售订单 → 生成 oms_outbound_order（source_type=PLATFORM，关联 erp_order_id）
   ↓ 货主选择物流产品（logistics_product_id，见 3.2）→ 锁定价格快照 logistics_fee
   ↓ 推送平台
SENT → 平台下架（不可再取消）→ PICKING → PACKED → SHIPPED（回传 tracking_no）→ COMPLETED
```

**关键业务规则**

- **平台单确认策略（BR-03）**：按店铺 `auto_confirm`，默认人工确认；未映射 SKU 强制人工确认。
- **下架后不可取消**：推送并被平台下架后，OMS 取消入口关闭。
- 选择物流产品是计费前提（链路二）；未选产品（如自提）则不产生物流费。

**依赖数据表**：`oms_outbound_order`（`source_type=PLATFORM`）、`erp_order`、`wms_logistics_product`。

---

## 2.5 自定义出库单（新增）★ 模糊点专项

**模块目标**：允许货主创建**非平台订单来源**的出库单（如 B2B 批发、调拨、赠品）并推送平台。

**用户角色**：货主运营/仓管。

**页面布局**：与销售出库单一致（搜索栏 + 列表 + 新建）。

**新建表单（两部分）**：① 出库信息（目的地、收件信息、物流产品选择）② 货品选择（搜索 SKU + 数量）。

**与销售出库单的数据模型区别**

| 维度 | 销售出库单 | 自定义出库单 |
|---|---|---|
| `source_type` | `PLATFORM` | `MANUAL` |
| 是否关联 ERP 平台订单 | **必须**（`erp_order_id` 非空） | **不关联**（`erp_order_id` 为 NULL） |
| 收件信息 | 来自平台订单 | 货主手工填写 |
| 下游流程 | 共用 `oms_outbound_order`，处理一致 | 同左 |

**依赖数据表**：`oms_outbound_order`（`source_type=MANUAL`，`erp_order_id IS NULL`）。

---

## 2.6 退货订单管理 / 自定义退货单

**模块目标**：货主从平台拉取可退货订单发起退货，或手工创建自定义退货单；货物退回后由平台质检入库（见 [1.4](#14-退货管理-模糊点专项质检环节)）。

**用户角色**：货主售后。

**核心业务流程**

```
退货订单：从平台拉取可退货订单 → 创建退货单 → 推送平台
自定义退货单：手工填写退货 SKU/数量 → 推送平台
   ▼
平台退货收货 → 质检（通过/失败）→ 回库（良品区/不良品区）→ 状态回传货主
```

**关键业务规则**

- 退货单 `source_type=RETURN`，复用入库链路 + 质检阶段。
- 货主可见质检结果（通过/失败、回库分区、质检照片），作为理赔依据。

**依赖数据表**：`oms_inbound_order`（`source_type=RETURN`）、`wms_return_qc_item`。

---

## 2.7 库存模块调整（核心）★ 模糊点专项

> 本节落实 C4：**ERP+OMS 不再拥有任何库存写入能力，库存唯一真源在 WMS。**

### 2.7.1 删除"盘点管理"

- **明确删除** ERP+OMS 侧的盘点管理菜单与功能（《系统文档》§盘点管理已指明）。
- 盘点是物理动作，归平台/WMS 服务商（见 [1.5](#15-盘点管理平台专属)）。
- 迁移动作：移除前端路由 `/wms/stock-take`（货主侧）、下线对应后端写接口。

### 2.7.2 库存总览 / 明细 / 记录：改为只读快照

| 模块 | 改造后行为 |
|---|---|
| **库存总览** | 只读。按货主 `erp_tenant_id` 聚合展示 SSOT 快照（总量/可用/锁定） |
| **库存明细** | 只读。展示批次级明细（SKU/库位/批次/数量/品质），数据 100% 来自 `wms_physical_inventory` |
| **库存记录** | 只读。展示库存变动流水 `wms_stock_flow`（入库/出库/盘点/退货） |

**数据交互逻辑（关键）**

```
货主库存查询请求
   ↓ Service 调用 WMS 库存查询接口（同进程 Bean，C3）
   ↓ WMS 侧按 erp_tenant_id 过滤 wms_physical_inventory（@InterceptorIgnore + 显式条件）
   ↓ 返回只读 VO（绝不暴露写接口）
   ↓ 货主端渲染（无任何"调整库存"按钮）
```

- 货主侧**完全没有**修改库存的入口（新增/编辑/删除批次均不存在）。
- 同步频率：BR-06 默认 30 分钟可配；关键动作（上架/签出）由 WMS 实时回传刷新快照。

### 2.7.3 库存预测 / 库存配置（增强）

**库存配置**升级为**两种预测模式**（对齐《系统文档》诉求）：

| 模式 | 说明 |
|---|---|
| **固定值模式** | 沿用现有逻辑，设定固定安全库存/补货点 |
| **自定义模式** | 用户自定义计算规则：选取历史数据源（近 1 周/1 月的平均销量、中值、众数等）+ 自定义系数，组合成预测公式 |

**自定义模式数据模型**（附录 A `oms_forecast_rule`）：存储规则表达式（数据源枚举 + 聚合函数 + 系数），预测时按规则动态计算。

**依赖数据表**：`wms_physical_inventory`（只读）、`wms_stock_flow`（只读）、`oms_forecast_rule`。

---

# 第三部分 WMS（服务商运营端）

> 用户群体：WMS 服务商（第一层租户），角色 `ROLE_WMS_MANAGER/OPERATOR`。
> 数据可见性：按 `wms_tenant_id` 隔离，只看自己货架上的货与自己的客户。

## 3.1 账户管理（开通货主）

**模块目标**：WMS 服务商为自己的客户（货主）开通 ERP+OMS 账号，并限定其可用的仓库/区域。

**用户角色**：WMS 服务商管理员。

**页面核心字段**：货主公司名、联系方式、地址、可用仓库/区域、信用额度（可选，参考易仓"申请额度"）。

**核心业务流程**

```
WMS 服务商【开户】
   ↓ 创建 sys_tenant(tenant_type=ERP_USER, parent_wms_tenant_id=本服务商)
   ↓ 创建货主管理员 sys_user + 绑定 + 授予 ROLE_TENANT_ADMIN
   ↓ 配置该货主可使用的仓库/区域范围
[ENABLED] ⇄ [DISABLED]
```

**关键业务规则**

- 货主必须挂在某个 WMS 服务商下（`parent_wms_tenant_id` 非空）——这是双层租户的强约束（C1）。
- 服务商只能管理自己开通的货主（`WHERE parent_wms_tenant_id = 本服务商`）。
- （可选）信用额度：货主消费累计到额度上限前可先用后付，结清后恢复。

**依赖数据表**：`sys_tenant`、`sys_user`、`sys_user_tenant`、（可选）`wms_client_credit`（附录 A）。

---

## 3.2 物流产品管理（链路二计费）

**模块目标**：WMS 服务商把物流能力包装成"物流产品"（按尺寸/取货/配送等维度组合定价），上架推送给自己的货主选择，作为对货主收费（链路二）的依据。

**用户角色**：WMS 服务商管理员。

**页面核心字段**（对齐 `wms_logistics_product`）：产品编码、名称、尺寸段、重量/体积上限、取货方式、配送方式、配送时效、目的地区域、包装、价格、币种、计价单位、可见性（PUBLIC/RESTRICTED）、状态。

**核心业务流程**

```
配置产品（组合维度 + 定价）→ 设可见性 → 上架
   ├─ PUBLIC：本服务商所有货主可见（统一默认价）
   └─ RESTRICTED：仅 wms_logistics_product_grant 授权的货主可见
   （可对特定货主设 custom_price 专属价）
   ▼
货主下单选产品 → 锁定价格快照 → 发货后生成 wms_client_billing_record
   ▼
月末汇总 wms_client_monthly_bill（按货主分别出账）→ 线下对账标记 PAID
```

**关键业务规则（决策 O / O-2）**

- 取价：`COALESCE(grant.custom_price, product.price)`；下单即锁快照，事后改价不影响已下订单。
- 下单前服务端二次校验所选产品对该货主确实可见（防前端绕过）。
- 计费幂等：同出库单 `biz_id` 不重复计费。

**依赖数据表**：`wms_logistics_product`、`wms_logistics_product_grant`、`wms_client_billing_record`、`wms_client_monthly_bill`、`oms_outbound_order`。

---

## 3.3 财务系统（收入/支出）

**模块目标**：WMS 服务商查看自己的两端财务。

| 方向 | 含义 | 数据源 |
|---|---|---|
| **收入** | 对货主的物流产品费（链路二应收） | `wms_client_monthly_bill`（按 `wms_tenant_id` 过滤） |
| **支出** | 应付平台的货架租金 + 操作费（链路一应付） | `wms_monthly_bill`（按 `wms_tenant_id` 过滤） |

**用户角色**：WMS 服务商管理员/财务。

**关键业务规则**

- 收入端：可下钻到每个货主的物流费明细（`wms_client_billing_record`）。
- 支出端：只读，由平台生成；服务商核对后线下付款。
- 货主侧也可查看自己的物流费明细（`wms_client_billing_record WHERE erp_tenant_id=本货主`）+ 链路一操作明细（决策 N：货架租金/司机费不透出给货主）。

**依赖数据表**：`wms_client_monthly_bill`、`wms_client_billing_record`、`wms_monthly_bill`。

---

# 附录 A：数据模型补充（《设计方案》待补建表）

> 以下表为本说明书新识别、《设计方案.md》尚未定义的表，建议同步补入设计方案的 §4 Schema。

```sql
-- A1. 仓库主表
CREATE TABLE wms_warehouse (
    id             BIGINT      NOT NULL AUTO_INCREMENT,
    warehouse_code VARCHAR(32) NOT NULL COMMENT '仓库编码',
    warehouse_name VARCHAR(100) NOT NULL,
    warehouse_type VARCHAR(20) NOT NULL DEFAULT 'OVERSEAS' COMMENT 'OVERSEAS海外仓/TRANSIT中转仓',
    country        VARCHAR(50), city VARCHAR(50), address VARCHAR(200),
    contact_name   VARCHAR(50), contact_phone VARCHAR(20),
    status         TINYINT     NOT NULL DEFAULT 1,
    create_time    DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time    DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id), UNIQUE KEY uk_code (warehouse_code)
) ENGINE=InnoDB COMMENT='仓库主表';

-- A2. 分区表（标准/不良品/退货/暂存）
CREATE TABLE wms_zone (
    id           BIGINT      NOT NULL AUTO_INCREMENT,
    warehouse_id BIGINT      NOT NULL,
    zone_type    VARCHAR(20) NOT NULL COMMENT 'STANDARD/DEFECTIVE/RETURN/TEMP',
    zone_name    VARCHAR(50) NOT NULL,
    allocatable  TINYINT     NOT NULL DEFAULT 1 COMMENT '1=可分配库存(标准/退货) 0=不可(不良品/暂存)',
    PRIMARY KEY (id), KEY idx_wh (warehouse_id)
) ENGINE=InnoDB COMMENT='仓库分区';

-- A3. 库位表
CREATE TABLE wms_location (
    id            BIGINT      NOT NULL AUTO_INCREMENT,
    warehouse_id  BIGINT      NOT NULL,
    zone_id       BIGINT      NOT NULL,
    rack_no       VARCHAR(20) NOT NULL COMMENT '排号(计费/分配单位)',
    layer_no      INT, column_no INT,
    location_code VARCHAR(50) NOT NULL COMMENT '如 A1-03-03',
    location_type VARCHAR(20) COMMENT '大/小库位',
    pick_type     VARCHAR(20) DEFAULT 'PICK' COMMENT 'PICK拣货/STORE存储',
    PRIMARY KEY (id),
    UNIQUE KEY uk_wh_loc (warehouse_id, location_code),
    KEY idx_wh_rack (warehouse_id, rack_no)
) ENGINE=InnoDB COMMENT='库位表';

-- A4. 退货质检明细
CREATE TABLE wms_return_qc_item (
    id               BIGINT      NOT NULL AUTO_INCREMENT,
    wms_tenant_id    BIGINT      NOT NULL,
    erp_tenant_id    BIGINT      NOT NULL,
    inbound_order_id BIGINT      NOT NULL COMMENT '关联退货入库单',
    sku_code         VARCHAR(64) NOT NULL,
    qc_status        VARCHAR(20) NOT NULL DEFAULT 'QC_PENDING'
                     COMMENT 'QC_PENDING/QC_PASS/QC_FAIL',
    qc_qty           INT         NOT NULL DEFAULT 0,
    target_zone_type VARCHAR(20) COMMENT '回库分区 STANDARD/RETURN/DEFECTIVE',
    photo_url        VARCHAR(500) COMMENT '次品+电子类强制拍照',
    qc_by            BIGINT, qc_at DATETIME,
    PRIMARY KEY (id), KEY idx_order (inbound_order_id)
) ENGINE=InnoDB COMMENT='退货质检明细';

-- A5. 盘点任务
CREATE TABLE wms_stock_take (
    id            BIGINT      NOT NULL AUTO_INCREMENT,
    wms_tenant_id BIGINT      NOT NULL,
    warehouse_id  BIGINT      NOT NULL,
    scope         VARCHAR(200) COMMENT '盘点范围(排/库位/SKU)',
    status        VARCHAR(20) NOT NULL DEFAULT 'DRAFT'
                  COMMENT 'DRAFT/COUNTING/REVIEW/DONE',
    create_time   DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id)
) ENGINE=InnoDB COMMENT='盘点任务';

-- A6. 库存自定义预测规则（货主）
CREATE TABLE oms_forecast_rule (
    id          BIGINT      NOT NULL AUTO_INCREMENT,
    tenant_id   BIGINT      NOT NULL,
    mode        VARCHAR(20) NOT NULL DEFAULT 'FIXED' COMMENT 'FIXED/CUSTOM',
    data_source VARCHAR(50) COMMENT 'AVG_7D/AVG_30D/MEDIAN/MODE...',
    agg_func    VARCHAR(20), coefficient DECIMAL(10,4) DEFAULT 1,
    expression  VARCHAR(500) COMMENT '自定义计算表达式',
    PRIMARY KEY (id), KEY idx_tenant (tenant_id)
) ENGINE=InnoDB COMMENT='库存预测规则';

-- A7. wms_rack_assignment 扩展（合同附件）
ALTER TABLE wms_rack_assignment
    ADD COLUMN contract_file_url VARCHAR(500) COMMENT '签署合同扫描件';
```

---

# 附录 B：OMS↔WMS 状态机全景

**入库链路**

```
OMS oms_inbound_order:   DRAFT → PENDING → SENT → RECEIVING → COMPLETED
WMS wms_inbound_order:                     PENDING → RECEIVING → PUTAWAY → COMPLETED
同步点：标记到货→RECEIVING；上架完成→COMPLETED（双向同进程回调，C3）
```

**出库链路**

```
OMS oms_outbound_order:  PENDING → SENT → PICKING → PACKED → SHIPPED → COMPLETED
WMS wms_outbound_order:           PENDING → ALLOCATING → PICKING → PACKING → PACKED → SHIPPED → COMPLETED
关键：SENT 后不可取消；SHIPPED 时扣减 SSOT 并产生链路二物流费
```

**退货链路**

```
RETURN_PENDING → QC_PENDING →（QC_PASS→良品区 / QC_FAIL→不良品区）→ PUTAWAY → COMPLETED
```

---

*文档结束 —— 本说明书与《设计方案.md》配套使用；二者冲突时，数据结构以《设计方案.md》为准，业务流程以本说明书为准。*
