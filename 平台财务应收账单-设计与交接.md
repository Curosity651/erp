# 海外仓平台 · 平台财务 · 应收账单 —— 设计与交接文档

> 面向接手的 AI/开发者（尤其是**后端**）。本页前端已完成，以 mock（含内存态）驱动。
> 说明：这页是什么、展示什么、前端怎么搭的、**后端需要实现的接口契约（对齐 `wms_monthly_bill`）**、以及如何切到真实数据。
> 截至 2026-07-01。

---

## 0. 背景与定位

**归属身份 = 海外仓平台 `OVERSEAS_PLATFORM`**（不是 WMS 服务商）。
这是**链路一（平台 → WMS 服务商）**的应收账单（业务需求 C6 / 1.7）。菜单 = `平台财务 → 应收账单`（menu id **170601**，path **`receivable-bill`**，parent `platform-finance` 170600），当前 DB 中 `sys_menu.uri` 仍为占位组件 `common/Placeholder`。

- **计费对象 = WMS 服务商**；货主的操作量通过 `wms_billing_record.erp_tenant_id` 归集到其所属服务商（决策 I）。
- **数据模型** = `wms_monthly_bill`（月度账单头）；底层来源 `wms_billing_record`（操作费流水）+ `wms_rack_assignment`（货架租金源）+ `wms_fee_rate_card`/`wms_tenant_rate_discount`（费率/折扣）。
- **后端现状**：`wms_monthly_bill` 实体/接口**尚未实现**（后端目前只有 `wms_rack_assignment`）。→ 前端 mock 先行，后端按第 3 节契约实现。

> 三身份隔离见 `底层数据表重构设计.md` / `海外仓平台数据分析-设计与交接.md`；`platform-finance` 顶级仅 `OVERSEAS_PLATFORM` 可见（`src/utils/menu-filter.ts`）。

---

## 1. 业务口径（来自业务需求 1.7）

**账单状态机（决策 K）**
```
月初 cron(0 0 2 1 * ?) 汇总上月
  货架租金 = Σ 当月有效 wms_rack_assignment.monthly_fee
  操作费   = 汇总 wms_billing_record（按 wms_tenant_id）
     ▼
DRAFT 草稿 → 平台核对 → CONFIRMED 已确认 → 线下对账收款 → 手动标 PAID 已付款
                                          └→ DISPUTED 争议 →（复核）→ CONFIRMED
```

- 结算**线下对账**，系统仅**手动**标记付款/争议状态。
- 个性化费率：`wms_tenant_rate_discount` 按百分比折扣叠加基础费率（决策 L）：`实收 = 基础单价 × (1+discount_pct) × 数量`。**金额由后端算好**，前端只展示。
- **费用科目**（对齐 `wms_monthly_bill`）：账期、WMS服务商、货架租金、入库费、出库费、配送费、退货费、验货费、司机费、合计、状态、确认时间、付款时间、备注。

---

## 2. 页面展示（v1）

三段式（搜索栏 + ProTable + 详情抽屉），与其它 WMS 列表页同构。

### 搜索栏
- **账期**：月份区间（`YYYY-MM` ~ `YYYY-MM`）
- **WMS服务商**：下拉（数据源 `GET /tenant/wms-operators` → `listWmsOperators`）
- **状态**：多选（草稿/已确认/已付款/争议）
- 按钮：搜索 / 重置 / **生成·重算账单**

### 列表（ProTable）
| 账期 | WMS服务商 | 货架租金 | 操作费小计 | 合计 | 状态 | 确认时间 | 付款时间 | 操作 |

- 「操作费小计」= 入库+出库+配送+退货+验货+司机 6 项之和（前端计算）。
- 「状态」彩色 tag；「操作」= 查看。

### 详情抽屉（点"查看"）—— **v1 到费用构成层，无底层下钻**
- **头部**：账期 / WMS服务商 / 状态 tag / 合计（大字）/ 确认时间 / 付款时间 / 争议原因（DISPUTED 时红条展示 `remark`）。
- **费用构成**：货架租金 → 操作费 6 项（缩进）→ 操作费小计 → 合计应收。
- **底部状态操作（严格跟状态机）**：
  - `DRAFT` → [确认账单]
  - `CONFIRMED` → [标记已付款] / [标记争议（弹窗填 remark）]
  - `DISPUTED` → [重新确认]
  - `PAID` → 只读

### 生成/重算弹窗
- 选**账期**（必填）+ **WMS服务商**（可选，不选=全部）。
- 语义：**草稿态账单重算覆盖，已确认/已付款跳过**；回显「新建 N / 重算 N / 跳过 N」。
- 说明：cron 之外的手动补跑/调试入口（决策 K 的 cron 生成不变，本按钮是补充）。

---

## 3. 后端需实现的接口契约 ⭐

前端 mock 驱动。后端实现下列接口并对齐 VO 后，把 `src/api/platform-finance/receivable/index.ts` 的 `USE_MOCK` 改 `false` 即接通，**前端零改动**。

**接口前缀建议**：`/platform-finance/monthly-bill`

| 方法 & 路径 | 作用 | 入参 | 返回 |
|---|---|---|---|
| `POST /page` | 分页查询 | body=`MonthlyBillQO`，query=`PageParam`(size,page,sort) | `PageResult<MonthlyBillVO>` |
| `GET /{id}` | 详情 | — | `MonthlyBillVO` |
| `POST /generate` | 生成/重算 | body=`GenerateBillDTO` | `GenerateBillResultVO` |
| `POST /{id}/confirm` | 草稿→已确认（争议→已确认亦复用） | — | `MonthlyBillVO` |
| `POST /{id}/pay` | 已确认→已付款 | — | `MonthlyBillVO` |
| `POST /{id}/dispute` | 已确认→争议 | body=`{ remark }` | `MonthlyBillVO` |

> 所有返回统一包 `ApiResult<T>`（`{ code, data, message }`，成功 `code=200`）。

### VO / DTO（权威在 `src/api/platform-finance/receivable/types.ts`）

```ts
type BillStatus = 'DRAFT' | 'CONFIRMED' | 'PAID' | 'DISPUTED'

MonthlyBillVO {            // 对齐 wms_monthly_bill
  id: number
  billMonth: string        // 账期 YYYY-MM
  wmsTenantId: number
  wmsTenantName: string
  rackFee: number          // 货架租金 = Σ 当月有效 rack.monthly_fee
  inboundFee: number       // 以下 6 项 = 汇总 wms_billing_record（按 wms_tenant_id）
  outboundFee: number
  deliveryFee: number
  returnFee: number
  inspectionFee: number
  driverFee: number
  totalAmount: number      // = 货架租金 + 6 项操作费
  status: BillStatus
  confirmedTime?: string   // YYYY-MM-DD HH:mm:ss
  paidTime?: string
  remark?: string          // 争议原因等
}

MonthlyBillQO {
  billMonthStart?: string  // YYYY-MM
  billMonthEnd?: string    // YYYY-MM
  wmsTenantId?: number
  statuses?: BillStatus[]
}

GenerateBillDTO {
  billMonth: string        // YYYY-MM
  wmsTenantId?: number     // 不传=当月全部服务商
}

GenerateBillResultVO {
  created: number          // 新建账单数
  recalculated: number     // 重算覆盖（原草稿）数
  skipped: number          // 已确认/已付款跳过数
}
```

### 后端实现提示
- **平台身份看全部服务商**，勿按单租户收窄；仅 `wmsTenantId` 非空时过滤。
- 生成逻辑与 cron 一致：货架租金取当月有效 `wms_rack_assignment.monthly_fee`（按有效天数/记录），操作费按 `wms_billing_record.wms_tenant_id` 汇总，叠加 `wms_tenant_rate_discount`。
- 状态流转做**幂等/前置校验**（如仅 DRAFT 可 confirm、仅 CONFIRMED 可 pay/dispute），非法流转返回错误码。
- 生成对**已确认/已付款**账单必须跳过（不可被重算覆盖），仅草稿可重算。
- **口径以《业务需求说明书》1.7 与《底层数据表重构设计.md》为准**，本文档只定前端需要的形状。

---

## 4. 前端文件清单（已完成）

**API 层** `src/api/platform-finance/receivable/`
- `types.ts` —— 契约（第 3 节即由此而来）。
- `mock.ts` —— mock + **内存态**（状态流转/生成重算真实生效；后端接通后可整删）。
- `index.ts` —— 4 个接口函数 + `USE_MOCK` 开关 + 后端接口清单注释。

**页面 + 组件** `src/views/platform-finance/receivable-bill/`
- `index.vue` —— 三段式主页（搜索/列表/生成按钮）。
- `components/ReceivableBillDrawer.vue` —— 详情抽屉 + 状态操作 + 争议弹窗。
- `components/GenerateBillModal.vue` —— 生成/重算弹窗。
- `constants.ts` —— 状态元数据、费用科目、`operationSubtotal`、`formatMoney`、`CURRENCY_SYMBOL`。

**路由** `src/router/dynamic-routes.ts`
- `COMPONENT_OVERRIDES['receivable-bill']` → 本页面，绕过占位组件 `common/Placeholder`，**不改数据库**。
- 若后端改 `sys_menu.uri` 指向真实组件，可移除此 override；二者取其一。

**校验**：`npx vue-tsc --noEmit` 通过；`npx eslint` 无 error/warning。前端走 Vite HMR，无需重打包。以 `super_admin`（海外仓平台入口）登录 →「平台财务 → 应收账单」查看。

---

## 5. 待确认 / 可迭代

- **⚠️ 结算币种**：前端暂用 `₽`(RUB)，集中在 `constants.ts` 的 `CURRENCY_SYMBOL`。若平台↔WMS服务商实际按 `¥`(CNY) 结算，改这一个常量。**请后端/业务确认**。
- **v2 底层下钻**（本次未做）：货架租金明细（仓库/排号/月租/有效天数）+ 操作费流水（费用类型 × 货主）。已在讨论中，按需迭代。
- **导出**：列表导出 Excel（当前未加）。
- **汇总统计**：可选加"本期应收合计/已收/未收"概览卡（当前未加）。

---

## 6. 一句话交接
> 前端页面 `src/views/platform-finance/receivable-bill/` 已完成并用 mock（含内存态）跑通；后端实现 `/platform-finance/monthly-bill/*`（契约见第 3 节 / `types.ts`，对齐 `wms_monthly_bill`）后，把 `src/api/platform-finance/receivable/index.ts` 的 `USE_MOCK` 改 `false` 即上线，前端不用动。
