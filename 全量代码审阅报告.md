# 全量代码审阅报告

> 范围:`erp-backend`(约 1043 个 Java 文件 + XML/SQL)与 `erp-frontend`(约 613 个 `.vue`/`.ts`)全量深度审阅。
> 性质:**仅记录、不修改任何代码**。
> 方法:14 个只读子代理分域并行精读(后端 9 域 + 前端 5 域),另加已完成的《库存正确性审计报告》;结果经交叉去重、按模块+严重级别合并,4 个反复出现的"待确认"项已由本人在源码/迁移脚本/配置中亲自核实定论。
> 日期:2026-07-06。
> 关联文档:WMS 核心库存(过账引擎/批次/流水/退货链路)的正确性缺陷见**《库存正确性审计报告》**,本报告只引用不重复。

---

## 一、总体结论(TL;DR)

系统整体架构是**清晰、有纪律**的:三平台身份隔离、MyBatis 双拦截器(行级租户 + 数据权限)、过账引擎单一账本、前端统一 `ApiResult` 契约 + fail-closed 菜单裁剪,主干设计正确。缺陷不在"架构错",而在**三条系统性裂缝**沿着各模块反复渗漏:

1. **"无租户上下文"语义不统一(头号系统性安全主题)** —— 读 fail-open(看到全部)、写盖默认租户 1、白名单拦截器无上下文时静默跳过注入。叠加异步 `ThreadLocal` 丢失,构成跨租户串数/串写的系统性风险。
2. **核心库存双写者冲突** —— `wms_inventory` 被"过账引擎增量"与"聚合器全量覆盖"两个语义互斥的写者同时改,详见《库存正确性审计报告》C-1。
3. **前端契约错配(v-model 命名 / 分页字段 / 取 id / 导出 blob)** —— 单点即导致整块功能静默失效(抽屉打不开、列表恒空、导出不下载、确认流程错单)。

**严重级别汇总(本报告新增,不含库存审计报告的 3C/7H/8M/7L):**

| 级别 | 后端 | 前端 | 合计 |
|---|---|---|---|
| Critical | 2 | 3 | 5 |
| High | 12 | 12 | 24 |
| Medium | 15 | 22 | 37 |
| Low | 若干 | 若干 | 40+ |

---

## 二、架构总览与评价

### 后端
- **分层**:`controller → service(ExtendServiceImpl) → mapper(MyBatis-Plus + XML)`;`WrappersX.lambdaQueryX`、`selectObjs`、`@Version` 乐观锁、`casOrderStatus` 状态机、`@Transactional`。规范统一。
- **租户隔离三拦截器**(`ErpMybatisPlusConfig`):
  - `TenantLineInnerInterceptor`(`ErpTenantLineHandler`)—— 白名单表自动注入 `WHERE tenant_id`(`erp_order/erp_order_item/wb_report_detail/shop/sku/sku_mapping/category/sales_target` 等);**`wms_*` 全部不在白名单**。
  - `DataPermissionInterceptor`(`WmsOwnerDataPermissionHandler`)—— 仅 6 张货主单据表 + `wms_region_inventory` 注入 `erp_tenant_id`。
  - **核心库存三表 `wms_inventory/wms_physical_inventory/wms_stock_flow` 不在任何白名单** → 隔离完全依赖 service 显式带 `erp_tenant_id`(系统性风险根源)。
- **正面基础设施(已核实,均为非 bug)**:
  - DB 唯一键含租户:`wms_inventory.uk_tenant_wh_sku(wms_tenant_id,erp_tenant_id,warehouse_id,sku_code)`、`wms_physical_inventory.uk_batch`、`wms_monthly_bill.uk_bill_month_tenant`、`wms_region_inventory.uk_tenant_region_sku`(V12/V25/V31)。
  - `DashboardService` 并行查询用 `supplyAsyncWithTenant` 在子线程重建 `erpTenantId/wmsTenantId`,**正确规避了 memory 记录的"异步丢租户上下文"坑**。
  - WB 财务同步执行器 `setCurrentTenant(task.getTenantId())` 显式传播租户;调度器无上下文读全部、逐任务 runAs,设计正确。
  - 金额 `/100`(戈比→卢布)每条路径恰好一次,逐路径核对**无 100× 错配**。
  - `TenantResolveInterceptor` 对 Web 请求 **fail-closed**:已登录未绑定→`UNBOUND(-999)` 哨兵(业务表 0 行);租户停用→403。

### 前端
- **契约基线**:axios 拦截器对 JSON 返回 `response.data`(即业务拿到一层 `ApiResult{code,data,message}`),`isSuccess(code===200)`;`json-bigint(storeAsString)` 保 ID 精度;`mergePageParam` 把 `current/pageSize→page/size`;ProTable 读 `result.data.records`;`qs arrayFormat:'repeat'`。全量扫描**未见 `.data.data` 双解包/`res.records` 越层的系统性错配**。
- **权限模型** `hasPermission` fail-closed;`menu-filter.ts` 身份菜单裁剪质量最高(未知身份返回 `[]`、成环守卫、系统/统计子树按实例隔离),与"三平台隔离"口径完全一致、**无缺陷**。
- 三平台订单(WB/Ozon/Yandex)逻辑抽到 `hooks/useOrder*` + `components/Order*Cell` 复用,结构良好。

---

## 三、系统性主题(跨模块,优先处理)

### S1. "无租户上下文"语义三处不一致(安全)
- **读 fail-open**:`ErpOwnerScopeService.readScope()` 无上下文返回 `null` → 查询不收窄 → 看到全部租户。
- **写盖默认租户 1**:`MyMetaObjectHandler` 无上下文时把 `tenant_id` 填默认 1。
- **白名单静默跳过**:`ErpTenantLineHandler` 无上下文时不注入条件(fail-open)。
- **叠加异步 `ThreadLocal` 丢失**(memory `async-loses-tenant-context`):并行/`CompletableFuture` 未显式传播即串数据。
- 影响面:system/tenant(C1)、product/shop(H3)、financial 对账(H2)、warehouse/return(H1)多域独立撞到同一根因。

### S2. `@PreAuthorize` / 越权覆盖不齐(安全,已重新定级)
- **核实结论**:安全白名单 `ignore-urls` 仅含 `/public /actuator /doc.html /swagger /captcha`,**不含** `/system/file`、`/tenant`、`/system/exchange-rate`。故这些接口**并非匿名公网可访问**,而是"**任意已登录身份**均可访问"(缺方法级 `@PreAuthorize` 或对象级 owner 校验的横向越权/IDOR)。据此已把 system/tenant 代理原 H3–H6 由"未授权公网访问"**降级为"越权/IDOR"**。
- 命中点:`OzonOrderController`、`LabelBatchController`、`DashboardController`、租户 provisioning、文件下载 IDOR。

### S3. 前端契约错配单点致整块失效
- v-model 命名 `visible` vs 仓库约定 `open`(过账详情抽屉、财务对账抽屉、多个财务组件)。
- 分页字段 `list` vs ProTable 契约 `records`(店铺列表)。
- 新建取 id(`ApiResult<void>` 却读 `res.data as number`)。
- 导出普遍缺 `responseType:'blob'` + 不调 `remoteFileDownload`(退货入库、销售出库、采购单路径前缀错)。

### S4. 重复实现与死代码坏味道
- 后端:`TaxRateValidator` 死代码(严格校验被 `GenericTaxRateValidator` 仅判 null 架空)。
- 前端:三套货币格式化(`amount-utils`/`currency-utils`/`financial-utils`)语义各异;两套 `getFileExtension`(带点/不带点,同名反义);`sanitizeFileName` 空实现;`SkuBriefVO` 断裂导入(多文件从未 re-export 的路径引入,`vue-tsc` 报错)。

---

## 四、按模块的 Bug 清单(按严重级别)

> WMS 核心库存(过账引擎/批次/流水/入库上架/出库/退货/盘点/调拨)的正确性缺陷见**《库存正确性审计报告》**(Critical 3 · High 7 · Medium 8 · Low 7),此处不重复,仅补充其未覆盖的鉴权/隔离/契约维度。

### 后端

#### 平台运营 / 账单(platform)
- **【High】** 账单生成无 `WMS_OPERATOR` 身份校验:任意身份可触发 `generate`。
- **【Medium】** 计费依赖 `wms_tenant_id` 可靠性(业务表该字段常为 0/脏,应走 `sys_tenant.parent_wms_tenant_id`)。
- **【Medium】** 折扣系数无上下界校验。
- **【Medium】** `DashboardController` 缺 `@PreAuthorize`。
- **范围提示**:memory 记录的"加权平均·分币种·按件分摊(A–E)"月度账务代码在 `platform.finance` 包,**不在本轮 financial 70 文件范围**,financial 代理仅读了 XML,**建议单独排期审计其 Java 实现**。

#### 系统 / 租户(system/tenant)
- **【Critical】** C1 无上下文数据权限 fail-open(见 S1)。
- **【High】** 无上下文写盖默认租户 1(见 S1)。
- **【High】** 白名单拦截器无上下文静默跳过注入(见 S1)。
- **【High】** 租户 provisioning 缺 `@PreAuthorize`/`is_admin` 校验。
- **【Critical→复核】** C2 验证码可重用;实测 `login-captcha:false`,登录未启用验证码,**风险面收窄**(仅其它启用验证码入口受影响)。
- **【High→Medium(重定级)】** 文件下载 IDOR、API Key 落日志、汇率走 HTTP —— 经 S2 核实为"已登录任意身份越权",非公网未授权,按越权定级。

#### 商品 / 店铺 / 同步(product/shop/sync)
- **【High】** 税率校验形同虚设:DTO 只匹配到 `GenericTaxRateValidator`(仅判 null),严格的 `TaxRateValidator` 是死代码 → 可写入 0/负/超精度税率污染定价。
- **【High】** 店铺平台 API Key(Ozon/WB/Yandex)**明文落库** `shop.credential` + 明文入内存 `TEST_CACHE`,无加密。
- **【High】** 无上下文跨租户读写 + SKU 唯一性失效(方案②白名单表无上下文即不注入,见 S1)。
- **【Medium】** `SkuMapping` 不大写归一致孤儿映射;`SyncCursor` 非原子竞态;品类树递归无环路防护(脏数据可死循环挂线程);凭证测试缓存单机 `Map` 集群失效。

#### 财务对账 / 统计(financial/statistics)
- **【High】** H1 财务明细去重键不含 `shop_id`:同租户多 WB 店铺 `rrd_id` 撞号会互相覆盖/丢数,或唯一键冲突整批失败。**待确认现网唯一索引是否含 shop_id**。
- **【High】** H2 对账原生 SQL 含 MySQL JSON `->>` 运算符,租户隔离全靠 `ErpTenantLineHandler` 的 JSqlParser 解析注入;若旧版解析失败静默跳过=跨租户串数、抛异常=500。**建议打印最终 SQL 实测**(与 S1 同源)。
- **【Medium】** 去重"读-改-写"非原子并发重复插/整批失败;任务 FAILED 无自动重试(财务明细静默不完整→对账大面积 ANOMALY);统计徽章 ANOMALY 口径与列表过滤不一致;对账实收跨币种直接相加;两屏达成率用不同日期列+平台口径;销售目标 `currency` 被忽略(非 RUB 目标对 RUB 销售)。

#### 订单(order)
- **【High】** `OzonOrderController` 缺 `@PreAuthorize`(见 S2)。
- **【High】** `LabelBatchController` 无鉴权。
- **【Medium】** WB 状态转换 fallback 到 `READY_TO_SHIP`(可复活已发货订单);复活订单无重新预占;月度销售统计口径 `!= CANCELED` 与 javadoc 声明的 `DELIVERED` 不符(高估营收)。

#### 入库 / 出库 / 仓库 / 退货(wms,鉴权/隔离补充)
- **【High】** 入库单写操作(编辑/提交/删除)缺 `assertOwnership` → 跨租户改单。
- **【High】** 入库 mapper 表名 `purchase_inbound_order_item` 缺 `wms_` 前缀 → SQL 错。
- **【High】** 拣货 `pick()` 无 CAS + **拣货分配表 `wms_outbound_pick_allocation` 无任何唯一键(已核实 V28)** → 并发重复拣货超扣,无 DB 兜底。
- **【High】** 批次/快照查询信任前端传入 `erpTenantId` → 跨租户读。
- **【Medium】** 调拨单无鉴权/隔离、信任前端 `erpTenantId`;仓库分页泄漏 FBO 仓;退货单号非集群安全;超退竞态;假乐观锁;`logisticsProductId` 无 owner 校验。

### 前端

#### 鉴权 / 会话(fe-auth)
- **【High】** 常量路由注册了货主表单页 → 跨身份直接 URL 访问。
- **【Medium】** 菜单加载 `length===0` 无限重定向循环;权限仅 UI 隐藏;403 分支缺可选链。

#### WMS 视图(fe-wms)
- **【Critical】** C1 过账"操作单详情"抽屉打不开:`StockPostingDetailDrawer` 用 `props.visible/update:visible` 且内部同名 ref 遮蔽,父用 `v-model:open` → watch 不触发,详情+明细分页全废。
- **【Critical】** C2 新建采购单"确认并提交"取 id 错配:`createPurchaseOrder` 声明 `ApiResult<void>`,新建分支却 `res.data as number` 传给 `confirm` → 错单。
- **【High】** 采购单导出路径前缀 `/purchase/order/export` 错(应 `/wms/purchase-order/export`,必 404);退货入库/销售出库导出"只弹成功不下载"(缺 blob);销售出库导出不带平台筛选;发货单消费 VO 不存在字段致发/入统计与在途恒 0;发货单权限码抽屉(`shipping-purchase-order`)与列表(`shipping-order`)不一致;退货合格数可超退货总数(库存超记);盘点表单页多处契约错配(疑废弃但 URL 可直达);库存区域下钻筛选失效展示全量。
- **【Medium】**(10 项)编辑仓库静默清空 address/remark;FlowList 筛选参数名与 QO 全线不匹配;安全库存快捷弹窗覆盖已有通知配置;销售出库确认页不展示缺货明细;付款金额硬编码 ¥;采购表单页未注册离开守卫;storage-overview 缺 isSuccess 守卫;破坏性库位/货架操作缺前端权限门;退货质检 FAIL 照片仅传 count 未上传;采购入库物流明细兜底缺口(`if(res.data)` 对空数组误判)。

#### 订单 / 商品 / 店铺(fe-order/product/shop)
- **【High】** 店铺列表恒空:`listShops` 返回 `{list}` 而 ProTable 读 `records`。
- **【High】** Ozon 商品总价列字段抄自 Yandex(`productTotalAmount` 等 VO 未声明)恒显 `-`;SKU 产品名搜索同时写 `chineseName`+`russianName`(AND 语义)几乎恒查空。
- **【Medium】** Yandex 打印 `platform="yandex"` 小写与 Ozon/WB 首字母大写不一致(打印历史可能恒空);品类"停用(status=0)"过滤被真值判断 `!status` 短路;SKU 供应商名回填死代码 + 重复 `name="supplierCode"`;确认发货 doRequest 失败也关窗刷新。

#### 平台财务(fe-platform-finance)
- **【High】** `OrderReconciliationDrawer` 详情永不加载(prop `visible` vs 父 `v-model:open`);3 个财务组件用 `visible/update:visible` 违反仓库 `open/update:open` 约定。
- **【Medium】** WB 同步"近 30 天"永不触发。

#### 组件 / 工具 / 公共层(fe-components)
- **【High】** `LovModal.handleClose` 在无 `searchOptions` 时 `undefined.clearForm()` 崩溃;`Lov.selectOptions` 用 `props.customOptionTitle` 未走兜底,未传时回显已选项即崩溃。
- **【Medium】**(8 项)反选池 `handleDeselect` 只改 keys 不改 rows(父拿脏行);`WarehouseSelect` 过滤变更后不清空失效选中;utils 同名函数语义冲突(`getFileExtension` 带点/不带点);`sanitizeFileName` 空实现却被当清洗步骤调用;三套货币格式化 + 字母码/数值码契约存疑;公共 `SkuMappingBadge` 反向依赖 `views/`(分层倒置/循环依赖);`i18n-data.ts` import 端点缺前导斜杠;`availableLocales` 含伪语言 `default-local-import`。

### Low / 坏味道(择要,完整见各子报告)
- 后端:批次分事务部分提交;`job/task.totalRecords` 因 update 计数虚高;`SalesTargetController.save` 收裸实体无校验。
- 前端:多处 `console.log` 遗留生产路径(货币渲染/分页加载/下载);`Lov` 位运算符 `| ''` 误用(应 `||`,被 `enableI18n:false` 硬编码掩盖且致 i18n 全失效);`fileDownload` 同步 `revokeObjectURL` 可中断大文件;`overrideProperties` source 缺键覆盖 target 为 undefined;`pruneTree` 改入参副作用;多处 page 接口类型漏 `PageResult` 包装(仅 TS 报错);预测图表 `buildZoneMarkAreas/buildStockoutMarkLine` 死代码;`emitter.off` 无 handler 清空全部监听;`api/tenant` 注释仍写"平台超管"与口径不符。

---

## 五、本人核实结论(原 4 项"待确认"定论)

| 项 | 核实结果 | 定论 |
|---|---|---|
| SecurityFilterChain 是否 permitAll `/system/file` `/tenant` `/system/exchange-rate` | `ignore-urls` 仅 `/public /actuator /doc.html /swagger /captcha`,**不含**上述路径 | **非公网未授权**;实为"任意已登录身份越权/IDOR",相关项按越权定级(S2) |
| `CurrencyUtils.formatCurrency` 除 100 是否致 CNY 100× | 确认 `divide(100)`;仅 Wb/Ozon 导出 CNY 列调用,前后端均按子单位处理 | **非 bug**(除非 `convertedAmount` 存主单位——需后端确认存储单位) |
| DB 唯一约束(库存/物理/账单/配置) | V12/V25/V31 均含租户维度唯一键;但 **V28 拣货分配表无唯一键** | 库存/账单有 DB 兜底;**拣货分配确认无兜底 → 证实 wms-outbound H1** |
| traceId 是否仅服务端 | 未在前端消费路径发现回传/日志泄漏 | 低风险,不单列 |

---

## 六、修复优先级建议

1. **P0(数据正确性/资金)**:库存双写者冲突(库存审计 C-1);过账详情抽屉 C1、新建采购单取 id C2(前端功能全废);财务明细去重键补 `shop_id`;拣货分配表补唯一键 + `pick()` 加 CAS。
2. **P0(安全)**:统一"无上下文"语义(读也 fail-closed);核心库存三表纳入隔离白名单或强制 service 带 `erp_tenant_id`;补 `OzonOrderController/LabelBatchController/DashboardController/provisioning` 的 `@PreAuthorize`;店铺 API Key 加密落库。
3. **P1(功能失效)**:店铺列表 `list→records`;导出统一 blob + `remoteFileDownload` + 修正路径前缀;FlowList/区域下钻筛选参数对齐 QO;财务/过账/组件抽屉 v-model 统一 `open`。
4. **P1(公共层健壮性)**:`LovModal/Lov` 崩溃兜底;合并三套货币格式化与两套文件工具、实现 `sanitizeFileName`、修 `SkuBriefVO` 导入路径。
5. **P2**:税率严格校验接回、财务口径统一(ANOMALY/达成率日期列/币种)、订单状态 fallback 与月度统计口径、各类 Medium/Low。

---

## 七、附录:审阅覆盖

- **后端 9 域**:platform 运营/账单、system/tenant 隔离基础设施、product/shop/sync、financial 对账 + statistics 统计、order 三平台、wms 入库、wms 出库、wms 仓库/退货、wms 库存过账盘点调整(+《库存正确性审计报告》5 代理)。
- **前端 5 域**:auth/会话、wms 视图(约 180 文件)、order/product/shop、平台财务、组件/工具/公共层。
- **未覆盖/建议续审**:`platform.finance` 月度账务 Java(A–E 分摊);pro-components 第三方库层;`components` 中 Button/Upload/Editor/Captcha 等基础件;SKU 文件上传两组件(OSS key 处理)。

> 本报告仅记录,未改动任何代码。修复实施建议按第六节优先级分批进行,每批改动后回归对应身份链路。
