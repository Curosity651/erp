# 后端实施 · 提示词

> 用途：把本文件正文整段复制，粘贴给另一个 Claude（后端会话），让它按前端已交付的 5 份"设计与交接文档"实现对应后端。
> 配套文档（项目根）：`海外仓平台数据分析-设计与交接.md`、`平台财务应收账单-设计与交接.md`、`下架拣货-设计与交接.md`、`打包签出-设计与交接.md`、`退货质检-设计与交接.md`。

---

你是本 ERP 项目的后端负责人。前端已由另一位完成了 5 个页面，全部以 mock 驱动、并为每个页面写好了"设计与交接文档"和明确的接口契约。你的任务：按这些契约实现对应的后端，让前端把 mock 开关关掉即可上线。你被授权全程自主进行，无需向我逐步确认——自行编译、跑测试、写迁移、迭代，直到交付。

## 一、先读这 5 份权威文档（项目根目录）
1. 海外仓平台数据分析-设计与交接.md   → 接口前缀 /api/platform-dashboard
2. 平台财务应收账单-设计与交接.md     → 接口前缀 /platform-finance/monthly-bill
3. 下架拣货-设计与交接.md             → 接口前缀 /wms/outbound-picking
4. 打包签出-设计与交接.md             → 接口前缀 /wms/outbound-shipping
5. 退货质检-设计与交接.md             → 接口前缀 /wms/return-qc
每份文档的「第 3 节 后端需实现的接口契约」是权威规格（VO/DTO 字段、路径、方法、返回）。字段以对应前端 src/api/.../types.ts 为最终准。
同时通读项目根 `底层数据表重构设计.md`、`业务需求说明书.md`（1.3/1.4/1.7 节）作为业务口径来源。

## 二、工程与环境（Git Bash）
- 后端 erp-backend：Spring Boot 2.7.18 + BallCat 2.0.0-SNAPSHOT + Java 8 + MyBatis-Plus + MySQL，主模块 admin。
- 数据库：MySQL 127.0.0.1:3307，库 erp，root/123456。
- 编译+测试（务必跑 test，别只 compile）：
  cd erp-backend && export JAVA_HOME=/c/tools/jdk8u492-b09 && /c/tools/apache-maven-3.9.16/bin/mvn -pl admin -am test -o
- 打包（先停 8081 再 clean）：
  /c/tools/apache-maven-3.9.16/bin/mvn -pl admin -am clean package -o -q -DskipTests
- 运行（必须 java 全路径）：
  /c/tools/jdk8u492-b09/bin/java.exe -jar admin/target/admin-2.0.0-SNAPSHOT.jar   # 端口 8081
- 停 8081（PowerShell）：
  Get-NetTCPConnection -LocalPort 8081 -State Listen | %{ Stop-Process -Id $_.OwningProcess -Force }
- 迁移：mysql --default-character-set=utf8mb4 -h127.0.0.1 -P3307 -uroot -p123456 erp < erp-backend/sql/migration/Vxx__*.sql
  先查 sql/migration 下当前最高版本号，从其后顺延（此前约定下一个是 V26，若已被占用则往后取）。DB 已备份，勿再备份。

## 三、架构铁律（务必遵守）
- 三个隔离身份：海外仓平台 OVERSEAS_PLATFORM(sys_tenant.id=-1，看全部货主) / WMS 服务商 WMS_OPERATOR / 货主 ERP_USER。勿用"平台超管"表述。
- 这 5 个页面全部归属【海外仓平台】：聚合/查询要"看全部货主"，不要按单租户收窄；仅当入参带 erpTenantId/wmsTenantId 等过滤条件时才过滤。
- 隔离机制：TenantContext(ThreadLocal, runAs)、ErpTenantLineHandler(SQL级租户注入)、WmsOwnerDataPermissionHandler(货主货物维)、ErpOwnerScopeService.readScope()。后台/定时任务无上下文时用 runAs 包裹，否则数据错归。
- 台账/库存读端点按货主收窄走 query service + ErpOwnerScopeService，勿动全局 handler（会破坏过账幂等）。
- 财务/库存改动稳着来：加事务 + 单测；wms_inventory 是 4 键(wms_tenant_id,erp_tenant_id,warehouse_id,sku_code)、wms_region_inventory/wms_inventory_config 按 (erp,...) 唯一——任何按 (warehouse/region,sku) 的 2 键 selectOne 在多货主下都会崩，必须带 erp。
- 软删=@TableLogic private Long deleted；台账/库存/流水表无 deleted；审计=create_by/create_time/update_by/update_time。
- **禁止改动范围**：不允许改变原有 ERP+OMS 系统中关于采购单、物流单、出库单等既有单据的逻辑设置，也不允许改动三方平台（Wildberries/Ozon 等）API 接入相关代码。你只做本次 5 个平台侧模块的新增/对接；若必须与既有单据交互，只能通过既有的回调/Outbox 接口读写状态，不得修改其原有业务逻辑。

## 四、OMS↔WMS 双向一致性（重点）
货主(ERP+OMS)会产生出库订单、退货单等单据并推送到平台(WMS)执行——出库单 oms_outbound_order→wms_outbound_order、退货/入库单 oms_inbound_order→wms 侧作业。你实现的平台侧每一步作业（收货/上架/下架/打包/签出/退货质检）的状态流转与库存变化，都必须准确回传并同步 OMS 对应单据（oms_outbound_order / oms_inbound_order / erp_order 等），保证【货主】与【平台】两种身份看到的单据状态、数量、库存快照始终一致。交互走 OMS↔WMS 同进程回调（C3）+ Outbox(oms_sync_outbox) 防丢单；且状态约束在两侧同时成立——例如下架后 OMS 端取消入口关闭、签出(SHIPPED)才真正扣物理库存并回传 tracking_no。任何写库存/改状态都要考虑幂等，避免同一单据被 OMS/WMS 重复触发导致数量或状态错乱。

## 五、统一接口约定
- 所有返回包 ApiResult<T> = { code, data, message }，成功 code=200。
- 分页：入参 PageParam{ size, page, sort }，返回 PageResult<T>{ records, total }。
- 路径/方法/字段严格对齐各文档第 3 节与前端 types.ts。字段名、可空性、枚举值不得改动（否则前端要改）。

## 六、各模块要点（细节看文档，勿遗漏铁律）
1. platform-dashboard：只读聚合，跨全部货主。运营总览KPI / 仓容利用率(库位占用+分区) / 吞吐趋势(单据数) / 货主排名。数据源见文档"后端实现提示"。
2. monthly-bill(应收账单)：对齐 wms_monthly_bill；计费对象=WMS服务商；货架租金=Σ有效 rack.monthly_fee，操作费=汇总 wms_billing_record(按 wms_tenant_id)，叠加 wms_tenant_rate_discount；状态机 DRAFT→CONFIRMED→PAID/DISPUTED，手动标记；/generate 草稿重算覆盖、已确认/已付款跳过；月初 cron 生成。
3. outbound-picking(下架)：FIFO 分配(仅良品/单仓/按 inbound_date→pick_order→id 升序，SELECT FOR UPDATE，reserved_qty+=take)；缺货整单挂起 BACKORDER 不可部分下架；下架转 PICKING 后不可取消；/preview 只算不锁，/pick 加行锁。
4. outbound-shipping(打包签出)：/pack PICKING→PACKED；/ship PACKED→SHIPPED——此刻才 quantity-=实发 并释放 reserved_qty；needPhoto(次品/电子类)必须有照片；关联 logistics_product 生成 wms_client_billing_record(幂等，同 biz_id 不重复)；同步 OMS、回传 tracking_no。
5. return-qc(退货质检)：wms_inbound_order(source_type=RETURN)+wms_return_qc_item；/receive RETURN_PENDING→QC_PENDING；/qc PASS→退货区/标准区 GOOD 生成新批次(新 inbound_date FIFO 重排)、FAIL→不良品区 DAMAGED、FAIL+电子类校验照片；转 COMPLETED 同步 OMS。

## 七、交付标准
- 每个模块：新建/复用实体+Mapper+Service+Controller+必要迁移(Vxx)；关键写路径加事务；幂等/前置状态校验齐全；配套单测，`mvn -pl admin -am test -o` 全绿。
- 打包部署到 8081，启动干净（无循环依赖/异常）。
- 每个模块联通后，把对应前端 src/api/.../index.ts 里的 `const USE_MOCK = true` 改为 false 做一次真实联调（这是唯一允许改动前端的地方，仅改这一行开关；其余前端一律不动）。
- 全程自主推进，遇到设计取舍按文档与业务需求就近决策并在提交说明里记录，不必逐步征询我。
