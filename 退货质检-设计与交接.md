# 海外仓平台 · 海外仓作业 · 退货质检 —— 设计与交接文档

> 面向接手的 AI/开发者（尤其是**后端**）。本页前端已完成，以 mock（含内存态）驱动。
> 说明：这页是什么、展示什么、前端怎么搭的、**后端需要实现的接口契约**、以及如何切到真实数据。
> 截至 2026-07-01。

---

## 0. 背景与定位

**归属身份 = 海外仓平台 `OVERSEAS_PLATFORM`** → 海外仓作业(ops) → **退货质检**（menu id **170505**，path **`return-qc`**，parent ops 170500）。当前 DB `sys_menu.uri` 仍为占位组件 `common/Placeholder`。用户角色 = WMS 退货收货员 + 质检员。

- **定位**：退货 = **"带质检的重新入库"**（业务需求 1.4）。复用入库单结构，`source_type=RETURN`，但状态机插入质检阶段。
- **后端现状**：退货执行/质检 controller **尚未实现**。→ 前端 mock 先行，后端按第 3 节契约实现。

> 本页是「海外仓作业」五页的最后一块。同批已完成：收货/上架（另一任务）、下架(拣货)、打包签出、退货质检（本页）。

---

## 1. 业务口径（业务需求 1.4）

**状态机（含质检三态）**
```
RETURN_PENDING(待退货收货)
   │ 收货员清点退回货物（录入实收数）
   ▼
QC_PENDING(待质检)
   │ 质检员逐 SKU 判定
   ├─ QC_PASS → 上架[退货区/标准区]，quality=GOOD（可再分配，生成新批次 FIFO 重排）
   └─ QC_FAIL → 上架[不良品区]，quality=DAMAGED（不可分配）
   ▼
COMPLETED(退货入库完成) ──同步──► OMS 退货单状态更新
```

**质检与库存处理规则（关键）**
| 质检结果 | 回库分区 | `wms_physical_inventory.quality` | 可否被出库分配 |
|---|---|---|---|
| 质检通过 PASS | 退货区 / 标准区 | GOOD | ✅ 可 |
| 质检失败 FAIL | 不良品区 | DAMAGED | ❌ 不可 |

- **次品+电子类强制拍照（BR-04）**：质检失败且为电子类时，必须上传质检照片留证。
- 质检通过的退货货物生成**新批次** `wms_physical_inventory`（新 `inbound_date`，FIFO 重新排队）。
- 质检结果写入退货单明细，作为后续与货主对账/理赔依据。

**依赖数据表**：`wms_inbound_order`(`source_type=RETURN`) + `wms_return_qc_item`(质检扩展)、`wms_physical_inventory`、`wms_zone`。

---

## 2. 页面展示（v1）

参照入库作业范式：搜索栏 + ProTable + 作业弹窗。

### 搜索栏
- 退货单号 / 状态（待收货/待质检/已完成）/ 货主（`ErpTenantSelect`）

### 列表（ProTable）
| 退货单号 | 货主 | 仓库 | SKU/件数 | 状态徽标 | 创建时间 | 操作 |

- **操作列跟状态**：`RETURN_PENDING`→[收货]；`QC_PENDING`→[质检]；`COMPLETED`→[查看]（只读复用质检弹窗）。

### 退货收货弹窗（ReturnReceiveModal）
- 明细：SKU / 应退 / **实收数**（录入）→ 确认 → 转 `QC_PENDING`。

### 质检弹窗（ReturnQcModal）—— 核心
- 逐 SKU 一行：SKU（电子类 tag）/ 实收 / **质检结果(通过·失败)** / **回库分区** / 品质(自动) / 库位 / 照片·备注。
- 结果=PASS → 分区可选 退货区/标准区，品质=良品；结果=FAIL → 分区固定不良品区，品质=次品。
- **FAIL + 电子类 → 出现照片上传，必传**（否则拦截）。
- 确认 → 质检+上架 → 转 `COMPLETED`。
- `readonly=true`（查看）复用同弹窗，展示既有质检结果。

---

## 3. 后端需实现的接口契约 ⭐

前端 mock 驱动。后端实现下列接口并对齐 VO 后，把 `src/api/wms/return-qc/index.ts` 的 `USE_MOCK` 改 `false` 即接通，**前端零改动**。

**接口前缀建议**：`/wms/return-qc`

| 方法 & 路径 | 作用 | 入参 | 返回 |
|---|---|---|---|
| `GET /page` | 分页 待收货/待质检/已完成 退货单 | query=`PageParam`(size,page,sort)+`ReturnQO` | `PageResult<ReturnOrderVO>`（列表不带 items） |
| `GET /{id}` | 退货单详情（含 items） | — | `ReturnOrderVO` |
| `POST /receive` | 退货收货 RETURN_PENDING→QC_PENDING | body=`ReturnReceiveDTO` | void |
| `POST /qc` | 质检+上架 QC_PENDING→COMPLETED | body=`ReturnQcDTO` | void |

> 统一包 `ApiResult<T>`（`{ code, data, message }`，成功 `code=200`）。

### VO / DTO（权威在 `src/api/wms/return-qc/types.ts`）

```ts
type ReturnStatus = 'RETURN_PENDING'|'QC_PENDING'|'COMPLETED'
type QcResult = 'PASS'|'FAIL'
type ReturnZone = 'RETURN'|'STANDARD'|'DEFECTIVE'

ReturnOrderVO {
  id: number
  returnNo: string
  erpTenantId: number
  ownerName: string
  warehouseId: number
  warehouseName: string
  skuKinds: number
  totalQty: number
  status: ReturnStatus
  createTime: string
  items?: ReturnOrderItemVO[]   // 详情才带
}

ReturnOrderItemVO {
  skuCode: string
  skuName?: string
  electronic: boolean           // 电子类 → 影响 FAIL 是否强制拍照
  expectedQty: number           // 应退数
  receivedQty?: number          // 实收数(收货后)
  // 质检后回填
  qcResult?: QcResult
  zone?: ReturnZone
  quality?: 'GOOD'|'DAMAGED'
  locationCode?: string
  qcRemark?: string
}

ReturnQO { returnNo?: string; status?: ReturnStatus; erpTenantId?: number }

ReturnReceiveDTO { returnOrderId: number; items: { skuCode: string; receivedQty: number }[] }

ReturnQcLineDTO {
  skuCode: string
  qcResult: QcResult
  zone: ReturnZone              // PASS→RETURN/STANDARD；FAIL→DEFECTIVE
  locationCode: string
  qcRemark?: string
  photoCount?: number           // FAIL+电子类必传(真实为文件 id 列表)
}
ReturnQcDTO { returnOrderId: number; lines: ReturnQcLineDTO[] }
```

### 后端实现提示
- **平台看全部货主**；`erpTenantId`/`status` 非空时过滤。
- `/receive`：仅 `RETURN_PENDING` 可收货；写 `receivedQty`；转 `QC_PENDING`。
- `/qc`：仅 `QC_PENDING` 可质检；**校验 FAIL+电子类必须有照片**，否则报错。
  - PASS：写 `zone∈{RETURN,STANDARD}`、`quality=GOOD`，**生成新批次 `wms_physical_inventory`（新 inbound_date，FIFO 重排）**，可分配。
  - FAIL：写 `zone=DEFECTIVE`、`quality=DAMAGED`，不可分配。
  - 质检结果写入 `wms_return_qc_item`（对账/理赔依据）；转 `COMPLETED`；同步 OMS 退货单。
- **口径以《业务需求说明书》1.4 与《底层数据表重构设计.md》附录 A 为准**。

---

## 4. 前端文件清单（已完成）

**API 层** `src/api/wms/return-qc/`
- `types.ts` / `mock.ts`（mock+内存态：收货、质检+上架流转）/ `index.ts`（4 接口 + `USE_MOCK` + 接口清单注释）。

**页面 + 组件** `src/views/platform/return-ops/`
- `ReturnQcPage.vue`（主页）/ `ReturnReceiveModal.vue`（收货）/ `ReturnQcModal.vue`（质检，含只读查看）/ `constants.ts`（状态·分区·质检 元数据）。

**路由** `src/router/dynamic-routes.ts`
- `COMPONENT_OVERRIDES['return-qc']` → 本页，绕过占位组件 `common/Placeholder`，**不改数据库**。

**校验**：`npx vue-tsc --noEmit` 通过；`npx eslint` 无 error/warning。前端走 Vite HMR。以 `super_admin`（海外仓平台入口）登录 →「海外仓作业 → 退货质检」查看。

---

## 5. 待确认 / 可迭代
- **照片存储**：mock 仅记录张数（`photoCount`）；后端接文件上传服务，DTO 改为文件 id 列表。
- **部分质检**：当前一次性提交全部 SKU 质检；如需分批质检可迭代。
- **理赔/对账联动**：质检结果写入后与货主对账的下游流程，另行排期。

---

## 6. 一句话交接
> 前端页面 `src/views/platform/return-ops/` 已完成并用 mock（含内存态）跑通；后端实现 `/wms/return-qc/*`（4 接口，契约见第 3 节 / `types.ts`）后，把 `src/api/wms/return-qc/index.ts` 的 `USE_MOCK` 改 `false` 即上线，前端不用动。**PASS 生成新批次、FAIL 落不良品区、FAIL+电子类强制拍照 三条规则务必在后端实现。**
