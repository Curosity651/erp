# 底层数据表重构 · 设计与决策记录

> 目的：从底层把"多门户 + 多租户"的数据模型一次性理清并固化。**本轮只做设计与决策记录；代码/迁移改动留到全部表讨论完后统一进行。**
> 状态图例：✅已定 / 🔶待定 / ⬜未讨论

---

## 一、整体模型（已确认）

**多门户（3 个隔离后台，同一套代码）**：海外仓平台 / WMS服务商 / 货主。
**多租户层级**：海外仓平台(单实例, `sys_tenant.id=-1`) → WMS服务商(N) → 货主(M)。每个实例 = `sys_tenant` 一行，靠 `tenant_type` 区分身份。
**物理货物两个正交归属维度**：`erp_tenant_id`=货主(货的主人)、`wms_tenant_id`=服务商(货架承租方)；物理仓库由海外仓平台提供（共享）。

**三个隔离层（Tier）**：
- **Tier G 全局共享**：无租户列，海外仓平台维护（菜单/字典/配置/物理基础设施等）。
- **Tier T 单租户私有**：`tenant_id` + MyBatis 拦截器自动注入；平台不可见（注入 -1→0 行）。用于身份表 + 货主 OMS 业务。
- **Tier W 货主货物维**：`erp_tenant_id`[+`wms_tenant_id`] + 自定义 DataPermission handler / 显式过滤；**平台可见全部**（去作业）。

---

## 二、全局命名约定 ✅（保持 `tenant_id`，不改名）

租户引用列含义（均为 `sys_tenant.id` 的逻辑外键）：
- **`tenant_id`** — Tier T：本行所属租户（拦截器自动注入隔离，平台不可见）。身份表 + 货主 OMS 全部用它。**保持现名不改**（避免 ~25 表大改名的 churn 与风险）。
- **`erp_tenant_id`** — Tier W：货主维（货的主人，tenant_type=ERP_USER）。
- **`wms_tenant_id`** — Tier W：服务商维（货架承租方，tenant_type=WMS_OPERATOR）。

两种隔离机制是**刻意的两类语义**，非补丁：
- Tier T「单一归属、仅归属者可见、平台不可见」→ MyBatis 多租户拦截器（按 `tenant_id`）。
- Tier W「货主×服务商双维、平台可见全部以作业」→ DataPermission handler（按 `erp_tenant_id`/`wms_tenant_id`）。

**决策**：放弃改名 `sys_tenant_id`；`tenant_id` 作为标准列名保留，文档明确其指向 `sys_tenant.id`。

---

## 二·补、关键问题与铁律

### ❗CRITICAL：后台同步任务租户错乱（已确认 bug）
**现象**：定时同步（Ozon/WB/Yandex 订单、WB 财务/供货/办公室、FBO 库存、库存预警）进来的数据**全部错误归到货主 #1**。
**根因**：①定时任务无登录用户→无 `TenantContext`；②方案②下拦截器无上下文时跳过注入；③同步代码全程无 `setTenantId`，`ErpOrder` 等无 tenantId 字段，完全靠拦截器自动填；④`erp_order.tenant_id NOT NULL DEFAULT 1` → 取默认值 1。结果：所有货主的同步订单都落到货主1，目标货主看不到自己订单、货主1看到别家订单。
**对比**：货主在网页里手动同步时有上下文（=该货主），反而正确；只有后台定时裸跑出错。

### 🔒 铁律：后台任务必须显式建立租户上下文
凡涉及租户数据的**定时/异步任务**，必须按"店铺→其货主"用 `TenantContext.runAs(shop.getTenantId(), …)` 包裹后再读写：
```java
for (Shop shop : shops) {                 // 裸跑查到所有店铺(跨租户)
    TenantContext.runAs(shop.getTenantId(), () -> syncShopOrders(shop));
}
```
读(游标/去重)与写(插入订单)都会落到正确货主。需让 `Shop`/`WbOffice` 等驱动表暴露 `tenantId` 字段以取得 runAs 入参。

## 三、逐表决策

### 第①组 身份与租户核心

| 表 | 作用 | 归属/可见 | 隔离 | 决策 |
|---|---|---|---|---|
| `sys_tenant` | 租户(实例/组织)注册表；非用户表。三类实例都在此，`tenant_type` 区分；`parent_wms_tenant_id` 记货主→服务商 | 平台级注册表 | 无隔离列（它本身是租户表）；读取按身份过滤 | ✅ 保持不变。海外仓平台单实例 id=-1；"服务商→平台"父子暂隐含(parent=NULL)，暂不改名 `parent_wms_tenant_id` |
| `sys_user` | 账号表（用户名/密码/昵称/状态/organization_id/tenant_id）。`tenant_id` → `sys_tenant.id`，是身份解析与数据隔离的唯一真源 | 每个实例私有自己的用户 | `tenant_id` + 拦截器 | ✅ 保持；**新增**：将 `is_admin` 从 `sys_user_tenant` 并入本表（见下）。列名 tenant_id/sys_tenant_id 取决于§二 |
| `sys_user_tenant` | (历史)用户↔租户绑定表，含 `is_admin` | — | `tenant_id` | ✅ **废弃**：租户归属已由 `sys_user.tenant_id` 承担，`is_admin` 并入 `sys_user`，本表删除 |
| `sys_role` | 角色（平台角色 tenant_id=-1） | 每个实例私有自己的角色 | `tenant_id` + 拦截器 | ✅ 保持。**铁律**：租户新建角色时 `code` 由系统按租户命名空间自动生成全局唯一值（如 `ROLE_<tenantId>_<seq>`），管理员只填**显示名**(name，可重复)；禁止管理员自填 code |
| `sys_user_role` | 用户↔角色关联（按 role_code） | 关联表 | 无列，靠 user/role | ✅ 保持 |
| `sys_role_menu` | 角色↔菜单(权限)关联（按 role_code） | 关联表 | 无列，靠 role_code | ✅ 保持。因角色 code 全局唯一且按租户命名空间化，**跨租户永不撞车**，各租户独立配置菜单互不影响 |
| `sys_menu` | 全系统功能/菜单/权限点总目录 | 全局共享，仅海外仓平台维护；可见性靠 menu-filter + role_menu | 无列（Tier G） | ✅ 保持 |
| `sys_organization` | 组织架构/部门 | 每个实例私有，三门户都可用 | `tenant_id` + 拦截器 | ✅ Tier T，按实例隔离，三门户统一可用（不搞特例） |

**第①组结论**：命名保持 `tenant_id`（不改名）；`sys_user_tenant` 废弃(is_admin 并入 sys_user)；其余保持。第①组 ✅ 完成。

### 第②组 系统配置与日志 ✅

| 表 | 作用 | 归属/可见 | 决策 |
|---|---|---|---|
| `sys_dict` / `sys_dict_item` | 数据字典(全系统下拉枚举) | 全局共享、仅海外仓平台维护 | ✅ Tier G 保持 |
| `sys_config` | 系统级参数 | 全局共享、仅海外仓平台维护 | ✅ Tier G 保持**真·系统参数**；但**库存预警配置移出**：不放平台全局，改为**每货主独立**(Tier W, `erp_tenant_id`)，具体存储在第⑧组 `wms_inventory_config` 时细化(货主级默认 + 区域/SKU 级覆盖，均按货主隔离) |
| `sys_file` | 上传文件元数据(头像/合同/附件/凭证) | 各实例用户上传 | ✅ 改 **Tier T (`tenant_id`)** 实例隔离，防越权 |
| `log_login_log` | 登录日志 | 同租户可见 | ✅ **保留**(此前"不要了"已反转)；Tier T (`tenant_id`)；需补 form-login 登录成功/失败写入(BallCat 只接 OAuth2 事件，form-login 没写) |
| `log_operation_log` | 操作日志 | 同租户可见 | ✅ Tier T：表加 `tenant_id`，写入时落当前租户(操作日志 handler 在请求线程可填)；读取按 `tenant_id` 直查，**弃用户名子查询补丁** |
| `log_access_log` | 访问日志(每请求一条) | 同租户可见 | ✅ Tier T：表加 `tenant_id`；异步写需在**请求线程先捕获租户**再传给 `AccessLogSaveThread`；读取按 `tenant_id` |

**第②组结论**：dict/config 全局保持；库存预警配置归货主(留待⑧组细化)；sys_file 实例隔离；三类日志均 Tier T、同租户可见(登录日志恢复)。第②组 ✅。
### 第③组 货主商品域 ✅

`sku` / `sku_files` / `sku_mapping` / `category` / `brand` / `supplier` —— 均货主私有主数据，**Tier T（`tenant_id`）+ 拦截器，保持不动**。`supplier` 确认为每货主各自维护（非平台共享供应商库）。第③组 ✅。
### 第④组 货主订单域 ✅

`erp_order` / `erp_order_item` / `erp_label_batch` / `erp_label_batch_file` / `erp_label_batch_item` / `shop` / `sync_cursor` —— 货主私有订单数据，**Tier T（`tenant_id`）保持不动**。
`erp_order_bak_20260227` —— 🗑️ 垃圾备份表，**删表**。第④组 ✅。
### 第⑤组 货主财务/其他 ⬜未讨论
### 第⑥组 WMS 物理基础设施 ✅

**核心：`wms_warehouse` 混两类仓，归属/可见性不同；表层均 Tier G（不加租户列），可见性按链推导收窄。**

| 表 | 作用 | 决策 |
|---|---|---|
| `wms_warehouse` | 仓库（OWN 自有仓 / FBO 仓） | ✅ Tier G、不加 `erp_tenant_id`。**可见性推导**：① OWN 仓(shopId=null) 走 `wms_rack_assignment` 链——服务商只看自己有货架分配的仓，货主只看其 parent 服务商有货架分配的 OWN 仓；② FBO 仓(shopId 有) 归货主(shopId→shop.tenant_id)，可见性=该货主(+其服务商/平台)；③ 平台看全部 |
| `wms_zone` / `wms_location` | 分区/库位（挂仓库下） | ✅ Tier G，可见性**跟随所属仓库** |
| `wms_region` | 区域主数据 | ✅ Tier G 平台维护 |
| `wms_platform_region_mapping` | 平台↔区域映射 | ✅ Tier G 平台配置 |
| `wms_region_inventory` | 区域级库存(regionId+sku+预占/在途) | ❗ 是**货主货物**，缺租户维 → 补 `erp_tenant_id`，Tier W（细节并入第⑦组） |

**两类仓模型（确认）**：OWN=平台建仓+建库位+把排分配给服务商，货主用其服务商的 OWN 仓；FBO=按店铺自动建、归货主、FBO 库存按 `erp_tenant_id` 隔离。**FBO 同步(`FboStockSyncFacade.syncAllShops`)无上下文遍历店铺、未按货主落 `erp_tenant_id`** → 属 CRITICAL 背景任务 bug，按店铺货主 `runAs` 修。第⑥组 ✅。
### 第⑦组 WMS 库存 ✅

| 表 | 作用 | 决策 |
|---|---|---|
| `wms_physical_inventory` | 批次级 SSOT（FIFO/品质/库位），仓库可用量唯一真源 | ✅ Tier W 双维(`erp_tenant_id`+`wms_tenant_id`) 保持 |
| `wms_inventory` | 聚合库存快照（含 OWN/FBO 行） | ✅ Tier W 双维保持；FBO 行 `erp_tenant_id` 由 FBO 同步按店铺货主落对 |
| `wms_stock_flow` | 库存流水 | ✅ Tier W 双维保持 |
| `wms_region_inventory` | 区域级预占/在途滚存（货主视角，服务预测+过账） | ✅ 补 **`erp_tenant_id` 单维**；键改 (erp_tenant_id, region_id, sku_code)。**不要双维**——区域汇总本就聚合掉服务商维；且货主只挂一个父服务商，服务商可由货主推导 |

**模型口径（确认）**：一个货主只属于一个服务商（`parent_wms_tenant_id`），故货主视角的滚存/单据单维 `erp_tenant_id` 即可，服务商维可推导。第⑦组 ✅。
### 第⑧组 WMS 货主单据 ✅

`wms_purchase_order`(+item/file/qc_item) / `wms_shipping_order`(+item/adjust) / `wms_purchase_inbound_order`(+item) / `wms_sales_outbound_order`(+item) / `wms_return_inbound_order` / `wms_logistics_provider` / `wms_inventory_config` —— 均货主单据，**Tier W 单维 `erp_tenant_id`**（按"货主只挂一个服务商"，无需双维）。明细/附件子表**无租户列、靠父单隔离**。

**库存预警默认**落 `wms_inventory_config`：货主级默认行(region_id/sku_code 空) + 区域/SKU 覆盖行，均按 `erp_tenant_id`。

**🔒 子表隔离铁律**：标准 `getDetail/update/delete` 已安全（先 `getById` 父单→被 handler 按 erp_tenant_id 过滤→`Assert.notNull`→再查明细）。但**明细选择类端点**（`PurchaseInbound /shipping-items`、`/shipping-detail`、`ShippingOrder /available-items`、`StockPosting /item/page` 等）**必须**：① 明细查询 JOIN 父表（handler 过滤父表 erp_tenant_id），或 ② 查明细前先校验父单归属——**严禁裸查明细表**。实现阶段逐个审计。第⑧组 ✅。
### 第⑨组 WMS 平台作业 ✅（新增"平台作业域"）

平台/服务商物理作业；**单头仓库级、明细+过账带双维（溯源 + 货主级/仓库级双重过账）、货主不可见作业单（只看 `wms_stock_flow`）。**

| 表 | 维度/归属 | 决策 |
|---|---|---|
| `wms_adjustment_order` / `wms_stocktake_order` / `wms_transfer_order`（单头） | 仓库级(warehouseId/源·目标仓)，无租户列 | ✅ 平台作业；可见性=平台全部 + 服务商按其仓库；货主不可见 |
| `..._item`（三者明细） | ➕ **`erp_tenant_id`+`wms_tenant_id` 双维** | ✅ 建单时从被操作批次/库存显式写入；用于"哪个服务商货架上、哪个货主的、哪个 SKU"溯源 |
| `wms_stock_posting`(+`_item`) | ➕ **`erp_tenant_id`+`wms_tenant_id` 双维** + warehouseId/regionId | ✅ 支撑 `GROUP BY erp_tenant_id`=货主过账、`GROUP BY warehouse_id/wms_tenant_id`=仓库/服务商过账 |
| `wms_fbo_sync_log` | 平台诊断日志(按店铺) | ✅ 平台可见 |

**可见性**：平台全部；服务商按仓库/`wms_tenant_id`；**货主不开放这些作业/过账的读**（货主仅通过 `wms_stock_flow` 看对自己货物的增减）。明细/过账上的 `erp_tenant_id` 用于数据溯源与账务汇总，**不**用作货主读权限入口。第⑨组 ✅。
### 第⑩组 杂项/全局 ✅

| 表 | 决策 |
|---|---|
| `exchange_rate` | ✅ Tier G 全局参考数据保持（平台维护，全局共享读） |
| `export_data` | ✅ 改 **Tier T（`tenant_id`）**：导出内容属某租户业务数据，须隔离防跨租户泄漏 |
| `wms_rack_assignment` | ✅ 服务商维（`wms_tenant_id`）：平台创建/看全部、服务商看自己被分配的货架、货主不涉及 |
| `sys_file` | ✅（第②组已定）Tier T（`tenant_id`）隔离 |

第⑩组 ✅。**全 10 组设计完成。**

---

## 五、全表 × Tier × 隔离机制 总览

- **Tier G 全局共享**（无租户列，平台维护，其余只读）：`sys_menu`/`sys_dict`/`sys_dict_item`/`sys_config`/`exchange_rate`/`wms_region`/`wms_platform_region_mapping`；物理设施 `wms_warehouse`/`wms_zone`/`wms_location`（可见性按链推导收窄）。
- **Tier T 单租户私有**（`tenant_id` + 拦截器，平台不可见）：`sys_user`/`sys_role`/`sys_organization`；`sku`/`sku_files`/`sku_mapping`/`category`/`brand`/`supplier`；`erp_order(_item)`/`erp_label_batch(_file/_item)`/`shop`/`sync_cursor`；`wb_*`/`sales_target`/`position`/`project_group`；`sys_file`/`export_data`；三类 `log_*`（`tenant_id`，写入落租户、读取按租户）。
- **Tier W 货主货物维**（`erp_tenant_id`[+`wms_tenant_id`]，handler/显式作用域，平台看全部）：`wms_physical_inventory`/`wms_inventory`/`wms_stock_flow`（双维）；`wms_region_inventory`（单维 erp）；货主单据 `wms_purchase_order`/`wms_shipping_order`/`wms_purchase_inbound_order`/`wms_sales_outbound_order`/`wms_return_inbound_order`/`wms_logistics_provider`/`wms_inventory_config`（单维 erp，子表靠父）。
- **平台作业域**（单头仓库级；明细+过账带双维做溯源/双重过账；货主不可见，看 stock_flow）：`wms_adjustment_order`/`wms_stocktake_order`/`wms_transfer_order`(+items)/`wms_stock_posting`(+item)/`wms_fbo_sync_log`。
- **服务商维**：`wms_rack_assignment`（`wms_tenant_id`）。
- **关联/无独立归属**：`sys_user_role`/`sys_role_menu`（靠父）。
- **删除**：`erp_order_bak_20260227`；**废弃**：`sys_user_tenant`（is_admin 并入 sys_user）。

## 六、铁律汇总
1. 角色 `code` 由系统按租户命名空间自动生成（`ROLE_<tenantId>_<seq>`），管理员只填显示名。
2. **后台任务必须 `TenantContext.runAs(店铺→货主)` 显式建上下文**再读写（否则数据错归货主1）。
3. 子表靠父隔离：明细端点必须 JOIN 父表或先校验父单归属，严禁裸查明细表。
4. Tier W 平台 context=-1 → 看全部（作业/回写按主键，不被 erp_tenant_id 挡）。

---

## 七·进度（执行中）
- ✅ **P1** V22 迁移已导入（is_admin/tenant_id/双维列/删 bak）。
- ✅ **P2** 后台同步租户上下文（Ozon/WB/Yd 订单、WB 办公室/供货、FBO、WB财务执行器 runAs；Shop/WbFinancialSyncTask 暴露 tenantId）。
- ✅ **P3** is_admin 并入 sys_user（废 sys_user_tenant 读写）+ 角色 code 自动命名空间 advice。
- ✅ **P4** 日志按租户（三日志入租户白名单，写入自动填 tenant_id、读取按租户=同租户可见；删 LogDataPermissionHandler/Composite 补丁）。**残留**：访问日志异步归属、登录日志 form-login 写入（低价值，延后）。
- ✅ **P8** sys_file/export_data 入租户白名单（文件隔离）。
- ✅ 已构建+部署+全量单测(35)通过+启动验证。
- ✅ **P6** 仓库可见性收窄：`WarehouseService.getWarehouseOptions/listOwnWarehouses` 按 `ownWarehouseScope()` 过滤（平台全部；服务商/货主=其(父)服务商有货架分配的 OWN 仓，经 `WmsRackAssignmentMapper.listWarehouseIdsByWmsTenant(WmsTenantContext)`）。FBO 仓可见性细化延后（FBO 库存已按 erp 过滤）。
- ✅ **P9** 删 sys_user_tenant 表(V23)+实体/Mapper 类。
- ✅ **P5+P7 核心 库存过账引擎按货主隔离**（2026-06-30 构建+部署+单测通过+启动验证）。已落地：
  - `StockPostingItemDTO`/`StockPostingDTO`/`StockPostingItem`/`StockPosting`/`RegionInventory` 全部加 `erpTenantId`(+`wmsTenantId`，wms 维暂留空)。
  - `RegionInventoryMapper.selectByTenantRegionSku(erp,region,sku)`（命中 uk_tenant_region_sku，平台 context=-1 不注入过滤时靠显式 erp 条件避免 selectOne TooManyResults）；`RegionInventoryService.getOrCreate/getReservedQuantity` 改为强制传 `erpTenantId`（null 抛错）。
  - `InventoryPostingEngine`：区域分组键改 `(erp,region,sku)`（抽 public static `regionGroupKey`）→ `getOrCreate(erp,region,sku)`；`buildWarehouseFlow/buildRegionFlow` 给 StockFlow 填 erp/wms。
  - `StockPostingService.post`：item 写 erp/wms；`buildMergeKey`（public static）+ 冲突校验键加 erp/wms；过账头写 erp/wms。
  - 上游 facade 全部按单据货主填：`SalesOutboundFacade`（区域释放+仓库扣减+预占校验 getReservedQuantity 传 erp）、`ShippingOrderFacade`（发货 IN_TRANSIT + 区域调整双 Item）、`WmsInboundExecutionService.postRegionInTransitOut`（取**物流单**货主，确保冲减发货时建立的同一条区域库存行）。
  - `wms_region_inventory` 纳入 `WmsOwnerDataPermissionHandler.OWNER_TABLES`；`RegionStockDataProvider.getRegionReserved` 改 selectList 求和，避免 selectOne 抛错。
  - 单测 `StockPostingTenantIsolationTest`（4 case）：不同货主同 SKU 的 mergeKey/regionGroupKey 必不相等、同货主必相等。
- ✅ **P5+ 收尾 A+B 已部署（2026-07-01，全栈，启动无错）**：
  - **A 库存配置按货主**：V25 `wms_inventory_config` uk_region_sku→uk_tenant_region_sku(erp,region,sku)；`selectByRegionAndSku` 改 selectList+findFirst 防 selectOne 崩（读取本就在 OWNER_TABLES 按货主过滤）。
  - **B 仓库级库存按货主（财务）**：wms_inventory 唯一键 4 键(wms,erp,warehouse,sku)。`InventoryService.getOrCreate(erpTenantId,warehouseId,skuCode)`+`selectByErpWarehouseSku`（忽略 wms 维命中聚合器同一行、建行 wms=0、Assert erp 非空）；`InventoryPostingEngine.processWarehouseItems` 按 (erp,仓,sku) 分组（`warehouseGroupKey`）；`getDetail`/调整校验/调拨校验/销售缺货校验全改 erp 精确版（`getByErpWarehouseAndSku`/`getStockMapByErpWarehouseAndSkuCodes`），消除多货主 selectOne 崩溃 + 跨货主混算；5 流程盖货主：销售出库/退货入库（单据货主）、盘点（全量从库存行自动带、追加 SKU 批次指定）、调整/调拨（**单头选一个货主**，头部 DTO erpTenantId → batchSave 盖各明细）；FBO 同步取上下文货主。前端新增 `ErpTenantSelect`（listErpTenants），调整/调拨表单头 + 盘点追加 SKU 选货主。单测扩 warehouseGroupKey（7/7 绿）。
  - 明细端点审计结论：/shipping-items、/available-items（INNER JOIN wms_purchase_order，在 OWNER_TABLES→自动 erp 过滤）、/shipping-detail（selectById on wms_shipping_order）=**安全**。
- ✅ **P5+ 收尾 C 已部署（2026-07-01，启动无错）**：台账读端点按货主收窄。三 QO（StockPostingQO/StockPostingItemQO/StockFlowQO）加 `erpTenantIds`（hidden）；`StockPostingQueryService`(queryPage/queryItemPage)、`StockFlowService`(queryPage) 注入 `ErpOwnerScopeService`，查询前 `qo.setErpTenantIds(readScope())`（货主=[自身]／服务商=[名下，空→哨兵 -999 失败闭合]／平台=null 不过滤，忽略前端传入）；三 mapper queryPage Wrapper 加 `.inIfPresent(getErpTenantId, qo.erpTenantIds)`；过账/流水 `getDetail` 加 `canAccess(head.erpTenantId)` 校验；调整/调拨 confirm 过账头补 `erpTenantId=items[0].erpTenantId`（供 getDetail 认领）。**未动全局 handler，幂等 `selectBySourceAndType` 不受影响。** P5+ A/B/C 全部完成。下一迁移版本号=V26。

## 七、分阶段实现计划（骨架；正由代码扫描补充精确触点）

> 原则：先 schema 一次性到位（dev 库已清空，低风险）→ 再按域改代码 → 每阶段独立可验证 → 最后全量回归。

- **P1 Schema 迁移 V22（一次性）**：sys_user 加 is_admin；sys_file/export_data/三日志 加 tenant_id；wms_region_inventory 加 erp_tenant_id（重建键）；adjustment/stocktake/transfer 明细 + stock_posting(+item) 加 erp_tenant_id+wms_tenant_id；wms_inventory_config 货主级默认行约定；删 erp_order_bak；（最后）删 sys_user_tenant。
- **P2 [CRITICAL] 后台任务租户上下文**：所有同步/定时任务按"店铺→货主" `TenantContext.runAs` 包裹；Shop 暴露 tenantId。
- **P3 身份/RBAC**：sys_user_tenant 退役（is_admin 读 sys_user）；角色创建 code 自动命名空间化。
- **P4 日志**：tenant_id 写入填充（操作日志 handler / 访问日志异步捕获 / 新增 form-login 登录日志监听）；读取端 LogDataPermissionHandler 改按 tenant_id 直查（弃用户名子查询）。
- **P5 WMS 货主域**：region_inventory 纳入作用域并按 erp 隔离；库存预警默认迁入 wms_inventory_config；明细选择端点审计（JOIN 父表/校验归属）。
- **P6 WMS 仓库可见性收窄**：warehouse 列表/options 按 OWN(货架链)/FBO(店铺) 过滤。
- **P7 WMS 平台作业双维**：明细+过账写入双维、平台/服务商按仓库可见。
- **P8 文件隔离**：sys_file/export_data tenant_id 写入+读取校验。
- **P9 清理 + 全量回归**：删 sys_user_tenant/bak、移除补丁；编译/单测/打包/三身份手测。

## 四、完整修改 PLAN（按阶段执行；含精确触点）

> 说明：扫描发现的"给 shop/warehouse 加 erp_tenant_id"建议**不采纳**——按本设计：shop 已是 Tier T(`tenant_id`)只需暴露字段；warehouse 不加列、可见性按链推导。

### P1 — Schema 迁移 `V22__foundation_refactor.sql`（一次性，需 MySQL）
- `sys_user` 加 `is_admin TINYINT DEFAULT 0`；回填（现存活跃用户：绑定 is_admin=1 的置 1）。
- `sys_file` 加 `tenant_id BIGINT NOT NULL DEFAULT 1`。
- `export_data` 加 `tenant_id BIGINT NOT NULL DEFAULT 1`。
- `log_login_log`/`log_operation_log`/`log_access_log` 加 `tenant_id BIGINT NULL`。
- `wms_region_inventory` 加 `erp_tenant_id BIGINT NOT NULL DEFAULT 1`；唯一键改 `(erp_tenant_id,region_id,sku_code)`。
- `wms_adjustment_order_item`/`wms_stocktake_order_item`/`wms_transfer_order_item` 各加 `erp_tenant_id`+`wms_tenant_id`。
- `wms_stock_posting`+`wms_stock_posting_item` 各加 `erp_tenant_id`+`wms_tenant_id`。
- 删 `erp_order_bak_20260227`。
- （末尾，代码切换后再删）`sys_user_tenant` —— 放到 P9 删，避免中途引用报错。

### P2 — [CRITICAL] 后台任务租户上下文
- `Shop` 实体暴露 `tenantId`（映射现有 `tenant_id` 列，只读用于 runAs）；`WbOffice` 同理（其表已 Tier T）。
- 各调度逐店铺包裹：`OzonOrderSyncService.syncAllShops` / `syncAllEnabledShopsUnfinishedOrderStatuses`、`WbOrderSyncService.syncAllEnabledShopOrders`、`WbOfficeSyncService.syncAllEnabledShopsOffices`、`YdOrderSyncService.syncAllShops/syncUnfinishedOrders`、`OzonFboStockSyncFacade.syncAllShops`(已有 `syncByShop(shop)`)、`InventoryAlertTask`(按货主循环) → `TenantContext.runAs(shop.getTenantId(), …)`。
- `WbFinancialSyncScheduler.scheduleTasks`：`WbFinancialSyncTask` 应带租户（其表 Tier T），executeTask 前 runAs 该任务租户。

### P3 — 身份/RBAC
- `is_admin` 改读 `sys_user`：`TenantIdentityService.currentIdentity` 用 `ErpAccountMapper` 取 is_admin（不再 `sysUserTenantMapper.selectByUserId`）；`SysUserAccount` 加 `isAdmin`。
- `TenantProvisionService.createTenantWithAdmin`：不再写 `sys_user_tenant`，改在 `sys_user` 上设 is_admin=1（admin 账号）。
- 角色创建 code 命名空间：新增 `RequestBodyAdvice`（或 AOP）拦截 `POST /system/role`，把前端传的 code 改写为 `ROLE_T<当前租户id>_<原值/序号>`，编辑禁改 code。

### P4 — 日志按租户
- 三日志表写入填 `tenant_id`：日志实体是 BallCat 框架类无 tenantId 字段 → 用 **MyBatis Inner 插入拦截器**（对 log_* 的 INSERT 注入 `tenant_id=TenantContext`）或在各写入点显式补；操作日志可在 `ErpOperationLogHandler` 同时补。
- 访问日志异步（`AccessLogSaveThread`）：在请求线程捕获 tenantId 传入。
- 登录日志：新增 form-login 认证成功/失败监听器写入（含 tenant_id）。
- `LogDataPermissionHandler.getSqlSegment` 改为按 `tenant_id` 直查（弃"用户名子查询"补丁）；`ownerColumnOf` 改判 `tenant_id`。

### P5 — WMS 货主域
- `wms_region_inventory` 纳入 `WmsOwnerDataPermissionHandler` 表集；`RegionInventoryService.getOrCreate/getRegionSummaryList`、`InventoryPostingEngine.processRegionItems` 按 `erp_tenant_id` 隔离（getOrCreate 带 erpTenantId；MetaObjectHandler 自动盖章）。
- 库存预警默认迁入 `wms_inventory_config`：`InventoryConfigService.getGlobalAlertConfig` 改读"当前货主的默认行(region/sku 空)"，不再读 sys_config 全局；`InventoryAlertTask` 按货主循环（runAs）取各货主默认 + 预警。
- 明细选择端点审计：`/wms/purchase-inbound/shipping-items`、`/shipping-detail`、`/wms/shipping-order/available-items`、`/wms/stock-posting/item/page` → 确保 JOIN 父表或先校验父单归属。

### P6 — 仓库可见性收窄（不加列）
- `WarehouseService.getWarehouseOptions/listOwnWarehouses/queryPage` 及 `WarehouseMapper` 查询：按身份过滤——平台全部；服务商 = 其有 `wms_rack_assignment` 的 OWN 仓 + 名下货主的 FBO 仓；货主 = 其 parent 服务商有货架分配的 OWN 仓 + 自己店铺(shopId→tenant)的 FBO 仓。
- 需要 `WmsRackAssignment` 按服务商查仓库 id 的方法、FBO 仓按 shopId→tenant 的过滤。

### P7 — WMS 平台作业双维
- `AdjustmentService.createAdjustment`/`StocktakeService.createStocktake`/`TransferOrderService.createTransferOrder` 创建明细时，从被操作的库存/批次显式写 `erp_tenant_id`+`wms_tenant_id`。
- `InventoryPostingEngine` 写 `StockPosting`/`StockPostingItem` 时填双维（来自被过账的库存行）；提供货主级/仓库级过账汇总查询。
- 这些作业/过账对货主不开放读（平台/服务商按仓库可见）。

### P8 — 文件隔离
- `sys_file`/`export_data` 纳入 Tier T 拦截器白名单（`tenant_id`）；上传/导出写入自动盖章（MetaObjectHandler 已对 `tenant_id`? 否则补）；下载/读取经拦截器过滤。

### P9 — 清理 + 全量回归
- 删 `sys_user_tenant` 表 + `SysUserTenant`/`SysUserTenantMapper`（确认无引用后）。
- 移除日志"用户名子查询"等遗留补丁。
- 全量编译 + 单测 + 打包 + 起服务；三身份手测 + 后台同步落租户验证。

---

## 四·补、原始细项清单（保留）

- [ ] 废弃 `sys_user_tenant`：`sys_user` 加 `is_admin`；迁移回填；改 `TenantProvisionService`/`TenantIdentityService` 等引用点；删表。
- [ ] （命名：保持 `tenant_id`，无改名任务）
- [ ] 角色创建流程：拦截/改造原生「角色管理」新建，`code` 由系统按 `ROLE_<tenantId>_<seq>` 自动生成（全局唯一），前端只让填显示名；编辑不可改 code。保证 `sys_role_menu` 跨租户不撞车。
- [ ] `sys_file` 加 `tenant_id` + 拦截器隔离 + 写入回填；核对文件读取/下载按租户校验。
- [ ] 库存预警「全局默认」从 sys_config 移出 → 每货主独立(Tier W)；与 `wms_inventory_config` 一起在⑧组细化。
- [ ] 三类日志加 `tenant_id`：操作日志(handler 请求线程填)、访问日志(请求线程捕获租户传异步线程)、登录日志(新增 form-login 成功/失败监听器写入)；读取端 `LogDataPermissionHandler` 改为按 `tenant_id` 直查（弃用户名子查询）。
- [ ] 删除垃圾表 `erp_order_bak_20260227`。
- [ ] **[CRITICAL] 后台同步任务租户上下文**：`OzonSyncScheduler`/`WbSyncScheduler`/`YdSyncScheduler` 及 `WbFinancialSyncScheduler`/`FboStockSyncScheduler`/`InventoryAlertTask` 等，逐店铺/逐货主 `TenantContext.runAs(...)` 包裹；`Shop` 等暴露 `tenantId`。修复后需校验历史误归到货主1的数据（清库后无存量，新同步起即正确）。
- [ ] 仓库可见性收窄：`/wms/warehouse` 列表/options 不再返回全部——OWN 仓按 `wms_rack_assignment`(服务商↔仓)链过滤（货主沿 parent 服务商），FBO 仓按 shopId→货主过滤；分区/库位跟随仓库。
- [ ] `wms_region_inventory` 加 `erp_tenant_id` 单维；唯一键/查询改 (erp_tenant_id, region_id, sku_code)；`RegionInventoryService.getOrCreate`/过账/预测按货主隔离。
- [ ] 库存预警默认迁入 `wms_inventory_config`（货主级默认行 + 覆盖行，按 erp_tenant_id）；从 sys_config 移除全局默认。
- [ ] 审计明细选择类端点（/shipping-items、/shipping-detail、/available-items、/item/page 等）：确保 JOIN 父表或先校验父单归属，不裸查明细表。
- [ ] 平台作业明细加双维：`wms_adjustment_order_item`/`wms_stocktake_order_item`/`wms_transfer_order_item` 加 `erp_tenant_id`+`wms_tenant_id`，建单时从被操作库存显式写入。
- [ ] `wms_stock_posting`(+`_item`) 加 `erp_tenant_id`+`wms_tenant_id`，过账引擎写入时落双维；提供货主级/仓库级过账汇总。这些作业/过账对货主不开放读（货主看 stock_flow）。
- [ ] …（随讨论补充）
