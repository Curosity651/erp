# 库存正确性代码审计报告

> 审计范围:货主端入库 → 仓库上架 → 订单拉取 → 完整订单出库 → 退货 全链路的库存正确性(能否保证**每个货主/租户**的库存始终正确)。
> 性质:**仅记录、不修改任何代码**。
> 方法:5 个只读子代理分工扫描(核心账本引擎 / 入库+上架 / 订单拉取+出库 / 退货 / 横切租户与并发),结果经交叉验证、去重、合并。
> 日期:2026-07-06。代码根:`erp-backend/admin/src/main/java/com/erp/admin/wms/`。

---

## 0. 头号结论(TL;DR)

**`wms_inventory`(库存快照)存在两个语义互斥的写者,是全局头号缺陷,4 个独立子代理各自撞到同一根因:**

- **写者甲(过账引擎,增量)**:`StockPostingService.post` → `InventoryPostingEngine`,把订单预占/出库/入库以 **delta 增量**改 `wms_inventory` 的 available/reserved/in_transit,**从不回写批次表** `wms_physical_inventory`。
- **写者乙(聚合器,全量覆盖)**:`WmsInventoryAggregator.refreshSnapshot` 按 `Σ批次` **绝对值整行覆盖** available/reserved/damaged;上架/移库/报废/下架/签出每次都会触发它。

两者写**同一行**,一个增量、一个覆盖,**根本不兼容**。任何一次 `refreshSnapshot` 都会把"已确认未下架订单"的预占、已出库的扣减**凭空抹除/复活** → 超卖、预占丢失、库存高估或低估。这是"每个货主库存不正确"的核心成因(见 **C-1**)。

其余缺陷分两类:①并发守卫缺失(入库上架、下架、退货质检均无足够的 CAS/乐观锁/行锁 → 重复操作二次改库存);②退货链路有两套独立入口写同一账本且互不感知(见 **C-2**)。

**严重级别统计**:Critical 3 · High 7 · Medium 8 · Low 7。

---

## 1. 架构前提(判断依据)

三个 MyBatis 拦截器串联(`ErpMybatisPlusConfig`):
- `TenantLineInnerInterceptor`(`ErpTenantLineHandler`):只对白名单表注入 `tenant_id`,**`wms_*` 表全部不在白名单**。
- `DataPermissionInterceptor`(`WmsOwnerDataPermissionHandler`):只对 6 张货主单据表 + `wms_region_inventory` 注入 `erp_tenant_id`。
- **核心库存三表 `wms_inventory` / `wms_physical_inventory` / `wms_stock_flow` 不在任何拦截器白名单** → 租户隔离**完全依赖各 service 显式带 `erp_tenant_id`**。这是所有租户风险的根源。

正面基础设施确认:`wms_inventory` 有唯一键 `uk_tenant_wh_sku(wms_tenant_id, erp_tenant_id, warehouse_id, sku_code)`;`wms_physical_inventory` 有 `uk_batch` 且实体带 `@Version`;`Inventory` 实体带 `@Version`;`OptimisticLockerInnerInterceptor` 已启用。未显式配置事务隔离级别 → 按 MySQL InnoDB 默认 **REPEATABLE READ (RR)** 分析。

---

## 2. 缺陷明细(按严重级别排序)

字段说明:【级别】· 流程 · 位置 · 描述 · 触发场景 · 对库存正确性的影响 · 修复方向 · (来源:交叉验证)。

### 🔴 CRITICAL

#### C-1 双库存权威冲突:过账增量被聚合器"按批次重算"整行覆盖 → 系统性超卖/预占丢失
- **流程**:贯穿 订单拉取/确认预占、出库、入库上架、退货(所有会触发 `refreshSnapshot` 的动作)。
- **位置**:
  - 增量写者:`InventoryPostingEngine.java:106-166`(`processWarehouseItems`,`updateWithVersion` 按内存 delta 改 `wms_inventory`);调用入口 `StockPostingService.post`。预占:`CustomOutboundFacade.java:203-213`、`SalesOutboundFacade.java:317-326`(AVAILABLE−/RESERVED+)。
  - 覆盖写者:`WmsInventoryAggregator.refreshSnapshot:95-128`(尤其 L116-117 绝对值覆盖 `available=Σ(quantity−reserved_qty)`、`reserved=Σreserved_qty`);触发点遍布 `WmsPhysicalInventoryService`(putaway/reserve/scrap/move 各 `:91/117/133/158`)、`OutboundPickingService:214-216`、`OutboundShippingService:188-190`。
- **触发场景(纯顺序即可复现,无需并发)**:某货主同仓同 SKU 物理 100。① 上架 100 → 聚合器写 available=100;② 销售单确认预占 30(过账:RESERVED+30 / AVAILABLE−30)→ available=70, reserved=30(**批次仍 100/0**);③ 同 SKU 再上架 50 → `refreshSnapshot` 从批次重算 available=150、reserved=0 **覆盖** → 第②步预占彻底消失。出库同理:过账扣了 available 但批次未减,下一次 refresh 让已出库的货"复活"。
- **影响**:每个货主的 available 被反复"复活"/高估、reserved 与出库扣减被抹掉 → 跨订单超卖、预占丢失、账实不符。`AVAILABLE` 桶 `allowNegative=true`(见 L-1)使后续扣减无负库存保护,最终批次被扣穿或假缺货 BACKORDER。取消回补(`CustomOutboundFacade.cancel` doReversePosting `:232-273`)在预占已被抹后再加 available,会把 available 抬到**超过物理量**,进一步污染。
- **修复方向**:统一为**单一库存权威**。二选一:(a) 确认预占/出库也扣减批次层 `reserved_qty`/`quantity`,让聚合器成为唯一写者;(b) `refreshSnapshot` 只重算 available/damaged,reserved/in_transit 交给过账引擎、不做绝对覆盖。根治是让快照只有一个语义一致的写者。
- **来源**:核心账本#1、出库#1、退货#1(背景)、入库(HIGH-2 同机制)—— **4 代理独立收敛,置信度最高**。

#### C-2 退货链路两套独立入口写同一账本;`return_status` 默认值致同单被二次入库
- **流程**:退货。
- **位置**:路径①(货主/ERP侧一步到位)`ReturnInboundService.create():145-155,200-210` 直接 `StockPostingService.post(RETURN_RECEIVE)`(增量、不建批次);路径②(平台质检)`ReturnQcService.receive/qc:135-231` 走 `physicalInventoryService.putaway()`(建批次 + `refreshSnapshot` 全量重算);二者写同一行 `wms_inventory`。`V30__return_qc.sql:7`:`return_status` 默认 `RETURN_PENDING`。
- **触发场景**:货主在"退货入库"建单(库存已 +N,但 `create()` **从不设置 `returnStatus`** → DB 默认 RETURN_PENDING)→ 同一单**又出现在平台退货质检工作台**为"待收货"→ 平台收货+质检+上架,再入库一次。
- **影响**:两种坏法(取决于时序):① `create()` 的 delta 无对应批次,后续**任何** `refreshSnapshot` 都会按 Σ批次全量覆盖、把 create() 加的量抹掉 → 少计(与 C-1 同根);② 若走 QC 上架 → 双份过账/流水、且残品/报废分流口径两路径不一致 → 账实与流水皆不符。
- **修复方向**:退货只保留**单一写库入口**。要么 `create()` 只建单(status=RETURN_PENDING)、由 QC 路径唯一入库;要么 QC 工作台过滤掉已由 create() 入库的单并禁止二次上架。
- **来源**:退货#1。

#### C-3 乐观锁重试在同一事务内重复应用"已成功分组" → 重复过账、流水≠快照
- **流程**:所有多分组过账(多 SKU / 多仓单据,常见)。
- **位置**:`InventoryPostingEngine.java:47-60`(`execute` 的 `MAX_RETRY` 循环)+ `62-101/106-166`(`doExecute`/`processWarehouseItems`);外层 `StockPostingService.post` 为单一 `@Transactional`。
- **触发场景**:一次 `post()` 含 ≥2 个分组;前一个分组已 `updateWithVersion` 成功(未提交),后一个分组因**并发外部事务**改了其行导致 `updateWithVersion` 返回 0(`:162`)抛乐观锁 → `execute` 捕获后**整段重跑 `doExecute`**。因单事务前面的成功写入不回滚,`getOrCreate`(`:123`)在同事务读到自己刚改过(未提交)的行,把已成功分组的 delta **再加一遍**。
- **影响**:前序分组数量翻倍写入;流水仅在最终成功那次 `saveBatch`(`:95-97`)写一条,导致 `wms_stock_flow` 累计 ≠ `wms_inventory` 快照,难以对账。
- **修复方向**:重试粒度改为"仅重试失败分组",或每次尝试用独立新事务(`REQUIRES_NEW`)保证失败回滚;绝不能在同事务里基于已改状态重算增量。
- **来源**:核心账本#2。(对照:`WmsInventoryAggregator` 的重试因"按绝对值 set"反而无此问题,佐证本条判断。)

---

### 🟠 HIGH

#### H-1 入库上架无 `@Version`/行锁/CAS → 并发重复上架致某货主 available 翻倍
- **流程**:仓库上架(收货→上架)。
- **位置**:`WmsInboundExecutionService.java:171-245`(状态读 `:173-175`、状态写 `:244-245`,`updateById` 返回值被丢弃);实体 `PurchaseInboundOrder.java` **无 `@Version`**。
- **触发场景**:同一 `RECEIVED` 入库单被并发提交两次上架(双标签页/重试/双操作员)。两事务在 RC 下都读到 RECEIVED、都过 assert、都各自 `physicalInventoryService.putaway` 插一整套批次、都把状态更新成 COMPLETED(无版本条件,两次 update 均成功)。
- **影响**:`wms_physical_inventory` 出现两套重复批次,`refreshSnapshot` 重算 → 该货主该 SKU 的 available(次品则 damaged)**翻倍**。
- **补充**:`PurchaseInboundService.updateToSubmitted/update` 等处的 `if(!updated) throw OptimisticLockException` 是**假乐观锁**——实体无 `@Version`,`updateById` 只要行存在就返回 1,不检测并发修改。
- **修复方向**:上架读单 `SELECT … FOR UPDATE`;或加 `@Version` 并检查 `updateById` 返回值;或 `RECEIVED→COMPLETED` 用条件 CAS 并校验影响行数。
- **来源**:入库 HIGH-1。

#### H-2 下架(pick)缺幂等 CAS → 同单并发双分配/双扣减
- **流程**:订单出库(下架拣货)。
- **位置**:`OutboundPickingService.java:161-225`,状态校验仅在 Java 侧(`:165-173`),结尾 `salesOutboundMapper.updateById(order)`(`:218-222`)无条件写 PICKING,**无 `casOrderStatus`**。对照:签出 `OutboundShippingService.java:160-164` 用 `casOrderStatus(PACKED→SHIPPED)` 抢占。
- **触发场景**:两个并发下架请求(或双击)同时读到 CONFIRMED 均通过校验;`SELECT … FOR UPDATE` 只串行化批次访问、不保证订单级幂等。T1 分配+置 PICKING 提交后,T2 基于剩余可用**再分配一次**,为同一订单插入第二组 `wms_outbound_pick_allocation`。
- **影响**:签出时 `pickAllocationMapper.selectByOutboundOrderId`(`OutboundShippingService.java:167`)遍历**两组**分配,`quantity -= take` 执行两遍 → 物理库存**超扣一倍订单量**;或状态被并发覆盖为 BACKORDER。
- **修复方向**:下架入口用 `casOrderStatus(CONFIRMED/BACKORDER → PICKING)` 抢占;或对 `(outbound_order_id)` 分配写入加唯一约束/存在性检查。
- **来源**:出库#2。

#### H-3 聚合器 `refreshSnapshot` 乐观锁重试不重算批次 → RR 下形同虚设、RC 下丢更新低估
- **流程**:所有触发 refreshSnapshot 的并发同 SKU 作业(上架/下架/签出/移库/报废)。
- **位置**:`WmsInventoryAggregator.refreshSnapshot:95-128`——桶值 `b = aggregate(batches)` 在**重试循环之外**只算一次(`:96-98`),循环内(`:115-125`)只 `selectById` 重读 version、**不重算 `b`**。
- **触发场景 / 影响(依隔离级别)**:
  - **默认 RR**:循环内 `selectById` 仍是事务快照 → version 恒旧 → 3 次重试全失败 → 抛 `INVENTORY_VERSION_CONFLICT` 整单回滚。**fail-safe 但重试形同虚设**,并发同 SKU 作业频繁误报冲突需人工重试。
  - **若切 READ_COMMITTED**:重读拿到新 version 成功,但 `b` 仍是**旧快照**(漏并发已提交批次)→ 用陈旧聚合覆盖 → **快照永久性偏低(丢更新)**。
- **修复方向**:把 `aggregate(...)` 移进重试循环内每次重算;或改单条原子 UPSERT(`SUM` 子查询)消除"查-改-写"窗口。
- **代理分歧(已核对)**:入库代理判为"丢更新低估";核心账本代理判为"聚合器重试是安全的";横切代理据隔离级别调和二者(RR fail-safe / RC 丢更新)。**结论采信横切的调和口径**:当前默认 RR 下为"重试无效 + 误报冲突",改 RC 后升级为真实丢更新。**严重级别取决于线上事务隔离级别,需人工确认。**
- **来源**:入库 HIGH-2 · 核心账本(对照正面确认)· 横切 D-1。

#### H-4 退货质检行无数量字段 → 同 SKU 多行重复整批上架(超量)
- **流程**:退货质检。
- **位置**:`ReturnQcService.java:162-224`(尤其 `:194 int qty = nz(item.getReceivedQty())`);`ReturnQcDTO.ReturnQcLineDTO` **无 quantity 字段**。
- **触发场景**:实收 10,前端对同一 SKU 提交两行(良品一行+次品一行,或误提交两次)→ 循环内对同一 SKU 连做两次 `putaway(10)`;两行填不同库位可绕过库位独占守卫。
- **影响**:该货主库存 **+20**(实退 10),凭空多计。
- **修复方向**:行级增加 quantity;循环前校验同 SKU 汇总 = 实收数;多行按数量拆分而非各自整批。
- **来源**:退货#2。

#### H-5 退货收货不校验实收 ≤ 应退、可注入任意 SKU → 幽灵库存
- **流程**:退货收货。
- **位置**:`ReturnQcService.java:101-130`(`:124 setReceivedQty(nz(...))`,仅 null 兜底)。
- **触发场景**:退货总量 5,收货填 500;或新增一条与本单无关的高价 SKU 收货行。
- **影响**:质检上架按实收数 putaway → 该货主 available/damaged **凭空多计**;任意 SKU 被计入该货主库存。数量守恒被彻底突破。
- **修复方向**:`receivedQty` 上限=应退数;`line.skuCode` 必须属于退货单;`receivedQty ≥ 0` 显式断言。
- **来源**:退货#3。

#### H-6 退货质检状态流转非原子(TOCTOU)、无乐观锁 → 并发/重复提交双份上架
- **流程**:退货质检。
- **位置**:`ReturnQcService.java:137-141, 226-229`;`ReturnInboundOrder.java` **无 `@Version`**;状态 update 不带条件、无锁。
- **触发场景**:操作员双击或两请求并发。两事务都读到 QC_PENDING、都过守卫、都 putaway、都置 COMPLETED;各自事务开始时读的 `occupied` 集合互不可见,库位守卫拦不住。
- **影响**:同一退货**上架两次** → 该货主库存翻倍(幂等 E 与原子性 D 同时失守)。
- **修复方向**:实体加 `@Version`,或状态推进改条件 CAS `UPDATE … SET status=COMPLETED WHERE id=? AND return_status='QC_PENDING'` 并断言影响行数=1。
- **来源**:退货#4。

#### H-7 出库/待发/调拨的库存**展示**走"无货主维"查询 → 跨货主库存串数
- **流程**:订单拉取/待发货展示、调拨可用量展示。
- **位置**:`InventoryService.getStockMapByWarehouseAndSkuCodes` / `getAvailableStockMap` / `pageAvailableStock`(底层 `selectByWarehouseAndSkuCodes` / `selectAvailableStockPage` **仅按 `(warehouseId, skuCode)`**,`toMap` 用 `(a,b)->a` 折叠)。调用点:`SalesOutboundService.java:131`、`CustomOutboundService.java:145`、`SalesOutboundController.java:232`(货主待发货列表)、`TransferOrderService.java:253/263`。
- **触发场景**:多个货主在同一「自有仓(共享物理仓)」持有同一 SKU 时,`selectList` 返回多行,`toMap` 任意保留一行 → 货主 A 看到的 available 实为货主 B 的数值,shortage/stockStatus 计算错误。
- **影响**:跨货主库存数字泄露 + 误导性可用量/缺货状态。**边界**:这些均为**展示/状态**路径;权威扣减路径(`SalesOutboundFacade.checkStockSufficiency:228` 用带 `erpTenantId` 的重载、`TransferOrderService.validateItemQuantities:304`、过账引擎按货主分组)已正确带货主维,**不会导致超卖/错扣**。
- **修复方向**:展示路径统一改用带 `erpTenantId` 的重载,或删除无货主维方法。
- **来源**:横切 A-1。

---

### 🟡 MEDIUM

#### M-1 两写者建行键不一致(`wms_tenant_id`)→ 同 `(erp,wh,sku)` 分裂成两行(条件性)
- **位置**:`InventoryService.getOrCreate:77`(新建 `wmsTenantId=0`)vs `WmsInventoryAggregator.java:106`(新建用真实 `wmsTenantId`);读侧 `InventoryMapper.selectByErpWarehouseSku:57-64`(忽略 wms 维、`findFirst`)vs `selectByTenantKey:42-48`(精确匹配 wms 维)。唯一键含 wms 维。pick/ship 传 `0L`(`OutboundPickingService:183,215`、`OutboundShippingService:189`)而 `putaway` 用 `dto.getWmsTenantId()`。
- **触发场景**:当 `wms_tenant_id` 被填成非 0(引入真实服务商货架归属)时:聚合器 `selectByTenantKey(5,…)` 命不中 → 再插一行 wms=5;`GROUP BY sku_code SUM` 汇总把两行相加(重复计数),而 `findFirst` 取数只取一行(漏另一半);FIFO(`selectFifoAllocatableForUpdate` 按 wms=0 过滤)查不到 wms≠0 批次 → 假缺货 BACKORDER。
- **影响**:同一货主库存分裂/重复计数/假缺货。**当前上架口径 wms 恒为 0,问题被掩盖**;引入服务商货架维度即触发。
- **待确认**:部署中 `wms_tenant_id` 是否会非 0。
- **来源**:核心账本#3 · 出库#3 · 入库(补充)。

#### M-2 过账幂等靠"先查后插"、无 DB 唯一约束兜底,且幂等查询未按租户收窄
- **位置**:`StockPostingService.post:52-67`(`selectBySourceAndType` 后插入);`createPosting:256-266` 仅对 `posting_no` 处理 `DuplicateKeyException`;`StockPostingMapper.selectBySourceAndType:49-54` **不含租户条件**;迁移脚本 `wms_stock_posting` 仅普通索引(`V22:29`),**未见** `(source_type,source_id,posting_type)` 唯一键。
- **触发场景**:① 并发重复提交(双击/超时重试)两请求都读到 `existing==null` → 都过账 → 库存应用两次;② 若 `source_id` 非全局唯一,A 货主过账误命中 B 货主的既有记录 → B 的过账被"幂等"跳过(**漏过账**)。
- **修复方向**:补 `(source_type,source_id,posting_type[,erp_tenant_id])` 唯一索引,`createPosting` 捕获对应 `DuplicateKeyException` 走幂等返回。
- **待确认**:基线 DDL 是否已建该唯一索引;`source_id` 是否全局唯一。
- **来源**:核心账本#4。

#### M-3 区域库存汇总:可用量按查看者收窄、预占/在途疑似全租户合计(代理分歧,待确认)
- **位置**:`RegionInventoryService.getRegionSummaryList:94-154`。可用量 `:107` 经 `erpOwnerScopeService.readScope()` 收窄;预占 `selectReservedByRegionIds`、在途 `selectInTransitByRegionIds`。
- **代理分歧(必须人工核实)**:
  - 核心账本代理判定:预占/在途查询**无租户过滤**、汇总全部货主 → `regionAvailable = 本货主可用 − 全租户预占`,货主看到的区域可用被别家预占扣减(可为负)、在途偏大 → **跨租户串数(Medium bug)**。
  - 横切代理判定:`selectReservedByRegionIds` 查的是 `wms_region_inventory`(**在数据权限白名单**,自动注入 `erp_tenant_id`),与可用量口径一致 → **无问题**。
- **分歧根因**:两者对"预占/在途查询实际读哪张表、是否被数据权限拦截器注入租户条件"判断不同。**需人工确认** `selectReservedByRegionIds`/`selectInTransitByRegionIds` 的真实 SQL 目标表与是否命中 `WmsOwnerDataPermissionHandler` 白名单。
- **来源**:核心账本#5 vs 横切(正面确认)。

#### M-4 盘点服务缺 `assertPlatform()` → 越权可跨货主枚举/调整库存
- **位置**:`StocktakeService`(唯一无 `assertPlatform()` 的库存作业服务);`generateStocktakeItems:225` 用 `getAllByWarehouseId(warehouseId)`(无货主过滤)枚举该仓所有货主库存;`confirm:346` 逐货主过账调整。隔离仅靠菜单权限 `wms:stocktake:*`。
- **触发场景**:若 `wms:stocktake:add/confirm` 被误授予某货主/WMS 服务商,即可查看并调整**其他货主**在该仓库存。
- **影响**:跨货主库存枚举 + 潜在跨货主篡改(纵深防御缺失,与同类服务口径不一致)。
- **修复方向**:`create`/`confirm`/`saveItems` 入口加 `assertPlatform()`。
- **来源**:横切 A-2。

#### M-5 退货质检遍历"提交行"而非"已收货明细" → 漏行的货物永不入库且单据锁死
- **位置**:`ReturnQcService.java:162`(遍历 `dto.getLines()`)+ `226-229`(无条件置 COMPLETED)。
- **触发场景**:收货 2 个 SKU,质检只提交 1 行 → 第 2 个 SKU 的 `receivedQty` 永不进库,单据仍被置 COMPLETED 且状态机不允许再质检。
- **影响**:该货主实收货物**少计**(库存漏加),无补救路径。
- **修复方向**:质检必须覆盖全部已收货明细(Σ质检=Σ实收)方可 COMPLETED。
- **来源**:退货#5。

#### M-6 收货不校验实收 ≤ 应收(自定义入库链)→ available 超量放大
- **位置**:`WmsInboundExecutionService.receive:97-110`(实收原样写入,无上限);`PurchaseInboundService.validateItemQuantities:287-293` 仅在建单/编辑调用,收货链路未复用。
- **触发场景**:平台收货录入实收 > 应收。采购链有 `postRegionInTransitOut`→引擎"区域在途不足"兜底报错;但 **MANUAL/CUSTOM_RETURN 自定义链无 in_transit 出账**(`:114` 跳过),超量实收无拦截,经上架 `coversExactly` 全额流入 available。
- **影响**:自定义入库链下该货主 available 可被任意放大。
- **修复方向**:收货对每条明细校验 `actual ≤ expected`(自定义链尤需)。
- **来源**:入库 MEDIUM-2。

#### M-7 非规范 `quality` 字符串 → 批次从 available/damaged 两桶双双静默消失(不守恒)
- **位置**:聚合分桶 `WmsInventoryAggregator.aggregate:71-80`(大小写敏感 `"DAMAGED".equals(quality)`,否则走 GOOD 分支且需 `allocatable==1`);上架 `WmsInboundExecutionService.normalizeQuality:375-377`(仅 null/空→GOOD,其余原样透传);`InboundPutawayDTO.java:50-51` 的 `quality` 是自由 `String`、无枚举校验。
- **触发场景**:传入 `"damaged"`(小写)/`"DEFECTIVE"`/`"Bad"` 等。批次以该字符串落库、`allocatable=0`;聚合时 `"DAMAGED".equals("damaged")` 为 false → 进 GOOD 分支 → `allocatable!=1` → **既不计入 available 也不计入 damaged**。
- **影响**:批次实物存在但在货主快照里**凭空消失**;采购链已扣 in_transit 却没加回 → 数量不守恒、库存低估。
- **修复方向**:上架入口对 quality 做枚举白名单(仅 GOOD/DAMAGED,统一大写),非法即拒;聚合分桶大小写不敏感或 else 兜底计入 damaged。
- **来源**:入库 MEDIUM-1。

#### M-8 退货合格/不合格分流口径两路径不一致
- **位置**:`ReturnInboundService.java:176-197`(残品→DAMAGED、报废→SCRAP 不入库)vs `ReturnQcService.java:205-208`(FAIL 一律→DAMAGED 批次、**无报废/不入库分支**)。
- **影响**:同为"退货不合格",经不同入口得到不同的 damaged/scrap 结果,账目口径不统一;与 C-2 叠加时残品桶数值互相打架。
- **修复方向**:统一退货不合格的分流枚举与落桶规则。
- **来源**:退货#6。

---

### ⚪ LOW / 待确认

#### L-1 `AVAILABLE` 桶允许为负、账本层无超卖硬守卫
`StockBucket.java:14`(AVAILABLE allowNegative=true)+ `InventoryPostingEngine.validateConstraint:288-294`(可负桶跳过 <0 校验)。过量出库/预占可把 available 压到负数且不报错(`queryPage` 有 `NEGATIVE_STOCK` 过滤项佐证其可发生)。放大 C-1 后果。若为业务允许的预售需上层拦截,否则应在过账处显式校验。**来源**:核心账本#6。

#### L-2 多处 `updateById` 忽略乐观锁返回值
`OutboundPickingService.java:199-200`(pick 写批次,实体有 `@Version`)、`InventoryService.syncFboStock:389` 等未校验影响行数;与 ship(`:181-184` 显式校验 `updated!=1`)口径不一致。当前靠同事务 `FOR UPDATE` 兜底一般安全,一旦前提被破坏会静默丢扣减/丢更新。**来源**:出库#4 · 横切 D-3。

#### L-3 `getOrCreate`/聚合插入/区域库存为"查-不存在-插入",靠唯一键兜底
`InventoryService.getOrCreate:72`、`WmsInventoryAggregator:100-112`、`RegionInventoryService.getOrCreate:48` check-then-insert 未捕获 `DuplicateKeyException`。并发首建同一新键 → 第二个 insert 撞唯一键抛异常整单回滚(**无脏数据**,但偶发失败需重试)。建议捕获后回查复用。**来源**:横切 D-2。

#### L-4 看板 `today-summary`/`trend` 汇总未按租户收窄
`StockFlowMapper.xml selectTodaySummary:6-17`、`selectTrend:20-43` 仅按 `warehouse_id` 过滤、无 `erp_tenant_id`,跨货主合计。可能是仓库运营方看板的有意口径;若被货主端复用则数据串租户(需确认调用方)。注:`StockFlowService.queryPage/getDetail` 本身已按 `readScope()`/`canAccess()` 正确收窄。**来源**:核心账本#7。

#### L-5 FBO 手动同步未 `runAs(货主)`,归属正确性依赖 shop 表租户过滤副作用
`OzonFboStockSyncFacade.syncByShopId:101` 未 `TenantContext.runAs(shop.getTenantId())`(定时入口 `syncAllShops:86` 有)。`syncFboStock:385` 读当前租户作库存归属。现状因 `shopService.getById`(shop 在租户白名单)非属主查不到而提前抛错、不出事,但归属正确性依赖该副作用而非显式 `runAs`。建议与定时任务一致显式 `runAs`。**来源**:横切 A-3。

#### L-6 并发重复收货重复累加采购/物流"已收量"(非直接污染 wms_inventory)
`WmsInboundExecutionService.receive:114-125`:区域 in_transit OUT 走过账幂等不会重复;但 `shippingOrderItemService/purchaseOrderItemService.increaseReceivedQuantity`(`:117/:120`)非幂等,并发双收货会重复累加已收量 → 采购/物流"已入库"数虚高,间接影响后续可入库判断。随 H-1 的状态 CAS 一并可解。**来源**:入库 LOW-1。

#### L-7 建单/上架 `erpTenantId` 缺上下文时兜底为 1 → 可能错配货主
`PurchaseInboundService.resolveOwnerTenantId:527-530`(无上下文→`1L`)、`WmsInboundExecutionService.putaway:227`(`erpTenantId==null?1L`)。货主上下文缺失时把归属兜底成默认货主 1,若真实操作者非货主 1 则库存错配到货主 1(跨货主串数据)。正常有上下文时不触发。建议缺失即拒绝而非兜底。**来源**:入库 LOW-2。

---

## 3. 代理间分歧(如实标注,需人工裁决)

| # | 主题 | 分歧 | 处置 |
|---|---|---|---|
| 1 | **H-3 聚合器重试** | 入库代理:丢更新低估;核心代理:安全;横切代理:依隔离级别(RR fail-safe / RC 丢更新) | 采信横切的调和口径;严重级别取决于线上事务隔离级别,**需确认** |
| 2 | **M-3 区域汇总预占/在途** | 核心代理:无租户过滤→跨租户串数;横切代理:查 `wms_region_inventory`(白名单自动注入)→无问题 | **需人工核实** `selectReservedByRegionIds`/`selectInTransitByRegionIds` 的真实目标表与是否命中数据权限白名单 |

---

## 4. 已核对无问题(正面确认)

- **过账引擎租户隔离**:分组/合并键均含 `erpTenantId`(`buildMergeKey`、`warehouseGroupKey`、`regionGroupKey`、`getOrCreate` 强制 erpTenantId),单笔过账内部不跨货主合并。
- **下架防超卖**:`selectFifoAllocatableForUpdate`(`FOR UPDATE` 行锁)+ 两阶段(缺货整单挂起不部分下架)+ 批次 `@Version`。
- **签出幂等**:`casOrderStatus(PACKED→SHIPPED)` + 批次 `@Version`(`updated!=1` 抛 409)+ 计费 `bizId` 去重。
- **批次真源**:`WmsPhysicalInventoryService` reserve/release/scrap/locationTransfer 全 `@Transactional` + `updateBatchLocked`(`@Version`)+ 同事务聚合刷新 + 流水。
- **异步/并行丢租户上下文**:`wms/` 目录内**无** `CompletableFuture`/`parallelStream`/`@Async`/线程池;唯一后台入口 `InventoryAlertTask`、`syncAllShops` 均正确 `TenantContext.runAs(erpTenantId)` 逐货主建上下文。**本次审计范围内不存在"异步丢 TenantContext"问题。**
- **流水查询隔离**:`StockFlowService.queryPage/getDetail` 用 `readScope()`/`canAccess()` 收窄。
- **软删除**:入库单 `@TableLogic`,软删后无法再收货/上架。
- **库位独占/分区↔品质联动**:同次上架内库位去重 + 已占用拒绝 + zone_type 强校验,逻辑正确。
- `WmsInventoryAggregator` 自身乐观锁重试(按绝对值 set)是安全的——与 C-3 的"按已变状态重算增量"的不安全重试形成对照。

---

## 5. 覆盖清单(已扫描)

**Service(逐方法读)**:StockPostingService、InventoryPostingEngine、StockPostingItemService、WmsInventoryAggregator、InventoryService、WmsPhysicalInventoryService、StockFlowService、RegionInventoryService、RegionStockDataProvider、PurchaseInboundService、PurchaseInboundItemService、WmsInboundExecutionService、SalesOutboundService、CustomOutboundService、SalesOutboundItemService、OutboundPickingService、OutboundShippingService、ReturnInboundService、ReturnQcService、StocktakeService、StocktakeItemService、TransferOrderService、AdjustmentService、ErpOwnerScopeService。
**Facade**:SalesOutboundFacade、CustomOutboundFacade、OzonFboStockSyncFacade(并列举全部 facade)。
**Mapper(接口+XML)**:InventoryMapper、WmsPhysicalInventoryMapper(listForAggregate/selectFifoAllocatable[ForUpdate])、RegionInventoryMapper、StockPostingMapper、StockFlowMapper、SalesOutboundMapper(casOrderStatus)、WmsOutboundPickAllocationMapper、ReturnQcMapper、ReturnInboundMapper、PurchaseInboundMapper。
**实体/枚举**:Inventory、WmsPhysicalInventory、RegionInventory、StockFlow、StockPosting(Item)、PurchaseInboundOrder、ReturnInboundOrder、WmsReturnQcItem、StockBucket、PostingType、OutboundOrderStatus、ReturnQcStatus。
**基础设施**:ErpMybatisPlusConfig、ErpTenantLineHandler、WmsOwnerDataPermissionHandler、TenantContext、WmsSkuLookupMapper(@InterceptorIgnore)、OptimisticLockerInnerInterceptor。
**DDL 核对**:V12(uk_batch / uk_tenant_wh_sku 含 wms 维)、V22(wms_stock_posting 无来源唯一键)、V30(return_status 默认 RETURN_PENDING)。
**搜索模式**:`CompletableFuture|parallelStream|@Async|ExecutorService|Executors|new Thread|.parallel()`、`TenantContext|WmsTenantContext|runAs|readScope|InterceptorIgnore|tenantLine`、`casOrderStatus`、`@Version`、`isolation`、核心表无货主维查询调用点。

---

## 6. 待确认项汇总(交由人工核实后再定级/修复)

1. **线上事务隔离级别**(影响 H-3、C-3 的实际表现):默认 RR 还是被改成 RC?
2. **M-3**:`selectReservedByRegionIds`/`selectInTransitByRegionIds` 实际读哪张表、是否被数据权限拦截器注入 `erp_tenant_id`。
3. **M-1/M-2**:部署中 `wms_tenant_id` 是否会取非 0;`wms_stock_posting` 是否已有 `(source_type,source_id,posting_type)` 唯一索引;`source_id` 是否全局唯一。
4. **L-4**:`today-summary`/`trend` 看板是否被货主端页面复用(决定是否为串租户 bug)。

---

*本报告仅记录,不含任何代码修改。如需针对某条(尤其 C-1/C-2/C-3)展开修复方案,请指定条目。*
